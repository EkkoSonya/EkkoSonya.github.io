<?xml version="1.0" encoding="utf-8"?><?xml-stylesheet type="text/xsl" href="http://ekkosonya.cn/rss.xsl"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:dc="http://purl.org/dc/elements/1.1/">
  <channel>
    <atom:link href="http://ekkosonya.cn/rss.xml" rel="self" type="application/rss+xml"/>
    <title>EkkoSonya&amp;apos;s Blog</title>
    <link>http://ekkosonya.cn/</link>
    <description>笔记记录</description>
    <language>zh-CN</language>
    <pubDate>Mon, 30 Mar 2026 16:24:04 GMT</pubDate>
    <lastBuildDate>Mon, 30 Mar 2026 16:24:04 GMT</lastBuildDate>
    <generator>@vuepress/plugin-feed</generator>
    <docs>https://validator.w3.org/feed/docs/rss2.html</docs>
    <category>code</category>
    <item>
      <title>JVM 八股5 - 内存管理4 (对象内存布局)</title>
      <link>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JVM%20-%20%E5%85%AB%E8%82%A1/article5.html</link>
      <guid>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JVM%20-%20%E5%85%AB%E8%82%A1/article5.html</guid>
      <source url="http://ekkosonya.cn/rss.xml">JVM 八股5 - 内存管理4 (对象内存布局)</source>
      <description>对象内存布局 对象的内存布局是由 Java 虚拟机规范定义的，但具体的实现细节各有不同，如 HotSpot 和 OpenJ9 就不一样 常用的 HotSpot 为例： 对象在内存中包括三部分：对象头、实例数据和对齐填充 对象头 (Mark World + 类型指针 (指向 Class 对象)) + 实例数据 + 对齐填充 对象头 (8字节 + 8字节 ...</description>
      <category>code</category>
      <pubDate>Mon, 30 Mar 2026 16:21:54 GMT</pubDate>
      <content:encoded><![CDATA[<h2>对象内存布局</h2>
<p>对象的内存布局是由 Java 虚拟机规范定义的，但具体的实现细节各有不同，如 HotSpot 和 OpenJ9 就不一样</p>
<p>常用的 HotSpot 为例：</p>
<p>对象在内存中包括三部分：对象头、实例数据和对齐填充</p>
<blockquote>
<p>对象头 (Mark World + 类型指针 (指向 Class 对象)) + 实例数据 + 对齐填充</p>
<p>对象头 (8字节 + 8字节 (压缩 4 字节)): 12字节 (指针压缩)</p>
<p>对齐填充必须是 8 的倍数 (64位)</p>
</blockquote>
<figure><figcaption>alt text</figcaption></figure>
<h3>对象头作用</h3>
<p>对象头是对象存储在内存中的元信息，包含了Mark Word、类型指针等信息</p>
<p>Mark Word 存储了对象的运行时状态信息，包括锁、哈希值、GC 标记等。</p>
<p>在 64 位操作系统下占 8 个字节，32 位操作系统下占 4 个字节。</p>
<blockquote>
<p>Mark Word 的大小等于一个机器字长</p>
<p>32 位系统是 4 字节，64 位系统是 8 字节。</p>
</blockquote>
<p>类型指针指向对象所属类的元数据，也就是 Class 对象，用来支持多态、方法调用等功能。</p>
<p>除此之外，如果对象是数组类型，还会有一个额外的数组长度字段。占 4 个字节。</p>
<h4>类型指针会被压缩吗</h4>
<p>类型指针可能会被压缩，以节省内存空间。比如说在开启压缩指针的情况下占 4 个字节，否则占 8 个字节。</p>
<p>在 JDK 8 中，压缩指针默认是开启的</p>
<p>可以通过 <code>java -XX:+PrintFlagsFinal -version | grep UseCompressedOops</code> 命令来查看 JVM 是否开启了压缩指针</p>
<h3>实例数据</h3>
<p>实例数据是对象实际的字段值，也就是成员变量的值，按照字段在类中声明的顺序存储</p>
<p>JVM 会对这些数据进行对齐/重排，以提高内存访问速度</p>
<h4>对齐填充 (8 字节的倍数)</h4>
<p>由于 JVM 的内存模型要求对象的起始地址是 8 字节对齐（64 位 JVM 中），因此对象的总大小必须是 8 字节的倍数。</p>
<p>如果对象头和实例数据的总长度不是 8 的倍数，JVM 会通过填充额外的字节来对齐。</p>
<p>比如说，如果对象头 + 实例数据 = 14 字节，则需要填充 2 个字节，使总长度变为 16 字节</p>
<blockquote>
<p>因为 CPU 进行内存访问时，一次寻址的指针大小是 8 字节，正好是 L1 缓存行的大小。如果不进行内存对齐，则可能出现跨缓存行访问，导致额外的缓存行加载，CPU 的访问效率就会降低</p>
</blockquote>
<h3>new Object() 对象内存大小是多少</h3>
<blockquote>
<p>目前的操作系统都是 64 位的，并且 JDK 8 中的压缩指针是默认开启的</p>
</blockquote>
<p>在 64 位的 JVM 上，<code>new Object()</code> 的大小是 16 字节（12 字节的对象头 + 4 字节的对齐填充）。</p>
<p>对象头的大小是固定的</p>
<ul>
<li>在 32 位 JVM 上是 8 字节</li>
<li>在 64 位 JVM 上是 16 字节
<ul>
<li>如果开启了压缩指针，就是 12 字节。</li>
</ul>
</li>
</ul>
<p>实例数据的大小取决于对象的成员变量和它们的类型。对于new Object()来说，由于默认没有成员变量，因此我们可以认为此时的实例数据大小是 0</p>
<blockquote>
<p>假如 MyObject 对象有三个成员变量，分别是 int、long 和 byte 类型，那么它们占用的内存大小分别是 4 字节、8 字节和 1 字节</p>
</blockquote>
<p>考虑到对齐填充，MyObject 对象的总大小为 12（对象头） + 4（a） + 8（b） + 1（c） + 7（填充） = 32 字节</p>
<h3>JOL — 分析 JVM 对象工具</h3>
<div class="language-xml" data-ext="xml" data-title="xml"><pre class="language-xml"><code><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>dependency</span><span class="token punctuation">&gt;</span></span>
  <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>groupId</span><span class="token punctuation">&gt;</span></span>org.openjdk.jol<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>groupId</span><span class="token punctuation">&gt;</span></span>
  <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>artifactId</span><span class="token punctuation">&gt;</span></span>jol-core<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>artifactId</span><span class="token punctuation">&gt;</span></span>
  <span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>version</span><span class="token punctuation">&gt;</span></span>0.9<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>version</span><span class="token punctuation">&gt;</span></span>
<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>dependency</span><span class="token punctuation">&gt;</span></span>
</code></pre></div><p>简单示例</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">JOLSample</span> <span class="token punctuation">{</span>
  <span class="token keyword">public</span> <span class="token keyword">static</span> <span class="token keyword">void</span> <span class="token function">main</span><span class="token punctuation">(</span><span class="token class-name">String</span><span class="token punctuation">[</span><span class="token punctuation">]</span> args<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token comment">// 打印JVM详细信息（可选）</span>
    <span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span><span class="token constant">VM</span><span class="token punctuation">.</span><span class="token function">current</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">details</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

    <span class="token comment">// 创建Object实例</span>
    <span class="token class-name">Object</span> obj <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">Object</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

    <span class="token comment">// 打印Object实例的内存布局</span>
    <span class="token class-name">String</span> layout <span class="token operator">=</span> <span class="token class-name">ClassLayout</span><span class="token punctuation">.</span><span class="token function">parseInstance</span><span class="token punctuation">(</span>obj<span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">toPrintable</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span>layout<span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><h3>对象的引用大小</h3>
<p>在 64 位 JVM 上，未开启压缩指针时，对象引用占用 8 字节；开启压缩指针时，对象引用会被压缩到 4 字节</p>
<p>HotSpot 虚拟机默认是开启压缩指针的。</p>
<h2>JVM 怎么访问对象</h2>
<p>主流的方式有两种：句柄和直接指针</p>
<p>两种方式的区别在于：</p>
<ul>
<li>句柄是通过一个中间的句柄表来定位对象的</li>
<li>而直接指针则是通过引用直接指向对象的内存地址</li>
</ul>
<p>句柄的优点是，对象被移动时只需要修改句柄表中的指针，而不需要修改对象引用本身</p>
<figure><figcaption>alt text</figcaption></figure>
<p>在直接指针访问中，引用直接存储对象的内存地址；对象的实例数据和类型信息都存储在堆中固定的内存区域</p>
<p>优点是访问速度更快，因为少了一次句柄的寻址操作。缺点是如果对象在内存中移动，引用需要更新为新的地址</p>
<p>HotSpot 虚拟机主要使用直接指针来进行对象访问。</p>
<figure><figcaption>alt text</figcaption></figure>
<h2>对象引用方式</h2>
<p>四种，分别是强引用、软引用、弱引用和虚引用。</p>
<figure><figcaption>alt text</figcaption></figure>
<h3>强引用</h3>
<p>强引用是 Java 中最常见的引用类型</p>
<p>使用 new 关键字赋值的引用就是强引用，只要强引用关联着对象，垃圾收集器就不会回收这部分对象，即使内存不足</p>
<h3>软引用</h3>
<p>软引用于描述一些非必须对象，通过 SoftReference 类实现。软引用的对象在内存不足时会被回收</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token comment">// softRef 就是一个软引用</span>
<span class="token class-name">SoftReference</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">String</span><span class="token punctuation">&gt;</span></span> softRef <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">SoftReference</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token punctuation">&gt;</span></span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token class-name">String</span><span class="token punctuation">(</span><span class="token string">"PPP"</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre></div><h3>弱引用</h3>
<p>弱引用用于描述一些短生命周期的非必须对象，如 ThreadLocal 中的 Entry，就是通过 WeakReference 类实现的</p>
<p>弱引用的对象会在下一次垃圾回收时会被回收，不论内存是否充足</p>
<h3>虚引用</h3>
<p>虚引用主要用来跟踪对象被垃圾回收的过程，通过 PhantomReference 类实现。虚引用的对象在任何时候都可能被回收</p>
]]></content:encoded>
    </item>
    <item>
      <title>JVM 八股6 - 内存管理5 (java 堆介绍)</title>
      <link>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JVM%20-%20%E5%85%AB%E8%82%A1/article6.html</link>
      <guid>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JVM%20-%20%E5%85%AB%E8%82%A1/article6.html</guid>
      <source url="http://ekkosonya.cn/rss.xml">JVM 八股6 - 内存管理5 (java 堆介绍)</source>
      <description>Java 堆 (存放对象位置) 堆内存分区 Java 堆被划分为新生代和老年代两个区域 新生代又被划分为 Eden 空间和两个 Survivor 空间（From 和 To） 新创建的对象会被分配到 Eden 空间 当 Eden 区填满时，会触发一次 Minor GC，清除不再使用的对象 存活下来的对象会从 Eden 区移动到 Survivor 区。 对...</description>
      <category>code</category>
      <pubDate>Mon, 30 Mar 2026 16:21:54 GMT</pubDate>
      <content:encoded><![CDATA[<h2>Java 堆 (存放对象位置)</h2>
<h3>堆内存分区</h3>
<p>Java 堆被划分为新生代和老年代两个区域</p>
<p>新生代又被划分为 Eden 空间和两个 Survivor 空间（From 和 To）</p>
<p>新创建的对象会被分配到 Eden 空间</p>
<p>当 Eden 区填满时，会触发一次 Minor GC，清除不再使用的对象</p>
<p>存活下来的对象会从 Eden 区移动到 Survivor 区。</p>
<p>对象在新生代中经历多次 GC 后，如果仍然存活，会被移动到老年代</p>
<p>当老年代内存不足时，会触发 Major GC，对整个堆进行垃圾回收</p>
<figure><figcaption>alt text</figcaption></figure>
<h4>新生代的区域划分</h4>
<p>新生代的垃圾收集主要采用<strong>标记-复制算法</strong>，因为新生代的存活对象比较少，每次复制少量的存活对象效率比较高</p>
<p>基于这种算法，虚拟机将内存分为一块较大的 Eden 空间和两块较小的 Survivor 空间</p>
<p>每次分配内存只使用 Eden 和其中一块 Survivor</p>
<p>发生垃圾收集时，将 Eden 和 Survivor 中仍然存活的对象一次性复制到另外一块 Survivor 空间上</p>
<p>然后直接清理掉 Eden 和已用过的那块 Survivor 空间</p>
<p>默认 Eden 和 Survivor 的大小比例是 8∶1 (8:1:1)</p>
<figure><figcaption>alt text</figcaption></figure>
<h4>对象什么时候进入老年代</h4>
<p>对象通常会在年轻代中分配，随着时间的推移和垃圾收集的进程，某些满足条件的对象会进入到老年代中，如长期存活的对象</p>
<p>进入老年代的对象(满足一个即可)：</p>
<ul>
<li>长期存活的对象</li>
<li>大对象</li>
<li>动态年龄判断</li>
</ul>
<h5>长期存活的对象判断</h5>
<p>JVM 会为对象维护一个“年龄”计数器，记录对象在新生代中经历 Minor GC 的次数</p>
<p>每次 GC 未被回收的对象，其年龄会加 1</p>
<p>当超过一个特定阈值，默认值是 15，就会被认为老对象了，需要重点关照。</p>
<p>这个年龄阈值可以通过 JVM 参数-XX:MaxTenuringThreshold来设置。</p>
<p>可以通过 <code>jinfo -flag MaxTenuringThreshold $(jps | grep -i nacos | awk '{print $1}')</code> 来查看当前 JVM 的年龄阈值。</p>
<ul>
<li>如果应用中的对象存活时间较短，可以适当调大这个值，让对象在新生代多待一会儿</li>
<li>如果对象存活时间较长，可以适当调小这个值，让对象更快进入老年代，减少在新生代的复制次数</li>
</ul>
<h5>大对象判断</h5>
<p>大对象是指占用内存较大的对象，如大数组、长字符串等</p>
<p>其大小由 JVM 参数 -XX:PretenureSizeThreshold 控制，但在 JDK 8 中，默认值为 0，也就是说默认情况下，对象仅根据 GC 存活的次数来判断是否进入老年代</p>
<blockquote>
<p>G1 垃圾收集器中，大对象会直接分配到 HUMONGOUS 区域。当对象大小超过一个 Region 容量的 50% 时，会被认为是大对象</p>
</blockquote>
<figure><figcaption>alt text</figcaption></figure>
<h5>动态年龄判定</h5>
<p>如果 Survivor 区中所有对象的总大小超过了一定比例，通常是 Survivor 区的一半，那么年龄较小的对象也可能会被提前晋升到老年代</p>
<h3>堆内存如何分配</h3>
<p>在堆中为对象分配内存时，主要使用两种策略：<strong>指针碰撞</strong>和<strong>空闲列表</strong></p>
<figure><figcaption>alt text</figcaption></figure>
<p>这是因为如果年龄较小的对象在 Survivor 区中占用了较大的空间，会导致 Survivor 区中的对象复制次数增多，影响垃圾回收的效率</p>
<h3>对象一定分配在堆中吗</h3>
<p>默认情况下，Java 对象是在堆中分配的</p>
<p>但 JVM 会进行<strong>逃逸分析</strong>，来判断对象的<strong>生命周期是否只在方法内部</strong>，如果是的话，这个对象可以<strong>在栈上分配</strong></p>
<p>举例来说，下面的代码中，对象 new Person() 的生命周期只在 testStackAllocation 方法内部，因此 JVM 会将这个对象分配在栈上</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">testStackAllocation</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
  <span class="token class-name">Person</span> p <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">Person</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>  <span class="token comment">// 对象可能分配在栈上</span>
  p<span class="token punctuation">.</span>name <span class="token operator">=</span> <span class="token string">"1111"</span><span class="token punctuation">;</span>
  p<span class="token punctuation">.</span>age <span class="token operator">=</span> <span class="token number">18</span><span class="token punctuation">;</span>
  <span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span>p<span class="token punctuation">.</span>name<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre></div><h4>逃逸分析</h4>
<p>逃逸分析是一种 JVM 优化技术，用来分析对象的作用域和生命周期，判断对象是否逃逸出方法或线程</p>
<p>可以通过<strong>分析对象的引用流向</strong>，判断对象是否被方法返回、赋值到全局变量、传递到其他线程等，来确定对象是否逃逸。</p>
<p>如果对象没有逃逸，就可以进行:</p>
<ul>
<li>栈上分配 - 对象分配在栈上，方法结束自动回收</li>
<li>同步消除 - 去掉不必要的同步锁</li>
<li>标量替换 - 将对象拆解为标量直接使用</li>
</ul>
<p>以提高程序的性能</p>
<p>可以通过 <code>java -XX:+PrintFlagsFinal 2&gt;&amp;1 | findStr Escape</code> 来确认 JVM 是否开启了逃逸分析</p>
<h5>什么是逃逸分析</h5>
<p>根据对象逃逸的范围，可以分为方法逃逸和线程逃逸</p>
<p>当对象被方法外部的代码引用，<strong>生命周期超出了方法的范围</strong>，那么对象就必须分配在堆中，由垃圾收集器管理</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">public</span> <span class="token class-name">Person</span> <span class="token function">createPerson</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
  <span class="token keyword">return</span> <span class="token keyword">new</span> <span class="token class-name">Person</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 对象逃逸出方法</span>
<span class="token punctuation">}</span>
</code></pre></div><p>比如说 new Person() 创建的对象被返回，那么这个对象就逃逸出当前方法了</p>
<p>再比如说，对象被另外一个线程引用，生命周期超出了当前线程，那么对象就必须分配在堆中，并且线程之间需要同步</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">threadEscapeExample</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
  <span class="token class-name">Person</span> p <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">Person</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 对象逃逸到另一个线程</span>
  <span class="token keyword">new</span> <span class="token class-name">Thread</span><span class="token punctuation">(</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">-&gt;</span> <span class="token punctuation">{</span>
      <span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span>p<span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">start</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre></div><h5>好处</h5>
<ul>
<li>如果确定一个对象不会逃逸，那么就可以考虑栈上分配，对象占用的内存随着栈帧出栈后销毁，这样一来，垃圾收集的压力就降低很多</li>
<li>线程同步需要加锁，加锁就要占用系统资源，如果逃逸分析能够确定一个对象不会逃逸出线程，那么这个对象就不用加锁，从而减少线程同步的开销</li>
<li>如果对象的字段在方法中独立使用，JVM 可以将对象分解为标量变量，避免对象分配</li>
</ul>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">scalarReplacementExample</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
  <span class="token class-name">Point</span> p <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">Point</span><span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span>p<span class="token punctuation">.</span><span class="token function">getX</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">+</span> p<span class="token punctuation">.</span><span class="token function">getY</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre></div><p>如果 Point 对象未逃逸，JVM 可以优化为：</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">int</span> x <span class="token operator">=</span> <span class="token number">1</span><span class="token punctuation">;</span>
<span class="token keyword">int</span> y <span class="token operator">=</span> <span class="token number">2</span><span class="token punctuation">;</span>
<span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span>x <span class="token operator">+</span> y<span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre></div>]]></content:encoded>
    </item>
    <item>
      <title>JVM 八股7 - 内存管理6 (java 堆介绍)</title>
      <link>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JVM%20-%20%E5%85%AB%E8%82%A1/article7.html</link>
      <guid>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JVM%20-%20%E5%85%AB%E8%82%A1/article7.html</guid>
      <source url="http://ekkosonya.cn/rss.xml">JVM 八股7 - 内存管理6 (java 堆介绍)</source>
      <description>内存溢出和内存泄漏 内存溢出 内存溢出，俗称 OOM，是指当程序请求分配内存时，由于没有足够的内存空间，从而抛出 OutOfMemoryError 可能是因为堆、元空间、栈或直接内存不足导致的。可以通过优化内存配置、减少对象分配来解决。 例子 堆内存溢出： 是否处理过内存溢出 有，上传文件时，一开始是一次性上传，没有正确处理，导致一下子撑爆了内存，程序...</description>
      <category>code</category>
      <pubDate>Mon, 30 Mar 2026 16:21:54 GMT</pubDate>
      <content:encoded><![CDATA[<h2>内存溢出和内存泄漏</h2>
<h3>内存溢出</h3>
<p>内存溢出，俗称 OOM，是指当程序请求分配内存时，由于没有足够的内存空间，从而抛出 <code>OutOfMemoryError</code></p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token class-name">List</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">String</span><span class="token punctuation">&gt;</span></span> list <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">ArrayList</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token punctuation">&gt;</span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token keyword">while</span> <span class="token punctuation">(</span><span class="token boolean">true</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
  list<span class="token punctuation">.</span><span class="token function">add</span><span class="token punctuation">(</span><span class="token string">"OutOfMemory"</span><span class="token punctuation">.</span><span class="token function">repeat</span><span class="token punctuation">(</span><span class="token number">1000</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 无限增加内存</span>
<span class="token punctuation">}</span>
</code></pre></div><p>可能是因为堆、元空间、栈或直接内存不足导致的。可以通过优化内存配置、减少对象分配来解决。</p>
<h4>例子</h4>
<p>堆内存溢出：</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">class</span> <span class="token class-name">HeapSpaceErrorGenerator</span> <span class="token punctuation">{</span>
  <span class="token keyword">public</span> <span class="token keyword">static</span> <span class="token keyword">void</span> <span class="token function">main</span><span class="token punctuation">(</span><span class="token class-name">String</span><span class="token punctuation">[</span><span class="token punctuation">]</span> args<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token comment">// 第一步，创建一个大的容器</span>
    <span class="token class-name">List</span><span class="token operator">&lt;</span><span class="token keyword">byte</span><span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token operator">&gt;</span> bigObjects <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">ArrayList</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token punctuation">&gt;</span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">try</span> <span class="token punctuation">{</span>
      <span class="token comment">// 第二步，循环写入数据</span>
      <span class="token keyword">while</span> <span class="token punctuation">(</span><span class="token boolean">true</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
        <span class="token comment">// 第三步，创建一个大对象，一个大约 10M 的数组</span>
        <span class="token keyword">byte</span><span class="token punctuation">[</span><span class="token punctuation">]</span> bigObject <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token keyword">byte</span><span class="token punctuation">[</span><span class="token number">10</span> <span class="token operator">*</span> <span class="token number">1024</span> <span class="token operator">*</span> <span class="token number">1024</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
        <span class="token comment">// 第四步，将大对象添加到容器中</span>
        bigObjects<span class="token punctuation">.</span><span class="token function">add</span><span class="token punctuation">(</span>bigObject<span class="token punctuation">)</span><span class="token punctuation">;</span>
      <span class="token punctuation">}</span>
    <span class="token punctuation">}</span> <span class="token keyword">catch</span> <span class="token punctuation">(</span><span class="token class-name">OutOfMemoryError</span> e<span class="token punctuation">)</span> <span class="token punctuation">{</span>
      <span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span><span class="token string">"OutOfMemoryError 发生在 "</span> <span class="token operator">+</span> bigObjects<span class="token punctuation">.</span><span class="token function">size</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">+</span> <span class="token string">" 对象后"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
      <span class="token keyword">throw</span> e<span class="token punctuation">;</span>
    <span class="token punctuation">}</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><h4>是否处理过内存溢出</h4>
<p>有，上传文件时，一开始是一次性上传，没有正确处理，导致一下子撑爆了内存，程序直接崩溃了</p>
<blockquote>
<p>我记得是通过导出堆转储文件进行分析发现的</p>
</blockquote>
<p>使用 jmap 命令手动生成 Heap Dump 文件：</p>
<div class="language-bash" data-ext="sh" data-title="sh"><pre class="language-bash"><code>jmap <span class="token parameter variable">-dump:format</span><span class="token operator">=</span>b,file<span class="token operator">=</span>heap.hprof <span class="token operator">&lt;</span>pid<span class="token operator">&gt;</span>
</code></pre></div><p>然后使用 MAT、JProfiler 等工具进行分析，查看内存中的对象占用情况</p>
<h3>内存泄露</h3>
<p>内存泄漏是指程序在使用完内存后，未能及时释放，导致占用的内存无法再被使用。</p>
<p>随着时间的推移，内存泄漏会导致可用内存逐渐减少，最终导致内存溢出。</p>
<p>内存泄漏通常是因为<strong>长期存活的对象持有短期存活对象的引用</strong>，又没有及时释放，从而导致短期存活对象无法被回收而导致的</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">class</span> <span class="token class-name">MemoryLeakExample</span> <span class="token punctuation">{</span>
  <span class="token keyword">private</span> <span class="token keyword">static</span> <span class="token class-name">List</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">Object</span><span class="token punctuation">&gt;</span></span> staticList <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">ArrayList</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token punctuation">&gt;</span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">addObject</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    staticList<span class="token punctuation">.</span><span class="token function">add</span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token class-name">Object</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 对象不会被回收</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><blockquote>
<p>以 ThreadLocal 为例，ThreadLocalMap 的 key 是 ThreadLocal 对象的弱引用</p>
<p>如果 key 使用强引用，当 ThreadLocal 引用被置为 null 后，Entry 中的 key 仍然持有引用，导致 ThreadLocal 对象无法被回收。</p>
</blockquote>
<h4>原因</h4>
<blockquote>
<p>静态的集合中添加的对象越来越多，但却没有及时清理；静态变量的生命周期与应用程序相同，如果静态变量持有对象的引用，这些对象将无法被 GC 回收。</p>
</blockquote>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">class</span> <span class="token class-name">OOM</span> <span class="token punctuation">{</span>
 <span class="token keyword">static</span> <span class="token class-name">List</span> list <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">ArrayList</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

 <span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">oomTests</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">{</span>
   <span class="token class-name">Object</span> obj <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">Object</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

   list<span class="token punctuation">.</span><span class="token function">add</span><span class="token punctuation">(</span>obj<span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><blockquote>
<p>单例模式下对象持有的外部引用无法及时释放；单例对象在整个应用程序的生命周期中存活，如果单例对象持有其他对象的引用，这些对象将无法被回收</p>
</blockquote>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">class</span> <span class="token class-name">Singleton</span> <span class="token punctuation">{</span>
  <span class="token keyword">private</span> <span class="token keyword">static</span> <span class="token keyword">final</span> <span class="token class-name">Singleton</span> <span class="token constant">INSTANCE</span> <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">Singleton</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token keyword">private</span> <span class="token class-name">List</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">Object</span><span class="token punctuation">&gt;</span></span> objects <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">ArrayList</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token punctuation">&gt;</span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

  <span class="token keyword">public</span> <span class="token keyword">static</span> <span class="token class-name">Singleton</span> <span class="token function">getInstance</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">return</span> <span class="token constant">INSTANCE</span><span class="token punctuation">;</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><blockquote>
<p>数据库、IO、Socket 等连接资源没有及时关闭</p>
<p>ThreadLocal 的引用未被清理，线程退出后仍然持有对象引用；在线程执行完后，要调用 ThreadLocal 的 remove 方法进行清理</p>
</blockquote>
<h4>是否处理过内存泄露</h4>
<p>比如 ThreadLocal 没有及时清理导致出现了内存泄漏问题</p>
<ul>
<li>先用 <code>jps -l</code> 查看运行的 Java 进程 ID</li>
<li>使用 <code>top -p [pid]</code> 查看进程使用 CPU 和内存占用情况</li>
<li>使用 <code>top -Hp [pid]</code> 查看进程下的所有线程占用 CPU 和内存情况</li>
<li>抓取线程栈：<code>jstack -F 29452 &gt; 29452.txt</code>，可以多抓几次做个对比
<ul>
<li>看看有没有线程死锁、死循环或长时间等待这些问题</li>
</ul>
</li>
<li>可以使用 <code>jstat -gcutil [pid] 5000 10</code> 每隔 5 秒输出 GC 信息，输出 10 次，查看 YGC 和 Full GC 次数</li>
<li>生成 dump 文件，然后借助可视化工具分析哪个对象非常多，基本就能定位到问题根源了
<ul>
<li><code>jmap -dump:format=b,file=heap.hprof 10025 </code></li>
</ul>
</li>
<li>使用图形化工具分析，如 JDK 自带的 VisualVM，从菜单 &gt; 文件 &gt; 装入 dump 文件</li>
</ul>
<h2>栈溢出</h2>
<p>栈溢出发生在程序调用栈的深度超过 JVM 允许的最大深度时</p>
<p>栈溢出的本质是因为线程的栈空间不足，导致无法再为新的栈帧分配内存</p>
<figure><figcaption>alt text</figcaption></figure>
<p>当一个方法被调用时，JVM 会在栈中分配一个栈帧，用于存储该方法的执行信息。如果方法调用嵌套太深，栈帧不断压入栈中，最终会导致栈空间耗尽，抛出 StackOverflowError。</p>
<p>最常见的栈溢出场景就是递归调用，尤其是没有正确的终止条件下，会导致递归无限进行</p>
<p>另外，如果方法中定义了特别大的局部变量，栈帧会变得很大，导致栈空间更容易耗尽</p>
]]></content:encoded>
    </item>
    <item>
      <title>JVM 八股8 - 垃圾收集1</title>
      <link>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JVM%20-%20%E5%85%AB%E8%82%A1/article8.html</link>
      <guid>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JVM%20-%20%E5%85%AB%E8%82%A1/article8.html</guid>
      <source url="http://ekkosonya.cn/rss.xml">JVM 八股8 - 垃圾收集1</source>
      <description>垃圾收集机制 垃圾回收就是对堆内存中不再被引用的（不可达的）对象进行清除或回收 JVM 在做 GC 之前，会先搞清楚什么是垃圾，什么不是垃圾，通常会通过可达性分析算法来判断对象是否存活 在确定了哪些垃圾可以被回收后 垃圾收集器（如 CMS、G1、ZGC）要做的事情就是进行垃圾回收 可以采用： 标记清除算法 复制算法 标记整理算法 分代收集算法等 C++...</description>
      <category>code</category>
      <pubDate>Mon, 30 Mar 2026 16:21:54 GMT</pubDate>
      <content:encoded><![CDATA[<h2>垃圾收集机制</h2>
<p>垃圾回收就是对堆内存中不再被引用的（不可达的）对象进行清除或回收</p>
<p>JVM 在做 GC 之前，会先搞清楚什么是垃圾，什么不是垃圾，通常会通过<strong>可达性分析</strong>算法来判断对象是否存活</p>
<p>在确定了哪些垃圾可以被回收后</p>
<p>垃圾收集器（如 CMS、G1、ZGC）要做的事情就是进行垃圾回收</p>
<p>可以采用：</p>
<ul>
<li>标记清除算法</li>
<li>复制算法</li>
<li>标记整理算法</li>
<li>分代收集算法等</li>
</ul>
<blockquote>
<p>C++ 等语言创建对象要不断的去开辟空间，不用的时候又需要不断的去释放空间，既要写构造函数，又要写析构函数</p>
<p>于是，有人就提出，能不能写一段程序实现这块功能，每次创建对象、释放内存空间的时候复用这段代码</p>
<p>1960 年，John McCarthy 在设计 Lisp 语言时首次提出了垃圾回收的概念，用于自动管理内存，避免程序员手动释放内存带来的错误</p>
</blockquote>
<h3>垃圾回收的过程</h3>
<p>Java 的垃圾回收过程主要分为标记存活对象、清除无用对象、以及内存压缩/整理三个阶段</p>
<figure><figcaption>alt text</figcaption></figure>
<h4><code>finalize()</code> 方法</h4>
<p>如果对象在进行可达性分析后发现没有与 GC Roots 相连接的引用链，那它将会被第一次标记，随后进行一次筛选</p>
<p>筛选的条件是对象是否有必要执行 finalize()方法</p>
<p>如果对象在 finalize() 中成功拯救自己——只要重新与引用链上的任何一个对象建立关联即可。</p>
<p>譬如把自己 （this 关键字）赋值给某个类变量或者对象的成员变量，那在第二次标记时它就”逃过一劫“；但是如果没有抓住这个机会，那么对象就真的要被回收了</p>
<p>并且这个机会只有一次，之后就不能用</p>
<h3>垃圾判断算法</h3>
<p>既然 JVM 要做垃圾回收，就要搞清楚什么是垃圾，什么不是垃圾</p>
<ul>
<li>引用计数算法 (无法解决循环依赖)</li>
<li>可达性分析算法</li>
</ul>
<h4>引用计数法</h4>
<p>引用计数算法（Reachability Counting）是通过在对象头中分配一个空间来保存该对象被引用的次数（Reference Count）</p>
<p>如果该对象被其它对象引用，则它的引用计数加 1，如果删除对该对象的引用，那么它的引用计数就减 1，当该对象的引用计数为 0 时，那么该对象就会被回收</p>
<p>引用计数算法将垃圾回收分摊到整个应用程序的运行当中，而不是集中在垃圾收集时</p>
<p>因此，采用引用计数的垃圾收集不属于严格意义上的"Stop-The-World"的垃圾收集机制</p>
<blockquote>
<p>引用计数算法看似很美好，但实际上它存在一个很大的问题，那就是无法解决循环依赖的问题</p>
</blockquote>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">ReferenceCountingGC</span> <span class="token punctuation">{</span>

  <span class="token keyword">public</span> <span class="token class-name">Object</span> instance<span class="token punctuation">;</span>  <span class="token comment">// 对象属性，用于存储对另一个 ReferenceCountingGC 对象的引用</span>

  <span class="token keyword">public</span> <span class="token class-name">ReferenceCountingGC</span><span class="token punctuation">(</span><span class="token class-name">String</span> name<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token comment">// 构造方法</span>
  <span class="token punctuation">}</span>

  <span class="token keyword">public</span> <span class="token keyword">static</span> <span class="token keyword">void</span> <span class="token function">testGC</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token comment">// 创建两个 ReferenceCountingGC 对象</span>
    <span class="token class-name">ReferenceCountingGC</span> a <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">ReferenceCountingGC</span><span class="token punctuation">(</span><span class="token string">"沉默王二"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token class-name">ReferenceCountingGC</span> b <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">ReferenceCountingGC</span><span class="token punctuation">(</span><span class="token string">"沉默王三"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

    <span class="token comment">// 使 a 和 b 相互引用</span>
    a<span class="token punctuation">.</span>instance <span class="token operator">=</span> b<span class="token punctuation">;</span>
    b<span class="token punctuation">.</span>instance <span class="token operator">=</span> a<span class="token punctuation">;</span>

    <span class="token comment">// 将 a 和 b 设置为 null</span>
    a <span class="token operator">=</span> <span class="token keyword">null</span><span class="token punctuation">;</span>
    b <span class="token operator">=</span> <span class="token keyword">null</span><span class="token punctuation">;</span>

    <span class="token comment">// 这个位置是垃圾回收的触发点</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><figure><figcaption>alt text</figcaption></figure>
<h4>可达性分析算法</h4>
<p>可达性分析算法（Reachability Analysis）的基本思路是：</p>
<p>通过 GC Roots 作为起点，然后向下搜索，搜索走过的路径被称为 Reference Chain（引用链）</p>
<p>当一个对象到 GC Roots 之间没有任何引用相连时，即从 GC Roots 到该对象节点不可达，则证明该对象是需要垃圾收集的。</p>
<blockquote>
<p>目前比较主流的编程语言（包括Java），一般都会使用可达性分析算法来判断对象是否存活，它采用了类似于树结构的搜索机制</p>
</blockquote>
<figure><figcaption>alt text</figcaption></figure>
<p>通过可达性算法，成功解决了引用计数无法解决的问题-“循环依赖”，只要你无法与 GC Root 建立直接或间接的连接，系统就会判定你为可回收对象</p>
<figure><figcaption>alt text</figcaption></figure>
<p>所谓的 GC Roots，就是一组必须活跃的引用，不是对象，它们是程序运行时的起点，是一切引用链的源头。在 Java 中，GC Roots 包括以下几种：</p>
<ul>
<li>虚拟机栈中的引用（方法的参数、局部变量等）</li>
<li>本地方法栈中 JNI 的引用</li>
<li>类静态变量</li>
<li>运行时常量池中的常量（String 或 Class 类型）</li>
</ul>
<h5>虚拟机栈中的引用（方法的参数、局部变量等）</h5>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">StackReference</span> <span class="token punctuation">{</span>
  <span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">greet</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token class-name">Object</span> localVar <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">Object</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> 
    <span class="token comment">// 这里的 localVar 是一个局部变量，存在于虚拟机栈中</span>
    <span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span>localVar<span class="token punctuation">.</span><span class="token function">toString</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token punctuation">}</span>

  <span class="token keyword">public</span> <span class="token keyword">static</span> <span class="token keyword">void</span> <span class="token function">main</span><span class="token punctuation">(</span><span class="token class-name">String</span><span class="token punctuation">[</span><span class="token punctuation">]</span> args<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">new</span> <span class="token class-name">StackReference</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">greet</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><p>在 greet 方法中，localVar 是一个局部变量，存在于虚拟机栈中，可以被认为是 GC Roots</p>
<p>在 greet 方法执行期间，localVar 引用的对象是活跃的，因为它是从 GC Roots 可达的</p>
<p>当 greet 方法执行完毕后，localVar 的作用域结束，localVar 引用的 Object 对象不再由任何 GC Roots 引用（假设没有其他引用指向这个对象），因此它将有资格作为垃圾被回收掉</p>
<h5>本地方法栈中 JNI 的引用</h5>
<h5>类静态变量</h5>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">StaticFieldReference</span> <span class="token punctuation">{</span>
  <span class="token keyword">private</span> <span class="token keyword">static</span> <span class="token class-name">Object</span> staticVar <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">Object</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 类静态变量</span>

  <span class="token keyword">public</span> <span class="token keyword">static</span> <span class="token keyword">void</span> <span class="token function">main</span><span class="token punctuation">(</span><span class="token class-name">String</span><span class="token punctuation">[</span><span class="token punctuation">]</span> args<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span>staticVar<span class="token punctuation">.</span><span class="token function">toString</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><p>StaticFieldReference 类中的 staticVar 引用了一个 Object 对象，这个引用存储在元空间，可以被认为是 GC Roots</p>
<p>只要 StaticFieldReference 类未被卸载，staticVar 引用的对象都不会被垃圾回收</p>
<p>如果 StaticFieldReference 类被卸载（这通常发生在其类加载器被垃圾回收时），那么 staticVar 引用的对象也将有资格被垃圾回收（如果没有其他引用指向这个对象）</p>
<h5>运行时常量池中的常量</h5>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">ConstantPoolReference</span> <span class="token punctuation">{</span>
  <span class="token keyword">public</span> <span class="token keyword">static</span> <span class="token keyword">final</span> <span class="token class-name">String</span> <span class="token constant">CONSTANT_STRING</span> <span class="token operator">=</span> <span class="token string">"Hello, World"</span><span class="token punctuation">;</span> <span class="token comment">// 常量，存在于运行时常量池中</span>
  <span class="token keyword">public</span> <span class="token keyword">static</span> <span class="token keyword">final</span> <span class="token class-name">Class</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token operator">?</span><span class="token punctuation">&gt;</span></span> <span class="token constant">CONSTANT_CLASS</span> <span class="token operator">=</span> <span class="token class-name">Object</span><span class="token punctuation">.</span><span class="token keyword">class</span><span class="token punctuation">;</span> <span class="token comment">// 类类型常量</span>

  <span class="token keyword">public</span> <span class="token keyword">static</span> <span class="token keyword">void</span> <span class="token function">main</span><span class="token punctuation">(</span><span class="token class-name">String</span><span class="token punctuation">[</span><span class="token punctuation">]</span> args<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span><span class="token constant">CONSTANT_STRING</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span><span class="token constant">CONSTANT_CLASS</span><span class="token punctuation">.</span><span class="token function">getName</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><p>在 ConstantPoolReference 中，CONSTANT_STRING 和 CONSTANT_CLASS 作为常量存储在运行时常量池。它们可以用来作为 GC Roots。</p>
<p>这些常量引用的对象（字符串"Hello, World"和 Object.class 类对象）在常量池中，只要包含这些常量的 ConstantPoolReference 类未被卸载，这些对象就不会被垃圾回收</p>
<h5>如何识别 GC Root</h5>
<div class="language-plain" data-ext="plain" data-title="plain"><pre class="language-plain"><code>┌────────────────────────────────────────────────────────┐
│                   GC 开始时                             │
├────────────────────────────────────────────────────────┤
│                                                        │
│   1. 暂停所有线程                  │
│                                                        │
│   2. 扫描所有线程栈                                     │
│      ┌─────────┐                                       │
│      │ 线程1栈  │ → 找到所有局部变量、参数 → 标记为 GC Root │
│      │ 线程2栈  │                                       │
│      │ ...     │                                       │
│      └─────────┘                                       │
│                                                        │
│   3. 扫描元空间                                         │
│      ┌─────────────────┐                               │
│      │ 类的静态变量      │ → 标记为 GC Root               │
│      │ 运行时常量池常量  │ → 标记为 GC Root               │
│      └─────────────────┘                               │
│                                                        │
│   4. 从 GC Root 开始遍历引用链                          │
│      GC Root → 对象A → 对象B → 对象C                    │
│              ↘ 对象D → 对象E                           │
│                                                        │
│   5. 标记所有可达对象 = 存活                            │
│      未被标记的对象 = 垃圾                              │
│                                                        │
└────────────────────────────────────────────────────────┘
</code></pre></div>]]></content:encoded>
    </item>
    <item>
      <title>JVM 八股9 - 垃圾收集2 (STW + 垃圾收集算法)</title>
      <link>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JVM%20-%20%E5%85%AB%E8%82%A1/article9.html</link>
      <guid>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JVM%20-%20%E5%85%AB%E8%82%A1/article9.html</guid>
      <source url="http://ekkosonya.cn/rss.xml">JVM 八股9 - 垃圾收集2 (STW + 垃圾收集算法)</source>
      <description>STW &amp;quot;Stop The World&amp;quot;是 Java 垃圾收集中的一个重要概念。在垃圾收集过程中，JVM 会暂停所有的用户线程，这种暂停被称为&amp;quot;Stop The World&amp;quot;事件 这么做的主要原因是为了防止在垃圾收集过程中，用户线程修改了堆中的对象，导致垃圾收集器无法准确地收集垃圾 值得注意的是，&amp;quot;Stop The World&amp;quot;事件会对 Java 应用...</description>
      <category>code</category>
      <pubDate>Mon, 30 Mar 2026 16:21:54 GMT</pubDate>
      <content:encoded><![CDATA[<h2>STW</h2>
<p>"Stop The World"是 Java 垃圾收集中的一个重要概念。在垃圾收集过程中，JVM 会暂停所有的用户线程，这种暂停被称为"Stop The World"事件</p>
<p>这么做的主要原因是为了防止在垃圾收集过程中，用户线程修改了堆中的对象，导致垃圾收集器无法准确地收集垃圾</p>
<p>值得注意的是，"Stop The World"事件会对 Java 应用的性能产生影响</p>
<p>如果停顿时间过长，就会导致应用的响应时间变长，对于对实时性要求较高的应用，如交易系统、游戏服务器等，这种情况是不能接受的。</p>
<p>因此，在选择和调优垃圾收集器时，需要考虑其停顿时间。Java 中的一些垃圾收集器，如 G1 和 ZGC，都会尽可能地减少了"Stop The World"的时间，通过并发的垃圾收集，提高应用的响应性能。</p>
<p>总的来说，"Stop The World"是 Java 垃圾收集中必须面对的一个挑战，其目标是在保证内存的有效利用和应用的响应性能之间找到一个平衡。</p>
<div class="language-plain" data-ext="plain" data-title="plain"><pre class="language-plain"><code>┌────────────────────────────────────────────────────────┐
│                   GC 开始时                             │
├────────────────────────────────────────────────────────┤
│                                                        │
│   1. 暂停所有线程                  │
│                                                        │
│   2. 扫描所有线程栈                                     │
│      ┌─────────┐                                       │
│      │ 线程1栈  │ → 找到所有局部变量、参数 → 标记为 GC Root │
│      │ 线程2栈  │                                       │
│      │ ...     │                                       │
│      └─────────┘                                       │
│                                                        │
│   3. 扫描元空间                                         │
│      ┌─────────────────┐                               │
│      │ 类的静态变量      │ → 标记为 GC Root               │
│      │ 运行时常量池常量  │ → 标记为 GC Root               │
│      └─────────────────┘                               │
│                                                        │
│   4. 从 GC Root 开始遍历引用链                          │
│      GC Root → 对象A → 对象B → 对象C                    │
│              ↘ 对象D → 对象E                           │
│                                                        │
│   5. 标记所有可达对象 = 存活                            │
│      未被标记的对象 = 垃圾                              │
│                                                        │
└────────────────────────────────────────────────────────┘
</code></pre></div><h2>垃圾收集算法</h2>
<h2>垃圾收集器</h2>
]]></content:encoded>
    </item>
    <item>
      <title>JVM 八股1 - 整体架构</title>
      <link>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JVM%20-%20%E5%85%AB%E8%82%A1/article1.html</link>
      <guid>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JVM%20-%20%E5%85%AB%E8%82%A1/article1.html</guid>
      <source url="http://ekkosonya.cn/rss.xml">JVM 八股1 - 整体架构</source>
      <description>JVM 学习 JVM 可以帮助我们开发者更好地优化程序性能、避免内存问题 了解 JVM 的内存模型和垃圾回收机制，可以帮助我们更合理地配置内存、减少 GC 停顿 掌握 JVM 的类加载机制可以帮助我们排查类加载冲突或异常 JVM 还提供了很多调试和监控工具，可以帮助我们分析内存和线程的使用情况，从而解决内存溢出内存泄露等问题 所以主要内容：内存模型、垃...</description>
      <category>code</category>
      <pubDate>Sun, 29 Mar 2026 02:48:57 GMT</pubDate>
      <content:encoded><![CDATA[<h2>JVM</h2>
<p>学习 JVM 可以帮助我们开发者更好地优化程序性能、避免内存问题</p>
<ul>
<li>了解 JVM 的内存模型和垃圾回收机制，可以帮助我们更合理地配置内存、减少 GC 停顿</li>
<li>掌握 JVM 的类加载机制可以帮助我们排查类加载冲突或异常</li>
<li>JVM 还提供了很多调试和监控工具，可以帮助我们分析内存和线程的使用情况，从而解决内存溢出内存泄露等问题</li>
</ul>
<blockquote>
<p>所以主要内容：内存模型、垃圾回收机制、类加载机制、JVM 调试/监控 工具</p>
</blockquote>
<h3>什么是 JVM</h3>
<p>JVM，也就是 Java 虚拟机，它是 Java 实现跨平台的基石</p>
<p>程序运行之前，需要先通过编译器将 Java 源代码文件编译成 Java 字节码文件；</p>
<p>程序运行时，JVM 会对字节码文件进行逐行解释，翻译成机器码指令，并交给对应的操作系统去执行</p>
<figure><figcaption>alt text</figcaption></figure>
<p>这样就实现了 Java 一次编译，处处运行的特性</p>
<h4>执行 Hello world 的流程</h4>
<p>当我们写好一个 HelloWorld 程序，编译成 .class 文件后，执行 <code>java HelloWorld</code> 这条命令时</p>
<p>操作系统会创建一个 JVM 进程，JVM 进程启动起来后，会先初始化运行环境，包括<strong>创建类加载器</strong>、<strong>初始化内存空间</strong>、<strong>启动垃圾回收线程</strong>等等</p>
<p>接下来，JVM 的类加载器会去找 <code>HelloWorld.class</code> 这个文件，读取它的字节码内容</p>
<p>JVM 的执行引擎会将这些字节码翻译成机器码，有两种方式</p>
<ul>
<li>第一种是解释执行
<ul>
<li>JVM 的解释器会逐条读取字节码指令，然后把它翻译成机器码执行</li>
<li>这个过程是动态的，每次执行都要翻译一遍。所以解释执行的速度相对较慢</li>
</ul>
</li>
<li>第二种是即时编译
<ul>
<li>JVM 里有一个 JIT 编译器，他发现某段代码被频繁执行（比如循环里的代码或者热点方法）时，JIT 编译器就会把这段字节码<strong>直接编译成本地机器码</strong>并缓存到 codeCache 中</li>
<li>下次执行的时候不用再一行一行的解释，而是直接执行缓存后的机器码，执行效率会大幅提高</li>
</ul>
</li>
</ul>
<p>至于执行的时机</p>
<p>解释器的执行是立即的，当 JVM 启动后，就开始解释执行字节码了</p>
<p>JIT 编译器的执行时机是延后的，它<strong>需要先监控哪些代码是热点代码</strong>（通常需要执行数千次或者数万次），然后才会启动编译</p>
<blockquote>
<p>操作系统层面，JVM 进程是一个执行单元，由操作系统调度执行</p>
</blockquote>
<h4>JVM 特性</h4>
<ul>
<li>JVM 可以自动管理内存，通过垃圾回收器回收不再使用的对象并释放内存空间</li>
<li>JVM 包含一个即时编译器 JIT，它可以在运行时将热点代码缓存到 codeCache 中，下次执行的时候不用再一行一行的解释，而是直接执行缓存后的机器码，执行效率会大幅提高</li>
<li>任何可以通过 Java 编译的语言，比如说 Groovy、Kotlin、Scala 等，都可以在 JVM 上运行</li>
</ul>
<h4>JAVA 是解释性语言还是编译性语言</h4>
<p>| <strong>特性</strong> | <strong>编译型语言（如C/C++）</strong> | <strong>解释型语言（如Python、JavaScript）</strong> |
|</p>
]]></content:encoded>
    </item>
    <item>
      <title>JVM 八股2 - 内存管理1 (内存区介绍)</title>
      <link>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JVM%20-%20%E5%85%AB%E8%82%A1/article2.html</link>
      <guid>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JVM%20-%20%E5%85%AB%E8%82%A1/article2.html</guid>
      <source url="http://ekkosonya.cn/rss.xml">JVM 八股2 - 内存管理1 (内存区介绍)</source>
      <description>JVM 启动后，大致可以分为三个功能区：类加载器、运行时数据区以及执行引擎 内存管理 内存区其实是功能区中&amp;quot;运行时数据区&amp;quot;的具体细分 JVM 运行后，按照 Java 虚拟机规范，JVM 的内存区域可以细分为程序计数器、虚拟机栈、本地方法栈、堆和方法区 alt textalt text 其中方法区和堆是线程共享的，虚拟机栈、本地方法栈和程序计数器是线程私...</description>
      <category>code</category>
      <pubDate>Sun, 29 Mar 2026 02:48:57 GMT</pubDate>
      <content:encoded><![CDATA[<p>JVM 启动后，大致可以分为三个功能区：类加载器、运行时数据区以及执行引擎</p>
<h2>内存管理</h2>
<blockquote>
<p>内存区其实是功能区中"运行时数据区"的具体细分</p>
</blockquote>
<div class="language-plain" data-ext="plain" data-title="plain"><pre class="language-plain"><code>JVM（功能区视角）
│
├── 类加载器
│
├── 运行时数据区  ←──── 这部分就是"内存区"
│   ├── 程序计数器
│   ├── 虚拟机栈
│   ├── 本地方法栈
│   ├── 堆
│   └── 方法区
│
└── 执行引擎
    ├── JIT 编译器
    └── 垃圾回收器
</code></pre></div><p>JVM 运行后，按照 Java 虚拟机规范，JVM 的内存区域可以细分为<code>程序计数器</code>、<code>虚拟机栈</code>、<code>本地方法栈</code>、<code>堆</code>和<code>方法区</code></p>
<figure><figcaption>alt text</figcaption></figure>
<p>其中方法区和堆是线程共享的，虚拟机栈、本地方法栈和程序计数器是线程私有的</p>
<ul>
<li>方法区和堆：线程共享，随 JVM 创建和销毁</li>
<li>虚拟机栈、本地方法栈和程序计数器：线程隔离，随线程启动创建，结束后销毁</li>
</ul>
<blockquote>
<p>JVM 本质是一个 C/C++ 程序，它向操作系统申请内存，然后自己管理这些内存，划分成不同区域给 Java 程序用</p>
</blockquote>
<h3>程序计数器</h3>
<p>程序计数器也被称为 PC 寄存器，是一块较小的内存空间</p>
<p>它可以看作是当前线程所执行的字节码行号指示器</p>
<h3>虚拟机栈</h3>
<p>Java 虚拟机栈的生命周期与线程相同</p>
<p>当线程执行一个方法时，会创建一个对应的栈帧</p>
<p>用于存储局部变量表、操作数栈、动态链接、方法出口等信息，然后栈帧会被压入虚拟机栈中</p>
<blockquote>
<p>栈帧里会保存一个方法执行时需要的核心信息：变量放哪、计算怎么做、调用其他方法时怎么定位、执行完后回到哪里</p>
</blockquote>
<p>当方法执行完毕后，栈帧会从虚拟机栈中移除</p>
<figure><figcaption>alt text</figcaption></figure>
<h4>动态链接</h4>
<p>指的是 Java 方法调用时，怎么从符号引用定位到实际方法，保存了对运行时常量池中方法符号引用的关联，方便在方法调用时把“符号引用”解析成“实际要执行的方法入口”</p>
<h4>操作数栈</h4>
<p>是方法执行时做计算的临时工作区</p>
<p>比如：</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">int</span> c <span class="token operator">=</span> a <span class="token operator">+</span> b<span class="token punctuation">;</span>
</code></pre></div><p>JVM 不是直接写成“把 a 和 b 相加赋值给 c”，而是类似这样执行</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code>iload_1   <span class="token comment">// 把 a 压入操作数栈</span>
iload_2   <span class="token comment">// 把 b 压入操作数栈</span>
iadd      <span class="token comment">// 弹出栈顶两个数，相加，再把结果压回栈</span>
istore_3  <span class="token comment">// 把结果存到局部变量表的 c</span>
</code></pre></div><h4>栈帧</h4>
<p>栈帧是虚拟机栈中的基本单元，每个方法调用对应一个栈帧：</p>
<div class="language-plain" data-ext="plain" data-title="plain"><pre class="language-plain"><code>虚拟机栈（线程私有）
│
├── 栈帧3（方法C正在执行）  ←── 栈顶
│   ├── 局部变量表
│   ├── 操作数栈
│   ├── 动态链接
│   └── 方法返回地址
│
├── 栈帧2（方法B暂停中）
│   ├── 局部变量表
│   ├── 操作数栈
│   ├── 动态链接
│   └── 方法返回地址
│
└── 栈帧1（方法A暂停中）  ←── 栈底
    ├── 局部变量表
    ├── 操作数栈
    ├── 动态链接
    └── 方法返回地址
</code></pre></div><h4>一个什么都没有的空方法，空的参数都没有，那局部变量表里有没有变量</h4>
<p>对于静态方法，由于不需要访问实例对象 this，因此在局部变量表中不会有任何变量。</p>
<p>对于非静态方法，即使是一个完全空的方法，局部变量表中也会有一个用于存储 this 引用的变量。this 引用指向当前实例对象，在方法调用时被隐式传入</p>
<h3>本地方法栈</h3>
<p>本地方法栈与虚拟机栈相似</p>
<p>区别在于虚拟机栈是为 JVM 执行 Java 编写的方法服务的</p>
<p>而本地方法栈是为 Java 调用本地 native 方法服务的，通常由 C/C++ 编写</p>
<p>在本地方法栈中，主要存放了 native 方法的局部变量、动态链接和方法出口等信息</p>
<p>当一个 Java 程序调用一个 native 方法时，JVM 会切换到本地方法栈来执行这个方法</p>
<h4>本地方法栈的运行场景</h4>
<p>当 Java 应用需要与操作系统底层或硬件交互时，通常会用到本地方法栈。</p>
<p>比如调用操作系统的特定功能，如内存管理、文件操作、系统时间、系统调用等。</p>
<p>比如获取系统时间的 <code>System.currentTimeMillis()</code> 方法就是调用本地方法，来获取操作系统当前时间的</p>
<p>再比如 JVM 自身的一些底层功能也需要通过本地方法来实现。</p>
<p>像 Object 类中的 <code>hashCode()</code> 方法、<code>clone()</code> 方法等</p>
<h3>java 堆</h3>
<p>堆是 JVM 中最大的一块内存区域，被所有线程共享，在 JVM 启动时创建，主要用来存储 new 出来的对象</p>
<blockquote>
<p>存放 new 出来的对象</p>
<p>“堆”至少有两个常见概念：一个是数据结构里的堆（通常是完全二叉树），一个是内存管理里的堆（动态分配内存的区域）；JVM 说的堆指的是后者</p>
<p>与规规矩矩、先进后出的“栈（Stack）”不同，堆内存是一大片相对杂乱的空间。你可以随时在里面申请一块内存，用完后再释放。它的分配和释放顺序是随意的</p>
</blockquote>
<p>Java 中“几乎”所有的对象都会在堆中分配，堆也是垃圾收集器管理的目标区域</p>
<figure><figcaption>alt text</figcaption></figure>
<p>从内存回收的角度来看，由于垃圾收集器大部分都是基于分代收集理论设计的，所以堆又被细分为</p>
<ul>
<li>新生代
<ul>
<li>Eden空间</li>
<li>From Survivor空间</li>
<li>To Survivor空间</li>
</ul>
</li>
<li>老年代</li>
</ul>
<figure><figcaption>alt text</figcaption></figure>
<blockquote>
<p>从 JDK 7 开始，JVM 默认开启了逃逸分析，意味着如果某些方法中的对象引用没有被返回或者没有在方法体外使用，也就是未逃逸出去，那么对象可以直接在栈上分配内存</p>
</blockquote>
<h4>堆与栈的区别</h4>
<p>堆属于线程共享的内存区域，几乎所有 new 出来的对象都会堆上分配，生命周期不由单个方法调用所决定，可以在方法调用结束后继续存在，直到不再被任何变量引用，最后被垃圾收集器回收。</p>
<p>栈属于线程私有的内存区域，主要存储局部变量、方法参数、对象引用等，通常随着方法调用的结束而自动释放，不需要垃圾收集器处理。</p>
<h3>方法区</h3>
<p>方法区并不真实存在，属于 Java 虚拟机规范中的一个逻辑概念</p>
<p>用于存储已被 JVM 加载的类信息、常量、静态变量、即时编译器编译后的代码缓存等</p>
<p>在 HotSpot 虚拟机中</p>
<p>方法区的实现称为永久代 PermGen</p>
<p>但在 Java 8 及之后的版本中，已经被元空间 Metaspace 所替代</p>
<p>在Java虚拟机（JVM）中，类的元数据是存放在方法区的，而Class对象本身并不存放在方法区，而是存放在堆内存中</p>
<blockquote>
<p>当类加载器（ClassLoader）将 .class 文件加载到内存后，JVM 会提取其中的类信息</p>
<p>并将其转化为 JVM 内部的数据结构（在 HotSpot 虚拟机中称为 Klass 模型，比如 InstanceKlass）存储在方法区中</p>
</blockquote>
<p>当你执行 MyClass obj = new MyClass(); 时，发生了什么？</p>
<ul>
<li>查找： JVM 首先去方法区查找是否有 MyClass 的信息。</li>
<li>加载： 如果没有，类加载器读取 MyClass.class 文件。</li>
<li>存入方法区： JVM 解析字节码，把类的结构信息（方法、字段等）塞进方法区。</li>
<li>生成 Class 对象： JVM 紧接着在堆区中创建一个 java.lang.Class 类型的对象，代表 MyClass，并让它指向方法区里的那些数据。</li>
<li>实例化： 最后，JVM 根据方法区里的内存布局信息，在堆区为 obj 分配内存，并完成初始化。</li>
</ul>
<h4>作用</h4>
<p>方法区是JVM内存模型的一部分，主要用于存储以下内容：</p>
<p>类的元数据：包括类的名称、父类、修饰符、接口列表等。</p>
<ul>
<li>常量池：存储编译时的字面量和符号引用。</li>
<li>静态变量：类的静态字段。</li>
<li>方法字节码：类的方法代码和相关信息。</li>
</ul>
<p>方法区是所有线程共享的区域，JVM在加载类时会将类的元数据存储到方法区中，以便在运行时使用。</p>
<h4>Class 对象存储位置</h4>
<p>当JVM加载一个类时，会在堆内存中创建一个对应的<code>java.lang.Class</code>对象</p>
<p>这个Class对象是类的运行时表示，开发者可以通过反射机制访问类的详细信息，例如字段、方法和注解等</p>
<blockquote>
<p>方法区存储类的元数据，而Class对象是对这些元数据的引用。通过Class对象，开发者可以访问方法区中的类信息，例如字段、方法和常量池</p>
</blockquote>
<h3>变量的存放位置</h3>
<p>对于局部变量，它存储在当前方法栈帧中的局部变量表中。</p>
<p>当方法执行完毕，栈帧被回收，局部变量也会被释放</p>
<p>对于静态变量来说，它存储在 Java 虚拟机规范中的方法区中，在 Java 7 中是永久带，在 Java8 及以后 是元空间</p>
<h3>JVM 如何执行一个 Java 方法</h3>
<p>从 java Xxx 命令开始，操作系统会创建一个 JVM 进程；</p>
<p>JVM 进程中的线程执行 Java 方法时，每次方法调用都会在对应线程的虚拟机栈中创建一个栈帧，方法的参数、局部变量和计算过程都围绕这个栈帧展开</p>
<h4>方法执行和虚拟机栈的关系</h4>
<ul>
<li>每个线程都有自己的虚拟机栈</li>
<li>当线程调用一个方法时，会为该方法创建一个栈帧并压栈</li>
<li>方法执行结束后，栈帧出栈</li>
</ul>
<h4>栈帧里有什么</h4>
<ul>
<li>局部变量表</li>
<li>操作数栈</li>
<li>动态链接</li>
<li>方法返回地址</li>
</ul>
<h4>方法中的变量怎么访问</h4>
<ul>
<li>局部变量和参数存放在局部变量表中</li>
<li>JVM 不是按变量名访问，而是按 slot 槽位访问</li>
<li>槽位数量对某个方法来说在编译后就基本确定了，且可能发生复用</li>
</ul>
<h4>一次方法调用的本质</h4>
<ul>
<li>创建栈帧</li>
<li>参数入局部变量表</li>
<li>字节码指令操作局部变量表和操作数栈</li>
<li>执行结束后返回结果并弹出栈帧</li>
</ul>
<h4>思考</h4>
<p>方法执行时，JVM 总是操作当前线程虚拟机栈的栈顶栈帧；局部变量通过当前栈帧中的局部变量表按 slot 下标访问，而不是按变量名查找。</p>
<p>JVM 看的是 字节码</p>
<p>所以“找局部变量”这件事，本质是：</p>
<ul>
<li>当前线程拿到栈顶栈帧</li>
<li>JVM 执行当前字节码</li>
<li>字节码里写着访问哪个 slot</li>
<li>直接去局部变量表对应位置拿</li>
</ul>
<p>“当前方法”怎么知道自己对应哪个栈帧</p>
<p>因为线程的虚拟机栈本来就是：</p>
<ul>
<li>谁在执行，谁就在栈顶</li>
<li>当前正在执行的方法，对应的就是栈顶栈帧</li>
</ul>
<p>当前方法不需要“满栈找自己的栈帧”，因为它本来就在栈顶，JVM 执行时天然就拿的是当前栈顶帧。</p>
<h5>例子</h5>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">public</span> <span class="token keyword">int</span> <span class="token function">add</span><span class="token punctuation">(</span><span class="token keyword">int</span> a<span class="token punctuation">,</span> <span class="token keyword">int</span> b<span class="token punctuation">)</span> <span class="token punctuation">{</span>
  <span class="token keyword">int</span> c <span class="token operator">=</span> a <span class="token operator">+</span> b<span class="token punctuation">;</span>
  <span class="token keyword">return</span> c<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre></div><p>这个方法执行时，栈帧里可能是这样：</p>
<div class="language-plain" data-ext="plain" data-title="plain"><pre class="language-plain"><code>当前栈帧
├── 局部变量表
│   ├── slot0 = this
│   ├── slot1 = a
│   ├── slot2 = b
│   └── slot3 = c
├── 操作数栈
├── 动态链接
└── 返回地址
</code></pre></div><p>然后字节码大概会做这种事：</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code>iload_1    <span class="token comment">// 取 slot1，也就是 a</span>
iload_2    <span class="token comment">// 取 slot2，也就是 b</span>
iadd       <span class="token comment">// 相加</span>
istore_3   <span class="token comment">// 把结果放到 slot3，也就是 c</span>
iload_3    <span class="token comment">// 取 c</span>
ireturn    <span class="token comment">// 返回</span>
</code></pre></div>]]></content:encoded>
    </item>
    <item>
      <title>JVM 八股3 - 内存管理2 (native 介绍 | OOM 讨论)</title>
      <link>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JVM%20-%20%E5%85%AB%E8%82%A1/article3.html</link>
      <guid>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JVM%20-%20%E5%85%AB%E8%82%A1/article3.html</guid>
      <source url="http://ekkosonya.cn/rss.xml">JVM 八股3 - 内存管理2 (native 介绍 | OOM 讨论)</source>
      <description>JVM3 当 Java 应用需要与操作系统底层或硬件交互时，通常会用到本地方法栈。 native 方法 native 方法是在 Java 中通过 native 关键字声明的，用于调用非 Java 语言，如 C/C++ 编写的代码。Java 可以通过 JNI，也就是 Java Native Interface 与底层系统、硬件设备、或者本地库进行交互 J...</description>
      <category>code</category>
      <pubDate>Sun, 29 Mar 2026 02:48:57 GMT</pubDate>
      <content:encoded><![CDATA[<h2>JVM3</h2>
<p>当 Java 应用需要与操作系统底层或硬件交互时，通常会用到本地方法栈。</p>
<h3>native 方法</h3>
<p>native 方法是在 Java 中通过 native 关键字声明的，用于调用非 Java 语言，如 C/C++ 编写的代码。Java 可以通过 JNI，也就是 Java Native Interface 与底层系统、硬件设备、或者本地库进行交互</p>
<p>Java 的 native 方法本身没有 Java 代码实现，而是交给本地代码实现</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">public</span> <span class="token keyword">native</span> <span class="token keyword">void</span> <span class="token function">test</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre></div><p>这个方法真正的逻辑可能写在 C/C++ 里，然后编译成一个 .dll。</p>
<p>Java 运行时通过：</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token class-name">System</span><span class="token punctuation">.</span><span class="token function">loadLibrary</span><span class="token punctuation">(</span><span class="token string">"xxxx"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre></div><p>把这个动态库加载进来，再去调用里面对应的本地函数</p>
<p>你可以把它想成：</p>
<ul>
<li>Java 代码：负责声明“我要调用这个方法”</li>
<li>.dll：真正存放底层实现</li>
<li>JVM：负责把 .dll 加载进来并完成调用</li>
</ul>
<p>不同系统名字不一样：</p>
<ul>
<li>Windows：.dll</li>
<li>Linux：.so</li>
<li>macOS：.dylib</li>
</ul>
<h4>JNI (Java Native Interface)</h4>
<p>一般情况下，我们完全可以使用 Java 语言编写程序，但某些情况下，Java 可能满足不了需求，或者不能更好的满足需求，比如：</p>
<ul>
<li>标准的 Java 类库不支持。</li>
<li>我们已经用另一种语言，比如说 C/C++ 编写了一个类库，如何用 Java 代码调用呢？</li>
<li>某些运行次数特别多的方法，为了加快性能，需要用更接近硬件的语言（比如汇编）编写</li>
</ul>
<p>上面这三种需求，说到底就是如何用 Java 代码调用不同语言编写的代码。那么 JNI 应运而生了。</p>
<p>从 Java 1.1 开始，Java Native Interface (JNI)标准就成为 Java 平台的一部分，它允许 Java 代码和其他语言编写的代码进行交互</p>
<p>JNI 一开始是为了本地已编译语言，尤其是 C 和 C++而设计的，但是它并不妨碍你使用其他语言，只要调用约定受支持就可以了</p>
<p>使用 Java 与本地已编译的代码交互，通常会<strong>丧失平台可移植性</strong>，但是，有些情况下这样做是可以接受的，甚至是必须的</p>
<p>比如，使用一些旧的库，与硬件、操作系统进行交互，或者为了提高程序的性能。JNI 标准至少保证本地代码能工作能在任何 Java 虚拟机实现下</p>
<p>通过 JNI，我们就可以通过 Java 程序（代码）调用到操作系统相关的技术实现的库函数，从而与其他技术和系统交互；同时其他技术和系统也可以通过 JNI 提供的相应原生接口调用 Java 应用系统内部实现的功能</p>
<h5>缺点</h5>
<ul>
<li>程序不再跨平台。要想跨平台，必须在不同的系统环境下重新编译本地语言部分。</li>
<li>程序不再是绝对安全的，本地代码的不当使用可能导致整个程序崩溃。一个通用规则是，你应该让本地方法集中在少数几个类当中。这样就降低了 Java 和 C/C++ 之间的耦合性。</li>
</ul>
<h4>具体使用</h4>
<ul>
<li>编写带有 native 方法的 Java 类，生成.java 文件；</li>
<li>使用 javac 命令编译所编写的 Java 类，生成.class 文件；</li>
<li>使用 javah -jni java 类名 生成扩展名为 h 的头文件，也即生成 .h 文件；</li>
<li>使用 C/C++（或者其他编程想语言）实现本地方法，创建 .h 文件的实现，也就是创建 .cpp 文件实现.h 文件中的方法；</li>
<li>将 C/C++ 编写的文件生成动态连接库，生成 dll 文件；</li>
</ul>
<blockquote>
<p>dll 是 Dynamic Link Library，中文一般叫 动态链接库。</p>
<p>在 Windows 里，它本质上是一个可被其他程序在运行时加载的二进制库文件，扩展名就是 .dll</p>
</blockquote>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token comment">// 只要 Java 语法没问题，就能编译通过。</span>
<span class="token comment">// 这时候 没有 hello.dll 也没关系。</span>
<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">HelloJNI</span> <span class="token punctuation">{</span>
  <span class="token keyword">static</span> <span class="token punctuation">{</span>
      <span class="token class-name">System</span><span class="token punctuation">.</span><span class="token function">loadLibrary</span><span class="token punctuation">(</span><span class="token string">"hello"</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 加载名为 libhello.dylib 的动态链接库 (dylib 是 macOS 的动态链接库)</span>
  <span class="token punctuation">}</span>

  <span class="token comment">// 定义本地方法</span>
  <span class="token keyword">private</span> <span class="token keyword">native</span> <span class="token keyword">void</span> <span class="token function">helloJNI</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

  <span class="token keyword">public</span> <span class="token keyword">static</span> <span class="token keyword">void</span> <span class="token function">main</span><span class="token punctuation">(</span><span class="token class-name">String</span><span class="token punctuation">[</span><span class="token punctuation">]</span> args<span class="token punctuation">)</span> <span class="token punctuation">{</span>
      <span class="token keyword">new</span> <span class="token class-name">HelloJNI</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">helloJNI</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 调用本地方法</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><ul>
<li>ative 方法声明：像是在 Java 里先留了个“接口”</li>
<li>dll：真正的实现</li>
<li>编译时只检查声明</li>
<li>运行时才检查实现</li>
</ul>
<h5>编译 <code>HelloJNI.java</code></h5>
<p>在命令行通过 <code>javac HelloJNI.java</code> 来编译源代码</p>
<p>执行完毕后，会在 <code>HelloJNI.java</code> 所在目录下生成一个名为 <code>HelloJNI.h</code> 的头文件</p>
<h5>使用 c语言实现本地方法</h5>
<p>创建一个 C 文件 HelloJNI.c，实现本地方法 sayHello</p>
<div class="language-c" data-ext="c" data-title="c"><pre class="language-c"><code><span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">include</span> <span class="token string">&lt;stdio.h&gt;</span></span>
<span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">include</span> <span class="token string">&lt;jni.h&gt;</span></span>
<span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">include</span> <span class="token string">"HelloJNI.h"</span></span>

JNIEXPORT <span class="token keyword">void</span> JNICALL <span class="token function">Java_HelloJNI_helloJNI</span><span class="token punctuation">(</span>JNIEnv <span class="token operator">*</span>env<span class="token punctuation">,</span> jobject obj<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token function">printf</span><span class="token punctuation">(</span><span class="token string">"Hello, JNI!\n"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">return</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre></div><p>注意，这里需要引入 JNI 头文件，并且实现的方法名称需要与在 Java 中声明的名称一致（HelloJNI_helloJNI HelloJNI 类的 helloJNI 方法）</p>
<h5>对应编译脚本</h5>
<div class="language-bash" data-ext="sh" data-title="sh"><pre class="language-bash"><code><span class="token shebang important">#!/bin/bash</span>

<span class="token comment"># 编译 HelloJNI.c 文件</span>
gcc -I<span class="token string">"<span class="token variable">$JAVA_HOME</span>/include"</span> -I<span class="token string">"<span class="token variable">$JAVA_HOME</span>/include/darwin"</span> <span class="token parameter variable">-shared</span> <span class="token parameter variable">-o</span> libhello.dylib HelloJNI.c

<span class="token comment"># 把生成的 libhello.dylib 文件拷贝到当前目录</span>
<span class="token function">cp</span> libhello.dylib <span class="token builtin class-name">.</span>
</code></pre></div><ul>
<li>$JAVA_HOME 是 JDK 的安装路径，需要根据实际情况修改。</li>
<li>在 macOS 上，动态链接库（hello）的后缀是 .dylib，而不是 Linux 上的 .so。</li>
</ul>
<p>这里的 -I 选项是为了告诉编译器头文件的位置，$JAVA_HOME 是 Java 安装目录的路径</p>
<p>之后执行即可</p>
<figure><figcaption>alt text</figcaption></figure>
<h3>OOM 分析</h3>
<p>JAVA OOM 后是抛出异常，如果没有自定义捕获，是被默认捕获，所以退出当前线程，那此时如果另一个线程尝试 new 会触发 OOM 吗</p>
<p>核心结论是：<strong>不一定。另一个线程尝试 <code>new</code> 对象时，可能会成功，也可能会再次触发 OOM，这完全取决于导致 OOM 的内存是否随着前一个线程的退出而被释放。</strong></p>
<p>我们需要先纠正一个小细节：<code>OutOfMemoryError</code> 属于 <code>Error</code>，而不是 <code>Exception</code>，但它在发生时确实会像异常一样沿着调用栈向外抛出。如果一直没有被捕获（无论是 <code>try-catch</code> 还是线程的 <code>UncaughtExceptionHandler</code>），该线程就会终止退出。</p>
<p>以下是具体的场景分析：</p>
<h4>场景一：另一个线程 <strong>不会</strong> 触发 OOM（分配成功）</h4>
<p>如果导致 OOM 的那个线程，是因为在自己的执行逻辑中创建了<strong>大量局部对象</strong>（例如读取了一个超大文件并存放在该线程方法内的 <code>List</code> 中）而把内存撑爆的。</p>
<ul>
<li><strong>发生什么：</strong> 当这个线程因为 OOM 异常退出并结束后，它对应的虚拟机栈（VM Stack）会被销毁。</li>
<li><strong>GC 的作用：</strong> 这个线程持有的那些局部变量（GC Roots）将不复存在。这就意味着，它之前疯狂创建的那些占用大量内存的对象，变成了**不可达（Unreachable）**状态。</li>
<li><strong>结果：</strong> 此时如果有其他线程触发了垃圾回收（GC），或者 JVM 自动进行了 GC，这些庞大的内存就会被回收掉。因此，另一个线程再去 <code>new</code> 对象时，堆内存已经有了足够的空间，<strong>不会触发 OOM</strong>。</li>
</ul>
<h4>场景二：另一个线程 <strong>会</strong> 再次触发 OOM</h4>
<p>如果导致 OOM 的原因是一个<strong>全局性的内存泄漏</strong>，或者大量对象被长生命周期的对象（如静态变量、全局缓存、Spring 单例 Bean 等）所引用。</p>
<ul>
<li><strong>发生什么：</strong> 那个不幸的线程仅仅是“压死骆驼的最后一根稻草”。它在尝试 <code>new</code> 一个很小的对象时，发现堆内存已经满了，于是抛出了 OOM 并退出了。</li>
<li><strong>GC 的作用：</strong> 即使这个线程死掉了，它所释放的局部变量内存微乎其微。真正霸占内存的那些对象依然被全局引用着（强引用），GC 根本无法回收它们。</li>
<li><strong>结果：</strong> 堆内存依然处于几乎被撑爆的状态。当另一个线程尝试 <code>new</code> 新对象时，依然没有足够的连续空间，于是<strong>会再次触发 OOM</strong>。</li>
</ul>
<h3>场景三：时间差（并发竞争）导致的 OOM</h3>
<p>即使符合“场景一”（内存是可以被回收的），但如果另一个线程在第一个线程刚刚抛出 OOM 且<strong>垃圾回收器（GC）还未来得及执行清理工作</strong>的瞬间去 <code>new</code> 对象，它依然会因为当前时刻堆内存不足而触发 OOM。</p>
]]></content:encoded>
    </item>
    <item>
      <title>JVM 八股4 - 内存管理3</title>
      <link>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JVM%20-%20%E5%85%AB%E8%82%A1/article4.html</link>
      <guid>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JVM%20-%20%E5%85%AB%E8%82%A1/article4.html</guid>
      <source url="http://ekkosonya.cn/rss.xml">JVM 八股4 - 内存管理3</source>
      <description>虚拟机栈、本地方法栈、程序计数器 | 堆、方法区 不同 JDK 下内存区域变化 主要是方法区实现的不同 方法区是 JVM 规范里的概念, JVM 需要有这么一块区域来存类相关信息 HotSpot 实现层面：曾经用永久代、后来用元空间来实现 JDK 1.6 使用永久代来实现方法区： alt textalt text JDK 1.7 时仍然是永久带，但发生...</description>
      <category>code</category>
      <pubDate>Sun, 29 Mar 2026 02:48:57 GMT</pubDate>
      <content:encoded><![CDATA[<p>虚拟机栈、本地方法栈、程序计数器 | 堆、方法区</p>
<h2>不同 JDK 下内存区域变化</h2>
<p>主要是方法区实现的不同</p>
<blockquote>
<p>方法区是 JVM 规范里的概念, JVM 需要有这么一块区域来存类相关信息</p>
<p>HotSpot 实现层面：曾经用永久代、后来用元空间来实现</p>
</blockquote>
<p>JDK 1.6 使用永久代来实现方法区：</p>
<figure><figcaption>alt text</figcaption></figure>
<p>JDK 1.7 时仍然是永久带，但发生了一些细微变化，比如将字符串常量池、静态变量存放到了堆上。</p>
<figure><figcaption>alt text</figcaption></figure>
<p>在 JDK 1.8 时，直接在内存中划出了一块区域，叫<strong>元空间</strong>，来取代之前放在 JVM 内存中的永久代，并将<strong>运行时常量池</strong>、<strong>类常量池</strong>都移动到了元空间</p>
<figure><figcaption>alt text</figcaption></figure>
<h3>为什么使用元空间替代永久代</h3>
<p>客观上，永久代会导致 Java 应用程序更容易出现内存溢出的问题，因为它要受到 JVM 内存大小的限制</p>
<p>HotSpot 虚拟机的永久代大小可以通过 -XX：MaxPermSize 参数来设置，32 位机器默认的大小为 64M，64 位的机器则为 85M</p>
<p>问题是类元数据这类东西的大小很难估计：</p>
<ul>
<li>类多</li>
<li>动态生成类多</li>
<li>框架多（Spring、Hibernate、代理类、CGLIB）</li>
<li>热部署/类加载器反复创建</li>
</ul>
<p>就很容易出现：<code>OutOfMemoryError: PermGen space</code></p>
<p>此外</p>
<blockquote>
<p>类元数据和 Java 对象放在一起不太合适</p>
</blockquote>
<p>永久代属于 HotSpot 里一块比较特殊的 JVM 内存区域, 但类的元数据本质上和普通 Java 对象不是一类东西，把它们塞在同一套受限空间里，管理起来不够自然</p>
<p>元空间不再使用 JVM 的堆内存，而是直接使用操作系统的本地内存（Native Memory）</p>
<p>这意味着，只要你的服务器物理内存足够大，元空间就可以一直动态扩展，基本告别了类元数据塞满导致的 OOM 问题</p>
<blockquote>
<p>降低调优困难</p>
</blockquote>
<p>在过去，预估一个复杂的企业级应用到底需要多大的永久代是非常困难的。它不仅取决于项目本身的代码量，还取决于引入了多少第三方 Jar 包，以及运行时会动态生成多少类。设置小了容易 OOM，设置大了又浪费堆内存。</p>
<p>使用本地内存的元空间后，JVM 会根据应用程序的需要自动分配内存，大大降低了开发者对这部分内存管理的负担。当然，为了防止元空间无限扩张吞噬掉所有物理内存导致系统死机，也可以通过 <code>-XX:MaxMetaspaceSize</code> 来设置一个上限</p>
<blockquote>
<p>提升垃圾回收（GC）的效率</p>
</blockquote>
<p>永久代的垃圾回收效率一直很低。在永久代中进行 GC（通常伴随着 Full GC）去清理废弃的类和类加载器，条件非常苛刻，而且经常清理不干净，导致长时间的停顿（STW）</p>
<p>将元数据移到堆外的元空间后，简化了 JVM 堆内存的垃圾回收流程，也方便了对类元数据进行独立的、更高效的管理</p>
<h4>什么是本地内存</h4>
<p>简单来说：</p>
<p>JVM 本质上就是一个由 C/C++ 编写的普通的操作系统进程。</p>
<p>当你在命令行敲下 java xxx 时，你其实是在启动一个名为 java 的 C++ 程序。</p>
<p>操作系统（如 Linux）会为这个进程分配一个巨大的虚拟内存地址空间, 在这个空间里，JVM 进程可以完全按照操作系统的规则来申请和使用内存</p>
<h5>Java 堆内存</h5>
<p>在 JVM 进程启动时，它会根据你设置的参数（比如 -Xms 和 -Xmx），通过底层 C++ 代码一次性向操作系统申请一大块连续的内存空间</p>
<ul>
<li><strong>机制</strong>：在 JVM 进程启动时，它会根据你设置的参数（比如 <code>Xms</code> 和 <code>Xmx</code>），通过底层 C++ 代码<strong>一次性</strong>向操作系统申请一大块连续的内存空间。</li>
<li><strong>角色</strong>：JVM 就像是一个“二房东”，把这块大内存承包下来后，自己在内部建立了一套复杂的机制（比如年轻代、老年代、垃圾回收器 GC）来分配和管理这块土地。</li>
<li><strong>用途</strong>：你在 Java 代码里 <code>new</code> 出来的各种对象，都是由 JVM 这个二房东分配在这块“承包地”里的。</li>
</ul>
<h5>本地内存 / 堆外内存</h5>
<p>本地内存（Native Memory），也叫堆外内存，指的是 JVM 进程的虚拟内存空间中，没有被划分给 Java 堆的那部分内存</p>
<ul>
<li><strong>机制</strong>：因为 JVM 底层是 C++ 写的，所以当它需要内存时，它完全可以绕过自己管理的“Java 堆”，直接调用 C/C++ 的标准库函数（比如 <code>malloc()</code>、<code>free()</code> 或者 <code>mmap()</code>）向操作系统（OS）动态申请内存。</li>
<li><strong>操作方式</strong>：当 JVM 需要加载一个新类时，底层的 C++ 代码会直接调用系统的内存分配接口，在 Java 堆之外的进程空间里切出一块内存，用来存放这个类的元数据（这就是<strong>元空间</strong>的由来）。</li>
<li><strong>特点</strong>：这部分内存<strong>不受 Java 堆大小（<code>Xmx</code>）的限制</strong>，也不由 Java 的常规垃圾回收器直接管理。它的上限主要受限于操作系统分配给该进程的物理内存和虚拟内存总大小。</li>
</ul>
<h5>总结</h5>
<p>当你启动 JVM 并加载大量类时，底层发生的事情是这样的：</p>
<ol>
<li><strong>操作系统</strong>：启动 <code>java</code> 进程，分配虚拟内存空间。</li>
<li><strong>JVM (C++层面)</strong>：调用 <code>malloc</code> 申请一块大内存作为 <strong>Java 堆</strong>。</li>
<li><strong>程序运行</strong>：需要加载一个新类（比如动态生成了一个代理类）。</li>
<li><strong>JVM (C++层面)</strong>：发现是类元数据，不放进 Java 堆。再次调用 <code>malloc</code>，在堆外的<strong>本地内存</strong>中分配一块空间（即元空间 Metaspace），把类信息存进去。</li>
</ol>
<p>所以，JVM 能够“直接操作本地内存”，是因为 JVM 本身是本地实现的用户态进程，可以通过操作系统提供的内存分配接口申请和管理进程地址空间内的内存</p>
<p>除了元空间，其实我们在写 Java 后端高并发应用时，Java 代码本身也可以主动去操作这块“本地内存”（堆外内存），这在提升网络 I/O 性能上非常有用</p>
<h2>对象创建过程</h2>
<p>当我们使用 new 关键字创建一个对象时，JVM 首先会检查 new 指令的参数是否能在常量池中定位到类的符号引用</p>
<p>然后检查这个符号引用代表的类是否已被加载、解析和初始化。</p>
<p>如果没有，就先执行类加载</p>
<p>如果已经加载，JVM 会为对象分配内存完成初始化，比如数值类型的成员变量初始值是 0，布尔类型是 false，对象类型是 null。</p>
<p>接下来会设置对象头，里面包含了对象是哪个类的实例、对象的哈希码、对象的 GC 分代年龄等信息。</p>
<p>最后，JVM 会执行构造方法 <code>&lt;init&gt;</code> 完成赋值操作，将成员变量赋值为预期的值，比如 int age = 18，这样一个对象就创建完成了。</p>
<figure><figcaption>alt text</figcaption></figure>
<h3>Gemini 解释流程</h3>
<h4>JVM 对象创建的第一步 —— 类加载检查</h4>
<p>当程序执行到 <code>new</code> 关键字准备创建对象时</p>
<p>例如 <code>User user = new User();</code></p>
<p>JVM 并不是立刻就在堆里划分一块内存，而是必须先经历一个<strong>验明正身</strong>的过程</p>
<p>具体分为以下三个逻辑步骤：</p>
<h5>1. 遇到 <code>new</code> 指令：拿着“号码牌”找线索</h5>
<p>在 Java 编译后的字节码中，<code>new User()</code> 并不是直接写着“分配 User 对象的内存”，而是会被编译成类似这样的指令：</p>
<div class="language-Java" data-ext="Java" data-title="Java"><pre class="language-Java"><code>new #2
dup
invokespecial #3   // 调用构造方法 &lt;init&gt;`
</code></pre></div><p>这里的 <code>#2</code> 就像是一个号码牌</p>
<p>JVM 执行到 <code>new #2</code> 时，会拿着这个号码，去当前类的<strong>运行时常量池</strong>中查找它到底代表什么</p>
<h5>2. 解析符号引用：把“名字”翻译成“目标”</h5>
<p>JVM 在常量池中找到 <code>#2</code> 对应的项后，会发现这只是一个<strong>符号引用</strong>。</p>
<ul>
<li><strong>什么是符号引用？</strong>
<ul>
<li>简单来说，它就是一个字符串字面量（比如记录着类名全路径：<code>com/example/User</code>）</li>
<li>因为在编译成 <code>.class</code> 文件时，编译器根本不知道 <code>User</code> 这个类将来会被加载到内存的哪个物理地址，所以只能先用一个“名字”来占位。</li>
</ul>
</li>
</ul>
<h5>3. 核心检查：这个类真的准备好了吗？（触发类加载检查）</h5>
<p>拿到 <code>com/example/User</code> 这个名字后，JVM 必须要确认一件事：<strong>我要实例化的这个类，现在有没有被加载到内存（元空间）中？</strong></p>
<ul>
<li><strong>为什么一定要检查？</strong> 因为 JVM 如果要给 <code>User</code> 对象分配内存，就必须提前知道这个对象到底需要占多大空间（比如它有几个 <code>int</code> 字段，几个引用字段）。而这些关于类结构的信息（元数据），只有在这个类被完整加载、解析后，才会存在于内存中。</li>
<li><strong>检查的结果：</strong>
<ul>
<li><strong>如果已加载：</strong> 常量池里的那个符号引用（字符串）早就已经被替换成了直接指向元空间中该类元数据的<strong>直接引用</strong>（内存指针）。JVM 知道了对象大小，就可以顺利进入下一步——分配内存。</li>
<li><strong>如果未加载：</strong> 说明这是程序第一次用到这个类。JVM 必须立刻暂停对象的创建，先触发这个类的<strong>类加载过程</strong>（加载 -&gt; 链接 -&gt; 初始化），把 <code>.class</code> 文件吃透，搞清楚类的结构，然后再回头继续对象的实例化。</li>
</ul>
</li>
</ul>
<h5>实际解析过程</h5>
<blockquote>
<p>阶段一：在常量池里的“套娃”解析</p>
</blockquote>
<p>刚才说到 <code>new #2</code>，这个 <code>#2</code> 只是一个索引</p>
<p>在 <code>.class</code> 文件的结构里，它并不会直接把字符串塞在 <code>#2</code> 的位置。</p>
<ol>
<li><strong>第一层寻找：</strong> JVM 找到常量池的第 2 项（<code>#2</code>），发现这是一个专门用来表示类的结构（叫 <code>CONSTANT_Class_info</code>）。</li>
<li><strong>第二层寻找：</strong> 这个结构里面本身没有存英文字母，而是存了另一个索引（假设是 <code>#15</code>），意思是“这个类的名字存在第 15 项那里”。</li>
<li><strong>拿到真名：</strong> JVM 再顺着找到常量池的第 15 项（<code>CONSTANT_Utf8_info</code>），这才真正拔出了 <code>com/example/User</code> 这个标准的 UTF-8 字符串。</li>
</ol>
<blockquote>
<p>阶段二：拿着名字去“查字典” （核心机制）</p>
</blockquote>
<p>拿到了 <code>com/example/User</code> 这个字符串后，JVM 怎么知道内存里有没有这个类呢？总不能把整个元空间遍历一遍吧？</p>
<p>这个时候，JVM 会去查一本它内部维护的“花名册”——这在底层被称为 <strong>System Dictionary（系统字典）</strong></p>
<p>可以把它理解成一个巨大的全局哈希表（HashMap）。</p>
<p>这里有一个 JVM 领域非常重要的高级考点：</p>
<p><strong>JVM 查字典时，不能只靠字符串名字！</strong></p>
<ul>
<li>在 Java 的世界里，判断一个类是不是唯一的，标准是：<strong>“全限定类名 + 加载它的类加载器（ClassLoader）”</strong></li>
<li>为什么？因为 Tomcat 这样的容器里，可能会有不同的 Web 应用使用了同一个包名和类名的类，但它们是相互隔离的，靠的就是不同的 ClassLoader</li>
</ul>
<p><strong>所以，真实的查找动作是这样的：</strong></p>
<ol>
<li>JVM 拿着提取出来的字符串 <code>com/example/User</code>。</li>
<li>抓取当前正在执行这段代码的类（比如在 <code>OrderService</code> 里 new 的 User，那就拿到 <code>OrderService</code> 的类加载器）。</li>
<li>把这两者组合成一个 Key：<code>[com/example/User, AppClassLoader]</code>。</li>
<li>去 System Dictionary 这个哈希表里查：
<ul>
<li><strong>查到了（命中）：</strong> 返回一个指针，直接指向元空间（Metaspace）里该类的元数据地址。这就说明类已经加载好了，检查通过！</li>
<li><strong>没查到（Miss）：</strong> 说明这个类在这个加载器的管辖范围内还没被加载过。JVM 就会挂起当前的 <code>new</code> 指令，触发双亲委派机制去磁盘里找 <code>.class</code> 文件，执行完整的类加载流程。</li>
</ul>
</li>
</ol>
<h3>对象创建流程总结</h3>
<p>| 步骤 | 做了什么 | 关键点 |
|</p>
]]></content:encoded>
    </item>
    <item>
      <title>JUC 八股24 (实际场景)</title>
      <link>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JUC%20-%20%E5%85%AB%E8%82%A1/article24.html</link>
      <guid>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JUC%20-%20%E5%85%AB%E8%82%A1/article24.html</guid>
      <source url="http://ekkosonya.cn/rss.xml">JUC 八股24 (实际场景)</source>
      <description>实际场景 多线程打印奇偶数，怎么控制打印的顺序 使用 ReentrantLock + Condition 使用 synchronized + wait/notify </description>
      <category>code</category>
      <pubDate>Wed, 25 Mar 2026 00:00:00 GMT</pubDate>
      <content:encoded><![CDATA[<h2>实际场景</h2>
<h3>多线程打印奇偶数，怎么控制打印的顺序</h3>
<h4>使用 ReentrantLock + Condition</h4>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">import</span> <span class="token import"><span class="token namespace">java<span class="token punctuation">.</span>util<span class="token punctuation">.</span>concurrent<span class="token punctuation">.</span>locks<span class="token punctuation">.</span></span><span class="token class-name">Condition</span></span><span class="token punctuation">;</span>
<span class="token keyword">import</span> <span class="token import"><span class="token namespace">java<span class="token punctuation">.</span>util<span class="token punctuation">.</span>concurrent<span class="token punctuation">.</span>locks<span class="token punctuation">.</span></span><span class="token class-name">ReentrantLock</span></span><span class="token punctuation">;</span>

<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">OddEvenPrinterLock</span> <span class="token punctuation">{</span>
  <span class="token keyword">private</span> <span class="token keyword">int</span> count <span class="token operator">=</span> <span class="token number">1</span><span class="token punctuation">;</span>
  <span class="token keyword">private</span> <span class="token keyword">final</span> <span class="token keyword">int</span> <span class="token constant">MAX</span> <span class="token operator">=</span> <span class="token number">10</span><span class="token punctuation">;</span>
  
  <span class="token comment">// 主线程创建的锁对象</span>
  <span class="token keyword">private</span> <span class="token keyword">final</span> <span class="token class-name">ReentrantLock</span> lock <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">ReentrantLock</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> 
  <span class="token comment">// 创建两个条件变量，相当于两个休息室</span>
  <span class="token keyword">private</span> <span class="token keyword">final</span> <span class="token class-name">Condition</span> oddCondition <span class="token operator">=</span> lock<span class="token punctuation">.</span><span class="token function">newCondition</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token keyword">private</span> <span class="token keyword">final</span> <span class="token class-name">Condition</span> evenCondition <span class="token operator">=</span> lock<span class="token punctuation">.</span><span class="token function">newCondition</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

  <span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">printOdd</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
      <span class="token keyword">while</span> <span class="token punctuation">(</span>count <span class="token operator">&lt;=</span> <span class="token constant">MAX</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
          lock<span class="token punctuation">.</span><span class="token function">lock</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 尝试获取锁</span>
          <span class="token keyword">try</span> <span class="token punctuation">{</span>
              <span class="token keyword">while</span> <span class="token punctuation">(</span>count <span class="token operator">%</span> <span class="token number">2</span> <span class="token operator">==</span> <span class="token number">0</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
                oddCondition<span class="token punctuation">.</span><span class="token function">await</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 奇数线程去奇数休息室睡觉，并释放锁</span>
              <span class="token punctuation">}</span>
              <span class="token keyword">if</span> <span class="token punctuation">(</span>count <span class="token operator">&lt;=</span> <span class="token constant">MAX</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
                <span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span><span class="token class-name">Thread</span><span class="token punctuation">.</span><span class="token function">currentThread</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">getName</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">+</span> <span class="token string">" 打印奇数: "</span> <span class="token operator">+</span> count<span class="token punctuation">)</span><span class="token punctuation">;</span>
                count<span class="token operator">++</span><span class="token punctuation">;</span>
                evenCondition<span class="token punctuation">.</span><span class="token function">signal</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 明确去偶数休息室叫醒偶数线程</span>
              <span class="token punctuation">}</span>
          <span class="token punctuation">}</span> <span class="token keyword">catch</span> <span class="token punctuation">(</span><span class="token class-name">InterruptedException</span> e<span class="token punctuation">)</span> <span class="token punctuation">{</span>
              <span class="token class-name">Thread</span><span class="token punctuation">.</span><span class="token function">currentThread</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">interrupt</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
          <span class="token punctuation">}</span> <span class="token keyword">finally</span> <span class="token punctuation">{</span>
              lock<span class="token punctuation">.</span><span class="token function">unlock</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 切记在 finally 中释放锁</span>
          <span class="token punctuation">}</span>
      <span class="token punctuation">}</span>
  <span class="token punctuation">}</span>

  <span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">printEven</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
      <span class="token keyword">while</span> <span class="token punctuation">(</span>count <span class="token operator">&lt;=</span> <span class="token constant">MAX</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
          lock<span class="token punctuation">.</span><span class="token function">lock</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
          <span class="token keyword">try</span> <span class="token punctuation">{</span>
              <span class="token keyword">while</span> <span class="token punctuation">(</span>count <span class="token operator">%</span> <span class="token number">2</span> <span class="token operator">!=</span> <span class="token number">0</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
                  evenCondition<span class="token punctuation">.</span><span class="token function">await</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 偶数线程去偶数休息室睡觉，并释放锁</span>
              <span class="token punctuation">}</span>
              <span class="token keyword">if</span> <span class="token punctuation">(</span>count <span class="token operator">&lt;=</span> <span class="token constant">MAX</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
                  <span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span><span class="token class-name">Thread</span><span class="token punctuation">.</span><span class="token function">currentThread</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">getName</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">+</span> <span class="token string">" 打印偶数: "</span> <span class="token operator">+</span> count<span class="token punctuation">)</span><span class="token punctuation">;</span>
                  count<span class="token operator">++</span><span class="token punctuation">;</span>
                  oddCondition<span class="token punctuation">.</span><span class="token function">signal</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 明确去奇数休息室叫醒奇数线程</span>
              <span class="token punctuation">}</span>
          <span class="token punctuation">}</span> <span class="token keyword">catch</span> <span class="token punctuation">(</span><span class="token class-name">InterruptedException</span> e<span class="token punctuation">)</span> <span class="token punctuation">{</span>
              <span class="token class-name">Thread</span><span class="token punctuation">.</span><span class="token function">currentThread</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">interrupt</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
          <span class="token punctuation">}</span> <span class="token keyword">finally</span> <span class="token punctuation">{</span>
              lock<span class="token punctuation">.</span><span class="token function">unlock</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
          <span class="token punctuation">}</span>
      <span class="token punctuation">}</span>
  <span class="token punctuation">}</span>

  <span class="token keyword">public</span> <span class="token keyword">static</span> <span class="token keyword">void</span> <span class="token function">main</span><span class="token punctuation">(</span><span class="token class-name">String</span><span class="token punctuation">[</span><span class="token punctuation">]</span> args<span class="token punctuation">)</span> <span class="token punctuation">{</span>
      <span class="token class-name">OddEvenPrinterLock</span> printer <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">OddEvenPrinterLock</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
      <span class="token keyword">new</span> <span class="token class-name">Thread</span><span class="token punctuation">(</span>printer<span class="token operator">::</span><span class="token function">printOdd</span><span class="token punctuation">,</span> <span class="token string">"Thread-Odd"</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">start</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
      <span class="token keyword">new</span> <span class="token class-name">Thread</span><span class="token punctuation">(</span>printer<span class="token operator">::</span><span class="token function">printEven</span><span class="token punctuation">,</span> <span class="token string">"Thread-Even"</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">start</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><h4>使用 synchronized + wait/notify</h4>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">OddEvenPrinterSync</span> <span class="token punctuation">{</span>
  <span class="token keyword">private</span> <span class="token keyword">int</span> count <span class="token operator">=</span> <span class="token number">1</span><span class="token punctuation">;</span>
  <span class="token keyword">private</span> <span class="token keyword">final</span> <span class="token keyword">int</span> <span class="token constant">MAX</span> <span class="token operator">=</span> <span class="token number">10</span><span class="token punctuation">;</span>
  <span class="token keyword">private</span> <span class="token keyword">final</span> <span class="token class-name">Object</span> lock <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">Object</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 共享的锁对象</span>

  <span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">printOdd</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
      <span class="token keyword">while</span> <span class="token punctuation">(</span>count <span class="token operator">&lt;=</span> <span class="token constant">MAX</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
          <span class="token keyword">synchronized</span> <span class="token punctuation">(</span>lock<span class="token punctuation">)</span> <span class="token punctuation">{</span>
              <span class="token comment">// 如果当前是偶数，奇数线程就主动挂起等待</span>
              <span class="token keyword">while</span> <span class="token punctuation">(</span>count <span class="token operator">%</span> <span class="token number">2</span> <span class="token operator">==</span> <span class="token number">0</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
                  <span class="token keyword">try</span> <span class="token punctuation">{</span>
                      lock<span class="token punctuation">.</span><span class="token function">wait</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 释放锁，进入阻塞状态，等待被唤醒</span>
                  <span class="token punctuation">}</span> <span class="token keyword">catch</span> <span class="token punctuation">(</span><span class="token class-name">InterruptedException</span> e<span class="token punctuation">)</span> <span class="token punctuation">{</span>
                      <span class="token class-name">Thread</span><span class="token punctuation">.</span><span class="token function">currentThread</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">interrupt</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
                  <span class="token punctuation">}</span>
              <span class="token punctuation">}</span>
              <span class="token keyword">if</span> <span class="token punctuation">(</span>count <span class="token operator">&lt;=</span> <span class="token constant">MAX</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
                  <span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span><span class="token class-name">Thread</span><span class="token punctuation">.</span><span class="token function">currentThread</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">getName</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">+</span> <span class="token string">" 打印奇数: "</span> <span class="token operator">+</span> count<span class="token punctuation">)</span><span class="token punctuation">;</span>
                  count<span class="token operator">++</span><span class="token punctuation">;</span>
                  lock<span class="token punctuation">.</span><span class="token function">notify</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 活干完了，叫醒挂在 lock 上的其他线程（偶数线程）</span>
              <span class="token punctuation">}</span>
          <span class="token punctuation">}</span>
      <span class="token punctuation">}</span>
  <span class="token punctuation">}</span>

  <span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">printEven</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
      <span class="token keyword">while</span> <span class="token punctuation">(</span>count <span class="token operator">&lt;=</span> <span class="token constant">MAX</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
          <span class="token keyword">synchronized</span> <span class="token punctuation">(</span>lock<span class="token punctuation">)</span> <span class="token punctuation">{</span>
              <span class="token comment">// 如果当前是奇数，偶数线程就主动挂起等待</span>
              <span class="token keyword">while</span> <span class="token punctuation">(</span>count <span class="token operator">%</span> <span class="token number">2</span> <span class="token operator">!=</span> <span class="token number">0</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
                  <span class="token keyword">try</span> <span class="token punctuation">{</span>
                      lock<span class="token punctuation">.</span><span class="token function">wait</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> 
                  <span class="token punctuation">}</span> <span class="token keyword">catch</span> <span class="token punctuation">(</span><span class="token class-name">InterruptedException</span> e<span class="token punctuation">)</span> <span class="token punctuation">{</span>
                      <span class="token class-name">Thread</span><span class="token punctuation">.</span><span class="token function">currentThread</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">interrupt</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
                  <span class="token punctuation">}</span>
              <span class="token punctuation">}</span>
              <span class="token keyword">if</span> <span class="token punctuation">(</span>count <span class="token operator">&lt;=</span> <span class="token constant">MAX</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
                  <span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span><span class="token class-name">Thread</span><span class="token punctuation">.</span><span class="token function">currentThread</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">getName</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">+</span> <span class="token string">" 打印偶数: "</span> <span class="token operator">+</span> count<span class="token punctuation">)</span><span class="token punctuation">;</span>
                  count<span class="token operator">++</span><span class="token punctuation">;</span>
                  lock<span class="token punctuation">.</span><span class="token function">notify</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 活干完了，叫醒奇数线程</span>
              <span class="token punctuation">}</span>
          <span class="token punctuation">}</span>
      <span class="token punctuation">}</span>
  <span class="token punctuation">}</span>

  <span class="token keyword">public</span> <span class="token keyword">static</span> <span class="token keyword">void</span> <span class="token function">main</span><span class="token punctuation">(</span><span class="token class-name">String</span><span class="token punctuation">[</span><span class="token punctuation">]</span> args<span class="token punctuation">)</span> <span class="token punctuation">{</span>
      <span class="token class-name">OddEvenPrinterSync</span> printer <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">OddEvenPrinterSync</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
      <span class="token keyword">new</span> <span class="token class-name">Thread</span><span class="token punctuation">(</span>printer<span class="token operator">::</span><span class="token function">printOdd</span><span class="token punctuation">,</span> <span class="token string">"Thread-Odd"</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">start</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
      <span class="token keyword">new</span> <span class="token class-name">Thread</span><span class="token punctuation">(</span>printer<span class="token operator">::</span><span class="token function">printEven</span><span class="token punctuation">,</span> <span class="token string">"Thread-Even"</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">start</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div>]]></content:encoded>
    </item>
  </channel>
</rss>