<?xml version="1.0" encoding="utf-8"?><?xml-stylesheet type="text/xsl" href="http://ekkosonya.cn/rss.xsl"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:dc="http://purl.org/dc/elements/1.1/">
  <channel>
    <atom:link href="http://ekkosonya.cn/rss.xml" rel="self" type="application/rss+xml"/>
    <title>EkkoSonya&amp;apos;s Blog</title>
    <link>http://ekkosonya.cn/</link>
    <description>笔记记录</description>
    <language>zh-CN</language>
    <pubDate>Mon, 06 Apr 2026 14:42:20 GMT</pubDate>
    <lastBuildDate>Mon, 06 Apr 2026 14:42:20 GMT</lastBuildDate>
    <generator>@vuepress/plugin-feed</generator>
    <docs>https://validator.w3.org/feed/docs/rss2.html</docs>
    <category>code</category>
    <item>
      <title>hint - 注解理解2</title>
      <link>http://ekkosonya.cn/code/java_hint/hint8.html</link>
      <guid>http://ekkosonya.cn/code/java_hint/hint8.html</guid>
      <source url="http://ekkosonya.cn/rss.xml">hint - 注解理解2</source>
      <description>注解 什么是注解 @interface 注解就是一个继承了 java.lang.annotation.Annotation 的接口, 而对应的值实际上会被编译成对应的抽象方法 测试 通过 javac 编译后得再通过 javap 反编译可以等价于 元注解 元注解就是修饰注解的注解，全在 java.lang.annotation 包下已经定义好了 @Tar...</description>
      <category>code</category>
      <pubDate>Sun, 05 Apr 2026 00:00:00 GMT</pubDate>
      <content:encoded><![CDATA[<h2>注解</h2>
<h3>什么是注解 <code>@interface</code></h3>
<p>注解就是一个继承了 <code>java.lang.annotation.Annotation</code> 的接口, 而对应的值实际上会被编译成对应的抽象方法</p>
<h4>测试</h4>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token comment">// Penguin.java</span>
<span class="token keyword">import</span> <span class="token import"><span class="token namespace">java<span class="token punctuation">.</span>lang<span class="token punctuation">.</span>annotation<span class="token punctuation">.</span></span><span class="token class-name">ElementType</span></span><span class="token punctuation">;</span>
<span class="token keyword">import</span> <span class="token import"><span class="token namespace">java<span class="token punctuation">.</span>lang<span class="token punctuation">.</span>annotation<span class="token punctuation">.</span></span><span class="token class-name">Retention</span></span><span class="token punctuation">;</span>
<span class="token keyword">import</span> <span class="token import"><span class="token namespace">java<span class="token punctuation">.</span>lang<span class="token punctuation">.</span>annotation<span class="token punctuation">.</span></span><span class="token class-name">RetentionPolicy</span></span><span class="token punctuation">;</span>
<span class="token keyword">import</span> <span class="token import"><span class="token namespace">java<span class="token punctuation">.</span>lang<span class="token punctuation">.</span>annotation<span class="token punctuation">.</span></span><span class="token class-name">Target</span></span><span class="token punctuation">;</span>

<span class="token annotation punctuation">@Target</span><span class="token punctuation">(</span><span class="token punctuation">{</span><span class="token class-name">ElementType</span><span class="token punctuation">.</span><span class="token constant">TYPE</span><span class="token punctuation">,</span> <span class="token class-name">ElementType</span><span class="token punctuation">.</span><span class="token constant">METHOD</span><span class="token punctuation">}</span><span class="token punctuation">)</span>
<span class="token annotation punctuation">@Retention</span><span class="token punctuation">(</span><span class="token class-name">RetentionPolicy</span><span class="token punctuation">.</span><span class="token constant">RUNTIME</span><span class="token punctuation">)</span>
<span class="token keyword">public</span> <span class="token annotation punctuation">@interface</span> <span class="token class-name">Penguin</span> <span class="token punctuation">{</span>
    <span class="token class-name">String</span> <span class="token function">value</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token keyword">default</span> <span class="token string">""</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre></div><p>通过 <code>javac</code> 编译后得再通过 <code>javap</code> 反编译可以等价于</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token class-name">Compiled</span> from <span class="token string">"Penguin.java"</span>
<span class="token keyword">public</span> <span class="token keyword">interface</span> <span class="token class-name"><span class="token namespace">com<span class="token punctuation">.</span>ekko<span class="token punctuation">.</span></span>Penguin</span> <span class="token keyword">extends</span> <span class="token class-name"><span class="token namespace">java<span class="token punctuation">.</span>lang<span class="token punctuation">.</span>annotation<span class="token punctuation">.</span></span>Annotation</span> <span class="token punctuation">{</span>
  <span class="token keyword">public</span> <span class="token keyword">abstract</span> <span class="token class-name"><span class="token namespace">java<span class="token punctuation">.</span>lang<span class="token punctuation">.</span></span>String</span> <span class="token function">value</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre></div><h3>元注解</h3>
<p>元注解就是修饰注解的注解，全在 <code>java.lang.annotation</code> 包下已经定义好了</p>
<p>| 元注解 | 作用 |
|</p>
]]></content:encoded>
    </item>
    <item>
      <title>hint - 注解理解3</title>
      <link>http://ekkosonya.cn/code/java_hint/hint9.html</link>
      <guid>http://ekkosonya.cn/code/java_hint/hint9.html</guid>
      <source url="http://ekkosonya.cn/rss.xml">hint - 注解理解3</source>
      <description>注解3 自定义注解流程 自定义注解 测试类 然后在 Main.java 使用 编译 编译 Penguin.java 时，javac 看到它上面有 @Target 和 @Retention，会依次做这些事： 用 javap -v Penguin.class 可以看到生成结果： 编译 Main.java 时，javac 看到 @Penguin(&amp;quot;123&amp;quot;)...</description>
      <category>code</category>
      <pubDate>Sun, 05 Apr 2026 00:00:00 GMT</pubDate>
      <content:encoded><![CDATA[<h2>注解3</h2>
<h3>自定义注解流程</h3>
<h4>自定义注解</h4>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">import</span> <span class="token import"><span class="token namespace">java<span class="token punctuation">.</span>lang<span class="token punctuation">.</span>annotation<span class="token punctuation">.</span></span><span class="token class-name">ElementType</span></span><span class="token punctuation">;</span>
<span class="token keyword">import</span> <span class="token import"><span class="token namespace">java<span class="token punctuation">.</span>lang<span class="token punctuation">.</span>annotation<span class="token punctuation">.</span></span><span class="token class-name">Retention</span></span><span class="token punctuation">;</span>
<span class="token keyword">import</span> <span class="token import"><span class="token namespace">java<span class="token punctuation">.</span>lang<span class="token punctuation">.</span>annotation<span class="token punctuation">.</span></span><span class="token class-name">RetentionPolicy</span></span><span class="token punctuation">;</span>
<span class="token keyword">import</span> <span class="token import"><span class="token namespace">java<span class="token punctuation">.</span>lang<span class="token punctuation">.</span>annotation<span class="token punctuation">.</span></span><span class="token class-name">Target</span></span><span class="token punctuation">;</span>

<span class="token annotation punctuation">@Target</span><span class="token punctuation">(</span><span class="token punctuation">{</span><span class="token class-name">ElementType</span><span class="token punctuation">.</span><span class="token constant">TYPE</span><span class="token punctuation">,</span> <span class="token class-name">ElementType</span><span class="token punctuation">.</span><span class="token constant">METHOD</span><span class="token punctuation">}</span><span class="token punctuation">)</span>
<span class="token annotation punctuation">@Retention</span><span class="token punctuation">(</span><span class="token class-name">RetentionPolicy</span><span class="token punctuation">.</span><span class="token constant">RUNTIME</span><span class="token punctuation">)</span>
<span class="token keyword">public</span> <span class="token annotation punctuation">@interface</span> <span class="token class-name">Penguin</span> <span class="token punctuation">{</span>
    <span class="token class-name">String</span> <span class="token function">value</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token keyword">default</span> <span class="token string">""</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre></div><h4>测试类</h4>
<p>然后在 <code>Main.java</code> 使用</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">import</span> <span class="token import"><span class="token namespace">com<span class="token punctuation">.</span>ekko<span class="token punctuation">.</span>annotation<span class="token punctuation">.</span></span><span class="token class-name">Penguin</span></span><span class="token punctuation">;</span>

<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">Main</span> <span class="token punctuation">{</span>
    <span class="token annotation punctuation">@Penguin</span><span class="token punctuation">(</span><span class="token string">"123"</span><span class="token punctuation">)</span>
    <span class="token keyword">public</span> <span class="token keyword">static</span> <span class="token keyword">void</span> <span class="token function">main</span><span class="token punctuation">(</span><span class="token class-name">String</span><span class="token punctuation">[</span><span class="token punctuation">]</span> args<span class="token punctuation">)</span> <span class="token punctuation">{</span>
        <span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span><span class="token string">"Hello World"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><h4>编译</h4>
<p>编译 <code>Penguin.java</code> 时，javac 看到它上面有 <code>@Target</code> 和 <code>@Retention</code>，会依次做这些事：</p>
<div class="language-text" data-ext="text" data-title="text"><pre class="language-text"><code>javac 编译 Penguin.java
        ↓
发现 @Target({ElementType.TYPE, ElementType.METHOD})
        ↓
去 classpath 找 Target.class，加载进内存，提取两类信息：
  ① value() → ElementType[]            （用来校验你传的值类型合不合法）
  ② Target 自身的 @Retention(RUNTIME)  （说明 Target 运行时可见）
        ↓
发现 @Retention(RetentionPolicy.RUNTIME)
        ↓
去 classpath 找 Retention.class，同样提取信息
        ↓
校验通过，将注解信息写入 Penguin.class
  - 设置 ACC_ANNOTATION | ACC_INTERFACE 标志位
  - 隐式添加 implements java.lang.annotation.Annotation
  - 在 RuntimeVisibleAnnotations 属性中记录 @Target 和 @Retention 的值
</code></pre></div><p>用 <code>javap -v Penguin.class</code> 可以看到生成结果：</p>
<div class="language-plain" data-ext="plain" data-title="plain"><pre class="language-plain"><code>public interface com.ekko.annotation.Penguin extends java.lang.annotation.Annotation
  flags: ACC_PUBLIC, ACC_INTERFACE, ACC_ABSTRACT, ACC_ANNOTATION

  public abstract java.lang.String value();

RuntimeVisibleAnnotations:
  0: java.lang.annotation.Target(
       value=[TYPE, METHOD]
     )
  1: java.lang.annotation.Retention(
       value=RUNTIME
     )
</code></pre></div><p>编译 <code>Main.java</code> 时，javac 看到 <code>@Penguin("123")</code> 用在了 <code>main</code> 方法上：</p>
<div class="language-text" data-ext="text" data-title="text"><pre class="language-text"><code>发现 @Penguin("123") 标注在方法上
        ↓
加载 Penguin.class，读取它的 @Target → 允许 METHOD ✓
        ↓
读取 value() 的返回类型 → String，检查传入的 "123" 类型是否匹配 ✓
        ↓
读取 @Retention(RUNTIME) → 需要写入 .class 且运行时可见
        ↓
将注解信息写入 Main.class 的 RuntimeVisibleAnnotations 属性
</code></pre></div><h4>运行时读取</h4>
<p>注解标注了 <code>@Retention(RetentionPolicy.RUNTIME)</code>，所以可以在运行时通过反射读取：</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">import</span> <span class="token import"><span class="token namespace">com<span class="token punctuation">.</span>ekko<span class="token punctuation">.</span>annotation<span class="token punctuation">.</span></span><span class="token class-name">Penguin</span></span><span class="token punctuation">;</span>
<span class="token keyword">import</span> <span class="token import"><span class="token namespace">java<span class="token punctuation">.</span>lang<span class="token punctuation">.</span>reflect<span class="token punctuation">.</span></span><span class="token class-name">Method</span></span><span class="token punctuation">;</span>

<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">Main</span> <span class="token punctuation">{</span>
    <span class="token annotation punctuation">@Penguin</span><span class="token punctuation">(</span><span class="token string">"123"</span><span class="token punctuation">)</span>
    <span class="token keyword">public</span> <span class="token keyword">static</span> <span class="token keyword">void</span> <span class="token function">main</span><span class="token punctuation">(</span><span class="token class-name">String</span><span class="token punctuation">[</span><span class="token punctuation">]</span> args<span class="token punctuation">)</span> <span class="token keyword">throws</span> <span class="token class-name">Exception</span> <span class="token punctuation">{</span>
        <span class="token class-name">Method</span> method <span class="token operator">=</span> <span class="token class-name">Main</span><span class="token punctuation">.</span><span class="token keyword">class</span><span class="token punctuation">.</span><span class="token function">getMethod</span><span class="token punctuation">(</span><span class="token string">"main"</span><span class="token punctuation">,</span> <span class="token class-name">String</span><span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">.</span><span class="token keyword">class</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

        <span class="token keyword">if</span> <span class="token punctuation">(</span>method<span class="token punctuation">.</span><span class="token function">isAnnotationPresent</span><span class="token punctuation">(</span><span class="token class-name">Penguin</span><span class="token punctuation">.</span><span class="token keyword">class</span><span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
            <span class="token class-name">Penguin</span> penguin <span class="token operator">=</span> method<span class="token punctuation">.</span><span class="token function">getAnnotation</span><span class="token punctuation">(</span><span class="token class-name">Penguin</span><span class="token punctuation">.</span><span class="token keyword">class</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
            <span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span>penguin<span class="token punctuation">.</span><span class="token function">value</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 输出: 123</span>
        <span class="token punctuation">}</span>
    <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><p>反射读取流程：</p>
<div class="language-text" data-ext="text" data-title="text"><pre class="language-text"><code>method.getAnnotation(Penguin.class)
        ↓
JVM 去 Main.class 的 RuntimeVisibleAnnotations 属性里查找
        ↓
找到 @Penguin，value = "123"
        ↓
JVM 动态生成一个实现了 Penguin 接口的代理对象返回
        ↓
调用 penguin.value() → 返回 "123"
</code></pre></div><h3>SOURCE 级别</h3>
<p><code>SOURCE</code> 级注解在编译后就被丢弃，它靠的是 <strong>APT（Annotation Processing Tool，注解处理器）</strong> 在编译期处理，典型代表是 Lombok 的 <code>@Data</code>、<code>@Getter</code>。</p>
<div class="language-text" data-ext="text" data-title="text"><pre class="language-text"><code>你的 .java 源文件
        ↓
javac 启动，先运行所有注册的注解处理器（APT）
        ↓
APT 读取 AST（抽象语法树），发现 @Data 注解
        ↓
APT 自动生成 getter/setter/toString 等方法，插入 AST
        ↓
javac 继续编译（此时 AST 里已经有生成的代码了）
        ↓
输出 .class 文件（@Data 注解本身不写入）
</code></pre></div><p>自己写一个最简单的 APT，在编译时打印所有被 <code>@Penguin</code> 标注的方法：</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">import</span> <span class="token import"><span class="token namespace">javax<span class="token punctuation">.</span>annotation<span class="token punctuation">.</span>processing<span class="token punctuation">.</span></span><span class="token operator">*</span></span><span class="token punctuation">;</span>
<span class="token keyword">import</span> <span class="token import"><span class="token namespace">javax<span class="token punctuation">.</span>lang<span class="token punctuation">.</span>model<span class="token punctuation">.</span>element<span class="token punctuation">.</span></span><span class="token operator">*</span></span><span class="token punctuation">;</span>
<span class="token keyword">import</span> <span class="token import"><span class="token namespace">java<span class="token punctuation">.</span>util<span class="token punctuation">.</span></span><span class="token class-name">Set</span></span><span class="token punctuation">;</span>

<span class="token annotation punctuation">@SupportedAnnotationTypes</span><span class="token punctuation">(</span><span class="token string">"com.ekko.annotation.Penguin"</span><span class="token punctuation">)</span>
<span class="token annotation punctuation">@SupportedSourceVersion</span><span class="token punctuation">(</span><span class="token class-name"><span class="token namespace">javax<span class="token punctuation">.</span>lang<span class="token punctuation">.</span>model<span class="token punctuation">.</span></span>SourceVersion</span><span class="token punctuation">.</span><span class="token constant">RELEASE_17</span><span class="token punctuation">)</span>
<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">PenguinProcessor</span> <span class="token keyword">extends</span> <span class="token class-name">AbstractProcessor</span> <span class="token punctuation">{</span>

    <span class="token annotation punctuation">@Override</span>
    <span class="token keyword">public</span> <span class="token keyword">boolean</span> <span class="token function">process</span><span class="token punctuation">(</span><span class="token class-name">Set</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token operator">?</span> <span class="token keyword">extends</span> <span class="token class-name">TypeElement</span><span class="token punctuation">&gt;</span></span> annotations<span class="token punctuation">,</span> <span class="token class-name">RoundEnvironment</span> roundEnv<span class="token punctuation">)</span> <span class="token punctuation">{</span>
        <span class="token keyword">for</span> <span class="token punctuation">(</span><span class="token class-name">Element</span> element <span class="token operator">:</span> roundEnv<span class="token punctuation">.</span><span class="token function">getElementsAnnotatedWith</span><span class="token punctuation">(</span><span class="token class-name">Penguin</span><span class="token punctuation">.</span><span class="token keyword">class</span><span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
            processingEnv<span class="token punctuation">.</span><span class="token function">getMessager</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">printMessage</span><span class="token punctuation">(</span>
                <span class="token class-name"><span class="token namespace">javax<span class="token punctuation">.</span>tools<span class="token punctuation">.</span></span>Diagnostic<span class="token punctuation">.</span>Kind</span><span class="token punctuation">.</span><span class="token constant">NOTE</span><span class="token punctuation">,</span>
                <span class="token string">"发现 @Penguin 标注的元素: "</span> <span class="token operator">+</span> element<span class="token punctuation">.</span><span class="token function">getSimpleName</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
            <span class="token punctuation">)</span><span class="token punctuation">;</span>
        <span class="token punctuation">}</span>
        <span class="token keyword">return</span> <span class="token boolean">true</span><span class="token punctuation">;</span>
    <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><p>编译时会看到：</p>
<div class="language-text" data-ext="text" data-title="text"><pre class="language-text"><code>注意: 发现 @Penguin 标注的元素: main
</code></pre></div><h3>CLASS 级别</h3>
<p><code>CLASS</code> 注解会写入 <code>.class</code> 文件，但 JVM 加载时<strong>不读入内存</strong>，无法用反射获取。它的使用者是<strong>字节码操作工具</strong>，在 <code>.class</code> 文件加载进 JVM 之前直接修改字节码。</p>
<div class="language-text" data-ext="text" data-title="text"><pre class="language-text"><code>.class 文件（含 CLASS 级注解）
        ↓
字节码工具（ASM / Javassist）读取
        ↓
发现注解标记，按规则修改字节码（插入代码、替换方法体等）
        ↓
修改后的字节码交给 JVM 加载
        ↓
JVM 运行（注解已不可见，效果已生效）
</code></pre></div><p>典型代表：AspectJ 的编译期织入</p>
<h3>三种处理时机对比</h3>
<p>| | SOURCE + APT | CLASS + 字节码工具 | RUNTIME + 反射 |
|</p>
]]></content:encoded>
    </item>
    <item>
      <title>Spring系列八股13 - 事务</title>
      <link>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/Spring/article13.html</link>
      <guid>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/Spring/article13.html</guid>
      <source url="http://ekkosonya.cn/rss.xml">Spring系列八股13 - 事务</source>
      <description>事务 Spring 提供了两种事务管理方式，编程式事务和声明式事务 编程式事务就是我们要手动调用事务的开始、提交、回滚这些操作，虽然灵活但是代码比较繁琐 声明式事务只需要在需要事务的方法上加上 @Transactional 注解就好了，Spring 会帮我们自动处理事务的整个生命周期 声明式事务原理 Spring 的声明式事务管理是通过 AOP 和代理...</description>
      <category>code</category>
      <pubDate>Mon, 06 Apr 2026 14:40:01 GMT</pubDate>
      <content:encoded><![CDATA[<h2>事务</h2>
<p>Spring 提供了两种事务管理方式，编程式事务和声明式事务</p>
<p>编程式事务就是我们要手动调用事务的开始、提交、回滚这些操作，虽然灵活但是代码比较繁琐</p>
<p>声明式事务只需要在需要事务的方法上加上 <code>@Transactional</code> 注解就好了，Spring 会帮我们自动处理事务的整个生命周期</p>
<h3>声明式事务原理</h3>
<p>Spring 的声明式事务管理是通过 AOP 和代理机制实现的，大致可以分为两个阶段</p>
<p>第一个阶段发生在 <strong>Spring 容器启动时</strong>，它会扫描所有的 Bean</p>
<p>如果发现某个 Bean 的方法上标注了 <code>@Transactional</code> 注解，Spring 不会直接返回这个原始的 Bean 实例, 而是为这个 Bean 创建一个代理对象</p>
<p>这个代理对象拥有和原始对象完全相同的方法，但在内部悄悄地包裹了事务处理的逻辑。</p>
<p>第二个阶段发生在<strong>方法调用的运行阶段</strong>，当我们的代码调用那个被 <code>@Transactional</code> 注解修饰的方法时，实际上调用的是 Spring 创建的那个代理对象的方法</p>
<p>事务拦截器会在代理对象执行真正的业务逻辑之前，根据 <code>@Transactional</code> 注解的配置获取事务属性，比如传播行为、隔离级别等，然后通过事务管理器来开启一个新的事务。</p>
<p>并从数据库连接池获取一个连接，关闭其自动提交</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">TransactionInterceptor</span> <span class="token keyword">implements</span> <span class="token class-name">MethodInterceptor</span> <span class="token punctuation">{</span>
    <span class="token annotation punctuation">@Override</span>
    <span class="token keyword">public</span> <span class="token class-name">Object</span> <span class="token function">invoke</span><span class="token punctuation">(</span><span class="token class-name">MethodInvocation</span> invocation<span class="token punctuation">)</span> <span class="token keyword">throws</span> <span class="token class-name">Throwable</span> <span class="token punctuation">{</span>
        <span class="token comment">// 获取事务属性</span>
        <span class="token class-name">TransactionAttribute</span> txAttr <span class="token operator">=</span> <span class="token function">getTransactionAttribute</span><span class="token punctuation">(</span>invocation<span class="token punctuation">.</span><span class="token function">getMethod</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span> invocation<span class="token punctuation">.</span><span class="token function">getThis</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">getClass</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
        <span class="token comment">// 开始事务</span>
        <span class="token class-name">TransactionStatus</span> status <span class="token operator">=</span> transactionManager<span class="token punctuation">.</span><span class="token function">getTransaction</span><span class="token punctuation">(</span>txAttr<span class="token punctuation">)</span><span class="token punctuation">;</span>
        <span class="token keyword">try</span> <span class="token punctuation">{</span>
            <span class="token comment">// 执行目标方法</span>
            <span class="token class-name">Object</span> retVal <span class="token operator">=</span> invocation<span class="token punctuation">.</span><span class="token function">proceed</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
            <span class="token comment">// 提交事务</span>
            transactionManager<span class="token punctuation">.</span><span class="token function">commit</span><span class="token punctuation">(</span>status<span class="token punctuation">)</span><span class="token punctuation">;</span>
            <span class="token keyword">return</span> retVal<span class="token punctuation">;</span>
        <span class="token punctuation">}</span> <span class="token keyword">catch</span> <span class="token punctuation">(</span><span class="token class-name">Throwable</span> ex<span class="token punctuation">)</span> <span class="token punctuation">{</span>
            <span class="token comment">// 回滚事务</span>
            transactionManager<span class="token punctuation">.</span><span class="token function">rollback</span><span class="token punctuation">(</span>status<span class="token punctuation">)</span><span class="token punctuation">;</span>
            <span class="token keyword">throw</span> ex<span class="token punctuation">;</span>
        <span class="token punctuation">}</span>
    <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><p>接着，代理对象会调用原始 Bean 实例中真正的业务方法，如果业务方法顺利执行完毕，没有抛出任何异常，那么拦截器就会通过事务管理器提交事务，将之前的所有数据库操作永久保存</p>
<p>如果业务方法抛出了异常，拦截器会捕获到这个异常，并通过事务管理器回滚事务，将之前的所有数据库操作撤销</p>
<blockquote>
<p>最后，无论事务是提交还是回滚，拦截器都会释放数据库连接</p>
</blockquote>
<p>本质上就是：把"开启事务、执行业务、提交/回滚"这段重复代码抽出来，通过代理自动织入，不用你手写</p>
<p><code>@Transactional</code> 的核心是 <strong>事务管理</strong>，而 Spring 通过 <code>PlatformTransactionManager</code> 接口抽象了事务概念。理论上可以实现各种事务管理器：</p>
<p>| 事务管理器 | 用途 |
|</p>
]]></content:encoded>
    </item>
    <item>
      <title>Spring系列八股14 - MVC1 (基本概念)</title>
      <link>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/Spring/article14.html</link>
      <guid>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/Spring/article14.html</guid>
      <source url="http://ekkosonya.cn/rss.xml">Spring系列八股14 - MVC1 (基本概念)</source>
      <description>MVC MVC 分层介绍 MVC 全名是 Model View Controller，是模型(model)－视图(view)－控制器(controller)的缩写，一种软件设计典范 用一种业务逻辑、数据、界面显示分离的方法组织代码，将业务逻辑聚集到一个部件里面，在改进和个性化定制界面及用户交互的同时，不需要重新编写业务逻辑 视图(view)： 为用户提...</description>
      <category>code</category>
      <pubDate>Mon, 06 Apr 2026 14:40:01 GMT</pubDate>
      <content:encoded><![CDATA[<h2>MVC</h2>
<h3>MVC 分层介绍</h3>
<p>MVC 全名是 Model View Controller，是模型(model)－视图(view)－控制器(controller)的缩写，一种软件设计典范</p>
<p>用一种业务逻辑、数据、界面显示分离的方法组织代码，将业务逻辑聚集到一个部件里面，在改进和个性化定制界面及用户交互的同时，不需要重新编写业务逻辑</p>
<ul>
<li>视图(view)： 为用户提供使用界面，与用户直接进行交互。</li>
<li>模型(model)： 代表一个存取数据的对象或 JAVA POJO（Plain Old Java Object，简单java对象）
<ul>
<li>它也可以带有逻辑，主要用于承载数据，并对用户提交请求进行计算的模块，模型分为两类</li>
<li>一类称为数据承载 Bean
<ul>
<li>所谓数据承载 Bean 是指实体类（如：User类），专门为用户承载业务数据的</li>
</ul>
</li>
<li>一类称为业务处理 Bean
<ul>
<li>业务处理 Bean 是指 Service 或 Dao 对象，专门用于处理用户提交请求的。</li>
</ul>
</li>
</ul>
</li>
<li>控制器(controller)： 用于将用户请求转发给相应的 Model 进行处理，并根据 Model 的计算结果向用户提供相应响应。它使视图与模型分离</li>
</ul>
<figure><figcaption>alt text</figcaption></figure>
<p>流程步骤：</p>
<ul>
<li>用户通过View 页面向服务端提出请求，可以是表单请求、超链接请求、AJAX 请求等；</li>
<li>服务端 Controller 控制器接收到请求后对请求进行解析，找到相应的Model，对用户请求进行处理Model 处理；</li>
<li>将处理结果再交给 Controller（控制器其实只是起到了承上启下的作用）；
根据处理结果找到要作为向客户端发回的响应View 页面，页面经渲染后发送给客户端</li>
</ul>
<h4>DAO 层（Data Access Object）</h4>
<p>DAO 层是数据访问层，负责封装所有与数据库（或其他持久化存储）的交互逻辑</p>
<blockquote>
<p>在分层架构中的位置</p>
</blockquote>
<div class="language-text" data-ext="text" data-title="text"><pre class="language-text"><code>Controller 层（接收请求）
      ↓
Service 层（业务逻辑）
      ↓
DAO 层（数据访问）  ← 在这里
      ↓
Database（数据库）
</code></pre></div><blockquote>
<p>典型代码示例</p>
</blockquote>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">public</span> <span class="token keyword">interface</span> <span class="token class-name">UserDao</span> <span class="token punctuation">{</span>
  <span class="token class-name">User</span> <span class="token function">findById</span><span class="token punctuation">(</span><span class="token class-name">Long</span> id<span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token keyword">void</span> <span class="token function">insert</span><span class="token punctuation">(</span><span class="token class-name">User</span> user<span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token keyword">void</span> <span class="token function">update</span><span class="token punctuation">(</span><span class="token class-name">User</span> user<span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token keyword">void</span> <span class="token function">deleteById</span><span class="token punctuation">(</span><span class="token class-name">Long</span> id<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre></div><p>Service 层只调用 <code>userDao.findById(id)</code>，完全不写 SQL，这就是 DAO 的价值所在。</p>
<h5>常见实现方式</h5>
<ul>
<li><strong>MyBatis</strong>&nbsp;→ 手写 SQL，灵活可控</li>
<li><strong>Spring Data JPA / Hibernate</strong>&nbsp;→ 自动生成 SQL，快速开发</li>
<li><strong>JdbcTemplate</strong>&nbsp;→ 轻量级，接近原生 JDBC</li>
</ul>
<p>| | <strong>Mapper</strong> | <strong>Repository</strong> |
|</p>
]]></content:encoded>
    </item>
    <item>
      <title>Spring系列八股15 - MVC2 (Spring MVC)</title>
      <link>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/Spring/article15.html</link>
      <guid>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/Spring/article15.html</guid>
      <source url="http://ekkosonya.cn/rss.xml">Spring系列八股15 - MVC2 (Spring MVC)</source>
      <description>MVC Spring MVC 是 Spring 框架对 MVC 模式的具体实现，专门用于构建 Web 应用 核心组件 在 Spring Boot 项目中，DispatcherServlet 的启动是通过自动配置完成的 Spring Boot 会自动注册一个默认的 DispatcherServlet，并将其映射到 / 处理器映射 HandlerMappi...</description>
      <category>code</category>
      <pubDate>Mon, 06 Apr 2026 14:40:01 GMT</pubDate>
      <content:encoded><![CDATA[<h2>MVC</h2>
<p>Spring MVC 是 Spring 框架对 MVC 模式的具体实现，专门用于构建 Web 应用</p>
<div class="language-text" data-ext="text" data-title="text"><pre class="language-text"><code>MVC        →  一种设计思想（模式）
Spring MVC →  Spring 对这个思想的落地实现
</code></pre></div><h3>核心组件</h3>
<div class="language-text" data-ext="text" data-title="text"><pre class="language-text"><code>请求进来
   ↓
DispatcherServlet（前端控制器，所有请求的入口）
   ↓
HandlerMapping（找到对应的 Controller）
   ↓
Controller（处理请求，调用 Service）
   ↓
返回 ModelAndView / JSON
   ↓
响应给客户端
</code></pre></div><p>在 Spring Boot 项目中，<code>DispatcherServlet</code> 的启动是通过自动配置完成的</p>
<p>Spring Boot 会自动注册一个默认的 <code>DispatcherServlet</code>，并将其映射到 /</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token annotation punctuation">@Bean</span>
<span class="token keyword">public</span> <span class="token class-name">ServletRegistrationBean</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">DispatcherServlet</span><span class="token punctuation">&gt;</span></span> <span class="token function">dispatcherServletRegistration</span><span class="token punctuation">(</span><span class="token class-name">DispatcherServlet</span> dispatcherServlet<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token class-name">ServletRegistrationBean</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">DispatcherServlet</span><span class="token punctuation">&gt;</span></span> registration <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">ServletRegistrationBean</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token punctuation">&gt;</span></span><span class="token punctuation">(</span>dispatcherServlet<span class="token punctuation">,</span> <span class="token string">"/"</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 默认映射路径为 "/"</span>
    registration<span class="token punctuation">.</span><span class="token function">setName</span><span class="token punctuation">(</span><span class="token string">"dispatcherServlet"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">return</span> registration<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre></div><p>处理器映射 <code>HandlerMapping</code>，当一个请求进来时，前端控制器会询问处理器映射：“这个 URL 应该由哪个 Controller 的哪个方法来处理？”然后它就会根据 @RequestMapping、@GetMapping 这些注解来匹配请求</p>
<p>处理器 <code>Handler</code>，实际上就是我们写的 Controller 方法，这是真正处理业务逻辑的地方</p>
<p>处理器适配器 <code>HandlerAdapter</code>，负责调用该处理器的方法，并处理参数绑定、类型转换等</p>
<p>因为处理器可能有不同的类型，比如注解方式、实现接口方式等，处理器适配器就是为了统一调用方式</p>
<p>视图解析器 <code>ViewResolver</code>，处理完业务逻辑后，如果需要渲染视图，ViewResolver 会根据返回的视图名称解析实际的视图对象，比如 Thymeleaf</p>
<p>在前后端分离的项目中，这个组件更多用于返回 JSON 数据</p>
<p>异常处理器 <code>HandlerExceptionResolver</code>，捕获并处理请求处理过程中抛出的异常</p>
<p>通常，我们可以通过 <code>@ControllerAdvice</code> 和 <code>@ExceptionHandler</code> 来自定义异常处理逻辑，确保返回友好的错误响应</p>
<p>除此之外，还有文件上传解析器 <code>MultipartResolver</code>，用于处理文件上传请求；拦截器 <code>HandlerInterceptor</code>, 用于在请求处理前后执行一些额外的逻辑，比如权限校验、日志记录等</p>
<h3>Spring MVC 的工作流程</h3>
<p>简单来说，Spring MVC 是一个基于 Servlet 的请求处理框架</p>
<p>核心流程可以概括为：请求接收 → 路由分发 → 控制器处理 → 视图解析</p>
<figure><figcaption>alt text</figcaption></figure>
<figure><figcaption>alt text</figcaption></figure>
<p>用户发起的 HTTP 请求，首先会被 <code>DispatcherServlet</code> 捕获，这是 Spring MVC 的“前端控制器”，负责拦截所有请求，起到统一入口的作用</p>
<p><code>DispatcherServlet</code> 接收到请求后，会根据 URL、请求方法等信息，交给 <code>HandlerMapping</code> 进行路由匹配，查找对应的处理器，也就是 Controller 中的具体方法</p>
<p>找到对应 Controller 方法后，<code>DispatcherServlet</code> 会委托给处理器适配器 <code>HandlerAdapter</code> 进行调用</p>
<p>处理器适配器负责执行方法本身，并处理参数绑定、数据类型转换等</p>
<p>在注解驱动开发中，常用的是 <code>RequestMappingHandlerAdapter</code></p>
<p>这一层会把请求参数自动注入到方法形参中，并调用 <code>Controller</code> 执行实际的业务逻辑</p>
<p><code>Controller</code> 方法最终会返回结果，比如视图名称、<code>ModelAndView</code> 或直接返回 JSON 数据</p>
<p>当 <code>Controller</code> 方法返回视图名时，<code>DispatcherServlet</code> 会调用 <code>ViewResolver</code> 将其解析为实际的 View 对象，比如 Thymeleaf 页面</p>
<p>在前后端分离的接口项目中，这一步则<strong>通常是返回 JSON 数据</strong></p>
<p>最后，由 View 对象完成渲染，或者将 JSON 结果直接通过 DispatcherServlet 返回给客户端</p>
<h4>速记</h4>
<ol>
<li>用户发送请求至前端控制器 <code>DispatcherServlet</code></li>
<li><code>DispatcherServlet</code> 收到请求调用处理器映射器 <code>HandlerMapping</code></li>
<li>处理器映射器根据请求url找到具体的处理器，生成处理器执行链<code>HandlerExecutionChain</code> (包括处理器对象和处理器拦截器) 一并返回给<code>DispatcherServlet</code></li>
<li><code>DispatcherServlet</code>根据处理器<code>Handler</code>获取处理器适配器 <code>HandlerAdapter</code> 执行 <code>HandlerAdapter</code> 处理一系列的操作，如：参数封装，数据格式转换，数据验证等操作</li>
<li>执行处理器 <code>Handler</code> (<code>Controller</code>，也叫页面控制器)。</li>
<li><code>Handler</code> 执行完成返回 <code>ModelAndView</code></li>
<li><code>HandlerAdapter</code> 将 <code>Handler</code> 执行结果 <code>ModelAndView</code> 返回到<code>DispatcherServlet</code></li>
<li><code>DispatcherServlet</code> 将 <code>ModelAndView</code> 传给 <code>ViewReslover</code> 视图解析器</li>
<li><code>ViewReslover</code> 解析后返回具体 <code>View</code></li>
<li><code>DispatcherServlet</code> 对 <code>View</code> 进行渲染视图（即将模型数据model填充至视图中）。</li>
<li><code>DispatcherServlet</code> 响应用户</li>
</ol>
<h4>为什么不直接调用，要通过 HandlerAdapte</h4>
<p>因为 Controller 的写法不止一种，DispatcherServlet 不知道该怎么调用：</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token comment">// 写法1：最常见的注解方式</span>
<span class="token annotation punctuation">@RestController</span>
<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">UserController</span> <span class="token punctuation">{</span>
    <span class="token annotation punctuation">@GetMapping</span><span class="token punctuation">(</span><span class="token string">"/user"</span><span class="token punctuation">)</span>
    <span class="token keyword">public</span> <span class="token class-name">User</span> <span class="token function">getUser</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span> <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span> <span class="token punctuation">}</span>
<span class="token punctuation">}</span>

<span class="token comment">// 写法2：实现接口方式（老写法）</span>
<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">UserController</span> <span class="token keyword">implements</span> <span class="token class-name">Controller</span> <span class="token punctuation">{</span>
    <span class="token keyword">public</span> <span class="token class-name">ModelAndView</span> <span class="token function">handleRequest</span><span class="token punctuation">(</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">)</span> <span class="token punctuation">{</span> <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span> <span class="token punctuation">}</span>
<span class="token punctuation">}</span>

<span class="token comment">// 写法3：实现 HttpRequestHandler</span>
<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">UserController</span> <span class="token keyword">implements</span> <span class="token class-name">HttpRequestHandler</span> <span class="token punctuation">{</span>
    <span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">handleRequest</span><span class="token punctuation">(</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">)</span> <span class="token punctuation">{</span> <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span> <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><h5>HandlerAdapter 的作用</h5>
<p>相当于一个适配器/翻译官：</p>
<div class="language-text" data-ext="text" data-title="text"><pre class="language-text"><code>DispatcherServlet
      ↓ 我只会说："帮我调用这个 Controller"
HandlerAdapter
      ↓ 我来判断它是哪种类型，用对应的方式调用
Controller（不管是哪种写法，我都能处理）
</code></pre></div><p>每种 HandlerAdapter 认识一种 Controller：</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token comment">// RequestMappingHandlerAdapter 认识注解方式</span>
<span class="token comment">// 它知道要用反射找 @GetMapping 的方法来调用</span>
method<span class="token punctuation">.</span><span class="token function">invoke</span><span class="token punctuation">(</span>controller<span class="token punctuation">,</span> args<span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">)</span><span class="token punctuation">;</span>  <span class="token comment">// 反射调用 getUser()</span>

<span class="token comment">// SimpleControllerHandlerAdapter 认识接口方式  </span>
<span class="token comment">// 它知道直接调 handleRequest</span>
controller<span class="token punctuation">.</span><span class="token function">handleRequest</span><span class="token punctuation">(</span>request<span class="token punctuation">,</span> response<span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre></div><p>DispatcherServlet 只需要说：</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token comment">// DispatcherServlet 只跟 HandlerAdapter 说话，不管 Controller 长什么样</span>
handlerAdapter<span class="token punctuation">.</span><span class="token function">handle</span><span class="token punctuation">(</span>request<span class="token punctuation">,</span> response<span class="token punctuation">,</span> handler<span class="token punctuation">)</span><span class="token punctuation">;</span>  <span class="token comment">// 统一接口</span>
</code></pre></div><blockquote>
<p>就是不同 Controller 方法名、参数都不一样，Java 没法用同一行代码调用它们，HandlerAdapter 负责"认出"是哪种 Controller 再用对应的方式去调它</p>
</blockquote>
<h4>如果混合有继承接口的，也有注解的呢</h4>
<p>Spring 会注册多个 HandlerAdapter，按顺序逐个尝试，谁能处理谁上：</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token comment">// Spring 默认注册的适配器（有优先级顺序）</span>
<span class="token class-name">RequestMappingHandlerAdapter</span>      <span class="token comment">// 处理注解方式 @Controller</span>
<span class="token class-name">SimpleControllerHandlerAdapter</span>    <span class="token comment">// 处理实现 Controller 接口方式</span>
<span class="token class-name">HttpRequestHandlerAdapter</span>         <span class="token comment">// 处理实现 HttpRequestHandler 接口方式</span>
</code></pre></div><h5>处理流程</h5>
<div class="language-text" data-ext="text" data-title="text"><pre class="language-text"><code>DispatcherServlet 拿到请求
        ↓
遍历所有 HandlerAdapter，挨个问：
        ↓
RequestMappingHandlerAdapter：    "这个我能处理吗？" 
  → 是注解 Controller？能 ✅ → 直接调用
  → 是接口 Controller？不能 ❌ → 下一个

SimpleControllerHandlerAdapter：  "这个我能处理吗？"
  → 实现了 Controller 接口？能 ✅ → 直接调用
</code></pre></div><h5>代码角度</h5>
<p>每个 Adapter 都实现了 supports() 方法来判断自己能不能处理</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token comment">// RequestMappingHandlerAdapter</span>
<span class="token keyword">public</span> <span class="token keyword">boolean</span> <span class="token function">supports</span><span class="token punctuation">(</span><span class="token class-name">Object</span> handler<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">return</span> handler <span class="token keyword">instanceof</span> <span class="token class-name">HandlerMethod</span><span class="token punctuation">;</span>  <span class="token comment">// 注解方式才是 HandlerMethod</span>
<span class="token punctuation">}</span>

<span class="token comment">// SimpleControllerHandlerAdapter</span>
<span class="token keyword">public</span> <span class="token keyword">boolean</span> <span class="token function">supports</span><span class="token punctuation">(</span><span class="token class-name">Object</span> handler<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">return</span> handler <span class="token keyword">instanceof</span> <span class="token class-name">Controller</span><span class="token punctuation">;</span>  <span class="token comment">// 实现接口才是 Controller</span>
<span class="token punctuation">}</span>
</code></pre></div><p>DispatcherServlet 就是遍历调 supports()，谁返回 true 谁来处理</p>
]]></content:encoded>
    </item>
    <item>
      <title>Spring系列八股16 - MVC3 (Spring MVC 流程)</title>
      <link>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/Spring/article16.html</link>
      <guid>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/Spring/article16.html</guid>
      <source url="http://ekkosonya.cn/rss.xml">Spring系列八股16 - MVC3 (Spring MVC 流程)</source>
      <description>MVC3 前后端分离下 Spring MVC 流程 完整流程 @RestController @ResponseBody 告诉 Spring：不要找 View，把返回值直接转成 JSON 写进响应体 前后端分离下，Spring MVC 的 View 层消失了，Controller 只返回 JSON，由 @ResponseBody + HttpMessa...</description>
      <category>code</category>
      <pubDate>Mon, 06 Apr 2026 14:40:01 GMT</pubDate>
      <content:encoded><![CDATA[<h2>MVC3</h2>
<h3>前后端分离下 Spring MVC 流程</h3>
<div class="language-text" data-ext="text" data-title="text"><pre class="language-text"><code>传统 MVC：         前后端分离：
Controller         Controller
    ↓                  ↓
返回 ModelAndView   返回 JSON 数据
    ↓                  ↓
服务端渲染 HTML     前端自己渲染页面
</code></pre></div><h4>完整流程</h4>
<div class="language-text" data-ext="text" data-title="text"><pre class="language-text"><code>前端（Vue/React）发送请求
        ↓
DispatcherServlet 接收请求
        ↓
HandlerMapping 查路由表，找到对应 Controller 方法
        ↓
HandlerAdapter 调用 Controller 方法
        ↓
Controller 调用 Service → DAO → 数据库
        ↓
返回 Java 对象
        ↓
HttpMessageConverter 把对象转成 JSON
        ↓
前端收到 JSON，自己渲染页面
</code></pre></div><h4><code>@RestController</code></h4>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token annotation punctuation">@RestController</span>          <span class="token comment">// = @Controller + @ResponseBody</span>
<span class="token annotation punctuation">@RequestMapping</span><span class="token punctuation">(</span><span class="token string">"/user"</span><span class="token punctuation">)</span>
<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">UserController</span> <span class="token punctuation">{</span>
    <span class="token annotation punctuation">@GetMapping</span><span class="token punctuation">(</span><span class="token string">"/{id}"</span><span class="token punctuation">)</span>
    <span class="token keyword">public</span> <span class="token class-name">User</span> <span class="token function">getUser</span><span class="token punctuation">(</span><span class="token annotation punctuation">@PathVariable</span> <span class="token class-name">Long</span> id<span class="token punctuation">)</span> <span class="token punctuation">{</span>
        <span class="token keyword">return</span> userService<span class="token punctuation">.</span><span class="token function">getUser</span><span class="token punctuation">(</span>id<span class="token punctuation">)</span><span class="token punctuation">;</span>  <span class="token comment">// 直接返回对象，自动转 JSON</span>
    <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><p><code>@ResponseBody</code> 告诉 Spring：不要找 View，把返回值直接转成 JSON 写进响应体</p>
<p>前后端分离下，Spring MVC 的 View 层消失了，Controller 只返回 JSON，由 <code>@ResponseBody + HttpMessageConverter</code> 负责把 Java 对象序列化成 JSON 响应给前端</p>
<h3>HandlerMapping 和 HandlerAdapter</h3>
<h4><code>HandlerMapping</code></h4>
<p>作用：<code>HandlerMapping</code>负责将请求映射到处理器（Controller）</p>
<p>功能：根据请求的URL、请求参数等信息，找到处理请求的 Controller</p>
<p>类型：Spring 提供了多种<code>HandlerMapping</code>实现，如<code>BeanNameUrlHandlerMapping</code>、<code>RequestMappingHandlerMapping</code>等。</p>
<p>工作流程：根据请求信息确定要请求的处理器(Controller), <code>HandlerMapping</code> 可以根据URL、请求参数等规则确定对应的处理器。</p>
<h4><code>HandlerAdapter</code></h4>
<p>作用：<code>HandlerAdapter</code>负责调用处理器(Controller)来处理请求
功能：处理器(Controller)可能有不同的接口类型（Controller接口、<code>HttpRequestHandler</code>接口等），HandlerAdapter根据处理器的类型来选择合适的方法来调用处理器。
类型：Spring提供了多个HandlerAdapter实现，用于适配不同类型的处理器。
工作流程：根据处理器的接口类型，选择相应的HandlerAdapter来调用处理器</p>
<h3><code>HttpMessageConverter</code></h3>
<div class="language-text" data-ext="text" data-title="text"><pre class="language-text"><code>传统 MVC：         前后端分离：
Controller         Controller
    ↓                  ↓
返回 ModelAndView   返回 JSON 数据
    ↓                  ↓
服务端渲染 HTML     前端自己渲染页面
</code></pre></div><p>在前后端分离的项目中，负责 Java 对象 ↔ HTTP 报文之间的互相转换</p>
<h4>核心工作</h4>
<div class="language-text" data-ext="text" data-title="text"><pre class="language-text"><code>请求进来（前端 → 后端）
JSON 字符串  →  Java 对象      （反序列化）

响应出去（后端 → 前端）
Java 对象   →  JSON 字符串     （序列化）
</code></pre></div><h5>1. 读（Read）：将 HTTP 报文转换为 Java 对象</h5>
<p>当客户端（如浏览器、Postman）向你的接口发起请求，并在 Body 中携带了 JSON 数据时：</p>
<ul>
<li>你的 Controller 方法上通常会打上 <code>@RequestBody</code> 注解。</li>
<li>此时，Spring 会遍历已注册的 <code>HttpMessageConverter</code> 列表，找到能处理 <code>application/json</code> 类型的转换器。</li>
<li>转换器会读取 HTTP 请求的输入流（InputStream），并利用底层的 JSON 库（通常是 Jackson）将其反序列化为你方法参数中指定的 Java 对象。</li>
</ul>
<h5>2. 写（Write）：将 Java 对象转换为 HTTP 报文</h5>
<p>当你的业务逻辑处理完毕，需要将数据返回给前端时：</p>
<ul>
<li>你的 Controller 类上通常会打上 <code>@RestController</code>（自带 <code>@ResponseBody</code> 语义）。</li>
<li>你的方法直接 return 了一个 Java 对象（比如一个 <code>User</code> 实体或一个统装类 <code>Result&lt;T&gt;</code>）。</li>
<li>Spring 同样会找到合适的 <code>HttpMessageConverter</code>，将这个 Java 对象序列化成 JSON 字符串，并写入 HTTP 的输出流（OutputStream）返回给客户端</li>
</ul>
<h4>常见转换器</h4>
<p>Spring Boot 默认集成 Jackson，引入依赖就自动生效，不需要额外配置</p>
<p>| 转换器 | 处理类型 |
|</p>
]]></content:encoded>
    </item>
    <item>
      <title>Spring系列八股17 - MVC4 (RESTful)</title>
      <link>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/Spring/article17.html</link>
      <guid>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/Spring/article17.html</guid>
      <source url="http://ekkosonya.cn/rss.xml">Spring系列八股17 - MVC4 (RESTful)</source>
      <description>MVC4 RESTful RESTful（Representational State Transfer，表现层状态转化）本身并不是一种标准、协议或技术，而是一种软件架构风 当一个 Web 服务（API）的设计符合 REST 架构风格时，我们就称它为 RESTful API 简单来说，RESTful 提倡用 URL 定位资源，用 HTTP 动词（GET...</description>
      <category>code</category>
      <pubDate>Mon, 06 Apr 2026 14:40:01 GMT</pubDate>
      <content:encoded><![CDATA[<h2>MVC4</h2>
<h3>RESTful</h3>
<p>RESTful（Representational State Transfer，表现层状态转化）本身并不是一种标准、协议或技术，而是一种软件架构风</p>
<p>当一个 Web 服务（API）的设计符合 REST 架构风格时，我们就称它为 RESTful API</p>
<p>简单来说，RESTful 提倡用 URL 定位资源，用 HTTP 动词（GET, POST, PUT, DELETE 等）描述操作</p>
<h4>1. Resource（资源）</h4>
<p>在 REST 理念中，网络上的所有事物都被抽象为“资源”。一个资源可以是一个实体（如用户、订单、文章），也可以是一个集合（如用户列表）。</p>
<ul>
<li><strong>非 RESTful 风格的 URL（动词驱动）：</strong> 通常会在 URL 里写明动作，比如 <code>/getUser?id=1</code>、<code>/createUser</code>、<code>/deleteUser?id=1</code>。</li>
<li><strong>RESTful 风格的 URL（名词驱动）：</strong> URL 仅仅用来标识资源的位置，不包含动词。比如 <code>/users/1</code>、<code>/orders/123</code>。</li>
</ul>
<h4>2. Representational（表现层）</h4>
<p>资源在服务器端有一个具体的形态（比如数据库里的一条记录），但当它传输给客户端时，需要一种“表现形式”。
目前主流的 Web 开发中，这种表现形式通常是 <strong>JSON</strong>，早期也常用 XML。也就是说，客户端和服务器之间传递的是资源的 JSON 格式的数据。</p>
<h4>3. State Transfer（状态转化）</h4>
<p>HTTP 协议本身是无状态的（Stateless），这意味着所有的状态都保存在服务器端。如果客户端想要操作服务器端的资源，就必须通过某种手段让资源发生“状态转化”（比如创建、修改、删除）。
这种转化是通过 <strong>HTTP 动词</strong> 来实现的。在实际的后端开发中（例如使用 Spring Boot），这恰好完美对应了各种请求映射注解：</p>
<ul>
<li><strong>GET（查询）：</strong> 获取资源。
<ul>
<li><code>GET /api/users</code> -&gt; 获取所有用户列表</li>
<li><code>GET /api/users/1</code> -&gt; 获取 ID 为 1 的用户</li>
</ul>
</li>
<li><strong>POST（创建）：</strong> 新建资源。
<ul>
<li><code>POST /api/users</code> -&gt; 创建一个新用户（数据放在请求体 Body 中）</li>
</ul>
</li>
<li><strong>PUT（全量更新）：</strong> 更新资源的全部属性。
<ul>
<li><code>PUT /api/users/1</code> -&gt; 更新 ID 为 1 的用户的全部信息</li>
</ul>
</li>
<li><strong>PATCH（局部更新）：</strong> 更新资源的部分属性。
<ul>
<li><code>PATCH /api/users/1</code> -&gt; 仅更新 ID 为 1 的用户的某个字段（如密码）</li>
</ul>
</li>
<li><strong>DELETE（删除）：</strong> 删除资源。
<ul>
<li><code>DELETE /api/users/1</code> -&gt; 删除 ID 为 1 的用户</li>
</ul>
</li>
</ul>
]]></content:encoded>
    </item>
    <item>
      <title>Spring系列八股18 - SpringBoot (分析1)</title>
      <link>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/Spring/article18.html</link>
      <guid>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/Spring/article18.html</guid>
      <source url="http://ekkosonya.cn/rss.xml">Spring系列八股18 - SpringBoot (分析1)</source>
      <description>SpringBoot Spring Boot 可以说是 Spring 生态的一个重大突破，它极大地简化了 Spring 应用的开发和部署过程 以前我们用 Spring 开发项目的时候，需要配置一大堆 XML 文件，包括 Bean 的定义、数据源配置、事务配置等等，非常繁琐 而且还要手动管理各种 jar 包的依赖关系，很容易出现版本冲突的问题, 部署的时...</description>
      <category>code</category>
      <pubDate>Mon, 06 Apr 2026 14:40:01 GMT</pubDate>
      <content:encoded><![CDATA[<h2>SpringBoot</h2>
<p>Spring Boot 可以说是 Spring 生态的一个重大突破，它极大地简化了 Spring 应用的开发和部署过程</p>
<p>以前我们用 Spring 开发项目的时候，需要配置一大堆 XML 文件，包括 Bean 的定义、数据源配置、事务配置等等，非常繁琐</p>
<p>而且还要手动管理各种 jar 包的依赖关系，很容易出现版本冲突的问题, 部署的时候还要单独搭建 Tomcat 服务器，整个过程很复杂</p>
<p>Spring Boot 就是为了解决这些痛点而生的</p>
<blockquote>
<p>“约定大于配置”是 Spring Boot 最核心的理念</p>
</blockquote>
<p>它预设了很多默认配置，比如默认使用内嵌的 Tomcat 服务器，默认的日志框架是 Logback 等等。这样，我们开发者就只需要关注业务逻辑，不用再纠结于各种配置细节</p>
<blockquote>
<p>自动装配也是 Spring Boot 的一大特色</p>
</blockquote>
<p>它会根据项目中引入的依赖自动配置合适的 Bean</p>
<p>比如说，我们引入了 Spring Data JPA，Spring Boot 就会自动配置数据源；比如说，我们引入了 Spring Security，Spring Boot 就会自动配置安全相关的 Bean。</p>
<p>Spring Boot 还提供了很多开箱即用的功能，比如 Actuator 监控、DevTools 开发工具、Spring Boot Starter 等等。Actuator 可以让我们轻松监控应用的健康状态、性能指标等；DevTools 可以加快开发效率，比如自动重启、热部署等；Spring Boot Starter 则是一些预配置好的依赖集合，让我们可以快速引入某些常用的功能</p>
<p>优点：</p>
<ul>
<li>简化开发</li>
<li>快速启动</li>
<li>自动化配置</li>
</ul>
<h3>常用注解</h3>
<ul>
<li><code>@SpringBootApplication</code>：这是 Spring Boot 的核心注解，它是一个组合注解，包含了 <code>@Configuration</code>、<code>@EnableAutoConfiguration</code> 和 <code>@ComponentScan</code>。它标志着一个 Spring Boot 应用的入口</li>
<li><code>@SpringBootTest</code>：用于测试 Spring Boot 应用的注解，它会加载整个 Spring 上下文，适合集成测试</li>
</ul>
<h3>自动装配</h3>
<p>在 Spring Boot 中，开启自动装配的注解是<code>@EnableAutoConfiguration</code></p>
<p>这个注解会告诉 Spring 去扫描所有可用的自动配置类</p>
<p>Spring Boot 为了进一步简化，把这个注解包含到了 <code>@SpringBootApplication</code> 注解中</p>
<p>也就是说，当我们在主类上使用 <code>@SpringBootApplication</code> 注解时，实际上就已经开启了自动装配</p>
<h4>理解</h4>
<p>SpringBoot 定义了一套接口规范，这套规范规定：</p>
<p>SpringBoot 在启动时会扫描外部引用 jar 包中的 <code>META-INF/spring.factories</code> 文件，将文件中配置的类型信息加载到 Spring 容器（此处涉及到 JVM 类加载机制与 Spring 的容器知识），并执行类中定义的各种操作</p>
<p>对于外部 jar 来说，只需要按照 SpringBoot 定义的标准，就能将自己的功能装置进 SpringBoot</p>
<p>通俗来讲，自动装配就是通过注解或一些简单的配置就可以在 SpringBoot 的帮助下开启和配置各种功能，比如数据库访问、Web 开发</p>
<h4>原理</h4>
<p>首先点进 <code>@SpringBootApplication</code> 注解的内部</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token annotation punctuation">@Target</span><span class="token punctuation">(</span><span class="token punctuation">{</span><span class="token class-name">ElementType</span><span class="token punctuation">.</span><span class="token constant">TYPE</span><span class="token punctuation">}</span><span class="token punctuation">)</span>
<span class="token annotation punctuation">@Retention</span><span class="token punctuation">(</span><span class="token class-name">RetentionPolicy</span><span class="token punctuation">.</span><span class="token constant">RUNTIME</span><span class="token punctuation">)</span>
<span class="token annotation punctuation">@Documented</span>
<span class="token annotation punctuation">@Inherited</span>
<span class="token annotation punctuation">@SpringBootConfiguration</span>
<span class="token annotation punctuation">@EnableAutoConfiguration</span>
<span class="token annotation punctuation">@ComponentScan</span><span class="token punctuation">(</span>
    excludeFilters <span class="token operator">=</span> <span class="token punctuation">{</span><span class="token annotation punctuation">@Filter</span><span class="token punctuation">(</span>
    type <span class="token operator">=</span> <span class="token class-name">FilterType</span><span class="token punctuation">.</span><span class="token constant">CUSTOM</span><span class="token punctuation">,</span>
    classes <span class="token operator">=</span> <span class="token punctuation">{</span><span class="token class-name">TypeExcludeFilter</span><span class="token punctuation">.</span><span class="token keyword">class</span><span class="token punctuation">}</span>
<span class="token punctuation">)</span><span class="token punctuation">,</span> <span class="token annotation punctuation">@Filter</span><span class="token punctuation">(</span>
    type <span class="token operator">=</span> <span class="token class-name">FilterType</span><span class="token punctuation">.</span><span class="token constant">CUSTOM</span><span class="token punctuation">,</span>
    classes <span class="token operator">=</span> <span class="token punctuation">{</span><span class="token class-name">AutoConfigurationExcludeFilter</span><span class="token punctuation">.</span><span class="token keyword">class</span><span class="token punctuation">}</span>
<span class="token punctuation">)</span><span class="token punctuation">}</span>
<span class="token punctuation">)</span>
<span class="token keyword">public</span> <span class="token annotation punctuation">@interface</span> <span class="token class-name">SpringBootApplication</span> <span class="token punctuation">{</span>
  <span class="token punctuation">.</span><span class="token punctuation">.</span>
<span class="token punctuation">}</span>
</code></pre></div><ul>
<li><code>@Target({ElementType.TYPE})</code>: 该注解指定了这个注解可以用来标记在类上。在这个特定的例子中，这表示该注解用于标记配置类。</li>
<li><code>@Retention(RetentionPolicy.RUNTIME)</code>: 这个注解指定了注解的生命周期，即在运行时保留。这是因为 Spring Boot 在运行时扫描类路径上的注解来实现自动配置，所以这里使用了 RUNTIME 保留策略。</li>
<li><code>@Documented</code>: 该注解表示这个注解应该被包含在 Java 文档中。它是用于生成文档的标记，使开发者能够看到这个注解的相关信息。</li>
<li><code>@Inherited</code>: 这个注解指示一个被标注的类型是被继承的。在这个例子中，它表明这个注解可以被继承，如果一个类继承了带有这个注解的类，它也会继承这个注解。</li>
<li><code>@SpringBootConfiguration</code>: 这个注解表明这是一个 Spring Boot 配置类。如果点进这个注解内部会发现与标准的 <code>@Configuration</code> 没啥区别，只是为了表明这是一个专门用于 SpringBoot 的配置。</li>
<li><code>@EnableAutoConfiguration</code>: 这个注解是 Spring Boot 自动装配的核心。它告诉 Spring oot 启用自动配置机制，根据项目的依赖和配置自动配置应用程序的上下文。通过这个注解，SpringBoot 将尝试根据类路径上的依赖自动配置应用程序。</li>
<li><code>@ComponentScan</code>: 这个注解用于配置组件扫描的规则。在这里，它告诉 SpringBoot 在指定的包及其子包中查找组件，这些组件包括被注解的类、@Component 注解的类等。其中的 excludeFilters 参数用于指定排除哪些组件，这里使用了两个自定义的过滤器，分别是 TypeExcludeFilter 和 AutoConfigurationExcludeFilte</li>
</ul>
<blockquote>
<p>SpringBoot 利用 @ComponentScan 扫描当前项目（主类所在包及其子包）中标注了 @Component、@Service、@Controller 等注解的类，将其注册为 Bean</p>
<p>利用 @EnableAutoConfiguration 触发自动装配：借助 SpringFactoriesLoader 读取 classpath 各依赖 jar 包中 META-INF/spring.factories</p>
<p>Spring Boot 3.x 后改为 META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports）里预定义的自动配置类</p>
<p>再配合 @Conditional 系列注解按条件将 Bean 注册到容器</p>
</blockquote>
<p>| | <code>@ComponentScan</code> | <code>@EnableAutoConfiguration</code> |
|</p>
]]></content:encoded>
    </item>
    <item>
      <title>Spring系列八股19 - SpringBoot2 (自动装配分析 @Import)</title>
      <link>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/Spring/article19.html</link>
      <guid>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/Spring/article19.html</guid>
      <source url="http://ekkosonya.cn/rss.xml">Spring系列八股19 - SpringBoot2 (自动装配分析 @Import)</source>
      <description>SpringBoot @Import 它的核心作用是：将指定的类、配置类或动态生成的 Bean 注册到 Spring 的 IoC 容器中 @ComponentScan 只能扫描你项目包路径下的类。但如果你想导入一个外部 jar 包里的类，或者一个不在扫描路径下的配置类，@ComponentScan 就够不着了。 @Import 就是解决这个问题的。 引...</description>
      <category>code</category>
      <pubDate>Mon, 06 Apr 2026 14:40:01 GMT</pubDate>
      <content:encoded><![CDATA[<h2>SpringBoot</h2>
<h3><code>@Import</code></h3>
<p>它的核心作用是：将指定的类、配置类或动态生成的 Bean 注册到 Spring 的 IoC 容器中</p>
<p><code>@ComponentScan</code> 只能扫描你项目包路径下的类。但如果你想导入一个外部 jar 包里的类，或者一个不在扫描路径下的配置类，<code>@ComponentScan</code> 就够不着了。</p>
<p><code>@Import</code> 就是解决这个问题的。</p>
<h4>引入普通类或 <code>@Configuration</code> 类</h4>
<p>当你把项目拆分成了多个配置类，或者你想直接把某个没有加上 @Component 注解的第三方类注入到 Spring 容器时，就可以直接使用它</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token comment">// 被引入的配置类</span>
<span class="token annotation punctuation">@Configuration</span>
<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">DatabaseConfig</span> <span class="token punctuation">{</span>
    <span class="token comment">// ... 配置数据库 Bean</span>
<span class="token punctuation">}</span>

<span class="token comment">// 主配置类</span>
<span class="token annotation punctuation">@Configuration</span>
<span class="token annotation punctuation">@Import</span><span class="token punctuation">(</span><span class="token punctuation">{</span><span class="token class-name">DatabaseConfig</span><span class="token punctuation">.</span><span class="token keyword">class</span><span class="token punctuation">,</span> <span class="token class-name">User</span><span class="token punctuation">.</span><span class="token keyword">class</span><span class="token punctuation">}</span><span class="token punctuation">)</span> <span class="token comment">// 明确引入 DatabaseConfig 和 User 类</span>
<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">AppConfig</span> <span class="token punctuation">{</span>
<span class="token punctuation">}</span>
</code></pre></div><p>效果：DatabaseConfig 里面的 Bean 以及 User 类本身，都会被直接注册进 Spring 容器</p>
<h4>引入 <code>ImportSelector</code> 的实现类（动态引入机制）</h4>
<p>这是 Spring Boot 自动装配（Auto-Configuration）的核心秘密所在。你可以通过编写代码逻辑，动态决定要向容器中注入哪些配置类</p>
<ul>
<li>实现 ImportSelector 接口的 selectImports 方法，返回一个包含全限定类名的字符串数组。Spring 会把这些类注册进容器</li>
</ul>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">MyImportSelector</span> <span class="token keyword">implements</span> <span class="token class-name">ImportSelector</span> <span class="token punctuation">{</span>
    <span class="token annotation punctuation">@Override</span>
    <span class="token keyword">public</span> <span class="token class-name">String</span><span class="token punctuation">[</span><span class="token punctuation">]</span> <span class="token function">selectImports</span><span class="token punctuation">(</span><span class="token class-name">AnnotationMetadata</span> importingClassMetadata<span class="token punctuation">)</span> <span class="token punctuation">{</span>
        <span class="token comment">// 可以根据条件（比如环境变量、其他注解存在与否）动态返回要加载的类</span>
        <span class="token keyword">return</span> <span class="token keyword">new</span> <span class="token class-name">String</span><span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">{</span><span class="token string">"com.example.config.AConfig"</span><span class="token punctuation">,</span> <span class="token string">"com.example.config.BConfig"</span><span class="token punctuation">}</span><span class="token punctuation">;</span>
    <span class="token punctuation">}</span>
<span class="token punctuation">}</span>

<span class="token annotation punctuation">@Configuration</span>
<span class="token annotation punctuation">@Import</span><span class="token punctuation">(</span><span class="token class-name">MyImportSelector</span><span class="token punctuation">.</span><span class="token keyword">class</span><span class="token punctuation">)</span> 
<span class="token comment">// Spring 会调用其 selectImports 方法并注入返回的类</span>
<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">AppConfig</span> <span class="token punctuation">{</span><span class="token punctuation">}</span>
</code></pre></div><h4>引入 ImportBeanDefinitionRegistrar 的实现类（最底层、最灵活的用法）</h4>
<p>如果你需要更细粒度的控制，比如自己构建 BeanDefinition（Bean 的定义信息，包含作用域、懒加载等），你可以使用这种方式。</p>
<ul>
<li>MyBatis 的 <code>@MapperScan</code>、Feign 的 <code>@EnableFeignClients</code> 等框架整合注解，底层几乎全是用这个机制实现的，用来动态扫描并生成代理对象的 Bean。</li>
</ul>
<h4>原理分析</h4>
<blockquote>
<p>为什么设计一个实现类 ImportSelector 接口，然后@Import它就会调用对应的selectImports方法</p>
</blockquote>
<p>这并不是 Java 语言本身的某种自动机制，而是 Spring 容器在启动时，专门写了一段代码去“特殊照顾” ImportSelector 这个接口</p>
<h5>后置处理器 <code>ConfigurationClassPostProcessor</code></h5>
<p>在 Spring 启动时，有一个非常核心的“总管家”叫做 <code>ConfigurationClassPostProcessor</code>（它是一个 Bean 工厂后置处理器）</p>
<p>它的主要工作就是去解析所有的配置类（带有 <code>@Configuration</code>、<code>@Component</code>、<code>@Import</code> 等注解的类）</p>
<p>当这位总管家在解析某个配置类时，如果看到了 <code>@Import</code> 注解，它就会进入一个专门处理 Import 的解析器：<code>ConfigurationClassParser</code></p>
<p>在 <code>ConfigurationClassParser</code> 的源码中（具体是 <code>processImports</code> 方法），Spring 拿到你 <code>@Import</code> 进来的类之后，做了一个<strong>类型判断大分流</strong></p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token comment">// configClass：当前正在被解析的配置类（比如标了 @SpringBootApplication 的主类）</span>
<span class="token comment">// currentSourceClass：当前配置类的源信息</span>
<span class="token comment">// importCandidates：@Import 里传入的所有候选类</span>
<span class="token comment">// filter：过滤器，用于排除某些类</span>
<span class="token comment">// checkForCircularImports：是否要检查循环导入</span>
<span class="token keyword">private</span> <span class="token keyword">void</span> <span class="token function">processImports</span><span class="token punctuation">(</span><span class="token class-name">ConfigurationClass</span> configClass<span class="token punctuation">,</span> <span class="token class-name">SourceClass</span> currentSourceClass<span class="token punctuation">,</span> <span class="token class-name">Collection</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">SourceClass</span><span class="token punctuation">&gt;</span></span> importCandidates<span class="token punctuation">,</span> <span class="token class-name">Predicate</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">String</span><span class="token punctuation">&gt;</span></span> filter<span class="token punctuation">,</span> <span class="token keyword">boolean</span> checkForCircularImports<span class="token punctuation">)</span> <span class="token punctuation">{</span>

  <span class="token comment">// 如果没有任何 @Import 的候选类，直接跳过</span>
  <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token operator">!</span>importCandidates<span class="token punctuation">.</span><span class="token function">isEmpty</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>

      <span class="token comment">// 检查循环导入：比如 A @Import B，B 又 @Import A，这种情况直接报错</span>
      <span class="token keyword">if</span> <span class="token punctuation">(</span>checkForCircularImports <span class="token operator">&amp;&amp;</span> <span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">isChainedImportOnStack</span><span class="token punctuation">(</span>configClass<span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
          <span class="token keyword">this</span><span class="token punctuation">.</span>problemReporter<span class="token punctuation">.</span><span class="token function">error</span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token class-name">CircularImportProblem</span><span class="token punctuation">(</span>configClass<span class="token punctuation">,</span> <span class="token keyword">this</span><span class="token punctuation">.</span>importStack<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
      <span class="token punctuation">}</span> <span class="token keyword">else</span> <span class="token punctuation">{</span>
          <span class="token comment">// 把当前配置类压栈，用于后续循环导入的检测</span>
          <span class="token keyword">this</span><span class="token punctuation">.</span>importStack<span class="token punctuation">.</span><span class="token function">push</span><span class="token punctuation">(</span>configClass<span class="token punctuation">)</span><span class="token punctuation">;</span>

          <span class="token keyword">try</span> <span class="token punctuation">{</span>
              <span class="token comment">// 遍历所有 @Import 进来的候选类，逐一判断类型</span>
              <span class="token keyword">for</span><span class="token punctuation">(</span><span class="token class-name">SourceClass</span> candidate <span class="token operator">:</span> importCandidates<span class="token punctuation">)</span> <span class="token punctuation">{</span>

                  <span class="token comment">// ① 判断是否是 ImportSelector 类型（如 AutoConfigurationImportSelector）</span>
                  <span class="token keyword">if</span> <span class="token punctuation">(</span>candidate<span class="token punctuation">.</span><span class="token function">isAssignable</span><span class="token punctuation">(</span><span class="token class-name">ImportSelector</span><span class="token punctuation">.</span><span class="token keyword">class</span><span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
                      <span class="token class-name">Class</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token operator">?</span><span class="token punctuation">&gt;</span></span> candidateClass <span class="token operator">=</span> candidate<span class="token punctuation">.</span><span class="token function">loadClass</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
                      <span class="token comment">// 实例化这个 ImportSelector</span>
                      <span class="token class-name">ImportSelector</span> selector <span class="token operator">=</span> <span class="token punctuation">(</span><span class="token class-name">ImportSelector</span><span class="token punctuation">)</span><span class="token class-name">ParserStrategyUtils</span><span class="token punctuation">.</span><span class="token function">instantiateClass</span><span class="token punctuation">(</span>candidateClass<span class="token punctuation">,</span> <span class="token class-name">ImportSelector</span><span class="token punctuation">.</span><span class="token keyword">class</span><span class="token punctuation">,</span> <span class="token keyword">this</span><span class="token punctuation">.</span>environment<span class="token punctuation">,</span> <span class="token keyword">this</span><span class="token punctuation">.</span>resourceLoader<span class="token punctuation">,</span> <span class="token keyword">this</span><span class="token punctuation">.</span>registry<span class="token punctuation">)</span><span class="token punctuation">;</span>

                      <span class="token comment">// 如果 selector 自带了额外的排除过滤器，合并进来</span>
                      <span class="token class-name">Predicate</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">String</span><span class="token punctuation">&gt;</span></span> selectorFilter <span class="token operator">=</span> selector<span class="token punctuation">.</span><span class="token function">getExclusionFilter</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
                      <span class="token keyword">if</span> <span class="token punctuation">(</span>selectorFilter <span class="token operator">!=</span> <span class="token keyword">null</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
                          filter <span class="token operator">=</span> filter<span class="token punctuation">.</span><span class="token function">or</span><span class="token punctuation">(</span>selectorFilter<span class="token punctuation">)</span><span class="token punctuation">;</span>
                      <span class="token punctuation">}</span>

                      <span class="token comment">// 判断是否是「延迟执行」的 ImportSelector</span>
                      <span class="token keyword">if</span> <span class="token punctuation">(</span>selector <span class="token keyword">instanceof</span> <span class="token class-name">DeferredImportSelector</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
                          <span class="token comment">// DeferredImportSelector 不立即执行，等所有配置类都解析完再处理</span>
                          <span class="token comment">// AutoConfigurationImportSelector 就实现了这个接口</span>
                          <span class="token comment">// 目的是确保自动配置类在用户自定义的 Bean 之后再处理，避免覆盖用户配置</span>
                          <span class="token class-name">DeferredImportSelector</span> deferredImportSelector <span class="token operator">=</span> <span class="token punctuation">(</span><span class="token class-name">DeferredImportSelector</span><span class="token punctuation">)</span>selector<span class="token punctuation">;</span>
                          <span class="token keyword">this</span><span class="token punctuation">.</span>deferredImportSelectorHandler<span class="token punctuation">.</span><span class="token function">handle</span><span class="token punctuation">(</span>configClass<span class="token punctuation">,</span> deferredImportSelector<span class="token punctuation">)</span><span class="token punctuation">;</span>
                      <span class="token punctuation">}</span> <span class="token keyword">else</span> <span class="token punctuation">{</span>
                          <span class="token comment">// 普通 ImportSelector：立即调用 selectImports() 拿到要导入的类名数组</span>
                          <span class="token class-name">String</span><span class="token punctuation">[</span><span class="token punctuation">]</span> importClassNames <span class="token operator">=</span> selector<span class="token punctuation">.</span><span class="token function">selectImports</span><span class="token punctuation">(</span>currentSourceClass<span class="token punctuation">.</span><span class="token function">getMetadata</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
                          <span class="token class-name">Collection</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">SourceClass</span><span class="token punctuation">&gt;</span></span> importSourceClasses <span class="token operator">=</span> <span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">asSourceClasses</span><span class="token punctuation">(</span>importClassNames<span class="token punctuation">,</span> filter<span class="token punctuation">)</span><span class="token punctuation">;</span>
                          <span class="token comment">// 递归处理这些被导入的类（它们可能也有 @Import）</span>
                          <span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">processImports</span><span class="token punctuation">(</span>configClass<span class="token punctuation">,</span> currentSourceClass<span class="token punctuation">,</span> importSourceClasses<span class="token punctuation">,</span> filter<span class="token punctuation">,</span> <span class="token boolean">false</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
                      <span class="token punctuation">}</span>

                  <span class="token comment">// ② 判断是否是 ImportBeanDefinitionRegistrar 类型</span>
                  <span class="token punctuation">}</span> <span class="token keyword">else</span> <span class="token keyword">if</span> <span class="token punctuation">(</span>candidate<span class="token punctuation">.</span><span class="token function">isAssignable</span><span class="token punctuation">(</span><span class="token class-name">ImportBeanDefinitionRegistrar</span><span class="token punctuation">.</span><span class="token keyword">class</span><span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
                      <span class="token class-name">Class</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token operator">?</span><span class="token punctuation">&gt;</span></span> candidateClass <span class="token operator">=</span> candidate<span class="token punctuation">.</span><span class="token function">loadClass</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
                      <span class="token comment">// 实例化后先存起来，等配置类解析完再统一调用 registerBeanDefinitions()</span>
                      <span class="token class-name">ImportBeanDefinitionRegistrar</span> registrar <span class="token operator">=</span> <span class="token punctuation">(</span><span class="token class-name">ImportBeanDefinitionRegistrar</span><span class="token punctuation">)</span><span class="token class-name">ParserStrategyUtils</span><span class="token punctuation">.</span><span class="token function">instantiateClass</span><span class="token punctuation">(</span>candidateClass<span class="token punctuation">,</span> <span class="token class-name">ImportBeanDefinitionRegistrar</span><span class="token punctuation">.</span><span class="token keyword">class</span><span class="token punctuation">,</span> <span class="token keyword">this</span><span class="token punctuation">.</span>environment<span class="token punctuation">,</span> <span class="token keyword">this</span><span class="token punctuation">.</span>resourceLoader<span class="token punctuation">,</span> <span class="token keyword">this</span><span class="token punctuation">.</span>registry<span class="token punctuation">)</span><span class="token punctuation">;</span>
                      configClass<span class="token punctuation">.</span><span class="token function">addImportBeanDefinitionRegistrar</span><span class="token punctuation">(</span>registrar<span class="token punctuation">,</span> currentSourceClass<span class="token punctuation">.</span><span class="token function">getMetadata</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

                  <span class="token comment">// ③ 普通类（既不是 ImportSelector 也不是 Registrar）</span>
                  <span class="token punctuation">}</span> <span class="token keyword">else</span> <span class="token punctuation">{</span>
                      <span class="token comment">// 直接当作 @Configuration 配置类来处理，注册到容器中</span>
                      <span class="token keyword">this</span><span class="token punctuation">.</span>importStack<span class="token punctuation">.</span><span class="token function">registerImport</span><span class="token punctuation">(</span>currentSourceClass<span class="token punctuation">.</span><span class="token function">getMetadata</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span> candidate<span class="token punctuation">.</span><span class="token function">getMetadata</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">getClassName</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
                      <span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">processConfigurationClass</span><span class="token punctuation">(</span>candidate<span class="token punctuation">.</span><span class="token function">asConfigClass</span><span class="token punctuation">(</span>configClass<span class="token punctuation">)</span><span class="token punctuation">,</span> filter<span class="token punctuation">)</span><span class="token punctuation">;</span>
                  <span class="token punctuation">}</span>
              <span class="token punctuation">}</span>
          <span class="token punctuation">}</span> <span class="token keyword">catch</span> <span class="token punctuation">(</span><span class="token class-name">BeanDefinitionStoreException</span> ex<span class="token punctuation">)</span> <span class="token punctuation">{</span>
              <span class="token keyword">throw</span> ex<span class="token punctuation">;</span>
          <span class="token punctuation">}</span> <span class="token keyword">catch</span> <span class="token punctuation">(</span><span class="token class-name">Throwable</span> ex<span class="token punctuation">)</span> <span class="token punctuation">{</span>
              <span class="token keyword">throw</span> <span class="token keyword">new</span> <span class="token class-name">BeanDefinitionStoreException</span><span class="token punctuation">(</span><span class="token string">"Failed to process import candidates for configuration class ["</span> <span class="token operator">+</span> configClass<span class="token punctuation">.</span><span class="token function">getMetadata</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">getClassName</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">+</span> <span class="token string">"]: "</span> <span class="token operator">+</span> ex<span class="token punctuation">.</span><span class="token function">getMessage</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span> ex<span class="token punctuation">)</span><span class="token punctuation">;</span>
          <span class="token punctuation">}</span> <span class="token keyword">finally</span> <span class="token punctuation">{</span>
              <span class="token comment">// 无论成功还是失败，最后都要把当前类从栈里弹出</span>
              <span class="token keyword">this</span><span class="token punctuation">.</span>importStack<span class="token punctuation">.</span><span class="token function">pop</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
          <span class="token punctuation">}</span>
      <span class="token punctuation">}</span>

  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div>]]></content:encoded>
    </item>
    <item>
      <title>Spring系列八股20 - SpringBoot3 (Spring Boot 启动原理)</title>
      <link>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/Spring/article20.html</link>
      <guid>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/Spring/article20.html</guid>
      <source url="http://ekkosonya.cn/rss.xml">Spring系列八股20 - SpringBoot3 (Spring Boot 启动原理)</source>
      <description>SpringBoot alt textalt text 创建 SpringApplication 实例，并识别应用类型，比如说是标准的 Servlet Web 还是响应式的 WebFlux，然后准备监听器和初始化监听容器。 创建并准备 ApplicationContext，将主类作为配置源进行加载。 刷新 Spring 上下文，触发 Bean 的实例化...</description>
      <category>code</category>
      <pubDate>Mon, 06 Apr 2026 14:40:01 GMT</pubDate>
      <content:encoded><![CDATA[<h2>SpringBoot</h2>
<figure><figcaption>alt text</figcaption></figure>
<ul>
<li>创建 SpringApplication 实例，并识别应用类型，比如说是标准的 Servlet Web 还是响应式的 WebFlux，然后准备监听器和初始化监听容器。</li>
<li>创建并准备 ApplicationContext，将主类作为配置源进行加载。</li>
<li>刷新 Spring 上下文，触发 Bean 的实例化，比如说扫描并注册 @ComponentScan 指定路径下的 Bean</li>
<li>触发自动配置，在 Spring Boot 2.7 及之前是通过 spring.factories 加载的，3.x 是通过读取 AutoConfiguration.imports，并结合 @ConditionalOn 系列注解依据条件注册 Bean</li>
<li>如果引入了 Web 相关依赖，会创建并启动 Tomcat 容器，完成 HTTP 端口监听</li>
</ul>
<h3>Spring Boot 启动流程</h3>
<p>可以把 Spring Boot 容器比作一个汽车制造厂：</p>
<ul>
<li><strong>BeanDefinition (Bean 定义)</strong>：汽车的设计图纸。</li>
<li><strong>BeanFactoryPostProcessor</strong>：审阅和修改图纸的工程师（在真正造车之前，他们可以修改图纸，或者画新的图纸）。</li>
<li><strong>Bean (实例)</strong>：造出来的真正汽车</li>
</ul>
<h4>源码分析</h4>
<p>从启动类出发</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token annotation punctuation">@SpringBootApplication</span>
<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">Demo1Application</span> <span class="token punctuation">{</span>
    <span class="token keyword">public</span> <span class="token keyword">static</span> <span class="token keyword">void</span> <span class="token function">main</span><span class="token punctuation">(</span><span class="token class-name">String</span><span class="token punctuation">[</span><span class="token punctuation">]</span> args<span class="token punctuation">)</span> <span class="token punctuation">{</span>
        <span class="token class-name">SpringApplication</span><span class="token punctuation">.</span><span class="token function">run</span><span class="token punctuation">(</span><span class="token class-name">Demo1Application</span><span class="token punctuation">.</span><span class="token keyword">class</span><span class="token punctuation">,</span> args<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><h5>编译阶段</h5>
<div class="language-text" data-ext="text" data-title="text"><pre class="language-text"><code>src/main/java/
  ├── Demo1Application.java
  ├── controller/
  │    └── UserController.java
  ├── service/
  │    └── UserService.java
  └── entity/
       └── User.java

         ↓ mvn compile / 点击运行时 IDE 自动编译

target/classes/
  ├── Demo1Application.class
  ├── controller/
  │    └── UserController.class
  ├── service/
  │    └── UserService.class
  └── entity/
       └── User.class
</code></pre></div><p>所有 .java 文件都会被编译成对应的 .class 文件，注解也都带着</p>
<blockquote>
<p>但 Spring 运行时是从入口类出发，通过 @ComponentScan 按需扫描加载，不是一股脑全读进来</p>
</blockquote>
<div class="language-text" data-ext="text" data-title="text"><pre class="language-text"><code>src/ 下所有 .java  →  javac 编译  →  target/classes/ 下所有 .class
  - 普通注解（@Controller 等）：存入 .class 元数据
  - Lombok 注解（@Data 等）：APT 直接生成代码到 .class
  - 第三方包：检查类存不存在，仅此而已
</code></pre></div><h5><code>SpringApplication</code> 实例</h5>
<p>JVM 启动，进入 main 方法</p>
<p><code>primarySource</code>: 传入的启动类的 Class 对象</p>
<ul>
<li>以这个类所在的包为根包，进行组件扫描</li>
<li>读取这个类上的注解 —— @SpringBootApplication、@EnableAutoConfiguration 等都标在它上面</li>
<li>作为第一张图纸 —— 它本身会被注册为一个配置类（@Configuration），是整个容器解析的起点</li>
</ul>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">public</span> <span class="token keyword">static</span> <span class="token class-name">ConfigurableApplicationContext</span> <span class="token function">run</span><span class="token punctuation">(</span><span class="token class-name">Class</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token operator">?</span><span class="token punctuation">&gt;</span></span> primarySource<span class="token punctuation">,</span> <span class="token class-name">String</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span> args<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">return</span> <span class="token function">run</span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token class-name">Class</span><span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">{</span>primarySource<span class="token punctuation">}</span><span class="token punctuation">,</span> args<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>

<span class="token keyword">public</span> <span class="token keyword">static</span> <span class="token class-name">ConfigurableApplicationContext</span> <span class="token function">run</span><span class="token punctuation">(</span><span class="token class-name">Class</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token operator">?</span><span class="token punctuation">&gt;</span></span><span class="token punctuation">[</span><span class="token punctuation">]</span> primarySources<span class="token punctuation">,</span> <span class="token class-name">String</span><span class="token punctuation">[</span><span class="token punctuation">]</span> args<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">return</span> <span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token class-name">SpringApplication</span><span class="token punctuation">(</span>primarySources<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">run</span><span class="token punctuation">(</span>args<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre></div><p>最终以 入口类 <code>primarySource</code> 创建了 <code>SpringApplication</code> 实例，并执行了 run 方法</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">public</span> <span class="token class-name">SpringApplication</span><span class="token punctuation">(</span><span class="token class-name">Class</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token operator">?</span><span class="token punctuation">&gt;</span></span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span> primarySources<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">this</span><span class="token punctuation">(</span><span class="token punctuation">(</span><span class="token class-name">ResourceLoader</span><span class="token punctuation">)</span><span class="token keyword">null</span><span class="token punctuation">,</span> primarySources<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>

<span class="token keyword">public</span> <span class="token class-name">SpringApplication</span><span class="token punctuation">(</span><span class="token annotation punctuation">@Nullable</span> <span class="token class-name">ResourceLoader</span> resourceLoader<span class="token punctuation">,</span> <span class="token class-name">Class</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token operator">?</span><span class="token punctuation">&gt;</span></span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span> primarySources<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">this</span><span class="token punctuation">.</span>addCommandLineProperties <span class="token operator">=</span> <span class="token boolean">true</span><span class="token punctuation">;</span>
    <span class="token keyword">this</span><span class="token punctuation">.</span>addConversionService <span class="token operator">=</span> <span class="token boolean">true</span><span class="token punctuation">;</span>
    <span class="token keyword">this</span><span class="token punctuation">.</span>headless <span class="token operator">=</span> <span class="token boolean">true</span><span class="token punctuation">;</span>
    <span class="token keyword">this</span><span class="token punctuation">.</span>initializers <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">ArrayList</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">this</span><span class="token punctuation">.</span>listeners <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">ArrayList</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">this</span><span class="token punctuation">.</span>additionalProfiles <span class="token operator">=</span> <span class="token class-name">Collections</span><span class="token punctuation">.</span><span class="token function">emptySet</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">this</span><span class="token punctuation">.</span>applicationContextFactory <span class="token operator">=</span> <span class="token class-name">ApplicationContextFactory</span><span class="token punctuation">.</span><span class="token constant">DEFAULT</span><span class="token punctuation">;</span>
    <span class="token keyword">this</span><span class="token punctuation">.</span>applicationStartup <span class="token operator">=</span> <span class="token class-name">ApplicationStartup</span><span class="token punctuation">.</span><span class="token constant">DEFAULT</span><span class="token punctuation">;</span>
    <span class="token keyword">this</span><span class="token punctuation">.</span>properties <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">ApplicationProperties</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">this</span><span class="token punctuation">.</span>resourceLoader <span class="token operator">=</span> resourceLoader<span class="token punctuation">;</span>
    <span class="token class-name">Assert</span><span class="token punctuation">.</span><span class="token function">notNull</span><span class="token punctuation">(</span>primarySources<span class="token punctuation">,</span> <span class="token string">"'primarySources' must not be null"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">this</span><span class="token punctuation">.</span>primarySources <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">LinkedHashSet</span><span class="token punctuation">(</span><span class="token class-name">Arrays</span><span class="token punctuation">.</span><span class="token function">asList</span><span class="token punctuation">(</span>primarySources<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">this</span><span class="token punctuation">.</span>properties<span class="token punctuation">.</span><span class="token function">setWebApplicationType</span><span class="token punctuation">(</span><span class="token class-name">WebApplicationType</span><span class="token punctuation">.</span><span class="token function">deduce</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">this</span><span class="token punctuation">.</span>bootstrapRegistryInitializers <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">ArrayList</span><span class="token punctuation">(</span><span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">getSpringFactoriesInstances</span><span class="token punctuation">(</span><span class="token class-name">BootstrapRegistryInitializer</span><span class="token punctuation">.</span><span class="token keyword">class</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">setInitializers</span><span class="token punctuation">(</span><span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">getSpringFactoriesInstances</span><span class="token punctuation">(</span><span class="token class-name">ApplicationContextInitializer</span><span class="token punctuation">.</span><span class="token keyword">class</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">setListeners</span><span class="token punctuation">(</span><span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">getSpringFactoriesInstances</span><span class="token punctuation">(</span><span class="token class-name">ApplicationListener</span><span class="token punctuation">.</span><span class="token keyword">class</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">this</span><span class="token punctuation">.</span>mainApplicationClass <span class="token operator">=</span> <span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">deduceMainApplicationClass</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre></div><p>后面在 <code>run()</code> 里创建容器时，Spring Boot 会根据应用类型自动创建对应的 <code>ResourceLoader</code></p>
<blockquote>
<p><code>this.applicationContextFactory = ApplicationContextFactory.DEFAULT;</code> 构建 applicationContext 的工厂</p>
</blockquote>
<p>采用的是 <code>ApplicationContextFactory.DEFAULT</code></p>
<p>即</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">public</span> <span class="token keyword">interface</span> <span class="token class-name">ApplicationContextFactory</span> <span class="token punctuation">{</span>
    <span class="token class-name">ApplicationContextFactory</span> <span class="token constant">DEFAULT</span> <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">DefaultApplicationContextFactory</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span>
<span class="token punctuation">}</span>
</code></pre></div><blockquote>
<p><code>WebApplicationType.deduce()</code> 自动推断应用类型</p>
</blockquote>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">this</span><span class="token punctuation">.</span>properties<span class="token punctuation">.</span><span class="token function">setWebApplicationType</span><span class="token punctuation">(</span><span class="token class-name">WebApplicationType</span><span class="token punctuation">.</span><span class="token function">deduce</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>`
</code></pre></div><p>Spring Boot 会根据 classpath 里有没有某些类，自动判断你是什么类型的应用</p>
<div class="language-text" data-ext="text" data-title="text"><pre class="language-text"><code>有 DispatcherServlet → SERVLET（普通 Web 应用）
有 DispatcherHandler → REACTIVE（响应式 Web 应用）
都没有             → NONE（纯后台应用，不启动 Tomcat）
</code></pre></div><p>这决定了后面创建的是哪种 ApplicationContext</p>
<blockquote>
<p><code>deduceMainApplicationClass()</code>&nbsp;通过堆栈推断主类</p>
</blockquote>
<p><code>this.mainApplicationClass = this.deduceMainApplicationClass();</code></p>
<p>没有直接用 <code>primarySource</code>，而是通过<strong>分析当前调用堆栈</strong>，找到哪个类有 <code>main</code> 方法，用于后续打印日志：</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token comment">// 伪代码：</span>
<span class="token class-name">StackTraceElement</span><span class="token punctuation">[</span><span class="token punctuation">]</span> stack <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">RuntimeException</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">getStackTrace</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token keyword">for</span> <span class="token punctuation">(</span><span class="token class-name">StackTraceElement</span> element <span class="token operator">:</span> stack<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token string">"main"</span><span class="token punctuation">.</span><span class="token function">equals</span><span class="token punctuation">(</span>element<span class="token punctuation">.</span><span class="token function">getMethodName</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
        <span class="token keyword">return</span> <span class="token class-name">Class</span><span class="token punctuation">.</span><span class="token function">forName</span><span class="token punctuation">(</span>element<span class="token punctuation">.</span><span class="token function">getClassName</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>  <span class="token comment">// 找到了</span>
    <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><h5>启动 <code>run</code></h5>
<p>最终是在 <code>SpringApplication</code> 类的 <code>run</code> 函数</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">public</span> <span class="token class-name">ConfigurableApplicationContext</span> <span class="token function">run</span><span class="token punctuation">(</span><span class="token class-name">String</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span> args<span class="token punctuation">)</span> <span class="token punctuation">{</span>
  <span class="token comment">// 记录启动时间（用于最后算启动耗时）</span>
  <span class="token comment">// 还会看有没有 CoRC 快照</span>
  <span class="token class-name">Startup</span> startup <span class="token operator">=</span> <span class="token class-name">SpringApplication<span class="token punctuation">.</span>Startup</span><span class="token punctuation">.</span><span class="token function">create</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

  <span class="token comment">// 注册 JVM 关闭钩子，确保应用关闭时能优雅释放资源（关闭数据库连接池等）</span>
  <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token keyword">this</span><span class="token punctuation">.</span>properties<span class="token punctuation">.</span><span class="token function">isRegisterShutdownHook</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
      shutdownHook<span class="token punctuation">.</span><span class="token function">enableShutdownHookAddition</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token punctuation">}</span>

  <span class="token comment">// 创建引导上下文（给 Bootstrapper 扩展用，一般用不到）</span>
  <span class="token class-name">DefaultBootstrapContext</span> bootstrapContext <span class="token operator">=</span> <span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">createBootstrapContext</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

  <span class="token comment">// 注册了上下文</span>
  <span class="token class-name">ConfigurableApplicationContext</span> context <span class="token operator">=</span> <span class="token keyword">null</span><span class="token punctuation">;</span>

  <span class="token comment">// 设置 headless 模式（服务器环境没有显示器，告诉 JVM 不需要图形界面）</span>
  <span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">configureHeadlessProperty</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

  <span class="token comment">// 获取所有 SpringApplicationRunListener，准备广播启动事件</span>
  <span class="token class-name">SpringApplicationRunListeners</span> listeners <span class="token operator">=</span> <span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">getRunListeners</span><span class="token punctuation">(</span>args<span class="token punctuation">)</span><span class="token punctuation">;</span>
  listeners<span class="token punctuation">.</span><span class="token function">starting</span><span class="token punctuation">(</span>bootstrapContext<span class="token punctuation">,</span> <span class="token keyword">this</span><span class="token punctuation">.</span>mainApplicationClass<span class="token punctuation">)</span><span class="token punctuation">;</span>

  <span class="token keyword">try</span> <span class="token punctuation">{</span>
      <span class="token comment">// 包装命令行参数（就是 main 方法的 args）</span>
      <span class="token class-name">ApplicationArguments</span> applicationArguments <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">DefaultApplicationArguments</span><span class="token punctuation">(</span>args<span class="token punctuation">)</span><span class="token punctuation">;</span>

      <span class="token comment">// 准备环境：读取 application.properties / application.yml，处理 profile 等</span>
      <span class="token class-name">ConfigurableEnvironment</span> environment <span class="token operator">=</span> <span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">prepareEnvironment</span><span class="token punctuation">(</span>listeners<span class="token punctuation">,</span> bootstrapContext<span class="token punctuation">,</span> applicationArguments<span class="token punctuation">)</span><span class="token punctuation">;</span>

      <span class="token comment">// 打印 Banner</span>
      <span class="token class-name">Banner</span> printedBanner <span class="token operator">=</span> <span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">printBanner</span><span class="token punctuation">(</span>environment<span class="token punctuation">)</span><span class="token punctuation">;</span>

      <span class="token comment">// 创建一个空的 ApplicationContext 容器（只是 new 出来，还什么都没有）</span>
      <span class="token comment">// 这里便会注册对应的 PostProcessBeanFactory</span>
      context <span class="token operator">=</span> <span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">createApplicationContext</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

      <span class="token comment">// 把环境、Banner 等信息塞进容器，完成容器的预备工作</span>
      context<span class="token punctuation">.</span><span class="token function">setApplicationStartup</span><span class="token punctuation">(</span><span class="token keyword">this</span><span class="token punctuation">.</span>applicationStartup<span class="token punctuation">)</span><span class="token punctuation">;</span>
      <span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">prepareContext</span><span class="token punctuation">(</span>bootstrapContext<span class="token punctuation">,</span> context<span class="token punctuation">,</span> environment<span class="token punctuation">,</span> listeners<span class="token punctuation">,</span> applicationArguments<span class="token punctuation">,</span> printedBanner<span class="token punctuation">)</span><span class="token punctuation">;</span>

      <span class="token comment">// 上面是初始化工作</span>
      <span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">refreshContext</span><span class="token punctuation">(</span>context<span class="token punctuation">)</span><span class="token punctuation">;</span>

      <span class="token comment">// 扩展点，目前是空实现，留给子类重写用</span>
      <span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">afterRefresh</span><span class="token punctuation">(</span>context<span class="token punctuation">,</span> applicationArguments<span class="token punctuation">)</span><span class="token punctuation">;</span>
      <span class="token comment">// 记录启动耗时</span>
      <span class="token class-name">Duration</span> timeTakenToStarted <span class="token operator">=</span> startup<span class="token punctuation">.</span><span class="token function">started</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

      <span class="token comment">// 打印启动日志，就是你控制台看到的那行：</span>
      <span class="token comment">// Started MyApplication in 2.345 seconds (JVM running for 3.012)</span>
      <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token keyword">this</span><span class="token punctuation">.</span>properties<span class="token punctuation">.</span><span class="token function">isLogStartupInfo</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
          <span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token class-name">StartupInfoLogger</span><span class="token punctuation">(</span><span class="token keyword">this</span><span class="token punctuation">.</span>mainApplicationClass<span class="token punctuation">,</span> environment<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">logStarted</span><span class="token punctuation">(</span><span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">getApplicationLog</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span> startup<span class="token punctuation">)</span><span class="token punctuation">;</span>
      <span class="token punctuation">}</span>

      <span class="token comment">// 通知所有监听器"容器已启动"</span>
      listeners<span class="token punctuation">.</span><span class="token function">started</span><span class="token punctuation">(</span>context<span class="token punctuation">,</span> timeTakenToStarted<span class="token punctuation">)</span><span class="token punctuation">;</span>
      <span class="token comment">// 调用你自定义的 Runner</span>
      <span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">callRunners</span><span class="token punctuation">(</span>context<span class="token punctuation">,</span> applicationArguments<span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token punctuation">}</span> <span class="token keyword">catch</span> <span class="token punctuation">(</span><span class="token class-name">Throwable</span> ex<span class="token punctuation">)</span> <span class="token punctuation">{</span>
      <span class="token keyword">throw</span> <span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">handleRunFailure</span><span class="token punctuation">(</span>context<span class="token punctuation">,</span> ex<span class="token punctuation">,</span> listeners<span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token punctuation">}</span>

  <span class="token keyword">try</span> <span class="token punctuation">{</span>
      <span class="token keyword">if</span> <span class="token punctuation">(</span>context<span class="token punctuation">.</span><span class="token function">isRunning</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
          listeners<span class="token punctuation">.</span><span class="token function">ready</span><span class="token punctuation">(</span>context<span class="token punctuation">,</span> startup<span class="token punctuation">.</span><span class="token function">ready</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
      <span class="token punctuation">}</span>

      <span class="token keyword">return</span> context<span class="token punctuation">;</span>
  <span class="token punctuation">}</span> <span class="token keyword">catch</span> <span class="token punctuation">(</span><span class="token class-name">Throwable</span> ex<span class="token punctuation">)</span> <span class="token punctuation">{</span>
      <span class="token keyword">throw</span> <span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">handleRunFailure</span><span class="token punctuation">(</span>context<span class="token punctuation">,</span> ex<span class="token punctuation">,</span> <span class="token punctuation">(</span><span class="token class-name">SpringApplicationRunListeners</span><span class="token punctuation">)</span><span class="token keyword">null</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><p>核心连接点：<code>this.refreshContext(context);</code></p>
<ul>
<li><strong>在这行代码之前</strong>：全是 Spring Boot 在做准备工作（准备环境、打印 Banner、实例化一个空的容器）。</li>
<li><strong>在这行代码执行时</strong>：正式拉起 Spring 的 IoC 容器，开始前面我们讲的“解析图纸、画图纸、造汽车”的宏大工程</li>
</ul>
<h5>注册上下文 <code>ConfigurableApplicationContext context</code></h5>
<p>在初始化时，除了一些配置初始化以外，关键是在 <code>context</code> 的初始化</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token class-name">ConfigurableApplicationContext</span> context <span class="token operator">=</span> <span class="token keyword">null</span><span class="token punctuation">;</span>
</code></pre></div><p>先实例化了一个 null 的 context</p>
<blockquote>
<p>BeanFactory 只是一个"Bean 仓库"，ApplicationContext 是加了事件、国际化、资源等能力的"增强版仓库"，ConfigurableApplicationContext 再加上了控制容器生命周期的能力（启动/关闭）</p>
</blockquote>
<p>进一步，根据所获得的配置，来进行初始化一个当前没有注册任何 Bean 的容器</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code>context <span class="token operator">=</span> <span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">createApplicationContext</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span>
<span class="token keyword">protected</span> <span class="token class-name">ConfigurableApplicationContext</span> <span class="token function">createApplicationContext</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token class-name">ConfigurableApplicationContext</span> context <span class="token operator">=</span> <span class="token keyword">this</span><span class="token punctuation">.</span>applicationContextFactory<span class="token punctuation">.</span><span class="token function">create</span><span class="token punctuation">(</span><span class="token keyword">this</span><span class="token punctuation">.</span>properties<span class="token punctuation">.</span><span class="token function">getWebApplicationType</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token class-name">Assert</span><span class="token punctuation">.</span><span class="token function">state</span><span class="token punctuation">(</span>context <span class="token operator">!=</span> <span class="token keyword">null</span><span class="token punctuation">,</span> <span class="token string">"ApplicationContextFactory created null context"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">return</span> context<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre></div><p>这里的 <code>this.applicationContextFactory</code> 在初始化的时候是 <code>DefaultApplicationContextFactory</code> 这个类型</p>
<p>作用是根据之前推断出的 WebApplicationType 来决定创建哪种容器</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token comment">// DefaultApplicationContextFactory 内部逻辑（简化）：</span>
<span class="token keyword">public</span> <span class="token class-name">ConfigurableApplicationContext</span> <span class="token function">create</span><span class="token punctuation">(</span><span class="token class-name">WebApplicationType</span> webApplicationType<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">return</span> <span class="token keyword">switch</span> <span class="token punctuation">(</span>webApplicationType<span class="token punctuation">)</span> <span class="token punctuation">{</span>
        <span class="token keyword">case</span> <span class="token constant">SERVLET</span>   <span class="token operator">-&gt;</span> <span class="token keyword">new</span> <span class="token class-name">AnnotationConfigServletWebServerApplicationContext</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>  <span class="token comment">// 普通 Web</span>
        <span class="token keyword">case</span> <span class="token constant">REACTIVE</span>  <span class="token operator">-&gt;</span> <span class="token keyword">new</span> <span class="token class-name">AnnotationConfigReactiveWebServerApplicationContext</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 响应式</span>
        <span class="token keyword">case</span> <span class="token constant">NONE</span>      <span class="token operator">-&gt;</span> <span class="token keyword">new</span> <span class="token class-name">AnnotationConfigApplicationContext</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>                  <span class="token comment">// 非 Web</span>
    <span class="token punctuation">}</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre></div><p>一般都是 <code>AnnotationConfigServletWebServerApplicationContext</code></p>
<figure><figcaption>alt text</figcaption></figure>
<div class="language-text" data-ext="text" data-title="text"><pre class="language-text"><code>AnnotationConfigServletWebServerApplicationContext
  │
  ├─ 实现了 ApplicationContext（对外提供增强能力）
  │    └─ ApplicationContext 继承了 BeanFactory 接口
  │         所以 context 本身可以当 BeanFactory 用
  │
  └─ 内部持有一个 DefaultListableBeanFactory beanFactory
       └─ 真正存放 BeanDefinition、管理 Bean 的地方
</code></pre></div><h5><code>context</code> 初始化塞入的 5 个 BeanDefinition</h5>
<p>Spring 容器本身是一个“瞎子”，它不认识 @Autowired、@Bean 这些注解。 必须先注入这 5 个工具人（处理器），靠它们去解析你代码里的注解</p>
<p>internal 开头的名字是 Spring 故意起的"内部专用名"，和类名分开，为了防冲突 + 明确这是框架内部组件</p>
<figure><figcaption>alt text</figcaption></figure>
<blockquote>
<p><code>internalConfigurationAnnotationProcessor</code> (绝对的核心)</p>
</blockquote>
<ul>
<li><strong>真实类名：</strong> <code>ConfigurationClassPostProcessor</code></li>
<li><strong>它的职责：</strong> 这是 Spring 启动过程中最累的“包工头”。它负责解析所有的 <code>@Configuration</code> 配置类。</li>
<li><strong>具体工作：</strong> 当你在代码里写了 <code>@ComponentScan</code>、<code>@Import</code>、或者用 <code>@Bean</code> 声明了对象，都是由它负责扫描出来，并把它们也变成 <code>BeanDefinition</code> 塞进你现在看到的这个 Map 里。</li>
</ul>
<blockquote>
<p><code>internalAutowiredAnnotationProcessor</code> (依赖注入的灵魂)</p>
</blockquote>
<ul>
<li><strong>真实类名：</strong> <code>AutowiredAnnotationBeanPostProcessor</code></li>
<li><strong>它的职责：</strong> 顾名思义，它负责处理依赖注入。</li>
<li><strong>具体工作：</strong> 只要你在属性、方法或构造器上写了 <code>@Autowired</code> 或者 <code>@Value</code> 注解，这个处理器就会在 Bean 实例化的过程中，自动帮你把需要的对象找出来并赋值进去。</li>
</ul>
<blockquote>
<p><code>internalCommonAnnotationProcessor</code> (生命周期管家)</p>
</blockquote>
<ul>
<li><strong>真实类名：</strong> <code>CommonAnnotationBeanPostProcessor</code></li>
<li><strong>它的职责：</strong> 它负责处理 Java 标准（JSR-250）中的通用注解。</li>
<li><strong>具体工作：</strong> 如果你使用了 <code>@Resource</code>（按名称注入）、或者在方法上加了 <code>@PostConstruct</code>（初始化后执行）和 <code>@PreDestroy</code>（销毁前执行），就是由它来负责触发的。</li>
</ul>
<blockquote>
<p><code>internalEventListenerProcessor</code> (事件处理器)</p>
</blockquote>
<ul>
<li><strong>真实类名：</strong> <code>EventListenerMethodProcessor</code></li>
<li><strong>它的职责：</strong> Spring 的事件驱动机制核心。</li>
<li><strong>具体工作：</strong> 它会扫描所有 Spring 管理的 Bean。如果在某个方法上发现了 <code>@EventListener</code> 注解，它就会把这个方法注册为一个事件监听器（ApplicationListener），等系统有对应事件发布时去调用它。</li>
</ul>
<blockquote>
<p><code>internalEventListenerFactory</code> (事件工厂)</p>
</blockquote>
<ul>
<li><strong>真实类名：</strong> <code>DefaultEventListenerFactory</code></li>
<li><strong>它的职责：</strong> 它是第 4 个组件的“搭档”。</li>
<li><strong>具体工作：</strong> 配合 <code>EventListenerMethodProcessor</code> 工作，它的具体任务是将带有 <code>@EventListener</code> 注解的方法真正封装转换成 <code>ApplicationListener</code> 实例对象。</li>
</ul>
<h5>注册入口Bean</h5>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">prepareContext</span><span class="token punctuation">(</span>bootstrapContext<span class="token punctuation">,</span> context<span class="token punctuation">,</span> environment<span class="token punctuation">,</span> listeners<span class="token punctuation">,</span> applicationArguments<span class="token punctuation">,</span> printedBanner<span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre></div><div class="language-text" data-ext="text" data-title="text"><pre class="language-text"><code>prepareContext()
  │
  ├─ 把 environment 塞进 context
  ├─ 执行 ApplicationContextInitializer（初始化器，扩展点）
  ├─ 广播 contextPrepared 事件
  │
  ├─ 【关键】把 primarySource（Demo1Application）注册为第一张 BeanDefinition
  │    └─ 这是整个容器里第一张也是唯一一张"用户图纸"
  │         后面 refresh 里 ConfigurationClassPostProcessor 就从这里出发
  │
  └─ 广播 contextLoaded 事件
</code></pre></div><figure><figcaption>alt text</figcaption></figure>
<h5><code>refresh()</code></h5>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token comment">// SpringApplication.java</span>
<span class="token keyword">public</span> <span class="token class-name">ConfigurableApplicationContext</span> <span class="token function">run</span><span class="token punctuation">(</span><span class="token class-name">String</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span> args<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span>
    <span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">refreshContext</span><span class="token punctuation">(</span>context<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span>
<span class="token punctuation">}</span>

<span class="token keyword">private</span> <span class="token keyword">void</span> <span class="token function">refreshContext</span><span class="token punctuation">(</span><span class="token class-name">ConfigurableApplicationContext</span> context<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">if</span> <span class="token punctuation">(</span><span class="token keyword">this</span><span class="token punctuation">.</span>properties<span class="token punctuation">.</span><span class="token function">isRegisterShutdownHook</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
        shutdownHook<span class="token punctuation">.</span><span class="token function">registerApplicationContext</span><span class="token punctuation">(</span>context<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">}</span>

    <span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">refresh</span><span class="token punctuation">(</span>context<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>

<span class="token keyword">protected</span> <span class="token keyword">void</span> <span class="token function">refresh</span><span class="token punctuation">(</span><span class="token class-name">ConfigurableApplicationContext</span> applicationContext<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    applicationContext<span class="token punctuation">.</span><span class="token function">refresh</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre></div><p>这里的 <code>context</code> 是 <code>AnnotationConfigServletWebServerApplicationContext</code>, 其继承了 <code>ServletWebServerApplicationContext</code></p>
<p>对应的 <code>refresh</code></p>
<div class="language-text" data-ext="text" data-title="text"><pre class="language-text"><code>AnnotationConfigServletWebServerApplicationContext
  └─ 继承 ServletWebServerApplicationContext
       └─ 继承 GenericWebApplicationContext
            └─ 继承 GenericApplicationContext
                 └─ 继承 AbstractApplicationContext   ← refresh() 定义在这里
</code></pre></div><div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token comment">// ServletWebServerApplicationContext.java</span>
<span class="token keyword">public</span> <span class="token keyword">final</span> <span class="token keyword">void</span> <span class="token function">refresh</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token keyword">throws</span> <span class="token class-name">BeansException</span><span class="token punctuation">,</span> <span class="token class-name">IllegalStateException</span> <span class="token punctuation">{</span>
    <span class="token keyword">try</span> <span class="token punctuation">{</span>
        <span class="token keyword">super</span><span class="token punctuation">.</span><span class="token function">refresh</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">}</span> <span class="token keyword">catch</span> <span class="token punctuation">(</span><span class="token class-name">RuntimeException</span> var5<span class="token punctuation">)</span> <span class="token punctuation">{</span>
        <span class="token class-name">WebServer</span> webServer <span class="token operator">=</span> <span class="token keyword">this</span><span class="token punctuation">.</span>webServer<span class="token punctuation">;</span>
        <span class="token keyword">if</span> <span class="token punctuation">(</span>webServer <span class="token operator">!=</span> <span class="token keyword">null</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
            <span class="token keyword">try</span> <span class="token punctuation">{</span>
                webServer<span class="token punctuation">.</span><span class="token function">stop</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
                webServer<span class="token punctuation">.</span><span class="token function">destroy</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
            <span class="token punctuation">}</span> <span class="token keyword">catch</span> <span class="token punctuation">(</span><span class="token class-name">RuntimeException</span> stopOrDestroyEx<span class="token punctuation">)</span> <span class="token punctuation">{</span>
                var5<span class="token punctuation">.</span><span class="token function">addSuppressed</span><span class="token punctuation">(</span>stopOrDestroyEx<span class="token punctuation">)</span><span class="token punctuation">;</span>
            <span class="token punctuation">}</span>
        <span class="token punctuation">}</span>

        <span class="token keyword">throw</span> var5<span class="token punctuation">;</span>
    <span class="token punctuation">}</span>
<span class="token punctuation">}</span>

<span class="token comment">// AbstractApplicationContext.java</span>
<span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">refresh</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
  <span class="token comment">// 加锁，防止并发重复刷新容器</span>
  <span class="token keyword">this</span><span class="token punctuation">.</span>startupShutdownLock<span class="token punctuation">.</span><span class="token function">lock</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

  <span class="token keyword">try</span> <span class="token punctuation">{</span>
      <span class="token comment">// ━━━━━━━━━━━ 第一阶段：准备工作 ━━━━━━━━━━━</span>

      <span class="token comment">// 1 刷新前的预处理：校验必填属性、初始化事件集合等</span>
      <span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">prepareRefresh</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

      <span class="token comment">// 2 获取 BeanFactory（DefaultListableBeanFactory）</span>
      <span class="token comment">//    如果是 XML 配置，这里会解析 XML 加载 BeanDefinition</span>
      <span class="token comment">//    注解驱动的话，这里拿到的是一个已存在的空工厂</span>
      <span class="token class-name">ConfigurableListableBeanFactory</span> beanFactory <span class="token operator">=</span> <span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">obtainFreshBeanFactory</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

      <span class="token comment">// 3 给 BeanFactory 配置基础设施：</span>
      <span class="token comment">//    - 注册 ClassLoader</span>
      <span class="token comment">//    - 注册内置 Bean（如 environment、systemProperties）</span>
      <span class="token comment">//    - 添加几个默认的 BeanPostProcessor</span>
      <span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">prepareBeanFactory</span><span class="token punctuation">(</span>beanFactory<span class="token punctuation">)</span><span class="token punctuation">;</span>

      <span class="token keyword">try</span> <span class="token punctuation">{</span>
          <span class="token comment">// ━━━━━━━━━━━ 第二阶段：BeanDefinition 处理 ━━━━━━━━━━━</span>

          <span class="token comment">// 4 扩展点：子类可以在这里对 BeanFactory 做额外处理（模板方法）</span>
          <span class="token comment">//    Web 容器在这里注册了 request/session 作用域</span>
          <span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">postProcessBeanFactory</span><span class="token punctuation">(</span>beanFactory<span class="token punctuation">)</span><span class="token punctuation">;</span>

          <span class="token comment">// 5 【核心】执行所有 BeanFactoryPostProcessor</span>
          <span class="token comment">//    ConfigurationClassPostProcessor 就在这里被调用！</span>
          <span class="token comment">//    → 解析 @Configuration、@ComponentScan、@Import、@Bean</span>
          <span class="token comment">//    → 所有图纸（BeanDefinition）在这一步全部注册完毕</span>
          <span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">invokeBeanFactoryPostProcessors</span><span class="token punctuation">(</span>beanFactory<span class="token punctuation">)</span><span class="token punctuation">;</span>

          <span class="token comment">// 6 注册所有 BeanPostProcessor（只是注册，还没执行）</span>
          <span class="token comment">//    比如 AutowiredAnnotationBeanPostProcessor（处理 @Autowired）</span>
          <span class="token comment">//    比如 AopAwareObjenesisCglibAopProxy（处理 AOP）</span>
          <span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">registerBeanPostProcessors</span><span class="token punctuation">(</span>beanFactory<span class="token punctuation">)</span><span class="token punctuation">;</span>

          <span class="token comment">// ━━━━━━━━━━━ 第三阶段：初始化各种基础组件 ━━━━━━━━━━━</span>

          <span class="token comment">// 7 初始化国际化资源（i18n，MessageSource）</span>
          <span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">initMessageSource</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

          <span class="token comment">// 8 初始化事件广播器（ApplicationEventMulticaster）</span>
          <span class="token comment">//    后面发布事件靠它</span>
          <span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">initApplicationEventMulticaster</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

          <span class="token comment">// 9 扩展点：子类在这里做特殊初始化</span>
          <span class="token comment">//    SpringBoot Web 容器在这里启动内嵌 Tomcat！</span>
          <span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">onRefresh</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

          <span class="token comment">// 10 注册所有 ApplicationListener 监听器</span>
          <span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">registerListeners</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

          <span class="token comment">// ━━━━━━━━━━━ 第四阶段：实例化 Bean ━━━━━━━━━━━</span>

          <span class="token comment">// 11 【核心】实例化所有非懒加载的单例 Bean</span>
          <span class="token comment">//    @Autowired 注入、AOP 代理、@PostConstruct 都在这里执行</span>
          <span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">finishBeanFactoryInitialization</span><span class="token punctuation">(</span>beanFactory<span class="token punctuation">)</span><span class="token punctuation">;</span>

          <span class="token comment">// 12 收尾：清理缓存、发布 ContextRefreshedEvent 事件</span>
          <span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">finishRefresh</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

      <span class="token punctuation">}</span> <span class="token keyword">catch</span> <span class="token punctuation">(</span><span class="token class-name">Error</span> <span class="token operator">|</span> <span class="token class-name">RuntimeException</span> ex<span class="token punctuation">)</span> <span class="token punctuation">{</span>
          <span class="token comment">// 出错了：停掉 Lifecycle Bean，销毁已创建的 Bean，取消刷新</span>
          <span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">destroyBeans</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
          <span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">cancelRefresh</span><span class="token punctuation">(</span>ex<span class="token punctuation">)</span><span class="token punctuation">;</span>
          <span class="token keyword">throw</span> ex<span class="token punctuation">;</span>
      <span class="token punctuation">}</span>
  <span class="token punctuation">}</span> <span class="token keyword">finally</span> <span class="token punctuation">{</span>
      <span class="token keyword">this</span><span class="token punctuation">.</span>startupShutdownLock<span class="token punctuation">.</span><span class="token function">unlock</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><div class="language-text" data-ext="text" data-title="text"><pre class="language-text"><code>1 ~ 3  准备工作         → 配置好 BeanFactory 的基础设施
4 ~ 6  处理图纸         → 扫描注解，注册所有 BeanDefinition，登记 PostProcessor
7 ~ 10  初始化基础组件   → 国际化、事件广播、启动 Tomcat、注册监听器
11 ~ 12  造 Bean        → 按图纸实例化所有单例 Bean，完成注入和代理
</code></pre></div><p>处理图纸，会先根据初始的图纸，然后不断解析，又有新的图纸，不断循环</p>
<h5>BeanPostProcessor 和 BeanFactoryPostProcessor 区别</h5>
<p>| | <code>BeanFactoryPostProcessor</code> | <code>BeanPostProcessor</code> |
|</p>
]]></content:encoded>
    </item>
  </channel>
</rss>