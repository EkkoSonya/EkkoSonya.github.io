<?xml version="1.0" encoding="utf-8"?><?xml-stylesheet type="text/xsl" href="http://ekkosonya.cn/rss.xsl"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:dc="http://purl.org/dc/elements/1.1/">
  <channel>
    <atom:link href="http://ekkosonya.cn/rss.xml" rel="self" type="application/rss+xml"/>
    <title>EkkoSonya&amp;apos;s Blog</title>
    <link>http://ekkosonya.cn/</link>
    <description>笔记记录</description>
    <language>zh-CN</language>
    <pubDate>Thu, 02 Apr 2026 08:54:31 GMT</pubDate>
    <lastBuildDate>Thu, 02 Apr 2026 08:54:31 GMT</lastBuildDate>
    <generator>@vuepress/plugin-feed</generator>
    <docs>https://validator.w3.org/feed/docs/rss2.html</docs>
    <category>code</category>
    <item>
      <title>Spring系列八股1 - Spring 简介 + 常用注解</title>
      <link>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/Spring/article1.html</link>
      <guid>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/Spring/article1.html</guid>
      <source url="http://ekkosonya.cn/rss.xml">Spring系列八股1 - Spring 简介 + 常用注解</source>
      <description>Spring基础 什么是 Spring Spring 是一个 Java 后端开发框架，其最核心的作用是帮我们管理 Java 对象 其最重要的特性就是 IoC，也就是控制反转。以前我们要使用一个对象时，都要自己先 new 出来。但有了 Spring 之后，我们只需要告诉 Spring 我们需要什么对象，它就会自动帮我们创建好并注入到 Spring 容器当...</description>
      <category>code</category>
      <pubDate>Thu, 02 Apr 2026 08:51:45 GMT</pubDate>
      <content:encoded><![CDATA[<h2>Spring基础</h2>
<h3>什么是 Spring</h3>
<p>Spring 是一个 Java 后端开发框架，其最核心的作用是帮我们管理 Java 对象</p>
<p>其最重要的特性就是 IoC，也就是控制反转。以前我们要使用一个对象时，都要自己先 new 出来。但有了 Spring 之后，我们只需要告诉 Spring 我们需要什么对象，它就会自动帮我们创建好并注入到 Spring 容器当中</p>
<p>另外，Spring 还提供了 AOP，也就是面向切面编程，在我们需要做一些通用功能的时候特别有用，比如说日志记录、权限校验、事务管理这些，我们不用在每个方法里都写重复的代码，直接用 AOP 就能统一处理</p>
<p>Spring 的生态也特别丰富，像 Spring Boot 能让我们快速搭建项目，Spring MVC 能帮我们处理 web 请求，Spring Data 能帮我们简化数据库操作，Spring Cloud 能帮我们做微服务架构等等</p>
<figure><figcaption>alt text</figcaption></figure>
<h4>特性</h4>
<p>| <strong>核心思想</strong> | <strong>解决的问题</strong> | <strong>实现手段</strong> | <strong>典型应用场景</strong> |
|</p>
]]></content:encoded>
    </item>
    <item>
      <title>Spring系列八股2 - Bean1</title>
      <link>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/Spring/article2.html</link>
      <guid>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/Spring/article2.html</guid>
      <source url="http://ekkosonya.cn/rss.xml">Spring系列八股2 - Bean1</source>
      <description>Spring基础 Bean Bean 本质上就是由 Spring 容器管理的 Java 对象，但它和普通的 Java 对象有很大区别 普通的 Java 对象我们是通过 new 关键字创建的 而 Bean 是交给 Spring 容器来管理的，从创建到销毁都由容器负责。 从实际使用的角度来说，我们项目里的 Service、Dao、Controller 这些...</description>
      <category>code</category>
      <pubDate>Thu, 02 Apr 2026 08:51:45 GMT</pubDate>
      <content:encoded><![CDATA[<h2>Spring基础</h2>
<h3>Bean</h3>
<p>Bean 本质上就是由 Spring 容器管理的 Java 对象，但它和普通的 Java 对象有很大区别</p>
<p>普通的 Java 对象我们是通过 new 关键字创建的</p>
<p>而 Bean 是交给 Spring 容器来管理的，从创建到销毁都由容器负责。</p>
<p>从实际使用的角度来说，我们项目里的 Service、Dao、Controller 这些都是 Bean</p>
<p>比如 <code>UserService</code> 被标注了 <code>@Service</code> 注解，它就成了一个 Bean，Spring 会自动创建它的实例，管理它的依赖关系，当其他地方需要用到 <code>UserService</code> 的时候，Spring 就会把这个实例注入进去</p>
<p>这种依赖注入的方式让对象之间的关系变得松耦合</p>
<p>Spring 提供了多种 Bean 的配置方式，基于注解的方式是最常用的</p>
<figure><figcaption>alt text</figcaption></figure>
<h4>@Component 和 @Bean 区别</h4>
<p>首先从使用上来说，<code>@Component</code> 是标注在类上的，而 <code>@Bean</code> 是标注在方法上的</p>
<p><code>@Component</code> 告诉 Spring 这个类是一个组件，请把它注册为 Bean，而 <code>@Bean</code> 则告诉 Spring 请将这个方法返回的对象注册为 Bean</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token annotation punctuation">@Component</span>  <span class="token comment">// Spring自动创建UserService实例</span>
<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">UserService</span> <span class="token punctuation">{</span>
  <span class="token annotation punctuation">@Autowired</span>
  <span class="token keyword">private</span> <span class="token class-name">UserDao</span> userDao<span class="token punctuation">;</span>
<span class="token punctuation">}</span>

<span class="token annotation punctuation">@Configuration</span>
<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">AppConfig</span> <span class="token punctuation">{</span>
  <span class="token annotation punctuation">@Bean</span>  <span class="token comment">// 我们手动创建DataSource实例</span>
  <span class="token keyword">public</span> <span class="token class-name">DataSource</span> <span class="token function">dataSource</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
      <span class="token class-name">HikariDataSource</span> ds <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">HikariDataSource</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
      ds<span class="token punctuation">.</span><span class="token function">setJdbcUrl</span><span class="token punctuation">(</span><span class="token string">"jdbc:mysql://localhost:3306/test"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
      ds<span class="token punctuation">.</span><span class="token function">setUsername</span><span class="token punctuation">(</span><span class="token string">"root"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
      ds<span class="token punctuation">.</span><span class="token function">setPassword</span><span class="token punctuation">(</span><span class="token string">"123456"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
      <span class="token keyword">return</span> ds<span class="token punctuation">;</span>  <span class="token comment">// 返回给Spring管理</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><p>从控制权的角度来说，<code>@Component</code> 是由 Spring 自动创建和管理的</p>
<p>而 <code>@Bean</code> 则是由我们手动创建的，然后再交给 Spring 管理，我们对对象的创建过程有完全的控制权</p>
<blockquote>
<p>因为 <code>@Bean</code> 是作用在方法上的，Spring 只是管理这个方法最终 return 的对象</p>
</blockquote>
<h4>Bean 生命周期</h4>
<figure><figcaption>alt text</figcaption></figure>
<p>Bean 的生命周期可以分为 5 个主要阶段</p>
<p>实例化 -&gt; 设置属性 -&gt; 初始化 -&gt; 使用中 -&gt; 销毁</p>
<figure><figcaption>alt text</figcaption></figure>
<blockquote>
<p>第一个阶段是实例化</p>
</blockquote>
<p>Spring 容器会根据 <code>BeanDefinition</code>，通过反射调用 Bean 的构造方法创建对象实例</p>
<p>如果有多个构造方法，Spring 会根据依赖注入的规则选择合适的构造方法</p>
<blockquote>
<p>第二阶段是属性赋值</p>
</blockquote>
<p>这个阶段 Spring 会给 Bean 的属性赋值，包括通过 <code>@Autowired</code>、<code>@Resource</code> 这些注解注入的依赖对象，以及通过 <code>@Value</code> 注入的配置值</p>
<blockquote>
<p>第三阶段是初始化</p>
</blockquote>
<p>这个阶段会依次执行：</p>
<ul>
<li><code>@PostConstruct</code> 标注的方法</li>
<li><code>InitializingBean</code> 接口的 <code>afterPropertiesSet</code> 方法</li>
<li>通过 <code>@Bean</code> 的 <code>initMethod</code> 指定的初始化方法</li>
</ul>
<p>初始化后，Spring 还会调用所有注册的 BeanPostProcessor 后置处理方法。这个阶段经常用来创建代理对象，比如 AOP 代理</p>
<blockquote>
<p>第四阶段是使用 Bean</p>
</blockquote>
<p>比如我们的 Controller 调用 Service，Service 调用 DAO</p>
<blockquote>
<p>最后是销毁阶段。</p>
</blockquote>
<p>当容器关闭或者 Bean 被移除的时候，会依次执行：</p>
<ul>
<li><code>@PreDestroy</code> 标注的方法</li>
<li><code>DisposableBean</code> 接口的 <code>destroy</code> 方法</li>
<li>通过 <code>@Bean</code> 的 <code>destroyMethod</code> 指定的销毁方法</li>
</ul>
<figure><figcaption>alt text</figcaption></figure>
<h4>Aware 类型的接口</h4>
<p>Aware 接口在 Spring 中是一个很有意思的设计，它们的作用是让 Bean 能够感知到 Spring 容器的一些内部组件</p>
<p>从设计理念来说，Aware 接口实现了一种“回调”机制。正常情况下，Bean 不应该直接依赖 Spring 容器，这样可以保持代码的独立性。但有些时候，Bean 确实需要获取容器的一些信息或者组件，Aware 接口就提供了这样一个能力</p>
<p>我最常用的 Aware 接口是 ApplicationContextAware，它可以让 Bean 获取到 ApplicationContext 容器本身</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token annotation punctuation">@Component</span>
<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">SpringUtil</span> <span class="token keyword">implements</span> <span class="token class-name">ApplicationContextAware</span><span class="token punctuation">,</span> <span class="token class-name">EnvironmentAware</span><span class="token punctuation">,</span> <span class="token class-name">ApplicationListener</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">ContextClosedEvent</span><span class="token punctuation">&gt;</span></span> <span class="token punctuation">{</span>
  <span class="token keyword">private</span> <span class="token keyword">volatile</span> <span class="token keyword">static</span> <span class="token class-name">ApplicationContext</span> context<span class="token punctuation">;</span>
  <span class="token keyword">private</span> <span class="token keyword">volatile</span> <span class="token keyword">static</span> <span class="token class-name">Environment</span> environment<span class="token punctuation">;</span>

  <span class="token keyword">private</span> <span class="token keyword">static</span> <span class="token class-name">Binder</span> binder<span class="token punctuation">;</span>

  <span class="token annotation punctuation">@Override</span>
  <span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">setApplicationContext</span><span class="token punctuation">(</span><span class="token class-name">ApplicationContext</span> applicationContext<span class="token punctuation">)</span> <span class="token keyword">throws</span> <span class="token class-name">BeansException</span> <span class="token punctuation">{</span>
      <span class="token comment">// 容器启动时自动注入，方便后续获取bean</span>
      <span class="token class-name">SpringUtil</span><span class="token punctuation">.</span>context <span class="token operator">=</span> applicationContext<span class="token punctuation">;</span>
  <span class="token punctuation">}</span>

  <span class="token annotation punctuation">@Override</span>
  <span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">setEnvironment</span><span class="token punctuation">(</span><span class="token class-name">Environment</span> environment<span class="token punctuation">)</span> <span class="token punctuation">{</span>
      <span class="token class-name">SpringUtil</span><span class="token punctuation">.</span>environment <span class="token operator">=</span> environment<span class="token punctuation">;</span>
      binder <span class="token operator">=</span> <span class="token class-name">Binder</span><span class="token punctuation">.</span><span class="token function">get</span><span class="token punctuation">(</span>environment<span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token punctuation">}</span>

  <span class="token annotation punctuation">@Override</span>
  <span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">onApplicationEvent</span><span class="token punctuation">(</span><span class="token class-name">ContextClosedEvent</span> event<span class="token punctuation">)</span> <span class="token punctuation">{</span>
      <span class="token keyword">if</span> <span class="token punctuation">(</span>context <span class="token operator">==</span> event<span class="token punctuation">.</span><span class="token function">getApplicationContext</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
          context <span class="token operator">=</span> <span class="token keyword">null</span><span class="token punctuation">;</span>
          environment <span class="token operator">=</span> <span class="token keyword">null</span><span class="token punctuation">;</span>
          binder <span class="token operator">=</span> <span class="token keyword">null</span><span class="token punctuation">;</span>
      <span class="token punctuation">}</span>
  <span class="token punctuation">}</span>

  <span class="token doc-comment comment">/**
   * 获取ApplicationContext
   *
   * <span class="token keyword">@return</span>
   */</span>
  <span class="token keyword">public</span> <span class="token keyword">static</span> <span class="token class-name">ApplicationContext</span> <span class="token function">getContext</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
      <span class="token keyword">return</span> context<span class="token punctuation">;</span>
  <span class="token punctuation">}</span>

  <span class="token doc-comment comment">/**
   * 获取bean
   *
   * <span class="token keyword">@param</span> <span class="token parameter">bean</span>
   * <span class="token keyword">@param</span> <span class="token class-name"><span class="token punctuation">&lt;</span>T<span class="token punctuation">&gt;</span></span>
   * <span class="token keyword">@return</span>
   */</span>
  <span class="token keyword">public</span> <span class="token keyword">static</span> <span class="token keyword">boolean</span> <span class="token function">isActive</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
      <span class="token keyword">return</span> context <span class="token keyword">instanceof</span> <span class="token class-name">ConfigurableApplicationContext</span>
              <span class="token operator">?</span> <span class="token punctuation">(</span><span class="token punctuation">(</span><span class="token class-name">ConfigurableApplicationContext</span><span class="token punctuation">)</span> context<span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">isActive</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
              <span class="token operator">:</span> context <span class="token operator">!=</span> <span class="token keyword">null</span><span class="token punctuation">;</span>
  <span class="token punctuation">}</span>

  <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span>
<span class="token punctuation">}</span>
</code></pre></div><h4>init-method 和 destroy-method</h4>
<p>init-method 指定的初始化方法会在 Bean 的初始化阶段被调用，具体的执行顺序是：</p>
<ul>
<li>先执行 @PostConstruct 标注的方法</li>
<li>然后执行 InitializingBean 接口的 afterPropertiesSet() 方法</li>
<li>最后再执行 init-method 指定的方法</li>
</ul>
<p>也就是说，init-method 是在所有其他初始化方法之后执行的</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token annotation punctuation">@Component</span>
<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">MyService</span> <span class="token punctuation">{</span>
  <span class="token annotation punctuation">@Autowired</span>
  <span class="token keyword">private</span> <span class="token class-name">UserDao</span> userDao<span class="token punctuation">;</span>
  
  <span class="token annotation punctuation">@PostConstruct</span>
  <span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">postConstruct</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
      <span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span><span class="token string">"1. @PostConstruct执行"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token punctuation">}</span>
  
  <span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">customInit</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>  <span class="token comment">// 通过@Bean的initMethod指定</span>
      <span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span><span class="token string">"3. init-method执行"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>

<span class="token annotation punctuation">@Configuration</span>
<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">AppConfig</span> <span class="token punctuation">{</span>
  <span class="token annotation punctuation">@Bean</span><span class="token punctuation">(</span>initMethod <span class="token operator">=</span> <span class="token string">"customInit"</span><span class="token punctuation">)</span>
  <span class="token keyword">public</span> <span class="token class-name">MyService</span> <span class="token function">myService</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
      <span class="token keyword">return</span> <span class="token keyword">new</span> <span class="token class-name">MyService</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><p>destroy-method 会在 Bean 销毁阶段被调用。</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token annotation punctuation">@Component</span>
<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">MyService</span> <span class="token punctuation">{</span>
  <span class="token annotation punctuation">@PreDestroy</span>
  <span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">preDestroy</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span><span class="token string">"1. @PreDestroy执行"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token punctuation">}</span>
  
  <span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">customDestroy</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>  <span class="token comment">// 通过@Bean的destroyMethod指定</span>
    <span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span><span class="token string">"3. destroy-method执行"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><p>不过在实际开发中，通常用 @PostConstruct 和 @PreDestroy 就够了，它们更简洁</p>
]]></content:encoded>
    </item>
    <item>
      <title>Spring系列八股3 - Bean2 (作用域 + 线程安全问题)</title>
      <link>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/Spring/article3.html</link>
      <guid>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/Spring/article3.html</guid>
      <source url="http://ekkosonya.cn/rss.xml">Spring系列八股3 - Bean2 (作用域 + 线程安全问题)</source>
      <description>Spring基础 Bean的作用域 单例模式 (默认) Bean 的作用域决定了 Bean 实例的生命周期和创建策略，singleton 是默认的作用域 整个 Spring 容器中只会有一个 Bean 实例。不管在多少个地方注入这个 Bean，拿到的都是同一个对象 生命周期和 Spring 容器相同，容器启动时创建，容器销毁时销毁 实际开发中，像 Se...</description>
      <category>code</category>
      <pubDate>Thu, 02 Apr 2026 08:51:45 GMT</pubDate>
      <content:encoded><![CDATA[<h2>Spring基础</h2>
<h3>Bean的作用域</h3>
<h4>单例模式 (默认)</h4>
<p>Bean 的作用域决定了 Bean 实例的生命周期和创建策略，singleton 是默认的作用域</p>
<p>整个 Spring 容器中只会有一个 Bean 实例。不管在多少个地方注入这个 Bean，拿到的都是同一个对象</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token annotation punctuation">@Component</span>  <span class="token comment">// 默认就是singleton</span>
<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">UserService</span> <span class="token punctuation">{</span>
  <span class="token comment">// 整个应用中只有一个UserService实例</span>
<span class="token punctuation">}</span>
</code></pre></div><p>生命周期和 Spring 容器相同，容器启动时创建，容器销毁时销毁</p>
<p>实际开发中，像 Service、Dao 这些业务组件基本都是单例的，因为单例既能节省内存，又能提高性能</p>
<h4>多例模式</h4>
<p>当把 scope 设置为 prototype 时，每次从容器中获取 Bean 的时候都会创建一个新的实例</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token annotation punctuation">@Component</span>
<span class="token annotation punctuation">@Scope</span><span class="token punctuation">(</span><span class="token string">"prototype"</span><span class="token punctuation">)</span>
<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">OrderProcessor</span> <span class="token punctuation">{</span>
  <span class="token comment">// 每次注入或获取都是新的实例</span>
<span class="token punctuation">}</span>
</code></pre></div><p>当需要处理一些有状态的 Bean 时会用到 prototype，比如每个订单处理器需要维护不同的状态信息</p>
<h4>注入问题</h4>
<p>需要注意的是，在 singleton Bean 中注入 prototype Bean 时要小心，因为 singleton Bean 只创建一次，所以 prototype Bean 也只会注入一次。这时候可以用 @Lookup 注解或者 ApplicationContext 来动态获取</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token annotation punctuation">@Component</span>
<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">SingletonService</span> <span class="token punctuation">{</span>
  <span class="token comment">// 错误的做法，prototypeBean只会注入一次</span>
  <span class="token annotation punctuation">@Autowired</span>
  <span class="token keyword">private</span> <span class="token class-name">PrototypeBean</span> prototypeBean<span class="token punctuation">;</span>
  
  <span class="token comment">// 正确的做法，每次调用都获取新实例</span>
  <span class="token annotation punctuation">@Lookup</span>
  <span class="token keyword">public</span> <span class="token class-name">PrototypeBean</span> <span class="token function">getPrototypeBean</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
      <span class="token keyword">return</span> <span class="token keyword">null</span><span class="token punctuation">;</span>  <span class="token comment">// Spring会重写这个方法</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><h4>其他模式</h4>
<p>除了 singleton 和 prototype，Spring 还支持其他作用域，比如 request、session、application 和 websocket</p>
<ul>
<li>如果作用于是 request，表示在 Web 应用中，每个 HTTP 请求都会创建一个新的 Bean 实例，请求结束后 Bean 就被销毁</li>
<li>如果作用于是 session，表示在 Web 应用中，每个 HTTP 会话都会创建一个新的 Bean 实例，会话结束后 Bean 被销毁</li>
<li>application 作用域表示在整个应用中只有一个 Bean 实例，类似于 singleton，但它的生命周期与 ServletContext 绑定</li>
<li>websocket 作用域表示在 WebSocket 会话中每个连接都有自己的 Bean 实例。WebSocket 连接建立时创建，连接关闭时销毁</li>
</ul>
<h3>单例Bean会存在线程安全问题吗</h3>
<p>首先要明确一点。Spring 容器本身保证了 Bean 创建过程的线程安全，也就是说不会出现多个线程同时创建同一个单例 Bean 的情况。</p>
<blockquote>
<p>线程安全的创建过程主要靠 synchronized 实现</p>
</blockquote>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token comment">// 单例缓存</span>
<span class="token keyword">private</span> <span class="token keyword">final</span> <span class="token class-name">Map</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">String</span><span class="token punctuation">,</span> <span class="token class-name">Object</span><span class="token punctuation">&gt;</span></span> singletonObjects <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">ConcurrentHashMap</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token punctuation">&gt;</span></span><span class="token punctuation">(</span><span class="token number">256</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token keyword">public</span> <span class="token class-name">Object</span> <span class="token function">getSingleton</span><span class="token punctuation">(</span><span class="token class-name">String</span> beanName<span class="token punctuation">,</span> <span class="token class-name">ObjectFactory</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token operator">?</span><span class="token punctuation">&gt;</span></span> singletonFactory<span class="token punctuation">)</span> <span class="token punctuation">{</span>
  <span class="token keyword">synchronized</span> <span class="token punctuation">(</span><span class="token keyword">this</span><span class="token punctuation">.</span>singletonObjects<span class="token punctuation">)</span> <span class="token punctuation">{</span>  <span class="token comment">// 关键：同步锁</span>
    <span class="token class-name">Object</span> singletonObject <span class="token operator">=</span> <span class="token keyword">this</span><span class="token punctuation">.</span>singletonObjects<span class="token punctuation">.</span><span class="token function">get</span><span class="token punctuation">(</span>beanName<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">if</span> <span class="token punctuation">(</span>singletonObject <span class="token operator">==</span> <span class="token keyword">null</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
      singletonObject <span class="token operator">=</span> singletonFactory<span class="token punctuation">.</span><span class="token function">getObject</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
      <span class="token keyword">this</span><span class="token punctuation">.</span>singletonObjects<span class="token punctuation">.</span><span class="token function">put</span><span class="token punctuation">(</span>beanName<span class="token punctuation">,</span> singletonObject<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">}</span>
    <span class="token keyword">return</span> singletonObject<span class="token punctuation">;</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><p>但是 Bean 创建完成后的使用过程，Spring 就不管了</p>
<p>换句话说，单例 Bean 在被创建后，如果它的内部状态是可变的，那么在多线程环境下就可能会出现线程安全问题</p>
<p>如果 Bean 中没有成员变量，或者成员变量都是不可变的，final 修饰的，那么就不存在线程安全问题</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token annotation punctuation">@Service</span>
<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">UserServiceImpl</span> <span class="token keyword">implements</span> <span class="token class-name">UserService</span> <span class="token punctuation">{</span>
  <span class="token annotation punctuation">@Resource</span>
  <span class="token keyword">private</span> <span class="token class-name">UserDao</span> userDao<span class="token punctuation">;</span>
  <span class="token annotation punctuation">@Autowired</span>
  <span class="token keyword">private</span> <span class="token class-name">CountService</span> countService<span class="token punctuation">;</span>
  <span class="token comment">// 只有依赖注入的无状态字段</span>
<span class="token punctuation">}</span>

<span class="token annotation punctuation">@Service</span>
<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">ConfigService</span> <span class="token punctuation">{</span>
  <span class="token keyword">private</span> <span class="token keyword">final</span> <span class="token class-name">String</span> appName<span class="token punctuation">;</span>  <span class="token comment">// final修饰，不可变</span>
  
  <span class="token keyword">public</span> <span class="token class-name">ConfigService</span><span class="token punctuation">(</span><span class="token annotation punctuation">@Value</span><span class="token punctuation">(</span><span class="token string">"${app.name}"</span><span class="token punctuation">)</span> <span class="token class-name">String</span> appName<span class="token punctuation">)</span> <span class="token punctuation">{</span>
      <span class="token keyword">this</span><span class="token punctuation">.</span>appName <span class="token operator">=</span> appName<span class="token punctuation">;</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><h4>如何解决单例 Bean 的线程安全问题</h4>
<blockquote>
<p>无状态优先，ThreadLocal 隔离，JUC 工具，加锁保底，改作用域兜后</p>
</blockquote>
<p>| 方案 | 适用场景 | 核心思想 |
|</p>
]]></content:encoded>
    </item>
    <item>
      <title>Spring系列八股4 - Bean3 (Autowired注解 + 自动装配)</title>
      <link>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/Spring/article4.html</link>
      <guid>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/Spring/article4.html</guid>
      <source url="http://ekkosonya.cn/rss.xml">Spring系列八股4 - Bean3 (Autowired注解 + 自动装配)</source>
      <description>Spring基础 Autowired注解 原理 @Autowired 是 Spring 实现依赖注入的核心注解，其实现原理基于反射机制和 BeanPostProcessor 接口 整个过程分为两个主要阶段 第一个阶段是依赖收集阶段，发生在 Bean 实例化之后、属性赋值之前 Autowired 的 Processor 会扫描 Bean 的所有字段、方法...</description>
      <category>code</category>
      <pubDate>Thu, 02 Apr 2026 08:51:45 GMT</pubDate>
      <content:encoded><![CDATA[<h2>Spring基础</h2>
<h3>Autowired注解</h3>
<h4>原理</h4>
<p><code>@Autowired</code> 是 Spring 实现依赖注入的核心注解，其实现原理基于反射机制和 BeanPostProcessor 接口</p>
<p>整个过程分为两个主要阶段</p>
<p>第一个阶段是<strong>依赖收集</strong>阶段，发生在 Bean 实例化之后、属性赋值之前</p>
<p><code>Autowired</code> 的 <code>Processor</code> 会扫描 Bean 的所有字段、方法和构造方法，找出标注了 <code>@Autowired</code> 注解的地方，然后把这些信息封装成 <code>Injection</code> 元数据对象缓存起来</p>
<p>这个过程用到了大量的反射操作，需要分析类的结构、注解信息等等</p>
<p>第二个阶段是<strong>依赖注入</strong>阶段，Spring 会取出之前缓存的 Injection 元数据对象，然后逐个处理每个注入点</p>
<p>对于每个 <code>@Autowired</code> 标注的字段或方法，Spring 会<strong>根据类型</strong>去容器中查找匹配的 Bean</p>
<p>在具体的注入过程中，Spring 会使用反射来设置字段的值或者调用 setter 方法。比如对于字段注入，会调用 Field.set() 方法；对于 setter 注入，会调用 Method.invoke() 方法。</p>
<h4>IDEA不推荐使用@Autowired注解原因</h4>
<p>第一个是字段注入不利于单元测试。字段注入需要使用反射或 Spring 容器才能注入依赖，测试更复杂；而构造方法注入可以直接通过构造方法传入 Mock 对象，测试起来更简单</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token comment">// 字段注入的测试困难</span>
<span class="token annotation punctuation">@Test</span>
<span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">testUserService</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
  <span class="token class-name">UserService</span> userService <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">UserService</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token comment">// 无法直接设置userRepository，需要反射或Spring容器</span>
  <span class="token comment">// userService.userRepository = Mockito.mock(UserRepository.class);</span>
  <span class="token comment">// 需要手动设置依赖，测试不方便</span>
  <span class="token class-name">ReflectionTestUtils</span><span class="token punctuation">.</span><span class="token function">setField</span><span class="token punctuation">(</span>userService<span class="token punctuation">,</span> <span class="token string">"userRepository"</span><span class="token punctuation">,</span> <span class="token class-name">Mockito</span><span class="token punctuation">.</span><span class="token function">mock</span><span class="token punctuation">(</span><span class="token class-name">UserRepository</span><span class="token punctuation">.</span><span class="token keyword">class</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  userService<span class="token punctuation">.</span><span class="token function">doSomething</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token comment">// ...</span>
<span class="token punctuation">}</span>

<span class="token comment">// 构造方法注入的测试简单</span>
<span class="token annotation punctuation">@Test</span>
<span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">testUserService</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
  <span class="token class-name">UserRepository</span> mockRepository <span class="token operator">=</span> <span class="token class-name">Mockito</span><span class="token punctuation">.</span><span class="token function">mock</span><span class="token punctuation">(</span><span class="token class-name">UserRepository</span><span class="token punctuation">.</span><span class="token keyword">class</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token class-name">UserService</span> userService <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">UserService</span><span class="token punctuation">(</span>mockRepository<span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 直接注入</span>
<span class="token punctuation">}</span>
</code></pre></div><p>第二个是字段注入会隐藏循环依赖问题，而构造方法注入会在项目启动时就去检查依赖关系，能更早发现问题。</p>
<p>第三个是构造方法注入可以使用 final 字段确保依赖在对象创建时就被初始化，避免了后续修改的风险。</p>
<h3>自动装配</h3>
<p>自动装配的本质就是让 Spring 容器自动帮我们完成 Bean 之间的依赖关系注入，而不需要我们手动去指定每个依赖</p>
<p>简单来说，就是“我们不用告诉 Spring 具体怎么注入，Spring 自己会想办法找到合适的 Bean 注入进来”。</p>
<p>自动装配的工作原理简单来说就是，Spring 容器在启动时自动扫描 <code>@ComponentScan</code> 指定包路径下的所有类</p>
<p>然后根据类上的注解，比如 <code>@Autowired、@Resource</code> 等，来判断哪些 Bean 需要被自动装配</p>
<p>之后分析每个 Bean 的依赖关系，在创建 Bean 的时候，根据装配规则自动找到合适的依赖 Bean，最后根据反射将这些依赖注入到目标 Bean 中</p>
<h4><code>@ComponentScan</code></h4>
<p>@ComponentScan 只是告诉 Spring 去哪里找，但只有带特定注解的类才会被注册为 Bean</p>
<div class="language-text" data-ext="text" data-title="text"><pre class="language-text"><code>阶段1：扫描 + 注册 Bean 定义
────────────────────────────
@ComponentScan 扫描 → 发现有 @Component 等注解 → 注册 Bean 定义


阶段2：创建实例 + 自动装配
────────────────────────────
创建 Bean 实例 → 发现 @Autowired → 找到依赖 Bean → 注入
</code></pre></div><h5>注册条件</h5>
<div class="language-text" data-ext="text" data-title="text"><pre class="language-text"><code>@Component      // 通用组件
@Service        // 服务层
@Repository     // 数据访问层
@Controller     // 控制器层
@Configuration  // 配置类（内部的 @Bean 方法也会注册）
</code></pre></div><h5>Spring Boot（通常不需要写）</h5>
<p><code>@SpringBootApplication</code> 已经内置了 <code>@ComponentScan</code>，默认扫描启动类所在包及其子包</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token annotation punctuation">@SpringBootApplication</span>  <span class="token comment">// 内含 @ComponentScan</span>
<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">Application</span> <span class="token punctuation">{</span>
  <span class="token keyword">public</span> <span class="token keyword">static</span> <span class="token keyword">void</span> <span class="token function">main</span><span class="token punctuation">(</span><span class="token class-name">String</span><span class="token punctuation">[</span><span class="token punctuation">]</span> args<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token class-name">SpringApplication</span><span class="token punctuation">.</span><span class="token function">run</span><span class="token punctuation">(</span><span class="token class-name">Application</span><span class="token punctuation">.</span><span class="token keyword">class</span><span class="token punctuation">,</span> args<span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><div class="language-text" data-ext="text" data-title="text"><pre class="language-text"><code>com.example
├── Application.java      ← 启动类
├── controller/           ← 自动扫描到
├── service/              ← 自动扫描到
└── dao/                  ← 自动扫描到
</code></pre></div><blockquote>
<p>想自定义扫描范围</p>
</blockquote>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token annotation punctuation">@SpringBootApplication</span>
<span class="token annotation punctuation">@ComponentScan</span><span class="token punctuation">(</span>basePackages <span class="token operator">=</span> <span class="token punctuation">{</span><span class="token string">"com.example"</span><span class="token punctuation">,</span> <span class="token string">"com.other"</span><span class="token punctuation">}</span><span class="token punctuation">)</span>  <span class="token comment">// 自定义</span>
<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">Application</span> <span class="token punctuation">{</span><span class="token punctuation">}</span>
</code></pre></div><h5>传统 Spring 项目（必须写）</h5>
<p>没有 @ComponentScan，Spring 不会扫描注解，Bean 只能通过其他方式注册：</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token comment">// 方式一：XML 配置</span>
<span class="token operator">&lt;</span>bean id<span class="token operator">=</span><span class="token string">"userService"</span> <span class="token keyword">class</span><span class="token operator">=</span><span class="token string">"com.example.UserService"</span><span class="token operator">/</span><span class="token operator">&gt;</span>

<span class="token comment">// 方式二：@Bean 方法</span>
<span class="token annotation punctuation">@Configuration</span>
<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">AppConfig</span> <span class="token punctuation">{</span>
  <span class="token annotation punctuation">@Bean</span>
  <span class="token keyword">public</span> <span class="token class-name">UserService</span> <span class="token function">userService</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">return</span> <span class="token keyword">new</span> <span class="token class-name">UserService</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>

<span class="token comment">// 方式三：@Import</span>
<span class="token annotation punctuation">@Import</span><span class="token punctuation">(</span><span class="token class-name">UserService</span><span class="token punctuation">.</span><span class="token keyword">class</span><span class="token punctuation">)</span>
</code></pre></div><h4>自动装配类型</h4>
<p>Spring 的自动装配方式有好几种，在 XML 配置时代，主要有 byName、byType、constructor 和 autodetect 四种方式</p>
<p>到了注解驱动时代，用得最多的是 <code>@Autowired</code> 注解，默认按照类型装配</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token annotation punctuation">@Service</span>
<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">UserService</span> <span class="token punctuation">{</span>
  <span class="token annotation punctuation">@Autowired</span>  <span class="token comment">// 按类型自动装配</span>
  <span class="token keyword">private</span> <span class="token class-name">UserRepository</span> userRepository<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre></div><p>其次还有 <code>@Resource</code> 注解，它默认按照名称装配，如果找不到对应名称的 Bean，就会按类型装配</p>
<p>Spring Boot 的自动装配还有一套更高级的机制，通过 <code>@EnableAutoConfiguration</code> 和各种 <code>@Conditional</code> 注解来实现，这个是框架级别的自动装配，会根据 classpath 中的类和配置来自动配置 Bean</p>
<figure><figcaption>alt text</figcaption></figure>
]]></content:encoded>
    </item>
    <item>
      <title>Spring系列八股5 - 设计模式 + 三级依赖</title>
      <link>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/Spring/article5.html</link>
      <guid>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/Spring/article5.html</guid>
      <source url="http://ekkosonya.cn/rss.xml">Spring系列八股5 - 设计模式 + 三级依赖</source>
      <description>Spring基础 设计模式 Spring 框架里面确实用了很多设计模式 工厂模式 (管理 Bean)、单例模式 (IOC)、代理模式 (AOP) 模版模式、观察者模式 涉及： 工厂模式 BeanFactory 就是一个典型的工厂，它负责创建和管理所有的 Bean 对象 我们平时用的 ApplicationContext 其实也是 BeanFactory...</description>
      <category>code</category>
      <pubDate>Thu, 02 Apr 2026 08:51:45 GMT</pubDate>
      <content:encoded><![CDATA[<h2>Spring基础</h2>
<h3>设计模式</h3>
<p>Spring 框架里面确实用了很多设计模式</p>
<p>工厂模式 (管理 Bean)、单例模式 (IOC)、代理模式 (AOP)</p>
<p>模版模式、观察者模式</p>
<p>涉及：</p>
<ul>
<li>工厂模式
<ul>
<li>BeanFactory 就是一个典型的工厂，它负责创建和管理所有的 Bean 对象</li>
<li>我们平时用的 ApplicationContext 其实也是 BeanFactory 的一个实现</li>
<li>当我们通过 @Autowired 获取一个 Bean 的时候，底层就是通过工厂模式来创建和获取对象的。</li>
</ul>
</li>
<li>单例模式
<ul>
<li>单例模式也是 Spring 的默认行为。默认情况下，Spring 容器中的 Bean 都是单例的，整个应用中只会有一个实例</li>
<li>这样可以节省内存，提高性能。当然我们也可以通过 @Scope 注解来改变 Bean 的作用域，比如设置为 prototype 就是每次获取都创建新实例。</li>
</ul>
</li>
<li>代理模式
<ul>
<li>代理模式在 AOP 中用得特别多。Spring AOP 的底层实现就是基于动态代理的，对于实现了接口的类用 JDK 动态代理，没有实现接口的类用 CGLIB 代理。比如我们用 @Transactional 注解的时候，Spring 会为我们的类创建一个代理对象，在方法执行前后添加事务处理逻辑。</li>
</ul>
</li>
<li>模版模式
<ul>
<li>模板方法模式在 Spring 里也很常见，比如 JdbcTemplate。它定义了数据库操作的基本流程：获取连接、执行 SQL、处理结果、关闭连接，但是具体的 SQL 语句和结果处理逻辑由我们来实现。</li>
</ul>
</li>
<li>观察者模式
<ul>
<li>观察者模式在 Spring 的事件机制中有所体现。我们可以通过 ApplicationEvent 和 ApplicationListener 来实现事件的发布和监听。比如用户注册成功后，我们可以发布一个用户注册事件，然后有多个监听器来处理后续的业务逻辑，比如发送邮件、记录日志等。</li>
</ul>
</li>
<li>适配器模式</li>
<li>策略模式</li>
</ul>
<h4>Spring 如何实现单例模式</h4>
<p>传统的单例模式是在类的内部控制只能创建一个实例，比如用 private 构造方法加 static getInstance() 这种方式</p>
<p>但是 Spring 的单例是<strong>容器级别</strong>的，同一个 Bean 在整个 Spring 容器中只会有一个实例。</p>
<p>具体的实现机制是这样的：</p>
<p>Spring 在启动的时候会把所有的 Bean 定义信息加载进来，然后在 <code>DefaultSingletonBeanRegistry</code> 这个类里面维护了一个叫 <code>singletonObjects</code> 的 <code>ConcurrentHashMap</code>，这个 Map 就是用来存储单例 Bean 的</p>
<p>key 是 Bean 的名称，value 就是 Bean 的实例对象。</p>
<p>当我们第一次获取某个 Bean 的时候，Spring 会先检查 singletonObjects 这个 Map 里面有没有这个 Bean，如果没有就会创建一个新的实例，然后放到 Map 里面。后面再获取同一个 Bean 的时候，直接从 Map 里面取就行了，这样就保证了单例。</p>
<p>还有一个细节就是 Spring 为了解决循环依赖的问题，还用了三级缓存。除了 singletonObjects 这个一级缓存，还有 earlySingletonObjects 二级缓存和 singletonFactories 三级缓存</p>
<p>这样即使有循环依赖，Spring 也能正确处理。</p>
<h3>循环依赖问题</h3>
<p>简单来说就是两个或多个 Bean 相互依赖，比如说 A 依赖 B，B 依赖 A，或者 C 依赖 C，就成了循环依赖</p>
<figure><figcaption>alt text</figcaption></figure>
<h4>Spring 解决方案</h4>
<p>Spring 通过三级缓存机制来解决循环依赖：</p>
<ul>
<li>一级缓存：存放完全初始化好的单例 Bean</li>
<li>二级缓存：存放提前暴露的 Bean，实例化完成，但未初始化完成</li>
<li>三级缓存：存放 Bean 工厂，用于生成提前暴露的 Bean</li>
</ul>
<figure><figcaption>alt text</figcaption></figure>
<h5>例子</h5>
<figure><figcaption>alt text</figcaption></figure>
<p>第 1 步：开始创建 Bean A</p>
<ul>
<li>Spring 调用 A 的构造方法，创建 A 的实例。此时 A 对象已存在，但 b属性还是 null。</li>
<li>将 A 的对象工厂放入三级缓存。</li>
<li>开始进行 A 的属性注入。</li>
</ul>
<p>第 2 步：A 需要注入 B，开始创建 Bean B</p>
<ul>
<li>发现需要 B，但 B 还不存在，所以开始创建 B。</li>
<li>调用 B 的构造方法，创建 B 的实例。此时 B 对象已存在，但 a 属性还是 null。</li>
<li>将 B 的对象工厂放入三级缓存。</li>
<li>开始进行 B 的属性注入</li>
</ul>
<p>第 3 步：B 需要注入 A，从缓存中获取 A。</p>
<ul>
<li>B 需要注入 A，先从一级缓存找 A，没找到。</li>
<li>再从二级缓存找 A，也没找到。</li>
<li>最后从三级缓存找 A，找到了 A 的对象工厂。</li>
<li>调用 A 的对象工厂得到 A 的实例。这时 A 已经实例化了，虽然还没完全初始化。</li>
<li>将 A 从三级缓存移到二级缓存。</li>
<li>B 拿到 A 的引用，完成属性注入。</li>
</ul>
<p>第 4 步：B 完成初始化。</p>
<ul>
<li>B 的属性注入完成，执行 @PostConstruct 等初始化逻辑。</li>
<li>B 完全创建完成，从三级缓存移除，放入一级缓存。</li>
</ul>
<p>第 5 步：A 完成初始化。</p>
<ul>
<li>回到 A 的创建过程，A 拿到完整的 B 实例，完成属性注入。</li>
<li>A 执行初始化逻辑，创建完成。</li>
<li>A 从二级缓存移除，放入一级缓存。</li>
</ul>
<p>用代码来模拟这个过程，是这样的：</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token comment">// 模拟Spring的解决过程</span>
<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">CircularDependencyDemo</span> <span class="token punctuation">{</span>
  <span class="token comment">// 三级缓存</span>
  <span class="token class-name">Map</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">String</span><span class="token punctuation">,</span> <span class="token class-name">Object</span><span class="token punctuation">&gt;</span></span> singletonObjects <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">HashMap</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token punctuation">&gt;</span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token class-name">Map</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">String</span><span class="token punctuation">,</span> <span class="token class-name">Object</span><span class="token punctuation">&gt;</span></span> earlySingletonObjects <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">HashMap</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token punctuation">&gt;</span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token class-name">Map</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">String</span><span class="token punctuation">,</span> <span class="token class-name">ObjectFactory</span><span class="token punctuation">&gt;</span></span> singletonFactories <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">HashMap</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token punctuation">&gt;</span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  
  <span class="token keyword">public</span> <span class="token class-name">Object</span> <span class="token function">getBean</span><span class="token punctuation">(</span><span class="token class-name">String</span> beanName<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token comment">// 先从一级缓存获取</span>
    <span class="token class-name">Object</span> bean <span class="token operator">=</span> singletonObjects<span class="token punctuation">.</span><span class="token function">get</span><span class="token punctuation">(</span>beanName<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">if</span> <span class="token punctuation">(</span>bean <span class="token operator">!=</span> <span class="token keyword">null</span><span class="token punctuation">)</span> <span class="token keyword">return</span> bean<span class="token punctuation">;</span>
    
    <span class="token comment">// 再从二级缓存获取</span>
    bean <span class="token operator">=</span> earlySingletonObjects<span class="token punctuation">.</span><span class="token function">get</span><span class="token punctuation">(</span>beanName<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">if</span> <span class="token punctuation">(</span>bean <span class="token operator">!=</span> <span class="token keyword">null</span><span class="token punctuation">)</span> <span class="token keyword">return</span> bean<span class="token punctuation">;</span>
    
    <span class="token comment">// 最后从三级缓存获取</span>
    <span class="token class-name">ObjectFactory</span> factory <span class="token operator">=</span> singletonFactories<span class="token punctuation">.</span><span class="token function">get</span><span class="token punctuation">(</span>beanName<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">if</span> <span class="token punctuation">(</span>factory <span class="token operator">!=</span> <span class="token keyword">null</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
      bean <span class="token operator">=</span> factory<span class="token punctuation">.</span><span class="token function">getObject</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
      earlySingletonObjects<span class="token punctuation">.</span><span class="token function">put</span><span class="token punctuation">(</span>beanName<span class="token punctuation">,</span> bean<span class="token punctuation">)</span><span class="token punctuation">;</span>  <span class="token comment">// 移到二级缓存</span>
      singletonFactories<span class="token punctuation">.</span><span class="token function">remove</span><span class="token punctuation">(</span>beanName<span class="token punctuation">)</span><span class="token punctuation">;</span>        <span class="token comment">// 从三级缓存移除</span>
    <span class="token punctuation">}</span>
    
    <span class="token keyword">return</span> bean<span class="token punctuation">;</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><h5>哪些情况下Spring无法解决循环依赖</h5>
<p>第一种，构造方法的循环依赖，这种情况 Spring 会直接抛出 BeanCurrentlyInCreationException 异常</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token annotation punctuation">@Component</span>
<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">A</span> <span class="token punctuation">{</span>
  <span class="token keyword">private</span> <span class="token class-name">B</span> b<span class="token punctuation">;</span>
  
  <span class="token keyword">public</span> <span class="token class-name">A</span><span class="token punctuation">(</span><span class="token class-name">B</span> b<span class="token punctuation">)</span> <span class="token punctuation">{</span>  <span class="token comment">// 构造方法注入</span>
    <span class="token keyword">this</span><span class="token punctuation">.</span>b <span class="token operator">=</span> b<span class="token punctuation">;</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>

<span class="token annotation punctuation">@Component</span>
<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">B</span> <span class="token punctuation">{</span>
  <span class="token keyword">private</span> <span class="token class-name">A</span> a<span class="token punctuation">;</span>
  
  <span class="token keyword">public</span> <span class="token class-name">B</span><span class="token punctuation">(</span><span class="token class-name">A</span> a<span class="token punctuation">)</span> <span class="token punctuation">{</span>  <span class="token comment">// 构造方法注入</span>
    <span class="token keyword">this</span><span class="token punctuation">.</span>a <span class="token operator">=</span> a<span class="token punctuation">;</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><p>因为构造方法注入发生在实例化阶段，创建 A 的时候必须先有 B，但创建 B又必须先有 A，这时候两个对象都还没创建出来，无法提前暴露到缓存中</p>
<p>第二种，prototype 作用域的循环依赖。prototype 作用域的 Bean 每次获取都会创建新实例，Spring 无法缓存这些实例，所以也无法解决循环依赖</p>
<h4>为什么需要三级缓存而不是两级 (*****)</h4>
<p>Spring 三级缓存是为了解决循环依赖中 AOP 代理不一致的问题</p>
<blockquote>
<p>三级缓存（singletonFactories）里面存放的，实际上是一个叫 ObjectFactory 的泛型接口对象</p>
</blockquote>
<p>如果只有二级缓存：暴露的是原始对象，但其他 Bean 注入时可能需要代理对象，导致注入的是原始对象，破坏了代理语义</p>
<p>三级缓存存的是 ObjectFactory (本身就是一段 lamda 表达式)，在 getEarlyBeanReference() 中判断：</p>
<ul>
<li>如果需要代理 → 提前创建代理对象，放入二级缓存并返回</li>
<li>如果不需要代理 → 直接返回原始对象</li>
</ul>
<p>这样保证循环依赖中注入的都是"正确的对象"</p>
<h5>ObjectFactory</h5>
<p>在 Spring 的源码里，ObjectFactory 是一个非常简单的函数式接口，里面只有一个方法</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token annotation punctuation">@FunctionalInterface</span>
<span class="token keyword">public</span> <span class="token keyword">interface</span> <span class="token class-name">ObjectFactory</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">T</span><span class="token punctuation">&gt;</span></span> <span class="token punctuation">{</span>
  <span class="token class-name">T</span> <span class="token function">getObject</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token keyword">throws</span> <span class="token class-name">BeansException</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre></div><p>通俗地理解，它就是一个“获取对象的钩子（回调函数）”。它本身不是对象，而是一段“教你怎么拿到对象”的代码逻辑</p>
<p>在 JDK 8 以后，它在源码里通常表现为一个 Lambda 表达式</p>
<h5>原因</h5>
<p>要理解第三级缓存的精妙之处，我们必须结合 循环依赖 和 AOP（动态代理） 来看</p>
<p>假设有两个单例 Bean：ServiceA 和 ServiceB，它们互相 <code>@Autowired</code> 依赖了对方。</p>
<p>并且，ServiceA 上加了 <code>@Transactional</code> 注解（意味着最终放入容器的 ServiceA 必须是一个 AOP 代理对象，而不是原始对象）</p>
<p>如果没有第三级缓存，只有一、二级缓存，流程会是这样惨烈的状况：</p>
<ol>
<li><strong>A 实例化</strong>：Spring <code>new</code> 出了 A 的原始对象（半成品）。</li>
<li><strong>A 属性注入</strong>：发现需要 B，于是去创建 B。</li>
<li><strong>B 实例化</strong>：Spring <code>new</code> 出了 B 的原始对象（半成品）。</li>
<li><strong>B 属性注入</strong>：发现需要 A。此时 B 去缓存里找 A。</li>
<li><strong>致命缺陷出现</strong>：如果缓存里直接放的是 A 的半成品原始对象，B 就会把这个<strong>原始 A</strong> 注入到自己的属性里。但等 B 创建完，A 继续走生命周期，在最后一步（初始化后置处理）时，A 发现自己有事务注解，于是生成了一个 <strong>A 的代理对象</strong>。</li>
<li><strong>最终结果</strong>：Spring 容器里最终保存的是 A 的代理对象，<strong>但是 B 肚子里装的却是 A 的原始对象！</strong> 两者不一致，事务全盘失效。</li>
</ol>
<h5>作用</h5>
<p>Spring 引入了存放 ObjectFactory 的第三级缓存。真实的源码流程是这样的：</p>
<ul>
<li><strong>A 实例化</strong>：<code>new</code> 出 A 的原始对象（半成品）。</li>
<li><strong>放入三级缓存</strong>：此时，Spring 并不直接把半成品的 A 放入缓存，而是<strong>构造了一个 Lambda 表达式（ObjectFactory），把它塞进了三级缓存</strong>。
<ul>
<li>这段 Lambda 的核心逻辑是：<code>() -&gt; getEarlyBeanReference(beanName, mbd, bean)</code>。</li>
<li><em>翻译成人话就是：“如果有人现在就要引用我，请执行这段逻辑来决定给他什么对象。”</em></li>
</ul>
</li>
<li><strong>A 属性注入</strong>：发现需要 B，去创建 B。</li>
<li><strong>B 实例化与注入</strong>：B 实例化后发现需要 A，于是去缓存找。一二级缓存没有，但在第三级缓存找到了 A 留下的那个 <code>ObjectFactory</code>（Lambda 表达式）。</li>
<li><strong>调用钩子（见证奇迹的时刻）</strong>：B 调用了 <code>ObjectFactory.getObject()</code>。此时触发了那段 Lambda 表达式的代码
<ul>
<li>这段代码（<code>SmartInstantiationAwareBeanPostProcessor</code>）会进行判断：
<ul>
<li>发现 A 需要被 AOP 增强！</li>
<li>于是，<strong>Spring 在这个时候（提前）为 A 创建了 AOP 代理对象</strong>。</li>
<li>把这个代理对象返回给了 B。</li>
</ul>
</li>
</ul>
</li>
<li><strong>缓存升级</strong>：为了防止后面还有 C、D 也来要 A 的引用，Spring 会把刚才生成的 A 的代理对象放入<strong>第二级缓存</strong>（<code>earlySingletonObjects</code>），并把 A 从<strong>第三级缓存</strong>中删掉。</li>
<li><strong>B 顺利完工</strong>：B 把 A 的代理对象注入到自己肚子里，完成创建，返回给 A。</li>
<li><strong>A 顺利完工</strong>：A 拿到 B，完成自己的属性注入。走到生命周期最后一步时，A 发现自己已经被提前代理过了，就不再重复代理，直接把代理对象放入<strong>第一级缓存</strong>（<code>singletonObjects</code>）</li>
</ul>
<h5>总结</h5>
<ul>
<li><strong>第三级缓存里放的“工厂”</strong>：其实是一段 Lambda 回调逻辑（<code>ObjectFactory</code>），它手里攥着半成品的目标对象。</li>
<li><strong>它的终极使命</strong>：处理 AOP 代理的“提前暴露”问题。如果发生循环依赖，并且目标对象需要被代理，这个“工厂”就会在被调用时，临时把原始对象加工成代理对象返回出去，保证全局注入的都是同一个代理对象。如果没有循环依赖，这个第三级缓存里的工厂就永远不会被调用，默默地等待 Bean 创建完成后被清理掉。</li>
</ul>
<h4>缺少二级缓存会怎么样</h4>
<p>二级缓存 earlySingletonObjects 主要是用来存放那些已经通过三级缓存的对象工厂创建出来的早期 Bean 引用</p>
<p>假设我们有 A、B、C 三个 Bean，A 依赖 B 和 C，B 和 C 都依赖 A，形成了一个复杂的循环依赖。</p>
<p>在没有二级缓存的情况下，每次 B 或者 C 需要获取 A 的时候，都需要调用三级缓存中 A 的 <code>ObjectFactory.getObject()</code> 方法</p>
<p>这意味着如果 A 需要被代理的话，代理对象可能会被重复创建多次，这显然是不合理的。</p>
<p>举个具体的例子</p>
<p>比如 A 有 <code>@Transactional</code> 注解需要被 AOP 代理，B 在初始化的时候需要 A，会调用一次对象工厂创建 A 的代理对象</p>
<p>接着 C 在初始化的时候也需要 A，又会调用一次对象工厂，可能又创建了一个 A 的代理对象</p>
<p>这样 B 和 C 拿到的可能就是两个不同的 A 代理对象，这就违反了单例 Bean 的语义。</p>
<blockquote>
<p>二级缓存就是为了解决这个问题。当第一次通过对象工厂创建了 A 的早期引用之后，就把这个引用放到二级缓存中，同时从三级缓存中移除对象工厂</p>
<p>后续如果再有其他 Bean 需要 A，就直接从二级缓存中获取，不需要再调用对象工厂了</p>
</blockquote>
<h4>源码</h4>
<p>当 B 在做属性注入，发现自己需要 A，去向 Spring 要 A 的时候，流程会走到 <code>DefaultSingletonBeanRegistry.java</code> 的 <code>getSingleton()</code> 方法：</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">protected</span> <span class="token class-name">Object</span> <span class="token function">getSingleton</span><span class="token punctuation">(</span><span class="token class-name">String</span> beanName<span class="token punctuation">,</span> <span class="token keyword">boolean</span> allowEarlyReference<span class="token punctuation">)</span> <span class="token punctuation">{</span>
  <span class="token comment">// 【第一道防线：成品区】</span>
  <span class="token comment">// 尝试从第一级缓存（singletonObjects）中获取完整的成品 Bean。</span>
  <span class="token class-name">Object</span> singletonObject <span class="token operator">=</span> <span class="token keyword">this</span><span class="token punctuation">.</span>singletonObjects<span class="token punctuation">.</span><span class="token function">get</span><span class="token punctuation">(</span>beanName<span class="token punctuation">)</span><span class="token punctuation">;</span>

  <span class="token comment">// 如果一级缓存没有拿到，并且这个 Bean 当前正处于“正在创建中”的状态。</span>
  <span class="token comment">// （注：判断“正在创建中”非常关键！这是循环依赖发生的唯一前提，比如 A 创建时触发了 B，B 又回头来找 A）</span>
  <span class="token keyword">if</span> <span class="token punctuation">(</span>singletonObject <span class="token operator">==</span> <span class="token keyword">null</span> <span class="token operator">&amp;&amp;</span> <span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">isSingletonCurrentlyInCreation</span><span class="token punctuation">(</span>beanName<span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token comment">// 【第二道防线：半成品区（无锁快查）】</span>
    <span class="token comment">// 尝试从第二级缓存（earlySingletonObjects）中获取提前暴露的“半成品”或“代理对象”。</span>
    <span class="token comment">// 这里没有加锁，是为了在绝大多数情况下追求极致的读取性能。</span>
    singletonObject <span class="token operator">=</span> <span class="token keyword">this</span><span class="token punctuation">.</span>earlySingletonObjects<span class="token punctuation">.</span><span class="token function">get</span><span class="token punctuation">(</span>beanName<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token comment">// 如果二级缓存也没拿到，并且当前允许进行提前引用（通常都是 true）</span>
    <span class="token keyword">if</span> <span class="token punctuation">(</span>singletonObject <span class="token operator">==</span> <span class="token keyword">null</span> <span class="token operator">&amp;&amp;</span> allowEarlyReference<span class="token punctuation">)</span> <span class="token punctuation">{</span>
        <span class="token comment">// 【进入加锁警戒区】</span>
        <span class="token comment">// 接下来要动用三级缓存并修改 Map 结构了，为了防止多线程环境下的数据错乱，必须加全局锁。</span>
        <span class="token keyword">synchronized</span><span class="token punctuation">(</span><span class="token keyword">this</span><span class="token punctuation">.</span>singletonObjects<span class="token punctuation">)</span> <span class="token punctuation">{</span>
          <span class="token comment">// === DCL 双重检查锁（Double-Check Locking）开始 ===</span>
          <span class="token comment">// 第 1 次复查：再查一次一级缓存。</span>
          <span class="token comment">// （防范场景：当前线程在门外等锁的这短暂瞬间，可能别的线程已经彻底走完生命周期，把成品 Bean 放进一级缓存了）</span>
          singletonObject <span class="token operator">=</span> <span class="token keyword">this</span><span class="token punctuation">.</span>singletonObjects<span class="token punctuation">.</span><span class="token function">get</span><span class="token punctuation">(</span>beanName<span class="token punctuation">)</span><span class="token punctuation">;</span>
          <span class="token keyword">if</span> <span class="token punctuation">(</span>singletonObject <span class="token operator">==</span> <span class="token keyword">null</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
              <span class="token comment">// 第 2 次复查：再查一次二级缓存。</span>
              <span class="token comment">// （防范场景：刚才在门外等锁时，可能别的线程已经抢先触发了下面的三级缓存逻辑，把半成品提拔到二级缓存了）</span>
              singletonObject <span class="token operator">=</span> <span class="token keyword">this</span><span class="token punctuation">.</span>earlySingletonObjects<span class="token punctuation">.</span><span class="token function">get</span><span class="token punctuation">(</span>beanName<span class="token punctuation">)</span><span class="token punctuation">;</span>
              <span class="token keyword">if</span> <span class="token punctuation">(</span>singletonObject <span class="token operator">==</span> <span class="token keyword">null</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
                <span class="token comment">// 【最终防线：图纸与工厂区】</span>
                <span class="token comment">// 如果前两级缓存确实都没有，去第三级缓存（singletonFactories）里找那个 Lambda 表达式工厂。</span>
                <span class="token class-name">ObjectFactory</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token operator">?</span><span class="token punctuation">&gt;</span></span> singletonFactory <span class="token operator">=</span> <span class="token punctuation">(</span><span class="token class-name">ObjectFactory</span><span class="token punctuation">)</span><span class="token keyword">this</span><span class="token punctuation">.</span>singletonFactories<span class="token punctuation">.</span><span class="token function">get</span><span class="token punctuation">(</span>beanName<span class="token punctuation">)</span><span class="token punctuation">;</span>
                
                <span class="token keyword">if</span> <span class="token punctuation">(</span>singletonFactory <span class="token operator">!=</span> <span class="token keyword">null</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
                  <span class="token comment">// 核心动作爆发：调用 getObject()，真正执行那段 Lambda 表达式！</span>
                  <span class="token comment">// （如果在 AOP 环境下，就是在这里提前动态生成了代理对象）</span>
                  singletonObject <span class="token operator">=</span> singletonFactory<span class="token punctuation">.</span><span class="token function">getObject</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
                  
                  <span class="token comment">// 【缓存升级操作】</span>
                  <span class="token comment">// 1. 把刚生成的对象放进第二级缓存。</span>
                  <span class="token comment">// （意义重大：如果一会儿还有 C、D 也要依赖 A，它们在门外查第二级缓存就能直接拿到了，绝不能让 Lambda 被重复执行，保证单例唯一）</span>
                  <span class="token keyword">this</span><span class="token punctuation">.</span>earlySingletonObjects<span class="token punctuation">.</span><span class="token function">put</span><span class="token punctuation">(</span>beanName<span class="token punctuation">,</span> singletonObject<span class="token punctuation">)</span><span class="token punctuation">;</span>
                  
                  <span class="token comment">// 2. 过河拆桥：从第三级缓存中删掉这个工厂。</span>
                  <span class="token comment">// （它已经生产出对象并放进二级缓存了，完成了历史使命，用完即焚）</span>
                  <span class="token keyword">this</span><span class="token punctuation">.</span>singletonFactories<span class="token punctuation">.</span><span class="token function">remove</span><span class="token punctuation">(</span>beanName<span class="token punctuation">)</span><span class="token punctuation">;</span>
                <span class="token punctuation">}</span>
              <span class="token punctuation">}</span>
          <span class="token punctuation">}</span>
          <span class="token comment">// === DCL 双重检查锁结束 ===</span>
        <span class="token punctuation">}</span>
    <span class="token punctuation">}</span>
  <span class="token punctuation">}</span>

  <span class="token comment">// 凯旋而归：返回最终找到的（或刚刚提前生成的）对象</span>
  <span class="token keyword">return</span> singletonObject<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre></div>]]></content:encoded>
    </item>
    <item>
      <title>Spring系列八股6 - Spring 启动分析</title>
      <link>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/Spring/article6.html</link>
      <guid>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/Spring/article6.html</guid>
      <source url="http://ekkosonya.cn/rss.xml">Spring系列八股6 - Spring 启动分析</source>
      <description>Spring基础 最简单的 Spring 模版 运行源码 this(): 负责准备环境。 register(): 负责注入最初始的元数据（可以理解为给 Spring 一个“线头”）。 refresh(): 则是一个庞大的、标准的模板方法（Template Method 设计模式） 顺着 register() 给的线头，refresh() 里的 Conf...</description>
      <category>code</category>
      <pubDate>Thu, 02 Apr 2026 08:51:45 GMT</pubDate>
      <content:encoded><![CDATA[<h2>Spring基础</h2>
<h3>最简单的 Spring 模版</h3>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">package</span> <span class="token namespace">com<span class="token punctuation">.</span>ekko</span><span class="token punctuation">;</span>

<span class="token keyword">import</span> <span class="token import"><span class="token namespace">org<span class="token punctuation">.</span>springframework<span class="token punctuation">.</span>context<span class="token punctuation">.</span>annotation<span class="token punctuation">.</span></span><span class="token class-name">AnnotationConfigApplicationContext</span></span><span class="token punctuation">;</span>
<span class="token keyword">import</span> <span class="token import"><span class="token namespace">org<span class="token punctuation">.</span>springframework<span class="token punctuation">.</span>context<span class="token punctuation">.</span>annotation<span class="token punctuation">.</span></span><span class="token class-name">Configuration</span></span><span class="token punctuation">;</span>

<span class="token annotation punctuation">@Configuration</span>
<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">Main</span> <span class="token punctuation">{</span>
    <span class="token keyword">public</span> <span class="token keyword">static</span> <span class="token keyword">void</span> <span class="token function">main</span><span class="token punctuation">(</span><span class="token class-name">String</span><span class="token punctuation">[</span><span class="token punctuation">]</span> args<span class="token punctuation">)</span> <span class="token punctuation">{</span>
      <span class="token comment">// 这就是启动 Spring 容器最核心、最简单的一行代码</span>
      <span class="token class-name">AnnotationConfigApplicationContext</span> context <span class="token operator">=</span>
              <span class="token keyword">new</span> <span class="token class-name">AnnotationConfigApplicationContext</span><span class="token punctuation">(</span><span class="token class-name">Main</span><span class="token punctuation">.</span><span class="token keyword">class</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

      <span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span><span class="token string">"Spring 容器启动完成！"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><h4>运行源码</h4>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token comment">// Spring 框架源码：AnnotationConfigApplicationContext.java</span>

<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">AnnotationConfigApplicationContext</span> <span class="token keyword">extends</span> <span class="token class-name">GenericApplicationContext</span> <span class="token keyword">implements</span> <span class="token class-name">AnnotationConfigRegistry</span> <span class="token punctuation">{</span>
  <span class="token keyword">private</span> <span class="token keyword">final</span> <span class="token class-name">AnnotatedBeanDefinitionReader</span> reader<span class="token punctuation">;</span>
  <span class="token keyword">private</span> <span class="token keyword">final</span> <span class="token class-name">ClassPathBeanDefinitionScanner</span> scanner<span class="token punctuation">;</span>

  <span class="token keyword">public</span> <span class="token class-name">AnnotationConfigApplicationContext</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token class-name">StartupStep</span> createAnnotatedBeanDefReader <span class="token operator">=</span> <span class="token keyword">this</span><span class="token punctuation">.</span><span class="token function">getApplicationStartup</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">start</span><span class="token punctuation">(</span><span class="token string">"spring.context.annotated-bean-reader.create"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">this</span><span class="token punctuation">.</span>reader <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">AnnotatedBeanDefinitionReader</span><span class="token punctuation">(</span><span class="token keyword">this</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    createAnnotatedBeanDefReader<span class="token punctuation">.</span><span class="token function">end</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">this</span><span class="token punctuation">.</span>scanner <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">ClassPathBeanDefinitionScanner</span><span class="token punctuation">(</span><span class="token keyword">this</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token punctuation">}</span>

  <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span>

  <span class="token keyword">public</span> <span class="token class-name">AnnotationConfigApplicationContext</span><span class="token punctuation">(</span><span class="token class-name">Class</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token operator">?</span><span class="token punctuation">&gt;</span></span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span> componentClasses<span class="token punctuation">)</span> <span class="token punctuation">{</span>
      <span class="token comment">// 第一步：调用无参构造函数</span>
      <span class="token comment">// 动作：在这里会隐式地初始化一个 BeanFactory，并实例化“读取器 (Reader)”和“扫描器 (Scanner)”。</span>
      <span class="token comment">// 意义：准备好了处理注解的工具。</span>
      <span class="token keyword">this</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
      
      <span class="token comment">// 第二步：注册配置类</span>
      <span class="token comment">// 动作：把你传入的 AppConfig.class 解析成一个 BeanDefinition，并放入 beanDefinitionMap 中。</span>
      <span class="token comment">// 意义：把启动入口（图纸）交给 Spring。</span>
      <span class="token function">register</span><span class="token punctuation">(</span>componentClasses<span class="token punctuation">)</span><span class="token punctuation">;</span>
      
      <span class="token comment">// 第三步：刷新容器（核心大戏！）</span>
      <span class="token comment">// 动作：包含 7 大核心步骤的 refresh() 方法。</span>
      <span class="token comment">// 意义：完成包扫描、执行后置处理器、实例化所有单例 Bean 等全部生命周期。</span>
      <span class="token function">refresh</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token punctuation">}</span>

  <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span>

<span class="token punctuation">}</span>
</code></pre></div><ul>
<li><code>this()</code>: 负责准备环境。</li>
<li><code>register()</code>: 负责注入最初始的元数据（可以理解为给 Spring 一个“线头”）。</li>
<li><code>refresh()</code>: 则是一个庞大的、标准的模板方法（Template Method 设计模式）
<ul>
<li>顺着 register() 给的线头，refresh() 里的 <code>ConfigurationClassPostProcessor</code> 就会顺藤摸瓜，把包下所有的 <code>@Component</code> 都扫描出来。</li>
</ul>
</li>
</ul>
<p><code>register()</code> 是把传入的配置类注册成 <code>BeanDefinition</code>，给 Spring 一个"线头"，后续 <code>refresh()</code> 中的 <code>ConfigurationClassPostProcessor</code> 会从这个线头开始扫描</p>
<h3>Spring 启动分析</h3>
<p>Spring 容器的启动过程，本质上就是执行 <code>AbstractApplicationContext</code> 类中大名鼎鼎的 <code>refresh()</code> 方法的过程</p>
<p>这个方法是加了 synchronized 锁的，也就是所谓的“刷新容器”</p>
<p>它不仅包含了“图纸归档”（Bean 注册），更统揽了从环境准备到对象实例化的全局工作</p>
<p>如果将整个启动流程按照 <code>refresh()</code> 方法的核心源码骨架来拆解，可以归纳为以下七个核心阶段</p>
<h4>1. 准备阶段 (Prepare Refresh) 以及 初始化 BeanFactory</h4>
<p>这是启动的“热身”环节</p>
<p>Spring 会在这个阶段记录容器的启动时间，标记当前容器的状态为“激活”，并初始化一些上下文环境中的占位符属性（Property Sources）</p>
<p>同时，它也会验证那些被标记为“必须存在”的系统属性是否都已经配置到位。</p>
<p>Spring 会在这一步创建一个全新的 <code>DefaultListableBeanFactory</code>（也就是那个核心容器对象）</p>
<p>对应源码：<code>obtainFreshBeanFactory()</code> 和 <code>prepareBeanFactory()</code></p>
<ul>
<li>获取工厂：其实在 <code>new AnnotationConfigApplicationContext()</code> 的第一步 this() 中，底层就已经创建好了一个 <code>DefaultListableBeanFactory</code>。这里主要是把它取出来。</li>
<li>准备工具：Spring 会给这个工厂配置上类加载器（ClassLoader）、表达式解析器（SpEL），并默默地塞几个系统级的 Bean 进去（比如 Environment）。</li>
</ul>
<h4>2. 加载图纸 (Obtain &amp; Prepare BeanFactory)</h4>
<p>对应源码：<code>invokeBeanFactoryPostProcessors(beanFactory)</code></p>
<p><strong>这是 <code>refresh</code> 中极其关键的一步，也是连接你之前代码的核心！</strong></p>
<ol>
<li><strong>解析 <code>@ComponentScan</code></strong>：Spring 容器目前只有 <code>AppConfig</code> 这一张图纸。Spring 会找到内部默认注册的 <code>ConfigurationClassPostProcessor</code>（它就是一个超高级的 <code>BeanFactoryPostProcessor</code>），由它去读取 <code>AppConfig</code> 上的包扫描路径。</li>
<li><strong>生成新图纸</strong>：顺着路径，它扫描到了你写的 <code>Pig</code> 类（带有 <code>@Component</code>）。于是，系统通过上一个环节聊到的 <code>doRegisterBean()</code> 逻辑，把 <code>Pig</code> 也变成了 <code>BeanDefinition</code> 存入 Map。至此，所有图纸收集完毕。</li>
</ol>
<h4>3. 执行工厂后置处理器 (Invoke BeanFactoryPostProcessors)</h4>
<p>图纸加载完毕，但在正式”动工”之前，Spring 允许我们对图纸进行最后的修改。</p>
<ul>
<li><strong>调用自定义扩展</strong>：图纸齐了，Spring 开始在 Map 里寻找哪些图纸实现了 <code>BeanFactoryPostProcessor</code> 接口。它发现了你的 <code>Pig</code> 类！</li>
<li><strong>实例化并执行</strong>：Spring 提前把 <code>Pig</code> 实例化，然后调用了它的 <code>postProcessBeanFactory</code> 方法。<strong>此时，你的控制台打印出了 <code>"Hello World"</code>。</strong></li>
</ul>
<p>调用实现了 <code>BeanDefinitionRegistryPostProcessor</code> 和 <code>BeanFactoryPostProcessor</code> 接口 的类</p>
<p><strong>执行顺序</strong>：</p>
<ol>
<li>先执行所有实现了 <code>BeanDefinitionRegistryPostProcessor</code> 接口的类的 <code>postProcessBeanDefinitionRegistry</code> 方法</li>
<li>再执行所有实现了 <code>BeanFactoryPostProcessor</code> 接口的类的 <code>postProcessBeanFactory</code> 方法</li>
</ol>
<p><strong>典型应用</strong>：最核心的 <code>ConfigurationClassPostProcessor</code> 就在这个阶段工作，它会：</p>
<ul>
<li>解析 <code>@Configuration</code>、<code>@ComponentScan</code>、<code>@Import</code>、<code>@PropertySource</code> 等注解</li>
<li>完成包扫描，把扫描到的 Bean 定义注册到容器中</li>
</ul>
<p><strong>这个阶段结束后，所有 Bean 的定义信息彻底锁定</strong></p>
<p>比如假设我们有个 <code>Pig</code> 类</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token annotation punctuation">@Component</span>
<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">Pig</span> <span class="token keyword">implements</span> <span class="token class-name">BeanFactoryPostProcessor</span> <span class="token punctuation">{</span>
  <span class="token annotation punctuation">@Override</span>
  <span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">postProcessBeanFactory</span><span class="token punctuation">(</span><span class="token class-name">ConfigurableListableBeanFactory</span> configurableListableBeanFactory<span class="token punctuation">)</span> <span class="token keyword">throws</span> <span class="token class-name">BeansException</span> <span class="token punctuation">{</span>
      <span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span><span class="token string">"Hello World"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><p>就会被提前实例化</p>
<h4>4. 注册 Bean 后置处理器 (Register BeanPostProcessors)</h4>
<blockquote>
<p>上一步处理的是 BeanFactory 的后置处理器（管图纸的），这一步处理的是 Bean 的后置处理器（管实例对象的）</p>
</blockquote>
<p><strong>注意，这一步只是“注册”，不是执行</strong></p>
<p>Spring 会从 <code>beanDefinitionMap</code> 中找出所有实现了 <code>BeanPostProcessor</code> 接口的类，把它们提前实例化，并按照优先级（PriorityOrdered、Ordered 等）放入一个特定的集合中</p>
<p>这些后置处理器是 AOP 生效的基础，它们将在后续 Bean 的“初始化前后”被调用，用来对普通的 Bean 进行功能增强（比如生成代理对象）</p>
<blockquote>
<p>（比如专门负责处理 <code>@Autowired</code> 的，或者专门负责生成 AOP 动态代理的）</p>
</blockquote>
<h4>5. 初始化基础设施 (Init MessageSource, EventMulticaster, etc.)</h4>
<p>这是 Spring 为了支撑更庞大架构而准备的底层设施：</p>
<ul>
<li><strong>MessageSource</strong>：初始化国际化（i18n）组件。</li>
<li><strong>ApplicationEventMulticaster</strong>：初始化事件广播器。Spring 的事件驱动模型（发布/订阅）就是依赖这个组件运作的。</li>
<li><strong>onRefresh()</strong>：这是一个模板方法，留给子类扩展的。<strong>这里非常关键，如果是 Spring Boot 环境下，它会在这里启动内嵌的 Web 容器（比如 Tomcat 或 Undertow）。</strong></li>
</ul>
<p>如果你是用 Spring Boot 启动（比如使用了 <code>ServletWebServerApplicationContext</code>），Spring Boot 会重写这个方法，在这里利用代码去 <code>new Tomcat()</code> 并启动内嵌的 Web 服务器。</p>
<h4>6. 实例化所有非懒加载单例 (Finish BeanFactory Initialization)</h4>
<blockquote>
<p>这是整个启动流程中最耗时、最复杂的一步，真正的对象全都在这里诞生</p>
</blockquote>
<p>对应的方法是 <code>finishBeanFactoryInitialization(beanFactory)</code></p>
<p>Spring 会遍历 <code>beanDefinitionMap</code> 中所有的图纸，把那些<strong>不是懒加载的单例 Bean</strong> 全部创建出来。底层核心是调用 <code>getBean(beanName)</code> 方法：</p>
<ol>
<li><strong>实例化 (Instantiation)</strong>：利用 Java 反射，调用类的构造函数（通常是无参构造）在堆内存中 <code>new</code> 出一个半成品对象。</li>
<li><strong>属性填充 (PopulateBean)</strong>：也就是依赖注入。遇到 <code>@Autowired</code>，就去容器里找对应的 Bean 塞进去（如果遇到循环依赖，就在这里通过“三级缓存”解决）。</li>
<li><strong>初始化 (InitializeBean)</strong>：
<ul>
<li><strong>执行前置处理</strong>：刚才招募的 <code>BeanPostProcessor</code> 上场，执行 <code>postProcessBeforeInitialization</code>。</li>
<li><strong>执行初始化方法</strong>：执行 <code>@PostConstruct</code> 注解标注的方法，或者 <code>InitializingBean</code> 接口的 <code>afterPropertiesSet</code>。</li>
<li><strong>执行后置处理（AOP 爆发点）</strong>：执行 <code>postProcessAfterInitialization</code>。<strong>如果这个 Bean 需要被事务拦截或切面增强，Spring 就会在这一步悄悄地把返回的对象替换成一个 CGLIB 或 JDK 动态代理对象。</strong></li>
</ul>
</li>
<li><strong>入库</strong>：完全搞定的 Bean（或其代理对象），会被正式放入名为 <code>singletonObjects</code> 的 <code>ConcurrentHashMap</code>（一级缓存）中。</li>
</ol>
<p><strong>三级缓存解决循环依赖</strong>：</p>
<ul>
<li><strong>一级缓存</strong> <code>singletonObjects</code>：完整的单例 Bean</li>
<li><strong>二级缓存</strong> <code>earlySingletonObjects</code>：提前暴露的半成品 Bean（已实例化但未初始化）</li>
<li><strong>三级缓存</strong> <code>singletonFactories</code>：ObjectFactory，用于生成早期引用（处理 AOP 代理）</li>
</ul>
<p>在这个阶段结束时，所有的核心单例对象都已经稳稳地躺在一级缓存中。</p>
<h4>7. 完成刷新与收尾 (Finish Refresh)</h4>
<p>所有组件都就位后，容器就算启动成功了</p>
<p>Spring 会在这一步清理一些启动过程中的临时缓存，初始化生命周期处理器（LifecycleProcessor），并向整个系统广播一个 <code>ContextRefreshedEvent</code> 事件</p>
<p>很多开发者会监听这个事件，用来在 Spring 容器启动完毕后执行一些自定义的业务逻辑（比如加载缓存、启动定时任务等）。</p>
]]></content:encoded>
    </item>
    <item>
      <title>JVM 八股14 - IDEA 运行流程</title>
      <link>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JVM%20-%20%E5%85%AB%E8%82%A1/article14.html</link>
      <guid>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JVM%20-%20%E5%85%AB%E8%82%A1/article14.html</guid>
      <source url="http://ekkosonya.cn/rss.xml">JVM 八股14 - IDEA 运行流程</source>
      <description>IDEA 点击运行后的流程 java 是用来运行 .class 文件的，所以在用 java 之前，需要先用 javac 编译。 IDEA 帮我们自动处理了这个过程。 1. 保存与读取配置 自动保存未保存的文件： IDEA 会把编辑器里所有修改过但还未保存到磁盘的文件进行落盘。这是为什么你在 IDEA 里几乎不需要手动 Ctrl + S 的原因。 读取运...</description>
      <category>code</category>
      <pubDate>Wed, 01 Apr 2026 06:44:41 GMT</pubDate>
      <content:encoded><![CDATA[<h2>IDEA 点击运行后的流程</h2>
<div class="language-text" data-ext="text" data-title="text"><pre class="language-text"><code>.java 文件  --javac--&gt;  .class 文件  --java--&gt;  执行
(源代码)    (编译)      (字节码)      (运行)     (程序)
</code></pre></div><p><code>java</code> 是用来运行 <code>.class</code> 文件的，所以在用 <code>java</code> 之前，需要先用 <code>javac</code> 编译。</p>
<p>IDEA 帮我们自动处理了这个过程。</p>
<h3>1. 保存与读取配置</h3>
<ul>
<li><strong>自动保存未保存的文件：</strong> IDEA 会把编辑器里所有修改过但还未保存到磁盘的文件进行落盘。这是为什么你在 IDEA 里几乎不需要手动 <code>Ctrl + S</code> 的原因。</li>
<li><strong>读取运行配置：</strong> IDEA 会读取你当前选中的 <strong>Run/Debug Configuration</strong>，包括：
<ul>
<li>Main Class（主类）</li>
<li>环境变量</li>
<li>JVM 参数（比如 <code>-Xmx</code>、<code>-Xms</code>）</li>
<li>Program arguments（程序参数）</li>
</ul>
</li>
</ul>
<h3>2. 构建与编译</h3>
<p>在真正运行之前，代码必须被编译成字节码（<code>.class</code> 文件）。</p>
<ul>
<li><strong>判断构建委派：</strong>
<ul>
<li>如果配置了将构建和运行委派给 Maven 或 Gradle（Spring Boot 项目常见），IDEA 会直接调用对应命令（如 <code>mvn clean compile</code> 或 <code>gradlew classes</code>）。</li>
<li>如果没有委派，IDEA 会使用自带的增量编译器（JPS - Java Project System）。</li>
</ul>
</li>
<li><strong>增量编译：</strong> IDEA 不会每次都全量编译整个项目。它通过内部的依赖图分析出哪些 <code>.java</code> 文件被修改过，以及哪些依赖它们的文件需要重新编译，然后仅将这些文件交给 <code>javac</code> 编译成 <code>.class</code> 文件，输出到 <code>target/classes</code> 或 <code>out/production</code> 目录下。</li>
</ul>
<h3>3. 组装启动命令</h3>
<p>编译完成后，IDEA 开始拼装启动 JVM 所需的命令。</p>
<ul>
<li><strong>构建 Classpath：</strong> 这是最庞大的一步。IDEA 会遍历你项目的依赖树，把所有第三方 <code>.jar</code> 包的绝对路径、你自己模块的 <code>classes</code> 输出目录，全部用分号（Windows）或冒号（Linux/macOS）拼接起来。</li>
<li><strong>拼接完整命令：</strong> 结合第 1 步读取到的配置，IDEA 会生成一条类似这样的命令：</li>
</ul>
<div class="language-text" data-ext="text" data-title="text"><pre class="language-text"><code>/path/to/jdk/bin/java.exe -Xmx1024m -javaagent:/idea/lib/idea_rt.jar
-classpath /your/project/out/classes;/m2/repo/spring.jar...
com.your.package.MainClass arg1 arg2
</code></pre></div><blockquote>
<p><code>idea_rt.jar</code> 是 IDEA 注入的一个代理，用于捕获控制台输出和优雅地停止进程。</p>
</blockquote>
<h3>4. 启动进程</h3>
<ul>
<li><strong>Fork 新进程：</strong> IDEA 向操作系统发出请求，根据刚才拼接好的命令，Fork 出一个新的子进程来运行 <code>java</code> 应用程序。</li>
<li><strong>启动 JVM：</strong> 操作系统启动 JVM 实例，分配初始内存（堆、栈、方法区等）。</li>
</ul>
<h3>5. 类加载与执行</h3>
<p>到这一步，接力棒正式交给了 JVM。</p>
<ul>
<li><strong>加载主类：</strong> JVM 的应用程序类加载器（Application ClassLoader）会根据 IDEA 传过来的 Classpath，去磁盘里找到主类的 <code>.class</code> 文件，并将其加载到内存中。</li>
<li><strong>执行 <code>main</code> 方法：</strong> JVM 找到主类中的 <code>public static void main(String[] args)</code> 方法，将 IDEA 传过来的程序参数作为 <code>args</code> 传入，正式开始执行业务逻辑（例如启动 Spring 容器、初始化 Tomcat 等）。</li>
</ul>
<h3>6. 控制台输出绑定</h3>
<ul>
<li><strong>重定向输入输出：</strong> JVM 运行时产生的标准输出（<code>System.out</code>）和标准错误输出（<code>System.err</code>）会被 IDEA 捕获。</li>
<li><strong>渲染日志：</strong> IDEA 将这些日志流渲染到你底部的 Run 窗口中。如果你在控制台输入了文字，IDEA 也会通过标准输入（<code>System.in</code>）传回给运行中的 JVM 进程。</li>
</ul>
]]></content:encoded>
    </item>
    <item>
      <title>JVM 八股15 - javac 和 java 命令</title>
      <link>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JVM%20-%20%E5%85%AB%E8%82%A1/article15.html</link>
      <guid>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JVM%20-%20%E5%85%AB%E8%82%A1/article15.html</guid>
      <source url="http://ekkosonya.cn/rss.xml">JVM 八股15 - javac 和 java 命令</source>
      <description>编译与运行 javac：编译器，将 .java 源文件编译成 .class 字节码 java：运行器，启动 JVM 加载并执行 .class 文件 简单包结构 vs 复杂包结构 简单包结构（默认包） 复杂包结构 目录结构必须对应： javac 编译命令 java 运行命令 sourcepath 和 classpath 区别 示例 默认类路径 当你运行 ...</description>
      <category>code</category>
      <pubDate>Wed, 01 Apr 2026 06:44:41 GMT</pubDate>
      <content:encoded><![CDATA[<h2>编译与运行</h2>
<div class="language-text" data-ext="text" data-title="text"><pre class="language-text"><code>.java 文件  --javac--&gt;  .class 文件  --java--&gt;  执行
(源代码)    (编译)      (字节码)      (运行)     (程序)
</code></pre></div><ul>
<li><strong><code>javac</code></strong>：编译器，将 <code>.java</code> 源文件编译成 <code>.class</code> 字节码</li>
<li><strong><code>java</code></strong>：运行器，启动 JVM 加载并执行 <code>.class</code> 文件</li>
</ul>
]]></content:encoded>
    </item>
    <item>
      <title>JVM 八股16 - 类加载器分析</title>
      <link>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JVM%20-%20%E5%85%AB%E8%82%A1/article16.html</link>
      <guid>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JVM%20-%20%E5%85%AB%E8%82%A1/article16.html</guid>
      <source url="http://ekkosonya.cn/rss.xml">JVM 八股16 - 类加载器分析</source>
      <description>jdk jre | jar jar JAR = Java ARchive（Java 归档文件） 简单说：JAR 就是打包好的 .class 文件 + 资源文件，类似 .zip 格式。 使用 分析 类加载器分析 只有通过反射才能用我们的自定义吗，比如我把.java编译好后，用java命令启动，是用什么类加载器 用 java 命令启动时，默认使用的是什么类...</description>
      <category>code</category>
      <pubDate>Wed, 01 Apr 2026 06:44:41 GMT</pubDate>
      <content:encoded><![CDATA[<h2>jdk jre | jar</h2>
<div class="language-text" data-ext="text" data-title="text"><pre class="language-text"><code>┌─────────────────────────────────────────┐
│              JDK                         │
│  ┌────────────────────────────────────┐ │
│  │              JRE                    │ │
│  │  ┌────────────────────────────────┐│ │
│  │  │            JVM                 ││ │
│  │  │  (Java Virtual Machine)        ││ │
│  │  │   - 加载 .class                ││ │
│  │  │   - 执行字节码                 ││ │
│  │  └────────────────────────────────┘│ │
│  │   + 核心类库 (rt.jar)              │ │
│  │   - java.lang.*                    │ │
│  │   - java.util.*                    │ │
│  └────────────────────────────────────┘ │
│   + 开发工具                             │
│   - javac (编译器)                      │
│   - java (运行器)                       │
│   - javadoc (文档生成)                  │
│   - jdb (调试器)                        │
│   - jmap, jstack (性能分析)             │
└─────────────────────────────────────────┘
</code></pre></div><h3>jar</h3>
<p>JAR = Java ARchive（Java 归档文件）</p>
<p>简单说：JAR 就是打包好的 .class 文件 + 资源文件，类似 .zip 格式。</p>
<div class="language-text" data-ext="text" data-title="text"><pre class="language-text"><code>my-library.jar
├── META-INF/
│   └── MANIFEST.MF      # 清单文件（描述 jar 信息）
├── com/
│   └── example/
│       ├── MyClass.class
│       └── MyService.class
├── config/
│   └── application.properties
└── ...其他资源文件
</code></pre></div><p>使用</p>
<div class="language-text" data-ext="text" data-title="text"><pre class="language-text"><code># 编译时指定 classpath
javac -cp "lib/mysql-connector.jar" MyApp.java

# 运行时指定 classpath
java -cp "myapp.jar;lib/mysql-connector.jar" com.example.Main
</code></pre></div><p>分析</p>
<div class="language-text" data-ext="text" data-title="text"><pre class="language-text"><code>主程序
  ↓
运行时扫描 plugins/ 目录
  ↓
发现 plugin-a.jar, plugin-b.jar
  ↓
用 URLClassLoader 加载 jar 中的实现类
  ↓
通过接口调用插件功能
</code></pre></div><h2>类加载器分析</h2>
<blockquote>
<p>只有通过反射才能用我们的自定义吗，比如我把.java编译好后，用java命令启动，是用什么类加载器</p>
</blockquote>
<ul>
<li>用 java 命令启动时，默认使用的是什么类加载器？ 默认使用的是 <code>AppClassLoader</code>（应用程序类加载器，也叫系统类加载器）</li>
<li>只有通过反射才能用自定义 ClassLoader 吗？ 不是的。 虽然首次实例化通常需要反射，但在实际开发中，我们通常会结合<strong>接口和隐式加载机制</strong>来完美避开后续繁琐的反射调用</li>
</ul>
<h3>用 java 命令启动时的类加载流程</h3>
<p>当你编译好代码，在终端输入 <code>java com.example.Main</code> 并回车时，JVM 启动并初始化类加载器体系，流程如下：</p>
<ol>
<li><strong>Bootstrap ClassLoader（引导类加载器）：</strong> 最先启动，由 C/C++ 实现，负责加载 JDK 最核心的类库（如 <code>java.lang.*</code>、<code>rt.jar</code> 等）。</li>
<li><strong>ExtClassLoader（扩展类加载器）：</strong> 接着启动，负责加载 Java 的扩展库（如 <code>jre/lib/ext</code> 目录下的类）。<em>注：Java 9 模块化之后改名为 PlatformClassLoader。</em></li>
<li><strong>AppClassLoader（应用程序类加载器）：</strong> 最后启动。<strong>它就是你用 <code>java</code> 命令启动主类的默认加载器。</strong> 它负责去你配置的 <code>classpath</code>（或默认的当前目录）下找到 <code>com.example.Main.class</code> 并加载执行。</li>
</ol>
<p><strong>总结：</strong> 只要是你放在项目 Classpath 里的代码，或者通过 <code>java -cp</code> 运行的代码，入口类都是被 AppClassLoader 加载的。</p>
<h3>只有通过反射才能用自定义加载器吗</h3>
<p>如果你的 SecretClass 只存在于外部目录，而在你写代码的当前工程里不存在，编译器（javac）在编译阶段是找不到这个类的。如果你直接写 SecretClass obj = new SecretClass();，代码根本编译不通过。</p>
<p>为了绕过编译期检查，我们才使用了反射。</p>
<p>但实际工程中（如 Tomcat、热部署框架、插件系统），频繁使用反射性能很差且代码丑陋</p>
<p>常见的优雅替代方案有以下两种：</p>
<h4>面向接口编程（最常用的最佳实践）</h4>
<p>接口由 AppClassLoader 加载，实现类由自定义 ClassLoader 加载</p>
<h5>定义一个接口</h5>
<p>放在主工程中（编译期可见）</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">public</span> <span class="token keyword">interface</span> <span class="token class-name">MyPlugin</span> <span class="token punctuation">{</span>
  <span class="token keyword">void</span> <span class="token function">start</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre></div><h5>编写实现类</h5>
<p>并把它编译后的 <code>.class</code> 放到外部目录（让自定义类加载器去加载）：</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">MyPluginImpl</span> <span class="token keyword">implements</span> <span class="token class-name">MyPlugin</span> <span class="token punctuation">{</span>
  <span class="token annotation punctuation">@Override</span>
  <span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">start</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
      <span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span><span class="token string">"插件启动了！"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><h5>在主程序中调用</h5>
<p>只需要在创建实例的那一瞬间使用反射（或 <code>newInstance</code>），然后将其强转为接口类型。后续所有的调用都像普通代码一样：</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token comment">// 1. 加载外部的实现类</span>
<span class="token class-name">Class</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token operator">?</span><span class="token punctuation">&gt;</span></span> pluginClass <span class="token operator">=</span> myCustomLoader<span class="token punctuation">.</span><span class="token function">loadClass</span><span class="token punctuation">(</span><span class="token string">"com.example.MyPluginImpl"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token comment">// 2. 实例化并强转为接口类型（只需这里用一次反射/实例化）</span>
<span class="token class-name">MyPlugin</span> plugin <span class="token operator">=</span> <span class="token punctuation">(</span><span class="token class-name">MyPlugin</span><span class="token punctuation">)</span> pluginClass<span class="token punctuation">.</span><span class="token function">getDeclaredConstructor</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">newInstance</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token comment">// 3. 正常调用方法，无需反射！</span>
plugin<span class="token punctuation">.</span><span class="token function">start</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre></div><h4>利用“隐式加载机制”</h4>
<p>JVM 类加载有一个重要特性：<strong>如果类 A 是由某个类加载器加载的，那么类 A 内部引用的所有其他类（包括 new 出来的类），默认都会由加载类 A 的这个类加载器去加载</strong>（前提是这些类还没被加载过）。</p>
<p>这意味着，你只需要用自定义 ClassLoader（结合反射或接口）把<strong>入口类</strong>加载进来，这个入口类内部如果写了 <code>User user = new User();</code>，JVM 会自动用你的自定义 ClassLoader 去加载 <code>User</code>，全程不需要你写一行反射代码。</p>
]]></content:encoded>
    </item>
    <item>
      <title>JVM 八股17 - JVM 调优</title>
      <link>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JVM%20-%20%E5%85%AB%E8%82%A1/article17.html</link>
      <guid>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JVM%20-%20%E5%85%AB%E8%82%A1/article17.html</guid>
      <source url="http://ekkosonya.cn/rss.xml">JVM 八股17 - JVM 调优</source>
      <description>JVM 调优 性能检测工具 系统层面 (可以监控系统整体的资源使用情况，比如说内存、CPU、IO 使用情况、网络使用情况) top iostat netstat vmstat JDK 自带的命令行工具层面 jps jstat jinfo jmap jhat jstack jcmd 可以查看 JVM 运行时信息、内存使用情况、堆栈信息等 jmap 使用 ...</description>
      <category>code</category>
      <pubDate>Wed, 01 Apr 2026 06:44:41 GMT</pubDate>
      <content:encoded><![CDATA[<h2>JVM 调优</h2>
<h3>性能检测工具</h3>
<ul>
<li>系统层面 (可以监控系统整体的资源使用情况，比如说内存、CPU、IO 使用情况、网络使用情况)
<ul>
<li>top</li>
<li>iostat</li>
<li>netstat</li>
<li>vmstat</li>
</ul>
</li>
<li>JDK 自带的命令行工具层面
<ul>
<li>jps</li>
<li>jstat</li>
<li>jinfo</li>
<li>jmap</li>
<li>jhat</li>
<li>jstack</li>
<li>jcmd</li>
<li>可以查看 JVM 运行时信息、内存使用情况、堆栈信息等</li>
</ul>
</li>
</ul>
<h4>jmap 使用</h4>
<ul>
<li>一般会使用 <code>jmap -heap &lt;pid&gt;</code> 查看堆内存摘要，包括新生代、老年代、元空间等</li>
<li>或者使用 <code>jmap -histo &lt;pid&gt;</code> 查看对象分布</li>
</ul>
<h3>可视化的性能监控工具</h3>
<ul>
<li>JConsole：JDK 自带的监控工具，可以用来监视 Java 应用程序的运行状态，包括内存使用、线程状态、类加载、GC 等</li>
<li>VisualVM：一个基于 NetBeans 的可视化工具，在很长一段时间内，VisualVM 都是 Oracle 官方主推的故障处理工具。集成了多个 JDK 命令行工具的功能，非常友好</li>
<li>Java Mission Control：JMC 最初是 JRockit VM 中的诊断工具，但在 Oracle JDK7 Update 40 以后，就绑定到了 HotSpot VM 中。不过后来又被 Oracle 开源出来作为了一个单独的产品</li>
</ul>
<h3>JVM 的常见参数配置</h3>
<p>一个是堆的大小设置，然后是垃圾收集器的选择和时间，加上日志输出等</p>
<p>主要有 -Xms 设置初始堆大小，-Xmx 设置最大堆大小，-XX:+UseG1GC 使用 G1 垃圾收集器，-XX:MaxGCPauseMillis=n 设置最大垃圾回收停顿时间，-XX:+PrintGCDetails 输出 GC 详细日志等</p>
<h4>配置堆内存大小的参数</h4>
<ul>
<li><code>-Xms</code>：初始堆大小</li>
<li><code>-Xmx</code>：最大堆大小</li>
<li><code>-XX:NewSize=n</code>：设置年轻代大小</li>
<li><code>-XX:NewRatio=n</code>：设置年轻代和年老代的比值。如：n 为 3 表示年轻代和年老代比值为 1：3，年轻代占总和的 1/4</li>
<li><code>-XX:SurvivorRatio=n</code>：年轻代中 Eden 区与两个 Survivor 区的比值。如 n=3 表示 Eden 占 3 Survivor 占 2，一个 Survivor</li>
</ul>
<h4>配置 GC 收集器的参数</h4>
<ul>
<li><code>-XX:+UseSerialGC</code>：设置串行收集器</li>
<li><code>-XX:+UseParallelGC</code>：设置并行收集器</li>
<li><code>-XX:+UseParalledlOldGC</code>：设置并行老年代收集器</li>
<li><code>-XX:+UseConcMarkSweepGC</code>：设置并发收集器</li>
</ul>
<h4>配置并行收集的参数</h4>
<ul>
<li><code>-XX:MaxGCPauseMillis=n</code>：设置最大垃圾回收停顿时间</li>
<li><code>-XX:GCTimeRatio=n</code>：设置垃圾回收时间占程序运行时间的比例</li>
<li><code>-XX:+CMSIncrementalMode</code>：设置增量模式，适合单 CPU 环境</li>
<li><code>-XX:ParallelGCThreads=n</code>：设置并行收集器的线程数</li>
</ul>
<h4>打印 GC 回收的过程日志信息的参数</h4>
<ul>
<li><code>-XX:+PrintGC</code>：输出 GC 日志</li>
<li><code>-XX:+PrintGCDetails</code>：输出 GC 详细日志</li>
<li><code>-XX:+PrintGCTimeStamps</code>：输出 GC 的时间戳（以基准时间的形式）</li>
<li><code>-Xloggc:filename</code>：日志文件的输出路径</li>
</ul>
<h3>CPU 占用过高怎么排查</h3>
<p>使用 top 命令查看 CPU 占用情况，找到占用 CPU 较高的进程 ID</p>
<p>接着，使用 jstack 命令查看对应进程的线程堆栈信息</p>
<p>然后再使用 top 命令查看进程中线程的占用情况，找到占用 CPU 较高的线程 ID</p>
<p>接着在 jstack 的输出中搜索这个十六进制的线程 ID，找到对应的堆栈信息。最后，根据堆栈信息定位到具体的业务方法，查看是否有死循环、频繁的垃圾回收、资源竞争导致的上下文频繁切换等问题</p>
<blockquote>
<p>先找是哪个进程占用 CPU，然后进一步看这个进程有哪些线程，再看看到底是哪一个线程占用率高，就是分析该线程的堆栈信息</p>
</blockquote>
<h3>内存飙高问题</h3>
<p>内存飚高一般是因为创建了大量的 Java 对象导致的，如果持续飙高则说明垃圾回收跟不上对象创建的速度，或者内存泄漏导致对象无法回收</p>
<p>第一，先观察垃圾回收的情况，可以通过 jstat -gc PID 1000 查看 GC 次数和时间。或者使用 jmap -histo PID | head -20 查看堆内存占用空间最大的前 20 个对象类型。</p>
<p>第二步，通过 jmap 命令 dump 出堆内存信息。</p>
<p>第三步，使用可视化工具分析 dump 文件，比如说 VisualVM，找到占用内存高的对象，再找到创建该对象的业务代码位置，从代码和业务场景中定位具体问题。</p>
<h3>频繁 minor gc 怎么办</h3>
<p>频繁的 Minor GC 通常意味着新生代中的对象频繁地被垃圾回收，可能是因为<strong>新生代空间设置的过小</strong>，或者是<strong>因为程序中存在大量的短生命周期对象</strong>（如临时变量）</p>
<p>可以使用 GC 日志进行分析，查看 GC 的频率和耗时，找到频繁 GC 的原因</p>
<div class="language-bash" data-ext="sh" data-title="sh"><pre class="language-bash"><code><span class="token parameter variable">-XX:+PrintGCDetails</span> <span class="token parameter variable">-Xloggc:gc.log</span>
</code></pre></div><p>如果是因为新生代空间不足，可以通过 -Xmn 增加新生代的大小，减缓新生代的填满速度</p>
<p>如果对象需要长期存活，但频繁从 Survivor 区晋升到老年代，可以通过 -XX:SurvivorRatio 参数调整 Eden 和 Survivor 的比例。默认比例是 8:1，表示 8 个空间用于 Eden，1 个空间用于 Survivor 区</p>
<h3>频繁 Full GC 怎么办</h3>
<p>频繁的 Full GC 通常意味着老年代中的对象频繁地被垃圾回收，可能是因为老年代空间设置的过小，或者是因为程序中存在大量的长生命周期对象。</p>
<ul>
<li>假如是因为大对象直接分配到老年代导致的 Full GC 频繁，可以通过 -XX:PretenureSizeThreshold 参数设置大对象直接进入老年代的阈值。
<ul>
<li>或者将大对象拆分成小对象，减少大对象的创建。比如说分页。</li>
</ul>
</li>
<li>假如是因为内存泄漏导致的频繁 Full GC，可以通过分析堆内存 dump 文件找到内存泄漏的对象，再找到内存泄漏的代码位置。</li>
<li>假如是因为长生命周期的对象进入到了老年代，要及时释放资源，比如说 ThreadLocal、数据库连接、IO 资源等。</li>
<li>假如是因为 GC 参数配置不合理导致的频繁 Full GC，可以通过调整 GC 参数来优化 GC 行为。或者直接更换更适合的 GC 收集器，如 G1、ZGC 等。</li>
</ul>
]]></content:encoded>
    </item>
  </channel>
</rss>