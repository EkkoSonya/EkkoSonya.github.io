<?xml version="1.0" encoding="utf-8"?><?xml-stylesheet type="text/xsl" href="http://ekkosonya.cn/rss.xsl"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:dc="http://purl.org/dc/elements/1.1/">
  <channel>
    <atom:link href="http://ekkosonya.cn/rss.xml" rel="self" type="application/rss+xml"/>
    <title>EkkoSonya&amp;apos;s Blog</title>
    <link>http://ekkosonya.cn/</link>
    <description>笔记记录</description>
    <language>zh-CN</language>
    <pubDate>Sun, 29 Mar 2026 02:51:02 GMT</pubDate>
    <lastBuildDate>Sun, 29 Mar 2026 02:51:02 GMT</lastBuildDate>
    <generator>@vuepress/plugin-feed</generator>
    <docs>https://validator.w3.org/feed/docs/rss2.html</docs>
    <category>code</category>
    <item>
      <title>JVM 八股1 - 整体架构</title>
      <link>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JVM%20-%20%E5%85%AB%E8%82%A1/article1.html</link>
      <guid>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JVM%20-%20%E5%85%AB%E8%82%A1/article1.html</guid>
      <source url="http://ekkosonya.cn/rss.xml">JVM 八股1 - 整体架构</source>
      <description>JVM 学习 JVM 可以帮助我们开发者更好地优化程序性能、避免内存问题 了解 JVM 的内存模型和垃圾回收机制，可以帮助我们更合理地配置内存、减少 GC 停顿 掌握 JVM 的类加载机制可以帮助我们排查类加载冲突或异常 JVM 还提供了很多调试和监控工具，可以帮助我们分析内存和线程的使用情况，从而解决内存溢出内存泄露等问题 所以主要内容：内存模型、垃...</description>
      <category>code</category>
      <pubDate>Sun, 29 Mar 2026 02:48:57 GMT</pubDate>
      <content:encoded><![CDATA[<h2>JVM</h2>
<p>学习 JVM 可以帮助我们开发者更好地优化程序性能、避免内存问题</p>
<ul>
<li>了解 JVM 的内存模型和垃圾回收机制，可以帮助我们更合理地配置内存、减少 GC 停顿</li>
<li>掌握 JVM 的类加载机制可以帮助我们排查类加载冲突或异常</li>
<li>JVM 还提供了很多调试和监控工具，可以帮助我们分析内存和线程的使用情况，从而解决内存溢出内存泄露等问题</li>
</ul>
<blockquote>
<p>所以主要内容：内存模型、垃圾回收机制、类加载机制、JVM 调试/监控 工具</p>
</blockquote>
<h3>什么是 JVM</h3>
<p>JVM，也就是 Java 虚拟机，它是 Java 实现跨平台的基石</p>
<p>程序运行之前，需要先通过编译器将 Java 源代码文件编译成 Java 字节码文件；</p>
<p>程序运行时，JVM 会对字节码文件进行逐行解释，翻译成机器码指令，并交给对应的操作系统去执行</p>
<figure><figcaption>alt text</figcaption></figure>
<p>这样就实现了 Java 一次编译，处处运行的特性</p>
<h4>执行 Hello world 的流程</h4>
<p>当我们写好一个 HelloWorld 程序，编译成 .class 文件后，执行 <code>java HelloWorld</code> 这条命令时</p>
<p>操作系统会创建一个 JVM 进程，JVM 进程启动起来后，会先初始化运行环境，包括<strong>创建类加载器</strong>、<strong>初始化内存空间</strong>、<strong>启动垃圾回收线程</strong>等等</p>
<p>接下来，JVM 的类加载器会去找 <code>HelloWorld.class</code> 这个文件，读取它的字节码内容</p>
<p>JVM 的执行引擎会将这些字节码翻译成机器码，有两种方式</p>
<ul>
<li>第一种是解释执行
<ul>
<li>JVM 的解释器会逐条读取字节码指令，然后把它翻译成机器码执行</li>
<li>这个过程是动态的，每次执行都要翻译一遍。所以解释执行的速度相对较慢</li>
</ul>
</li>
<li>第二种是即时编译
<ul>
<li>JVM 里有一个 JIT 编译器，他发现某段代码被频繁执行（比如循环里的代码或者热点方法）时，JIT 编译器就会把这段字节码<strong>直接编译成本地机器码</strong>并缓存到 codeCache 中</li>
<li>下次执行的时候不用再一行一行的解释，而是直接执行缓存后的机器码，执行效率会大幅提高</li>
</ul>
</li>
</ul>
<p>至于执行的时机</p>
<p>解释器的执行是立即的，当 JVM 启动后，就开始解释执行字节码了</p>
<p>JIT 编译器的执行时机是延后的，它<strong>需要先监控哪些代码是热点代码</strong>（通常需要执行数千次或者数万次），然后才会启动编译</p>
<blockquote>
<p>操作系统层面，JVM 进程是一个执行单元，由操作系统调度执行</p>
</blockquote>
<h4>JVM 特性</h4>
<ul>
<li>JVM 可以自动管理内存，通过垃圾回收器回收不再使用的对象并释放内存空间</li>
<li>JVM 包含一个即时编译器 JIT，它可以在运行时将热点代码缓存到 codeCache 中，下次执行的时候不用再一行一行的解释，而是直接执行缓存后的机器码，执行效率会大幅提高</li>
<li>任何可以通过 Java 编译的语言，比如说 Groovy、Kotlin、Scala 等，都可以在 JVM 上运行</li>
</ul>
<h4>JAVA 是解释性语言还是编译性语言</h4>
<p>| <strong>特性</strong> | <strong>编译型语言（如C/C++）</strong> | <strong>解释型语言（如Python、JavaScript）</strong> |
|</p>
]]></content:encoded>
    </item>
    <item>
      <title>JVM 八股2 - 内存管理1 (内存区介绍)</title>
      <link>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JVM%20-%20%E5%85%AB%E8%82%A1/article2.html</link>
      <guid>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JVM%20-%20%E5%85%AB%E8%82%A1/article2.html</guid>
      <source url="http://ekkosonya.cn/rss.xml">JVM 八股2 - 内存管理1 (内存区介绍)</source>
      <description>JVM 启动后，大致可以分为三个功能区：类加载器、运行时数据区以及执行引擎 内存管理 内存区其实是功能区中&amp;quot;运行时数据区&amp;quot;的具体细分 JVM 运行后，按照 Java 虚拟机规范，JVM 的内存区域可以细分为程序计数器、虚拟机栈、本地方法栈、堆和方法区 alt textalt text 其中方法区和堆是线程共享的，虚拟机栈、本地方法栈和程序计数器是线程私...</description>
      <category>code</category>
      <pubDate>Sun, 29 Mar 2026 02:48:57 GMT</pubDate>
      <content:encoded><![CDATA[<p>JVM 启动后，大致可以分为三个功能区：类加载器、运行时数据区以及执行引擎</p>
<h2>内存管理</h2>
<blockquote>
<p>内存区其实是功能区中"运行时数据区"的具体细分</p>
</blockquote>
<div class="language-plain" data-ext="plain" data-title="plain"><pre class="language-plain"><code>JVM（功能区视角）
│
├── 类加载器
│
├── 运行时数据区  ←──── 这部分就是"内存区"
│   ├── 程序计数器
│   ├── 虚拟机栈
│   ├── 本地方法栈
│   ├── 堆
│   └── 方法区
│
└── 执行引擎
    ├── JIT 编译器
    └── 垃圾回收器
</code></pre></div><p>JVM 运行后，按照 Java 虚拟机规范，JVM 的内存区域可以细分为<code>程序计数器</code>、<code>虚拟机栈</code>、<code>本地方法栈</code>、<code>堆</code>和<code>方法区</code></p>
<figure><figcaption>alt text</figcaption></figure>
<p>其中方法区和堆是线程共享的，虚拟机栈、本地方法栈和程序计数器是线程私有的</p>
<ul>
<li>方法区和堆：线程共享，随 JVM 创建和销毁</li>
<li>虚拟机栈、本地方法栈和程序计数器：线程隔离，随线程启动创建，结束后销毁</li>
</ul>
<blockquote>
<p>JVM 本质是一个 C/C++ 程序，它向操作系统申请内存，然后自己管理这些内存，划分成不同区域给 Java 程序用</p>
</blockquote>
<h3>程序计数器</h3>
<p>程序计数器也被称为 PC 寄存器，是一块较小的内存空间</p>
<p>它可以看作是当前线程所执行的字节码行号指示器</p>
<h3>虚拟机栈</h3>
<p>Java 虚拟机栈的生命周期与线程相同</p>
<p>当线程执行一个方法时，会创建一个对应的栈帧</p>
<p>用于存储局部变量表、操作数栈、动态链接、方法出口等信息，然后栈帧会被压入虚拟机栈中</p>
<blockquote>
<p>栈帧里会保存一个方法执行时需要的核心信息：变量放哪、计算怎么做、调用其他方法时怎么定位、执行完后回到哪里</p>
</blockquote>
<p>当方法执行完毕后，栈帧会从虚拟机栈中移除</p>
<figure><figcaption>alt text</figcaption></figure>
<h4>动态链接</h4>
<p>指的是 Java 方法调用时，怎么从符号引用定位到实际方法，保存了对运行时常量池中方法符号引用的关联，方便在方法调用时把“符号引用”解析成“实际要执行的方法入口”</p>
<h4>操作数栈</h4>
<p>是方法执行时做计算的临时工作区</p>
<p>比如：</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">int</span> c <span class="token operator">=</span> a <span class="token operator">+</span> b<span class="token punctuation">;</span>
</code></pre></div><p>JVM 不是直接写成“把 a 和 b 相加赋值给 c”，而是类似这样执行</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code>iload_1   <span class="token comment">// 把 a 压入操作数栈</span>
iload_2   <span class="token comment">// 把 b 压入操作数栈</span>
iadd      <span class="token comment">// 弹出栈顶两个数，相加，再把结果压回栈</span>
istore_3  <span class="token comment">// 把结果存到局部变量表的 c</span>
</code></pre></div><h4>栈帧</h4>
<p>栈帧是虚拟机栈中的基本单元，每个方法调用对应一个栈帧：</p>
<div class="language-plain" data-ext="plain" data-title="plain"><pre class="language-plain"><code>虚拟机栈（线程私有）
│
├── 栈帧3（方法C正在执行）  ←── 栈顶
│   ├── 局部变量表
│   ├── 操作数栈
│   ├── 动态链接
│   └── 方法返回地址
│
├── 栈帧2（方法B暂停中）
│   ├── 局部变量表
│   ├── 操作数栈
│   ├── 动态链接
│   └── 方法返回地址
│
└── 栈帧1（方法A暂停中）  ←── 栈底
    ├── 局部变量表
    ├── 操作数栈
    ├── 动态链接
    └── 方法返回地址
</code></pre></div><h4>一个什么都没有的空方法，空的参数都没有，那局部变量表里有没有变量</h4>
<p>对于静态方法，由于不需要访问实例对象 this，因此在局部变量表中不会有任何变量。</p>
<p>对于非静态方法，即使是一个完全空的方法，局部变量表中也会有一个用于存储 this 引用的变量。this 引用指向当前实例对象，在方法调用时被隐式传入</p>
<h3>本地方法栈</h3>
<p>本地方法栈与虚拟机栈相似</p>
<p>区别在于虚拟机栈是为 JVM 执行 Java 编写的方法服务的</p>
<p>而本地方法栈是为 Java 调用本地 native 方法服务的，通常由 C/C++ 编写</p>
<p>在本地方法栈中，主要存放了 native 方法的局部变量、动态链接和方法出口等信息</p>
<p>当一个 Java 程序调用一个 native 方法时，JVM 会切换到本地方法栈来执行这个方法</p>
<h4>本地方法栈的运行场景</h4>
<p>当 Java 应用需要与操作系统底层或硬件交互时，通常会用到本地方法栈。</p>
<p>比如调用操作系统的特定功能，如内存管理、文件操作、系统时间、系统调用等。</p>
<p>比如获取系统时间的 <code>System.currentTimeMillis()</code> 方法就是调用本地方法，来获取操作系统当前时间的</p>
<p>再比如 JVM 自身的一些底层功能也需要通过本地方法来实现。</p>
<p>像 Object 类中的 <code>hashCode()</code> 方法、<code>clone()</code> 方法等</p>
<h3>java 堆</h3>
<p>堆是 JVM 中最大的一块内存区域，被所有线程共享，在 JVM 启动时创建，主要用来存储 new 出来的对象</p>
<blockquote>
<p>存放 new 出来的对象</p>
<p>“堆”至少有两个常见概念：一个是数据结构里的堆（通常是完全二叉树），一个是内存管理里的堆（动态分配内存的区域）；JVM 说的堆指的是后者</p>
<p>与规规矩矩、先进后出的“栈（Stack）”不同，堆内存是一大片相对杂乱的空间。你可以随时在里面申请一块内存，用完后再释放。它的分配和释放顺序是随意的</p>
</blockquote>
<p>Java 中“几乎”所有的对象都会在堆中分配，堆也是垃圾收集器管理的目标区域</p>
<figure><figcaption>alt text</figcaption></figure>
<p>从内存回收的角度来看，由于垃圾收集器大部分都是基于分代收集理论设计的，所以堆又被细分为</p>
<ul>
<li>新生代
<ul>
<li>Eden空间</li>
<li>From Survivor空间</li>
<li>To Survivor空间</li>
</ul>
</li>
<li>老年代</li>
</ul>
<figure><figcaption>alt text</figcaption></figure>
<blockquote>
<p>从 JDK 7 开始，JVM 默认开启了逃逸分析，意味着如果某些方法中的对象引用没有被返回或者没有在方法体外使用，也就是未逃逸出去，那么对象可以直接在栈上分配内存</p>
</blockquote>
<h4>堆与栈的区别</h4>
<p>堆属于线程共享的内存区域，几乎所有 new 出来的对象都会堆上分配，生命周期不由单个方法调用所决定，可以在方法调用结束后继续存在，直到不再被任何变量引用，最后被垃圾收集器回收。</p>
<p>栈属于线程私有的内存区域，主要存储局部变量、方法参数、对象引用等，通常随着方法调用的结束而自动释放，不需要垃圾收集器处理。</p>
<h3>方法区</h3>
<p>方法区并不真实存在，属于 Java 虚拟机规范中的一个逻辑概念</p>
<p>用于存储已被 JVM 加载的类信息、常量、静态变量、即时编译器编译后的代码缓存等</p>
<p>在 HotSpot 虚拟机中</p>
<p>方法区的实现称为永久代 PermGen</p>
<p>但在 Java 8 及之后的版本中，已经被元空间 Metaspace 所替代</p>
<h3>变量的存放位置</h3>
<p>对于局部变量，它存储在当前方法栈帧中的局部变量表中。</p>
<p>当方法执行完毕，栈帧被回收，局部变量也会被释放</p>
<p>对于静态变量来说，它存储在 Java 虚拟机规范中的方法区中，在 Java 7 中是永久带，在 Java8 及以后 是元空间</p>
<h3>JVM 如何执行一个 Java 方法</h3>
<p>从 java Xxx 命令开始，操作系统会创建一个 JVM 进程；</p>
<p>JVM 进程中的线程执行 Java 方法时，每次方法调用都会在对应线程的虚拟机栈中创建一个栈帧，方法的参数、局部变量和计算过程都围绕这个栈帧展开</p>
<h4>方法执行和虚拟机栈的关系</h4>
<ul>
<li>每个线程都有自己的虚拟机栈</li>
<li>当线程调用一个方法时，会为该方法创建一个栈帧并压栈</li>
<li>方法执行结束后，栈帧出栈</li>
</ul>
<h4>栈帧里有什么</h4>
<ul>
<li>局部变量表</li>
<li>操作数栈</li>
<li>动态链接</li>
<li>方法返回地址</li>
</ul>
<h4>方法中的变量怎么访问</h4>
<ul>
<li>局部变量和参数存放在局部变量表中</li>
<li>JVM 不是按变量名访问，而是按 slot 槽位访问</li>
<li>槽位数量对某个方法来说在编译后就基本确定了，且可能发生复用</li>
</ul>
<h4>一次方法调用的本质</h4>
<ul>
<li>创建栈帧</li>
<li>参数入局部变量表</li>
<li>字节码指令操作局部变量表和操作数栈</li>
<li>执行结束后返回结果并弹出栈帧</li>
</ul>
<h4>思考</h4>
<p>方法执行时，JVM 总是操作当前线程虚拟机栈的栈顶栈帧；局部变量通过当前栈帧中的局部变量表按 slot 下标访问，而不是按变量名查找。</p>
<p>JVM 看的是 字节码</p>
<p>所以“找局部变量”这件事，本质是：</p>
<ul>
<li>当前线程拿到栈顶栈帧</li>
<li>JVM 执行当前字节码</li>
<li>字节码里写着访问哪个 slot</li>
<li>直接去局部变量表对应位置拿</li>
</ul>
<p>“当前方法”怎么知道自己对应哪个栈帧</p>
<p>因为线程的虚拟机栈本来就是：</p>
<ul>
<li>谁在执行，谁就在栈顶</li>
<li>当前正在执行的方法，对应的就是栈顶栈帧</li>
</ul>
<p>当前方法不需要“满栈找自己的栈帧”，因为它本来就在栈顶，JVM 执行时天然就拿的是当前栈顶帧。</p>
<h5>例子</h5>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">public</span> <span class="token keyword">int</span> <span class="token function">add</span><span class="token punctuation">(</span><span class="token keyword">int</span> a<span class="token punctuation">,</span> <span class="token keyword">int</span> b<span class="token punctuation">)</span> <span class="token punctuation">{</span>
  <span class="token keyword">int</span> c <span class="token operator">=</span> a <span class="token operator">+</span> b<span class="token punctuation">;</span>
  <span class="token keyword">return</span> c<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre></div><p>这个方法执行时，栈帧里可能是这样：</p>
<div class="language-plain" data-ext="plain" data-title="plain"><pre class="language-plain"><code>当前栈帧
├── 局部变量表
│   ├── slot0 = this
│   ├── slot1 = a
│   ├── slot2 = b
│   └── slot3 = c
├── 操作数栈
├── 动态链接
└── 返回地址
</code></pre></div><p>然后字节码大概会做这种事：</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code>iload_1    <span class="token comment">// 取 slot1，也就是 a</span>
iload_2    <span class="token comment">// 取 slot2，也就是 b</span>
iadd       <span class="token comment">// 相加</span>
istore_3   <span class="token comment">// 把结果放到 slot3，也就是 c</span>
iload_3    <span class="token comment">// 取 c</span>
ireturn    <span class="token comment">// 返回</span>
</code></pre></div>]]></content:encoded>
    </item>
    <item>
      <title>JVM 八股3 - 内存管理2 (native 介绍)</title>
      <link>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JVM%20-%20%E5%85%AB%E8%82%A1/article3.html</link>
      <guid>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JVM%20-%20%E5%85%AB%E8%82%A1/article3.html</guid>
      <source url="http://ekkosonya.cn/rss.xml">JVM 八股3 - 内存管理2 (native 介绍)</source>
      <description>JVM3 当 Java 应用需要与操作系统底层或硬件交互时，通常会用到本地方法栈。 native 方法 native 方法是在 Java 中通过 native 关键字声明的，用于调用非 Java 语言，如 C/C++ 编写的代码。Java 可以通过 JNI，也就是 Java Native Interface 与底层系统、硬件设备、或者本地库进行交互 J...</description>
      <category>code</category>
      <pubDate>Sun, 29 Mar 2026 02:48:57 GMT</pubDate>
      <content:encoded><![CDATA[<h2>JVM3</h2>
<p>当 Java 应用需要与操作系统底层或硬件交互时，通常会用到本地方法栈。</p>
<h3>native 方法</h3>
<p>native 方法是在 Java 中通过 native 关键字声明的，用于调用非 Java 语言，如 C/C++ 编写的代码。Java 可以通过 JNI，也就是 Java Native Interface 与底层系统、硬件设备、或者本地库进行交互</p>
<p>Java 的 native 方法本身没有 Java 代码实现，而是交给本地代码实现</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">public</span> <span class="token keyword">native</span> <span class="token keyword">void</span> <span class="token function">test</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre></div><p>这个方法真正的逻辑可能写在 C/C++ 里，然后编译成一个 .dll。</p>
<p>Java 运行时通过：</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token class-name">System</span><span class="token punctuation">.</span><span class="token function">loadLibrary</span><span class="token punctuation">(</span><span class="token string">"xxxx"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre></div><p>把这个动态库加载进来，再去调用里面对应的本地函数</p>
<p>你可以把它想成：</p>
<ul>
<li>Java 代码：负责声明“我要调用这个方法”</li>
<li>.dll：真正存放底层实现</li>
<li>JVM：负责把 .dll 加载进来并完成调用</li>
</ul>
<p>不同系统名字不一样：</p>
<ul>
<li>Windows：.dll</li>
<li>Linux：.so</li>
<li>macOS：.dylib</li>
</ul>
<h4>JNI (Java Native Interface)</h4>
<p>一般情况下，我们完全可以使用 Java 语言编写程序，但某些情况下，Java 可能满足不了需求，或者不能更好的满足需求，比如：</p>
<ul>
<li>标准的 Java 类库不支持。</li>
<li>我们已经用另一种语言，比如说 C/C++ 编写了一个类库，如何用 Java 代码调用呢？</li>
<li>某些运行次数特别多的方法，为了加快性能，需要用更接近硬件的语言（比如汇编）编写</li>
</ul>
<p>上面这三种需求，说到底就是如何用 Java 代码调用不同语言编写的代码。那么 JNI 应运而生了。</p>
<p>从 Java 1.1 开始，Java Native Interface (JNI)标准就成为 Java 平台的一部分，它允许 Java 代码和其他语言编写的代码进行交互</p>
<p>JNI 一开始是为了本地已编译语言，尤其是 C 和 C++而设计的，但是它并不妨碍你使用其他语言，只要调用约定受支持就可以了</p>
<p>使用 Java 与本地已编译的代码交互，通常会<strong>丧失平台可移植性</strong>，但是，有些情况下这样做是可以接受的，甚至是必须的</p>
<p>比如，使用一些旧的库，与硬件、操作系统进行交互，或者为了提高程序的性能。JNI 标准至少保证本地代码能工作能在任何 Java 虚拟机实现下</p>
<p>通过 JNI，我们就可以通过 Java 程序（代码）调用到操作系统相关的技术实现的库函数，从而与其他技术和系统交互；同时其他技术和系统也可以通过 JNI 提供的相应原生接口调用 Java 应用系统内部实现的功能</p>
<h5>缺点</h5>
<ul>
<li>程序不再跨平台。要想跨平台，必须在不同的系统环境下重新编译本地语言部分。</li>
<li>程序不再是绝对安全的，本地代码的不当使用可能导致整个程序崩溃。一个通用规则是，你应该让本地方法集中在少数几个类当中。这样就降低了 Java 和 C/C++ 之间的耦合性。</li>
</ul>
<h4>具体使用</h4>
<ul>
<li>编写带有 native 方法的 Java 类，生成.java 文件；</li>
<li>使用 javac 命令编译所编写的 Java 类，生成.class 文件；</li>
<li>使用 javah -jni java 类名 生成扩展名为 h 的头文件，也即生成 .h 文件；</li>
<li>使用 C/C++（或者其他编程想语言）实现本地方法，创建 .h 文件的实现，也就是创建 .cpp 文件实现.h 文件中的方法；</li>
<li>将 C/C++ 编写的文件生成动态连接库，生成 dll 文件；</li>
</ul>
<blockquote>
<p>dll 是 Dynamic Link Library，中文一般叫 动态链接库。</p>
<p>在 Windows 里，它本质上是一个可被其他程序在运行时加载的二进制库文件，扩展名就是 .dll</p>
</blockquote>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token comment">// 只要 Java 语法没问题，就能编译通过。</span>
<span class="token comment">// 这时候 没有 hello.dll 也没关系。</span>
<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">HelloJNI</span> <span class="token punctuation">{</span>
  <span class="token keyword">static</span> <span class="token punctuation">{</span>
      <span class="token class-name">System</span><span class="token punctuation">.</span><span class="token function">loadLibrary</span><span class="token punctuation">(</span><span class="token string">"hello"</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 加载名为 libhello.dylib 的动态链接库 (dylib 是 macOS 的动态链接库)</span>
  <span class="token punctuation">}</span>

  <span class="token comment">// 定义本地方法</span>
  <span class="token keyword">private</span> <span class="token keyword">native</span> <span class="token keyword">void</span> <span class="token function">helloJNI</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

  <span class="token keyword">public</span> <span class="token keyword">static</span> <span class="token keyword">void</span> <span class="token function">main</span><span class="token punctuation">(</span><span class="token class-name">String</span><span class="token punctuation">[</span><span class="token punctuation">]</span> args<span class="token punctuation">)</span> <span class="token punctuation">{</span>
      <span class="token keyword">new</span> <span class="token class-name">HelloJNI</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">helloJNI</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 调用本地方法</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><ul>
<li>ative 方法声明：像是在 Java 里先留了个“接口”</li>
<li>dll：真正的实现</li>
<li>编译时只检查声明</li>
<li>运行时才检查实现</li>
</ul>
<h5>编译 <code>HelloJNI.java</code></h5>
<p>在命令行通过 <code>javac HelloJNI.java</code> 来编译源代码</p>
<p>执行完毕后，会在 <code>HelloJNI.java</code> 所在目录下生成一个名为 <code>HelloJNI.h</code> 的头文件</p>
<h5>使用 c语言实现本地方法</h5>
<p>创建一个 C 文件 HelloJNI.c，实现本地方法 sayHello</p>
<div class="language-c" data-ext="c" data-title="c"><pre class="language-c"><code><span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">include</span> <span class="token string">&lt;stdio.h&gt;</span></span>
<span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">include</span> <span class="token string">&lt;jni.h&gt;</span></span>
<span class="token macro property"><span class="token directive-hash">#</span><span class="token directive keyword">include</span> <span class="token string">"HelloJNI.h"</span></span>

JNIEXPORT <span class="token keyword">void</span> JNICALL <span class="token function">Java_HelloJNI_helloJNI</span><span class="token punctuation">(</span>JNIEnv <span class="token operator">*</span>env<span class="token punctuation">,</span> jobject obj<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token function">printf</span><span class="token punctuation">(</span><span class="token string">"Hello, JNI!\n"</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">return</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre></div><p>注意，这里需要引入 JNI 头文件，并且实现的方法名称需要与在 Java 中声明的名称一致（HelloJNI_helloJNI HelloJNI 类的 helloJNI 方法）</p>
<h5>对应编译脚本</h5>
<div class="language-bash" data-ext="sh" data-title="sh"><pre class="language-bash"><code><span class="token shebang important">#!/bin/bash</span>

<span class="token comment"># 编译 HelloJNI.c 文件</span>
gcc -I<span class="token string">"<span class="token variable">$JAVA_HOME</span>/include"</span> -I<span class="token string">"<span class="token variable">$JAVA_HOME</span>/include/darwin"</span> <span class="token parameter variable">-shared</span> <span class="token parameter variable">-o</span> libhello.dylib HelloJNI.c

<span class="token comment"># 把生成的 libhello.dylib 文件拷贝到当前目录</span>
<span class="token function">cp</span> libhello.dylib <span class="token builtin class-name">.</span>
</code></pre></div><ul>
<li>$JAVA_HOME 是 JDK 的安装路径，需要根据实际情况修改。</li>
<li>在 macOS 上，动态链接库（hello）的后缀是 .dylib，而不是 Linux 上的 .so。</li>
</ul>
<p>这里的 -I 选项是为了告诉编译器头文件的位置，$JAVA_HOME 是 Java 安装目录的路径</p>
<p>之后执行即可</p>
<figure><figcaption>alt text</figcaption></figure>
]]></content:encoded>
    </item>
    <item>
      <title>JVM 八股4 - 内存管理3</title>
      <link>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JVM%20-%20%E5%85%AB%E8%82%A1/article4.html</link>
      <guid>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JVM%20-%20%E5%85%AB%E8%82%A1/article4.html</guid>
      <source url="http://ekkosonya.cn/rss.xml">JVM 八股4 - 内存管理3</source>
      <description>虚拟机栈、本地方法栈、程序计数器 | 堆、方法区 不同 JDK 下内存区域变化 主要是方法区实现的不同 方法区是 JVM 规范里的概念, JVM 需要有这么一块区域来存类相关信息 HotSpot 实现层面：曾经用永久代、后来用元空间来实现 JDK 1.6 使用永久代来实现方法区： alt textalt text JDK 1.7 时仍然是永久带，但发生...</description>
      <category>code</category>
      <pubDate>Sun, 29 Mar 2026 02:48:57 GMT</pubDate>
      <content:encoded><![CDATA[<p>虚拟机栈、本地方法栈、程序计数器 | 堆、方法区</p>
<h2>不同 JDK 下内存区域变化</h2>
<p>主要是方法区实现的不同</p>
<blockquote>
<p>方法区是 JVM 规范里的概念, JVM 需要有这么一块区域来存类相关信息</p>
<p>HotSpot 实现层面：曾经用永久代、后来用元空间来实现</p>
</blockquote>
<p>JDK 1.6 使用永久代来实现方法区：</p>
<figure><figcaption>alt text</figcaption></figure>
<p>JDK 1.7 时仍然是永久带，但发生了一些细微变化，比如将字符串常量池、静态变量存放到了堆上。</p>
<figure><figcaption>alt text</figcaption></figure>
<p>在 JDK 1.8 时，直接在内存中划出了一块区域，叫<strong>元空间</strong>，来取代之前放在 JVM 内存中的永久代，并将<strong>运行时常量池</strong>、<strong>类常量池</strong>都移动到了元空间</p>
<figure><figcaption>alt text</figcaption></figure>
<h3>为什么使用元空间替代永久代</h3>
<p>客观上，永久代会导致 Java 应用程序更容易出现内存溢出的问题，因为它要受到 JVM 内存大小的限制</p>
<p>HotSpot 虚拟机的永久代大小可以通过 -XX：MaxPermSize 参数来设置，32 位机器默认的大小为 64M，64 位的机器则为 85M</p>
<p>问题是类元数据这类东西的大小很难估计：</p>
<ul>
<li>类多</li>
<li>动态生成类多</li>
<li>框架多（Spring、Hibernate、代理类、CGLIB）</li>
<li>热部署/类加载器反复创建</li>
</ul>
<p>就很容易出现：<code>OutOfMemoryError: PermGen space</code></p>
<p>此外</p>
<blockquote>
<p>类元数据和 Java 对象放在一起不太合适</p>
</blockquote>
<p>永久代属于 HotSpot 里一块比较特殊的 JVM 内存区域, 但类的元数据本质上和普通 Java 对象不是一类东西，把它们塞在同一套受限空间里，管理起来不够自然</p>
<p>元空间不再使用 JVM 的堆内存，而是直接使用操作系统的本地内存（Native Memory）</p>
<p>这意味着，只要你的服务器物理内存足够大，元空间就可以一直动态扩展，基本告别了类元数据塞满导致的 OOM 问题</p>
<blockquote>
<p>降低调优困难</p>
</blockquote>
<p>在过去，预估一个复杂的企业级应用到底需要多大的永久代是非常困难的。它不仅取决于项目本身的代码量，还取决于引入了多少第三方 Jar 包，以及运行时会动态生成多少类。设置小了容易 OOM，设置大了又浪费堆内存。</p>
<p>使用本地内存的元空间后，JVM 会根据应用程序的需要自动分配内存，大大降低了开发者对这部分内存管理的负担。当然，为了防止元空间无限扩张吞噬掉所有物理内存导致系统死机，也可以通过 <code>-XX:MaxMetaspaceSize</code> 来设置一个上限</p>
<blockquote>
<p>提升垃圾回收（GC）的效率</p>
</blockquote>
<p>永久代的垃圾回收效率一直很低。在永久代中进行 GC（通常伴随着 Full GC）去清理废弃的类和类加载器，条件非常苛刻，而且经常清理不干净，导致长时间的停顿（STW）</p>
<p>将元数据移到堆外的元空间后，简化了 JVM 堆内存的垃圾回收流程，也方便了对类元数据进行独立的、更高效的管理</p>
<h4>什么是本地内存</h4>
<p>简单来说：</p>
<p>JVM 本质上就是一个由 C/C++ 编写的普通的操作系统进程。</p>
<p>当你在命令行敲下 java xxx 时，你其实是在启动一个名为 java 的 C++ 程序。</p>
<p>操作系统（如 Linux）会为这个进程分配一个巨大的虚拟内存地址空间, 在这个空间里，JVM 进程可以完全按照操作系统的规则来申请和使用内存</p>
<h5>Java 堆内存</h5>
<p>在 JVM 进程启动时，它会根据你设置的参数（比如 -Xms 和 -Xmx），通过底层 C++ 代码一次性向操作系统申请一大块连续的内存空间</p>
<ul>
<li><strong>机制</strong>：在 JVM 进程启动时，它会根据你设置的参数（比如 <code>Xms</code> 和 <code>Xmx</code>），通过底层 C++ 代码<strong>一次性</strong>向操作系统申请一大块连续的内存空间。</li>
<li><strong>角色</strong>：JVM 就像是一个“二房东”，把这块大内存承包下来后，自己在内部建立了一套复杂的机制（比如年轻代、老年代、垃圾回收器 GC）来分配和管理这块土地。</li>
<li><strong>用途</strong>：你在 Java 代码里 <code>new</code> 出来的各种对象，都是由 JVM 这个二房东分配在这块“承包地”里的。</li>
</ul>
<h5>本地内存 / 堆外内存</h5>
<p>本地内存（Native Memory），也叫堆外内存，指的是 JVM 进程的虚拟内存空间中，没有被划分给 Java 堆的那部分内存</p>
<ul>
<li><strong>机制</strong>：因为 JVM 底层是 C++ 写的，所以当它需要内存时，它完全可以绕过自己管理的“Java 堆”，直接调用 C/C++ 的标准库函数（比如 <code>malloc()</code>、<code>free()</code> 或者 <code>mmap()</code>）向操作系统（OS）动态申请内存。</li>
<li><strong>操作方式</strong>：当 JVM 需要加载一个新类时，底层的 C++ 代码会直接调用系统的内存分配接口，在 Java 堆之外的进程空间里切出一块内存，用来存放这个类的元数据（这就是<strong>元空间</strong>的由来）。</li>
<li><strong>特点</strong>：这部分内存<strong>不受 Java 堆大小（<code>Xmx</code>）的限制</strong>，也不由 Java 的常规垃圾回收器直接管理。它的上限主要受限于操作系统分配给该进程的物理内存和虚拟内存总大小。</li>
</ul>
<h5>总结</h5>
<p>当你启动 JVM 并加载大量类时，底层发生的事情是这样的：</p>
<ol>
<li><strong>操作系统</strong>：启动 <code>java</code> 进程，分配虚拟内存空间。</li>
<li><strong>JVM (C++层面)</strong>：调用 <code>malloc</code> 申请一块大内存作为 <strong>Java 堆</strong>。</li>
<li><strong>程序运行</strong>：需要加载一个新类（比如动态生成了一个代理类）。</li>
<li><strong>JVM (C++层面)</strong>：发现是类元数据，不放进 Java 堆。再次调用 <code>malloc</code>，在堆外的<strong>本地内存</strong>中分配一块空间（即元空间 Metaspace），把类信息存进去。</li>
</ol>
<p>所以，JVM 能够“直接操作本地内存”，是因为 JVM 本身是本地实现的用户态进程，可以通过操作系统提供的内存分配接口申请和管理进程地址空间内的内存</p>
<p>除了元空间，其实我们在写 Java 后端高并发应用时，Java 代码本身也可以主动去操作这块“本地内存”（堆外内存），这在提升网络 I/O 性能上非常有用</p>
<h2>对象创建过程</h2>
<p>当我们使用 new 关键字创建一个对象时，JVM 首先会检查 new 指令的参数是否能在常量池中定位到类的符号引用</p>
<p>然后检查这个符号引用代表的类是否已被加载、解析和初始化。</p>
<p>如果没有，就先执行类加载</p>
<p>如果已经加载，JVM 会为对象分配内存完成初始化，比如数值类型的成员变量初始值是 0，布尔类型是 false，对象类型是 null。</p>
<p>接下来会设置对象头，里面包含了对象是哪个类的实例、对象的哈希码、对象的 GC 分代年龄等信息。</p>
<p>最后，JVM 会执行构造方法 <code>&lt;init&gt;</code> 完成赋值操作，将成员变量赋值为预期的值，比如 int age = 18，这样一个对象就创建完成了。</p>
<figure><figcaption>alt text</figcaption></figure>
<h3>Gemini 解释流程</h3>
<h4>JVM 对象创建的第一步 —— 类加载检查</h4>
<p>当程序执行到 <code>new</code> 关键字准备创建对象时</p>
<p>例如 <code>User user = new User();</code></p>
<p>JVM 并不是立刻就在堆里划分一块内存，而是必须先经历一个<strong>验明正身</strong>的过程</p>
<p>具体分为以下三个逻辑步骤：</p>
<h5>1. 遇到 <code>new</code> 指令：拿着“号码牌”找线索</h5>
<p>在 Java 编译后的字节码中，<code>new User()</code> 并不是直接写着“分配 User 对象的内存”，而是会被编译成类似这样的指令：</p>
<div class="language-Java" data-ext="Java" data-title="Java"><pre class="language-Java"><code>new #2
dup
invokespecial #3   // 调用构造方法 &lt;init&gt;`
</code></pre></div><p>这里的 <code>#2</code> 就像是一个号码牌</p>
<p>JVM 执行到 <code>new #2</code> 时，会拿着这个号码，去当前类的<strong>运行时常量池</strong>中查找它到底代表什么</p>
<h5>2. 解析符号引用：把“名字”翻译成“目标”</h5>
<p>JVM 在常量池中找到 <code>#2</code> 对应的项后，会发现这只是一个<strong>符号引用</strong>。</p>
<ul>
<li><strong>什么是符号引用？</strong>
<ul>
<li>简单来说，它就是一个字符串字面量（比如记录着类名全路径：<code>com/example/User</code>）</li>
<li>因为在编译成 <code>.class</code> 文件时，编译器根本不知道 <code>User</code> 这个类将来会被加载到内存的哪个物理地址，所以只能先用一个“名字”来占位。</li>
</ul>
</li>
</ul>
<h5>3. 核心检查：这个类真的准备好了吗？（触发类加载检查）</h5>
<p>拿到 <code>com/example/User</code> 这个名字后，JVM 必须要确认一件事：<strong>我要实例化的这个类，现在有没有被加载到内存（元空间）中？</strong></p>
<ul>
<li><strong>为什么一定要检查？</strong> 因为 JVM 如果要给 <code>User</code> 对象分配内存，就必须提前知道这个对象到底需要占多大空间（比如它有几个 <code>int</code> 字段，几个引用字段）。而这些关于类结构的信息（元数据），只有在这个类被完整加载、解析后，才会存在于内存中。</li>
<li><strong>检查的结果：</strong>
<ul>
<li><strong>如果已加载：</strong> 常量池里的那个符号引用（字符串）早就已经被替换成了直接指向元空间中该类元数据的<strong>直接引用</strong>（内存指针）。JVM 知道了对象大小，就可以顺利进入下一步——分配内存。</li>
<li><strong>如果未加载：</strong> 说明这是程序第一次用到这个类。JVM 必须立刻暂停对象的创建，先触发这个类的<strong>类加载过程</strong>（加载 -&gt; 链接 -&gt; 初始化），把 <code>.class</code> 文件吃透，搞清楚类的结构，然后再回头继续对象的实例化。</li>
</ul>
</li>
</ul>
<h5>实际解析过程</h5>
<blockquote>
<p>阶段一：在常量池里的“套娃”解析</p>
</blockquote>
<p>刚才说到 <code>new #2</code>，这个 <code>#2</code> 只是一个索引</p>
<p>在 <code>.class</code> 文件的结构里，它并不会直接把字符串塞在 <code>#2</code> 的位置。</p>
<ol>
<li><strong>第一层寻找：</strong> JVM 找到常量池的第 2 项（<code>#2</code>），发现这是一个专门用来表示类的结构（叫 <code>CONSTANT_Class_info</code>）。</li>
<li><strong>第二层寻找：</strong> 这个结构里面本身没有存英文字母，而是存了另一个索引（假设是 <code>#15</code>），意思是“这个类的名字存在第 15 项那里”。</li>
<li><strong>拿到真名：</strong> JVM 再顺着找到常量池的第 15 项（<code>CONSTANT_Utf8_info</code>），这才真正拔出了 <code>com/example/User</code> 这个标准的 UTF-8 字符串。</li>
</ol>
<blockquote>
<p>阶段二：拿着名字去“查字典” （核心机制）</p>
</blockquote>
<p>拿到了 <code>com/example/User</code> 这个字符串后，JVM 怎么知道内存里有没有这个类呢？总不能把整个元空间遍历一遍吧？</p>
<p>这个时候，JVM 会去查一本它内部维护的“花名册”——这在底层被称为 <strong>System Dictionary（系统字典）</strong></p>
<p>可以把它理解成一个巨大的全局哈希表（HashMap）。</p>
<p>这里有一个 JVM 领域非常重要的高级考点：</p>
<p><strong>JVM 查字典时，不能只靠字符串名字！</strong></p>
<ul>
<li>在 Java 的世界里，判断一个类是不是唯一的，标准是：<strong>“全限定类名 + 加载它的类加载器（ClassLoader）”</strong></li>
<li>为什么？因为 Tomcat 这样的容器里，可能会有不同的 Web 应用使用了同一个包名和类名的类，但它们是相互隔离的，靠的就是不同的 ClassLoader</li>
</ul>
<p><strong>所以，真实的查找动作是这样的：</strong></p>
<ol>
<li>JVM 拿着提取出来的字符串 <code>com/example/User</code>。</li>
<li>抓取当前正在执行这段代码的类（比如在 <code>OrderService</code> 里 new 的 User，那就拿到 <code>OrderService</code> 的类加载器）。</li>
<li>把这两者组合成一个 Key：<code>[com/example/User, AppClassLoader]</code>。</li>
<li>去 System Dictionary 这个哈希表里查：
<ul>
<li><strong>查到了（命中）：</strong> 返回一个指针，直接指向元空间（Metaspace）里该类的元数据地址。这就说明类已经加载好了，检查通过！</li>
<li><strong>没查到（Miss）：</strong> 说明这个类在这个加载器的管辖范围内还没被加载过。JVM 就会挂起当前的 <code>new</code> 指令，触发双亲委派机制去磁盘里找 <code>.class</code> 文件，执行完整的类加载流程。</li>
</ul>
</li>
</ol>
<h3>对象创建流程总结</h3>
<p>| 步骤 | 做了什么 | 关键点 |
|</p>
]]></content:encoded>
    </item>
    <item>
      <title>JUC 八股24 (实际场景)</title>
      <link>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JUC%20-%20%E5%85%AB%E8%82%A1/article24.html</link>
      <guid>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JUC%20-%20%E5%85%AB%E8%82%A1/article24.html</guid>
      <source url="http://ekkosonya.cn/rss.xml">JUC 八股24 (实际场景)</source>
      <description>实际场景 多线程打印奇偶数，怎么控制打印的顺序 使用 ReentrantLock + Condition 使用 synchronized + wait/notify </description>
      <category>code</category>
      <pubDate>Wed, 25 Mar 2026 00:00:00 GMT</pubDate>
      <content:encoded><![CDATA[<h2>实际场景</h2>
<h3>多线程打印奇偶数，怎么控制打印的顺序</h3>
<h4>使用 ReentrantLock + Condition</h4>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">import</span> <span class="token import"><span class="token namespace">java<span class="token punctuation">.</span>util<span class="token punctuation">.</span>concurrent<span class="token punctuation">.</span>locks<span class="token punctuation">.</span></span><span class="token class-name">Condition</span></span><span class="token punctuation">;</span>
<span class="token keyword">import</span> <span class="token import"><span class="token namespace">java<span class="token punctuation">.</span>util<span class="token punctuation">.</span>concurrent<span class="token punctuation">.</span>locks<span class="token punctuation">.</span></span><span class="token class-name">ReentrantLock</span></span><span class="token punctuation">;</span>

<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">OddEvenPrinterLock</span> <span class="token punctuation">{</span>
  <span class="token keyword">private</span> <span class="token keyword">int</span> count <span class="token operator">=</span> <span class="token number">1</span><span class="token punctuation">;</span>
  <span class="token keyword">private</span> <span class="token keyword">final</span> <span class="token keyword">int</span> <span class="token constant">MAX</span> <span class="token operator">=</span> <span class="token number">10</span><span class="token punctuation">;</span>
  
  <span class="token comment">// 主线程创建的锁对象</span>
  <span class="token keyword">private</span> <span class="token keyword">final</span> <span class="token class-name">ReentrantLock</span> lock <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">ReentrantLock</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> 
  <span class="token comment">// 创建两个条件变量，相当于两个休息室</span>
  <span class="token keyword">private</span> <span class="token keyword">final</span> <span class="token class-name">Condition</span> oddCondition <span class="token operator">=</span> lock<span class="token punctuation">.</span><span class="token function">newCondition</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token keyword">private</span> <span class="token keyword">final</span> <span class="token class-name">Condition</span> evenCondition <span class="token operator">=</span> lock<span class="token punctuation">.</span><span class="token function">newCondition</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

  <span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">printOdd</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
      <span class="token keyword">while</span> <span class="token punctuation">(</span>count <span class="token operator">&lt;=</span> <span class="token constant">MAX</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
          lock<span class="token punctuation">.</span><span class="token function">lock</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 尝试获取锁</span>
          <span class="token keyword">try</span> <span class="token punctuation">{</span>
              <span class="token keyword">while</span> <span class="token punctuation">(</span>count <span class="token operator">%</span> <span class="token number">2</span> <span class="token operator">==</span> <span class="token number">0</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
                oddCondition<span class="token punctuation">.</span><span class="token function">await</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 奇数线程去奇数休息室睡觉，并释放锁</span>
              <span class="token punctuation">}</span>
              <span class="token keyword">if</span> <span class="token punctuation">(</span>count <span class="token operator">&lt;=</span> <span class="token constant">MAX</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
                <span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span><span class="token class-name">Thread</span><span class="token punctuation">.</span><span class="token function">currentThread</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">getName</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">+</span> <span class="token string">" 打印奇数: "</span> <span class="token operator">+</span> count<span class="token punctuation">)</span><span class="token punctuation">;</span>
                count<span class="token operator">++</span><span class="token punctuation">;</span>
                evenCondition<span class="token punctuation">.</span><span class="token function">signal</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 明确去偶数休息室叫醒偶数线程</span>
              <span class="token punctuation">}</span>
          <span class="token punctuation">}</span> <span class="token keyword">catch</span> <span class="token punctuation">(</span><span class="token class-name">InterruptedException</span> e<span class="token punctuation">)</span> <span class="token punctuation">{</span>
              <span class="token class-name">Thread</span><span class="token punctuation">.</span><span class="token function">currentThread</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">interrupt</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
          <span class="token punctuation">}</span> <span class="token keyword">finally</span> <span class="token punctuation">{</span>
              lock<span class="token punctuation">.</span><span class="token function">unlock</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 切记在 finally 中释放锁</span>
          <span class="token punctuation">}</span>
      <span class="token punctuation">}</span>
  <span class="token punctuation">}</span>

  <span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">printEven</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
      <span class="token keyword">while</span> <span class="token punctuation">(</span>count <span class="token operator">&lt;=</span> <span class="token constant">MAX</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
          lock<span class="token punctuation">.</span><span class="token function">lock</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
          <span class="token keyword">try</span> <span class="token punctuation">{</span>
              <span class="token keyword">while</span> <span class="token punctuation">(</span>count <span class="token operator">%</span> <span class="token number">2</span> <span class="token operator">!=</span> <span class="token number">0</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
                  evenCondition<span class="token punctuation">.</span><span class="token function">await</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 偶数线程去偶数休息室睡觉，并释放锁</span>
              <span class="token punctuation">}</span>
              <span class="token keyword">if</span> <span class="token punctuation">(</span>count <span class="token operator">&lt;=</span> <span class="token constant">MAX</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
                  <span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span><span class="token class-name">Thread</span><span class="token punctuation">.</span><span class="token function">currentThread</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">getName</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">+</span> <span class="token string">" 打印偶数: "</span> <span class="token operator">+</span> count<span class="token punctuation">)</span><span class="token punctuation">;</span>
                  count<span class="token operator">++</span><span class="token punctuation">;</span>
                  oddCondition<span class="token punctuation">.</span><span class="token function">signal</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 明确去奇数休息室叫醒奇数线程</span>
              <span class="token punctuation">}</span>
          <span class="token punctuation">}</span> <span class="token keyword">catch</span> <span class="token punctuation">(</span><span class="token class-name">InterruptedException</span> e<span class="token punctuation">)</span> <span class="token punctuation">{</span>
              <span class="token class-name">Thread</span><span class="token punctuation">.</span><span class="token function">currentThread</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">interrupt</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
          <span class="token punctuation">}</span> <span class="token keyword">finally</span> <span class="token punctuation">{</span>
              lock<span class="token punctuation">.</span><span class="token function">unlock</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
          <span class="token punctuation">}</span>
      <span class="token punctuation">}</span>
  <span class="token punctuation">}</span>

  <span class="token keyword">public</span> <span class="token keyword">static</span> <span class="token keyword">void</span> <span class="token function">main</span><span class="token punctuation">(</span><span class="token class-name">String</span><span class="token punctuation">[</span><span class="token punctuation">]</span> args<span class="token punctuation">)</span> <span class="token punctuation">{</span>
      <span class="token class-name">OddEvenPrinterLock</span> printer <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">OddEvenPrinterLock</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
      <span class="token keyword">new</span> <span class="token class-name">Thread</span><span class="token punctuation">(</span>printer<span class="token operator">::</span><span class="token function">printOdd</span><span class="token punctuation">,</span> <span class="token string">"Thread-Odd"</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">start</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
      <span class="token keyword">new</span> <span class="token class-name">Thread</span><span class="token punctuation">(</span>printer<span class="token operator">::</span><span class="token function">printEven</span><span class="token punctuation">,</span> <span class="token string">"Thread-Even"</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">start</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><h4>使用 synchronized + wait/notify</h4>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">OddEvenPrinterSync</span> <span class="token punctuation">{</span>
  <span class="token keyword">private</span> <span class="token keyword">int</span> count <span class="token operator">=</span> <span class="token number">1</span><span class="token punctuation">;</span>
  <span class="token keyword">private</span> <span class="token keyword">final</span> <span class="token keyword">int</span> <span class="token constant">MAX</span> <span class="token operator">=</span> <span class="token number">10</span><span class="token punctuation">;</span>
  <span class="token keyword">private</span> <span class="token keyword">final</span> <span class="token class-name">Object</span> lock <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">Object</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 共享的锁对象</span>

  <span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">printOdd</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
      <span class="token keyword">while</span> <span class="token punctuation">(</span>count <span class="token operator">&lt;=</span> <span class="token constant">MAX</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
          <span class="token keyword">synchronized</span> <span class="token punctuation">(</span>lock<span class="token punctuation">)</span> <span class="token punctuation">{</span>
              <span class="token comment">// 如果当前是偶数，奇数线程就主动挂起等待</span>
              <span class="token keyword">while</span> <span class="token punctuation">(</span>count <span class="token operator">%</span> <span class="token number">2</span> <span class="token operator">==</span> <span class="token number">0</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
                  <span class="token keyword">try</span> <span class="token punctuation">{</span>
                      lock<span class="token punctuation">.</span><span class="token function">wait</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 释放锁，进入阻塞状态，等待被唤醒</span>
                  <span class="token punctuation">}</span> <span class="token keyword">catch</span> <span class="token punctuation">(</span><span class="token class-name">InterruptedException</span> e<span class="token punctuation">)</span> <span class="token punctuation">{</span>
                      <span class="token class-name">Thread</span><span class="token punctuation">.</span><span class="token function">currentThread</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">interrupt</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
                  <span class="token punctuation">}</span>
              <span class="token punctuation">}</span>
              <span class="token keyword">if</span> <span class="token punctuation">(</span>count <span class="token operator">&lt;=</span> <span class="token constant">MAX</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
                  <span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span><span class="token class-name">Thread</span><span class="token punctuation">.</span><span class="token function">currentThread</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">getName</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">+</span> <span class="token string">" 打印奇数: "</span> <span class="token operator">+</span> count<span class="token punctuation">)</span><span class="token punctuation">;</span>
                  count<span class="token operator">++</span><span class="token punctuation">;</span>
                  lock<span class="token punctuation">.</span><span class="token function">notify</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 活干完了，叫醒挂在 lock 上的其他线程（偶数线程）</span>
              <span class="token punctuation">}</span>
          <span class="token punctuation">}</span>
      <span class="token punctuation">}</span>
  <span class="token punctuation">}</span>

  <span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">printEven</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
      <span class="token keyword">while</span> <span class="token punctuation">(</span>count <span class="token operator">&lt;=</span> <span class="token constant">MAX</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
          <span class="token keyword">synchronized</span> <span class="token punctuation">(</span>lock<span class="token punctuation">)</span> <span class="token punctuation">{</span>
              <span class="token comment">// 如果当前是奇数，偶数线程就主动挂起等待</span>
              <span class="token keyword">while</span> <span class="token punctuation">(</span>count <span class="token operator">%</span> <span class="token number">2</span> <span class="token operator">!=</span> <span class="token number">0</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
                  <span class="token keyword">try</span> <span class="token punctuation">{</span>
                      lock<span class="token punctuation">.</span><span class="token function">wait</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> 
                  <span class="token punctuation">}</span> <span class="token keyword">catch</span> <span class="token punctuation">(</span><span class="token class-name">InterruptedException</span> e<span class="token punctuation">)</span> <span class="token punctuation">{</span>
                      <span class="token class-name">Thread</span><span class="token punctuation">.</span><span class="token function">currentThread</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">interrupt</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
                  <span class="token punctuation">}</span>
              <span class="token punctuation">}</span>
              <span class="token keyword">if</span> <span class="token punctuation">(</span>count <span class="token operator">&lt;=</span> <span class="token constant">MAX</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
                  <span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span><span class="token class-name">Thread</span><span class="token punctuation">.</span><span class="token function">currentThread</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">getName</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">+</span> <span class="token string">" 打印偶数: "</span> <span class="token operator">+</span> count<span class="token punctuation">)</span><span class="token punctuation">;</span>
                  count<span class="token operator">++</span><span class="token punctuation">;</span>
                  lock<span class="token punctuation">.</span><span class="token function">notify</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 活干完了，叫醒奇数线程</span>
              <span class="token punctuation">}</span>
          <span class="token punctuation">}</span>
      <span class="token punctuation">}</span>
  <span class="token punctuation">}</span>

  <span class="token keyword">public</span> <span class="token keyword">static</span> <span class="token keyword">void</span> <span class="token function">main</span><span class="token punctuation">(</span><span class="token class-name">String</span><span class="token punctuation">[</span><span class="token punctuation">]</span> args<span class="token punctuation">)</span> <span class="token punctuation">{</span>
      <span class="token class-name">OddEvenPrinterSync</span> printer <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">OddEvenPrinterSync</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
      <span class="token keyword">new</span> <span class="token class-name">Thread</span><span class="token punctuation">(</span>printer<span class="token operator">::</span><span class="token function">printOdd</span><span class="token punctuation">,</span> <span class="token string">"Thread-Odd"</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">start</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
      <span class="token keyword">new</span> <span class="token class-name">Thread</span><span class="token punctuation">(</span>printer<span class="token operator">::</span><span class="token function">printEven</span><span class="token punctuation">,</span> <span class="token string">"Thread-Even"</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">start</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div>]]></content:encoded>
    </item>
    <item>
      <title>OS5 - 进程管理3</title>
      <link>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/article5.html</link>
      <guid>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/article5.html</guid>
      <source url="http://ekkosonya.cn/rss.xml">OS5 - 进程管理3</source>
      <description>OS Java 线程状态 Java 没有 Ready 状态，线程状态有 6 种： 相应操作下的不同： IO 密集 / CPU 密集 IO 操作时线程在等待，CPU 空闲 数据读取的过程 磁盘/网卡自己干活，不需要 CPU 参与： CPU 只需要发完这个指令，表示我要读取数据了，就可以切换到别的线程 那我写入了内存，但是线程此时被CPU切换掉了，等轮到他...</description>
      <category>code</category>
      <pubDate>Thu, 26 Mar 2026 05:53:24 GMT</pubDate>
      <content:encoded><![CDATA[<h2>OS</h2>
<h3>Java 线程状态</h3>
<p>Java 没有 Ready 状态，线程状态有 6 种：</p>
<div class="language-plain" data-ext="plain" data-title="plain"><pre class="language-plain"><code>NEW              新建
RUNNABLE         可运行（包含 Ready + Running）
BLOCKED          阻塞（等待锁）
WAITING          等待（wait/join/park 等）
TIMED_WAITING    限时等待
TERMINATED       终止
</code></pre></div><p>相应操作下的不同：</p>
<div class="language-plain" data-ext="plain" data-title="plain"><pre class="language-plain"><code>操作系统层面：
Running → 发起IO → Sleeping/Blocked → IO完成 → Ready → Running

Java 层面：
RUNNABLE → 发起IO → RUNNABLE（底层在等）→ IO完成 → RUNNABLE
                   ↑
             Java 看起来还是 RUNNABLE
             但实际在操作系统层面是 Sleeping
</code></pre></div><h3>IO 密集 / CPU 密集</h3>
<p>| 任务类型 | 线程状态 | CPU 使用 | 多线程效果 |
|</p>
]]></content:encoded>
    </item>
    <item>
      <title>OS6 - 进程管理4</title>
      <link>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/article6.html</link>
      <guid>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F/article6.html</guid>
      <source url="http://ekkosonya.cn/rss.xml">OS6 - 进程管理4</source>
      <description>OS 中断切换 以线程 A 发起读取磁盘文件为例 第一阶段：发起 I/O 时的“软中断”与主动切换（线程 A 交出 CPU） 当线程 A 执行到读取文件的代码时，它其实是没有权限直接控制底层硬件的，必须向操作系统老大哥（内核）求助。 系统调用（软中断/Trap）： 线程 A 调用 read() 函数，这会触发一个软中断（在 x86 架构下通常是 int...</description>
      <category>code</category>
      <pubDate>Thu, 26 Mar 2026 05:53:24 GMT</pubDate>
      <content:encoded><![CDATA[<h2>OS</h2>
<h3>中断切换</h3>
<p>以线程 A 发起读取磁盘文件为例</p>
<h4>第一阶段：发起 I/O 时的“软中断”与主动切换（线程 A 交出 CPU）</h4>
<p>当线程 A 执行到读取文件的代码时，它其实是没有权限直接控制底层硬件的，必须向操作系统老大哥（内核）求助。</p>
<ul>
<li>系统调用（软中断/Trap）： 线程 A 调用 read() 函数，这会触发一个软中断（在 x86 架构下通常是 int 0x80 指令或 syscall 指令）。CPU 收到这个信号后，会从“用户态”切换到“内核态”，开始执行操作系统的代码。</li>
<li>下达指令与阻塞： 操作系统内核接收到请求，把读取任务交给 DMA 和磁盘控制器。因为此时数据还没准备好，操作系统会将线程 A 的状态标记为 BLOCKED（阻塞状态），并把它移出 CPU 的运行队列。</li>
<li>第一次上下文切换（Context Switch）： 既然线程 A 被挂起了，CPU 不能闲着。操作系统的调度器（Scheduler）会立刻介入，从就绪队列里挑出一个线程 B，把 CPU 的控制权交给它。
<ul>
<li>此时，DMA 在默默搬运硬盘数据，而 CPU 正在全速运行线程 B 的代码。两者互不干扰。</li>
</ul>
</li>
</ul>
<h4>第二阶段：I/O 完成时的“硬中断”与被动打断（硬件通知 CPU）</h4>
<p>经过了漫长的等待（对 CPU 而言），DMA 终于把磁盘里的数据全都搬到了内存缓冲区里。这个时候，硬件需要告诉 CPU：“活儿干完了！”</p>
<ul>
<li>发出硬中断信号： DMA 控制器会向 CPU 的中断控制器（如 APIC）发送一个真实的电信号（硬件中断）。</li>
<li>打断当前线程（强制暂停）： CPU 只要启用了中断响应，它在执行完当前手里这一条机器指令后，会立刻强制暂停正在运行的线程 B。CPU 会把线程 B 当前的执行进度（程序计数器 PC、寄存器状态等）火速保存到栈里。</li>
<li>执行中断服务程序（ISR）： CPU 调转枪头，去内核里执行一段专门用来处理 I/O 完成的“中断服务程序（Interrupt Service Routine）”。</li>
<li>唤醒等待的线程： 在这个 ISR 代码中，操作系统发现：“哦！是之前线程 A 要的数据到了！”于是，操作系统把线程 A 的状态从 BLOCKED 改回 READY（就绪状态），把它重新扔进就绪队列里，等待下次被调度。</li>
</ul>
<h4>第三阶段：中断返回与最终的切换（线程 A 夺回 CPU）</h4>
<p>硬中断处理完毕后，CPU 接下来该运行谁呢？这取决于操作系统的调度策略。</p>
<ul>
<li>恢复现场： 通常情况下，如果没有触发更高优先级的抢占，CPU 会恢复刚刚被强制打断的线程 B 的现场，线程 B 就像什么都没发生过一样继续往下跑。</li>
<li>第二次上下文切换（线程 A 回归）： 直到未来的某一个时刻（也许是线程 B 的时间片用完了，或者线程 A 的优先级特别高），操作系统的调度器再次介入。它选中了刚刚被唤醒的线程 A。</li>
<li>满血复活： 线程 A 恢复执行，系统调用 read() 成功返回，线程 A 终于拿到了它梦寐以求的数据，继续执行后面的业务逻辑。</li>
</ul>
<h3>CPU</h3>
<p>CPU 是计算机的大脑，负责执行指令、做运算、控制数据流动。其他存储和 IO 设备是给它服务的</p>
<blockquote>
<p>中断指令就是让他们干活，然后 CPU 去干别的，直到被他们通知干完了</p>
</blockquote>
<h4>CPU 与 IO 设备的交互方式</h4>
<h5>轮询</h5>
<p>CPU 根本不切换线程，而是写一个死循环，每隔几纳秒就去读一次硬件的寄存器，看看状态位是不是变成了“完成”</p>
<p>缺点：CPU 一直忙等，浪费资源</p>
<h5>中断</h5>
<ul>
<li>特点： 硬件主动发送电信号打断 CPU。</li>
<li>优点： CPU 极其轻松。在硬件干活期间，CPU 可以去跑别的线程，一点也不浪费算力。</li>
<li>适用场景： 绝大多数场景，比如读取常规磁盘、普通网络请求、鼠标键盘点击</li>
</ul>
<div class="language-plain" data-ext="plain" data-title="plain"><pre class="language-plain"><code>CPU: 磁盘，去读这个文件（发指令）
磁盘: 收到
CPU: 切换去干别的事

... 时间流逝 ...

磁盘: CPU，我读完了！（发送中断信号）
CPU: 停下手头工作，处理中断
CPU: 好的，我来处理数据

优点：CPU 不用等待，效率高
</code></pre></div><h4>CPU 必须响应中断吗</h4>
<p>不一定，CPU 是有权“延后”甚至“忽略”某些中断的</p>
<p>可以用一个很生活化的场景来理解：你正在聚精会神地写一段核心代码，突然手机响了（收到中断通知）。你可能会马上接起（重要电话），也可能会按静音等会儿再打回去（推销电话），而且就算你要接，你通常也会把正在敲的那行代码敲完再接。</p>
<p>CPU 处理中断的逻辑与此惊人地一致，主要受以下三个规则的严格限制：</p>
<h5>1. 执行的颗粒度：必须执行完当前的“一条指令”</h5>
<p>CPU 响应中断的最小时间单位是<strong>机器指令周期</strong>，而不是进程的业务逻辑。</p>
<p>当 CPU 正在执行一条底层的机器指令（比如把寄存器 A 的值加到寄存器 B 里）时，即使电信号已经到达引脚，CPU 也<strong>绝对不会在一条机器指令执行到一半时停下来</strong>。
它一定会把当前这<strong>一条</strong>指令完整执行完毕，然后在进入下一条指令之前，专门腾出一个极其短暂的瞬间去检查：“刚才有中断信号来吗？”如果有，才会进行中断上下文切换。</p>
<h5>2. CPU 的“免打扰模式”：可屏蔽 vs 不可屏蔽</h5>
<p>这是最核心的机制。</p>
<p>中断在硬件层面上被分成了两大类：</p>
<ul>
<li><strong>可屏蔽中断（Maskable Interrupt, INTR）：</strong> 绝大多数我们讨论的 I/O 中断（如硬盘读写完成、网卡收到数据、键盘敲击）都属于这一类。
CPU 内部有一个特殊的标志位（在 x86 架构中叫 <code>IF</code>，Interrupt Flag）。当操作系统在执行某些<strong>极其核心、绝对不能被打断的代码</strong>（比如正在修改系统内核的重要数据结构，或者正在处理另一个更高级别的大中断）时，它会主动把这个标志位关掉（俗称<strong>关中断</strong>）。
在这个“免打扰模式”下，就算硬盘疯狂发来完成通知，CPU 也会充耳不闻。这些中断信号只能在外部的中断控制器（如 APIC）里排队等着，直到操作系统办完核心大事，重新<strong>开中断</strong>，CPU 才会去响应它们。</li>
<li><strong>不可屏蔽中断（Non-Maskable Interrupt, NMI）：</strong>
这是真正的“十万火急”，<strong>无视任何免打扰模式</strong>。
这类中断通常对应着极其严重的系统级硬件故障。比如：内存条烧了导致奇偶校验错误、主板电源马上就要断电了。一旦出现 NMI，CPU 必须立刻放下手头的一切（不管在干什么）强制跳转去执行紧急处理程序（通常结果就是直接蓝屏死机，或者尝试做最后的内存快照保存）。</li>
</ul>
<h5>3. 中断的“鄙视链”：优先级调度</h5>
<p>就算 CPU 当前没有开启免打扰模式，它也遵循严格的优先级。
如果 CPU 正在处理一个普通的网卡中断（正在把网络包拷进内存），此时突然来了一个时钟中断（级别更高）。CPU 会<strong>暂停当前正在处理的低级中断</strong>，去优先处理高级中断。这就是所谓的“中断嵌套”。
反之，如果正在处理高级中断时来了低级中断，低级中断就只能老老实实在外面排队。</p>
<h3>挂起与中断</h3>
<p><strong>进程/线程的挂起（阻塞）和被激活（唤醒），本质上全都是由“中断”来驱动的。</strong> 只不过，这里的“中断”分为两种不同的流派：<strong>硬件中断</strong>和<strong>软件中断（异常/陷阱）</strong>。我们可以把进程状态切换的过程，拆解成以下几个具体场景来看：</p>
<h4>1. 进程被“挂起”（移出 CPU）是怎么触发的？</h4>
<p>进程被挂起，通常有两种情况，分别对应软件中断和硬件中断：</p>
<ul>
<li><strong>主动挂起（软件中断/系统调用）：</strong> 当您的代码里写了 <code>Thread.sleep()</code>，或者尝试去拿一把已经被别人占用的锁，亦或是发起文件读取时。您的进程会通过一种特殊的汇编指令（如 <code>syscall</code>），主动触发一次<strong>软件中断（Trap）</strong>。
操作系统收到这个软件中断后，一看：“哦，你要睡觉/等数据啊，那你别占着茅坑了。”于是，操作系统把您的进程状态改成 <code>BLOCKED</code>，把它踢出 CPU。</li>
<li><strong>被动挂起（硬件中断/时钟中断）：</strong> 如果您写了一个死循环 <code>while(true)</code>，进程根本不想让出 CPU 怎么办？这就得靠<strong>时钟中断（Timer Interrupt）了。
计算机主板上有一个晶体振荡器，它每隔几毫秒就会向 CPU 发送一次硬件中断</strong>。CPU 收到信号后强制暂停当前进程，操作系统的调度器借机接管 CPU，一看：“你的时间片用完了，下去休息吧。”于是强制把该进程挂起（状态变为 <code>READY</code>）。</li>
</ul>
<h4>2. 进程被“激活通知”（唤醒）是怎么触发的？</h4>
<p>当一个进程在后台眼巴巴地等数据或等锁时，唤醒它的信号同样来自中断：</p>
<ul>
<li><strong>外部设备唤醒（硬件中断）：</strong> 就像我们之前聊的，如果是等硬盘数据或网络数据，当底层的网卡/磁盘把数据准备好后，会给 CPU 发送一个<strong>硬中断</strong>。操作系统的中断处理程序介入，找到那个正在等数据的进程，把它从挂起状态“激活”，重新塞回就绪队列。</li>
<li><strong>其他进程唤醒（软件中断）：</strong> 假设您的进程 A 在等一把锁，而进程 B 刚刚用完释放了这把锁。进程 B 在释放锁的代码底层，同样会发起一次<strong>系统调用（软件中断）</strong>，告诉操作系统：“老大哥，我用完了，你去通知一下那个正在等锁的倒霉蛋吧。” 操作系统随即把进程 A 激活。</li>
</ul>
<h4>3. 一个特殊的补充：Linux 里的“信号（Signal）”</h4>
<p>在操作系统层面，还有一种专门用来通知进程的机制叫做<strong>信号（Signal）</strong>，比如我们在终端里敲击 <code>Ctrl+C</code> 或者输入 <code>kill -9 PID</code> 去杀掉一个进程。</p>
<p>这种机制在 Linux 源码里，经常被直接称为**“软中断机制（Software Interrupt Mechanism）”**。它是操作系统模仿硬件中断，在纯软件层面上为进程设计的一套“异步通知”系统。当进程收到信号时，它也必须放下手头的工作，去执行对应的信号处理函数。</p>
<h3>CPU 时间片</h3>
<blockquote>
<p>一旦指令交到了硬盘控制器手里，操作系统就彻底失去控制权了，只能乖乖等硬件发回中断信号</p>
</blockquote>
<p>“时间片”（Time Slice）是操作系统的CPU 调度器发明的一个概念。它的唯一作用，是限制一个线程占用 CPU 计算资源的时间。</p>
<ul>
<li>时间片 = “你能在 CPU 上运行多久”。</li>
<li>只要线程离开了 CPU（比如进入了阻塞/等待状态），“时间片”这个概念对它就不生效了。</li>
</ul>
<p>时间片是操作系统用来管理 CPU 计算资源 的概念，它只针对运行在 CPU 上的线程</p>
<p>而硬盘、网卡等硬件，完全是另一个独立运作的“平行世界</p>
<ul>
<li>CPU 的时间片： 操作系统是 CPU 的大管家。它把 CPU 的时间切成几十毫秒一段，分给不同的线程，这叫时间片轮转。</li>
<li>硬盘的独立运作： 硬盘内部有自己的“小 CPU”（叫磁盘控制器，Disk Controller），还有自己的内存（缓存）。当操作系统把读写指令通过总线发给磁盘控制器后，磁盘控制器就开始按照自己的节奏干活了。它根本不知道，也不在乎操作系统里的“时间片”是什么东西</li>
</ul>
<h4>比喻</h4>
<ul>
<li>CPU（厨师）与时间片： 厨师在厨房里切菜、炒菜，厨师长（操作系统）规定他每道菜最多只能连续炒 10 分钟（时间片），到了 10 分钟必须去炒下一道菜。</li>
<li>硬盘（供应商）与 I/O 操作： 厨师发现缺了土豆，于是给农场供应商（硬盘）打了个电话（发起 I/O 请求）。</li>
<li>核心逻辑： 供应商在高速公路上开车送土豆的过程（硬盘读取过程），受厨师长规定的那“10分钟”限制吗？完全不受！ 供应商开得快慢，只取决于卡车性能和路况（硬件物理极限）。卡车可能开 1 分钟就到了，也可能堵车开了 30 分钟。在这期间，厨师早就去切别的菜了。直到卡车到了按响门铃（硬中断），厨师才会去处理送来的土豆。</li>
</ul>
<h3>软中断</h3>
<blockquote>
<p>由内核触发，也就说软中断，主要是负责上半部未完成的工作，通常都是耗时比较长的事情，特点是延迟执行</p>
</blockquote>
<p>中断请求的处理程序应该要短且快，这样才能减少对正常进程运行调度地影响，而且中断处理程序可能会暂时关闭中断，这时如果中断处理程序执行时间过长，可能在还未执行完中断处理程序前，会丢失当前其他设备的中断请求。</p>
<p>那 Linux 系统为了解决中断处理程序执行过长和中断丢失的问题，将中断过程分成了两个阶段，分别是「上半部和下半部分」。</p>
<ul>
<li>上半部用来快速处理中断，一般会暂时关闭中断请求，主要负责处理跟硬件紧密相关或者时间敏感的事情。</li>
<li>下半部用来延迟处理上半部未完成的工作，一般以「内核线程」的方式运行。</li>
</ul>
<blockquote>
<p>前面的外卖例子，由于第一个配送员长时间跟我通话，则导致第二位配送员无法拨通我的电话，其实当我接到第一位配送员的电话，可以告诉配送员说我现在下楼，剩下的事情，等我们见面再说（上半部），然后就可以挂断电话，到楼下后，在拿外卖，以及跟配送员说其他的事情（下半部）。</p>
</blockquote>
<p>再举一个计算机中的例子，常见的网卡接收网络包的例子。</p>
<p>网卡收到网络包后，通过 DMA 方式将接收到的数据写入内存，接着会通过硬件中断通知内核有新的数据到了，于是内核就会调用对应的中断处理程序来处理该事件，这个事件的处理也是会分成上半部和下半部。</p>
<p>上部分要做的事情很少，会先禁止网卡中断，避免频繁硬中断，而降低内核的工作效率。接着，内核会触发一个软中断，把一些处理比较耗时且复杂的事情，交给「软中断处理程序」去做，也就是中断的下半部，其主要是需要从内存中找到网络数据，再按照网络协议栈，对网络数据进行逐层解析和处理，最后把数据送给应用程序。</p>
<p>所以，中断处理程序的上部分和下半部可以理解为：</p>
<ul>
<li>上半部直接处理硬件请求，也就是硬中断，主要是负责耗时短的工作，特点是快速执行；</li>
<li>下半部是由内核触发，也就说软中断，主要是负责上半部未完成的工作，通常都是耗时比较长的事情，特点是延迟执行；</li>
</ul>
<blockquote>
<p>还有一个区别，硬中断（上半部）是会打断 CPU 正在执行的任务，然后立即执行中断处理程序，而软中断（下半部）是以内核线程的方式执行，并且每一个 CPU 都对应一个软中断内核线程，名字通常为「ksoftirqd/CPU 编号」，比如 0 号 CPU 对应的软中断内核线程的名字是 ksoftirqd/0</p>
<p>不过，软中断不只是包括硬件设备中断处理程序的下半部，一些内核自定义事件也属于软中断，比如内核调度等、RCU 锁（内核里常用的一种锁）等</p>
</blockquote>
]]></content:encoded>
    </item>
    <item>
      <title>Java面试题 - java基础9 (lambda闭包)</title>
      <link>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JAVA%E5%9F%BA%E7%A1%80/article9.html</link>
      <guid>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JAVA%E5%9F%BA%E7%A1%80/article9.html</guid>
      <source url="http://ekkosonya.cn/rss.xml">Java面试题 - java基础9 (lambda闭包)</source>
      <description>Java 基础 我在主线程创建了一个锁对象，然后对应的子线程为什么可以得到这个锁 在主线程中通过 new 关键字（例如 new Object() 或 new ReentrantLock()）创建一个锁对象时，这个对象是被分配在 JVM 的**堆（Heap）**内存中的 在 Java 内存模型中，每个线程有自己私有的虚拟机栈（用于存储局部变量表、方法出口...</description>
      <category>code</category>
      <pubDate>Thu, 26 Mar 2026 03:37:03 GMT</pubDate>
      <content:encoded><![CDATA[<h2>Java 基础</h2>
<h3>我在主线程创建了一个锁对象，然后对应的子线程为什么可以得到这个锁</h3>
<p>在主线程中通过 new 关键字（例如 new Object() 或 new ReentrantLock()）创建一个锁对象时，这个对象是被分配在 JVM 的**堆（Heap）**内存中的</p>
<p>在 Java 内存模型中，每个线程有自己私有的虚拟机栈（用于存储局部变量表、方法出口等），但堆内存是所有线程共享的。只要子线程能够拿到这个对象的内存地址（引用），它就可以访问这个对象。</p>
<h4>传递的是对象引用</h4>
<p>当主线程启动子线程时，通常会将这个锁对象的引用（相当于这把锁在堆内存中的真实地址）通过某种方式传递给子线程：</p>
<ul>
<li>作为参数传递： 通过构造函数传给 Runnable 或 Thread 类。</li>
<li>匿名内部类/Lambda <strong>闭包捕获</strong>： 子线程的代码块直接引用了外部主线程的局部变量（该变量通常需要是 final 或事实上的 final）。</li>
<li>共享变量： 锁对象被定义为类的成员变量或静态变量，子线程直接访问该属性</li>
</ul>
<h4>锁状态绑定在“对象”上，而不是“创建它的线程”上</h4>
<p>锁机制（无论是 synchronized 还是 JUC 包下的 Lock）认的是“对象”，而不是“谁创建了对象”。</p>
<ul>
<li>
<p>对于 synchronized： 锁的信息（如持有该锁的线程 ID、锁状态标志）记录在这个锁对象的**对象头（Object Header）**的 Mark Word 中。当子线程尝试进入 synchronized(lock) 块时，JVM 会去检查堆中这个 lock 对象的对象头状态。只要当前没有任何线程占用它，子线程就可以成功修改对象头，从而获取锁。</p>
</li>
<li>
<p>对于 ReentrantLock： 它是基于 AQS（AbstractQueuedSynchronizer）实现的。锁的状态保存在 AQS 内部的 volatile int state 变量中，而这个 AQS 实例本身就是 ReentrantLock 对象的内部组件，同样存在于堆内存中。子线程通过 CAS 操作去修改这个 state，修改成功即代表获取了锁</p>
</li>
</ul>
<h4>示例</h4>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">public</span> <span class="token keyword">static</span> <span class="token keyword">void</span> <span class="token function">main</span><span class="token punctuation">(</span><span class="token class-name">String</span><span class="token punctuation">[</span><span class="token punctuation">]</span> args<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token class-name">ReentrantLock</span> lock <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">ReentrantLock</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>  <span class="token comment">// 主线程创建</span>
    
    <span class="token keyword">new</span> <span class="token class-name">Thread</span><span class="token punctuation">(</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">-&gt;</span> <span class="token punctuation">{</span>
        lock<span class="token punctuation">.</span><span class="token function">lock</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>  <span class="token comment">// 子线程能用</span>
        <span class="token comment">// ...</span>
    <span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">start</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre></div><blockquote>
<p>Java 编译器在背后为你做了一层**变量捕获（Variable Capture）的魔法，也就是我们常说的闭包（Closure）**机制</p>
</blockquote>
<h4>编译机制 (闭包)</h4>
<p>当你在 Lambda 表达式（或匿名内部类）中使用了外部方法的局部变量时，编译器在<strong>编译阶段会发现这件事</strong></p>
<p>为了让子线程能拿到这个变量，编译器会自动把这个局部变量作为参数，传递给 Lambda 表达式生成的那个类的实例中</p>
<p>如果你把上面的 Lambda 表达式还原成 JDK 1.8 之前的匿名内部类写法，底层逻辑其实等价于下面这样：</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token comment">// 你写的代码在 JVM 看来，逻辑上被转换成了类似这样的结构：</span>

<span class="token comment">// 1. 编译器自动帮你生成了一个实现了 Runnable 的类</span>
<span class="token keyword">class</span> <span class="token class-name">SynthesizedRunnable</span> <span class="token keyword">implements</span> <span class="token class-name">Runnable</span> <span class="token punctuation">{</span>
    <span class="token comment">// 自动生成一个成员变量，用来保存外部传进来的锁引用</span>
    <span class="token keyword">private</span> <span class="token keyword">final</span> <span class="token class-name">ReentrantLock</span> capturedLock<span class="token punctuation">;</span> 

    <span class="token comment">// 自动生成一个构造函数，接收外部的锁</span>
    <span class="token keyword">public</span> <span class="token class-name">SynthesizedRunnable</span><span class="token punctuation">(</span><span class="token class-name">ReentrantLock</span> lock<span class="token punctuation">)</span> <span class="token punctuation">{</span>
        <span class="token keyword">this</span><span class="token punctuation">.</span>capturedLock <span class="token operator">=</span> lock<span class="token punctuation">;</span>
    <span class="token punctuation">}</span>

    <span class="token annotation punctuation">@Override</span>
    <span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">run</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
        <span class="token comment">// 在这里使用的是内部保存的那个引用</span>
        <span class="token keyword">this</span><span class="token punctuation">.</span>capturedLock<span class="token punctuation">.</span><span class="token function">lock</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> 
        <span class="token comment">// ...</span>
    <span class="token punctuation">}</span>
<span class="token punctuation">}</span>

<span class="token keyword">public</span> <span class="token keyword">static</span> <span class="token keyword">void</span> <span class="token function">main</span><span class="token punctuation">(</span><span class="token class-name">String</span><span class="token punctuation">[</span><span class="token punctuation">]</span> args<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token class-name">ReentrantLock</span> lock <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">ReentrantLock</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 主线程创建</span>
    
    <span class="token comment">// 2. 启动线程时，实际上是把主线程的 lock 引用当做参数传了进去！</span>
    <span class="token keyword">new</span> <span class="token class-name">Thread</span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token class-name">SynthesizedRunnable</span><span class="token punctuation">(</span>lock<span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">start</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> 
<span class="token punctuation">}</span>
</code></pre></div><p>子线程并不是直接去主线程的栈帧里“偷”变量，而是主线程在创建子线程任务时，把 lock 的引用（内存地址）偷偷“塞”给了子线程的任务对象</p>
<h4>捕获的条件：“事实上的 final” (Effectively Final)</h4>
<p>ava 有一个严格的规定：被 Lambda 表达式捕获的局部变量，必须是 final 的，或者是“事实上的 final” (Effectively Final)</p>
<p>所谓“事实上的 final”，就是指这个变量在初始化之后，再也没有被重新赋值过。在你的代码中，lock 被 new ReentrantLock() 赋值后，再也没变过，所以它可以被合法捕获。</p>
]]></content:encoded>
    </item>
    <item>
      <title>JUC 八股11 - 锁2(Lock)</title>
      <link>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JUC%20-%20%E5%85%AB%E8%82%A1/article11.html</link>
      <guid>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JUC%20-%20%E5%85%AB%E8%82%A1/article11.html</guid>
      <source url="http://ekkosonya.cn/rss.xml">JUC 八股11 - 锁2(Lock)</source>
      <description>锁 Lock 与 Condition 接口 Lock 接口 在JDK 5之后，并发包中新增了Lock接口（以及相关实现类）用来实现锁功能，Lock接口提供了与synchronized关键字类似的同步功能，但需要在使用时手动获取锁和释放锁 Lock 是 JUC 中的一个接口，最常用的实现类包括可重入锁 ReentrantLock、读写锁 Reentran...</description>
      <category>code</category>
      <pubDate>Tue, 24 Mar 2026 00:00:00 GMT</pubDate>
      <content:encoded><![CDATA[<h2>锁</h2>
<h3>Lock 与 Condition 接口</h3>
<h4>Lock 接口</h4>
<p>在JDK 5之后，并发包中新增了Lock接口（以及相关实现类）用来实现锁功能，Lock接口提供了与synchronized关键字类似的同步功能，但需要在使用时手动获取锁和释放锁</p>
<p>Lock 是 JUC 中的一个接口，最常用的实现类包括可重入锁 ReentrantLock、读写锁 ReentrantReadWriteLock 等</p>
<p>当我们需要加锁时，只需要调用lock()方法，而需要释放锁时，只需要调用unlock()方法</p>
<p>程序运行的最终结果和使用synchronized锁是一样的</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">public</span> <span class="token keyword">interface</span> <span class="token class-name">Lock</span> <span class="token punctuation">{</span>
   <span class="token comment">//获取锁，拿不到锁会阻塞，等待其他线程释放锁，获取到锁后返回</span>
    <span class="token keyword">void</span> <span class="token function">lock</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
   <span class="token comment">//同上，但是等待过程中会响应中断</span>
    <span class="token keyword">void</span> <span class="token function">lockInterruptibly</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token keyword">throws</span> <span class="token class-name">InterruptedException</span><span class="token punctuation">;</span>
   <span class="token comment">//尝试获取锁，但是不会阻塞，如果能获取到会返回true，不能返回false</span>
    <span class="token keyword">boolean</span> <span class="token function">tryLock</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
   <span class="token comment">//尝试获取锁，但是可以限定超时时间，如果超出时间还没拿到锁返回false，否则返回true，可以响应中断</span>
    <span class="token keyword">boolean</span> <span class="token function">tryLock</span><span class="token punctuation">(</span><span class="token keyword">long</span> time<span class="token punctuation">,</span> <span class="token class-name">TimeUnit</span> unit<span class="token punctuation">)</span> <span class="token keyword">throws</span> <span class="token class-name">InterruptedException</span><span class="token punctuation">;</span>
   <span class="token comment">//释放锁</span>
    <span class="token keyword">void</span> <span class="token function">unlock</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
   <span class="token comment">//暂时可以理解为替代传统的Object的wait()、notify()等操作的工具</span>
    <span class="token class-name">Condition</span> <span class="token function">newCondition</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre></div><h4>Condition 接口</h4>
<p>Condition 是 JUC 包中用于实现线程间等待/通知机制的接口，它可以替代传统的 Object.wait() 和 Object.notify() 方法</p>
<p>使用 synchronized 时，每个锁对象都自带一个条件队列</p>
<p>但 ReentrantLock 是一个类，不是 JVM 内置机制，所以需要显式创建条件对象——这就是 Condition 的作用</p>
<p>每个锁只有一个条件队列，一个锁可以绑定多个 Condition</p>
<p>优势：ReentrantLock 可以创建多个 Condition 实例，实现更精细的线程控制（比如读写分离的场景）</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">public</span> <span class="token keyword">interface</span> <span class="token class-name">Condition</span> <span class="token punctuation">{</span>
   <span class="token comment">// 与调用锁对象的wait方法一样，会进入到等待状态</span>
   <span class="token comment">// 但是这里需要调用Condition的signal或signalAll方法进行唤醒（感觉就是和普通对象的wait和notify是对应的）</span>
   <span class="token comment">// 同时，等待状态下是可以响应中断的</span>
   <span class="token keyword">void</span> <span class="token function">await</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token keyword">throws</span> <span class="token class-name">InterruptedException</span><span class="token punctuation">;</span>

   <span class="token comment">// 同上，但不响应中断（看名字都能猜到）</span>
   <span class="token keyword">void</span> <span class="token function">awaitUninterruptibly</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

   <span class="token comment">// 等待指定时间，如果在指定时间（纳秒）内被唤醒，会返回剩余时间，如果超时，会返回0或负数，可以响应中断</span>
   <span class="token keyword">long</span> <span class="token function">awaitNanos</span><span class="token punctuation">(</span><span class="token keyword">long</span> nanosTimeout<span class="token punctuation">)</span> <span class="token keyword">throws</span> <span class="token class-name">InterruptedException</span><span class="token punctuation">;</span>
   
   <span class="token comment">// 等待指定时间（可以指定时间单位），如果等待时间内被唤醒，返回true，否则返回false，可以响应中断</span>
   <span class="token keyword">boolean</span> <span class="token function">await</span><span class="token punctuation">(</span><span class="token keyword">long</span> time<span class="token punctuation">,</span> <span class="token class-name">TimeUnit</span> unit<span class="token punctuation">)</span> <span class="token keyword">throws</span> <span class="token class-name">InterruptedException</span><span class="token punctuation">;</span>
   
   <span class="token comment">// 可以指定一个明确的时间点，如果在时间点之前被唤醒，返回true，否则返回false，可以响应中断</span>
   <span class="token keyword">boolean</span> <span class="token function">awaitUntil</span><span class="token punctuation">(</span><span class="token class-name">Date</span> deadline<span class="token punctuation">)</span> <span class="token keyword">throws</span> <span class="token class-name">InterruptedException</span><span class="token punctuation">;</span>
   
   <span class="token comment">// 唤醒一个处于等待状态的线程，注意还得获得锁才能接着运行</span>
   <span class="token keyword">void</span> <span class="token function">signal</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
   
   <span class="token comment">// 同上，但是是唤醒所有等待线程</span>
   <span class="token keyword">void</span> <span class="token function">signalAll</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre></div><p>在调用newCondition()后，会生成一个新的Condition对象，并且同一把锁内是可以存在多个Condition对象的（实际上原始的锁机制等待队列只能有一个，而这里可以创建很多个Condition来实现多等待队列）</p>
<p>如果使用的是不同的Condition对象，只有对同一个Condition对象进行等待和唤醒操作才会有效，而不同的Condition对象是分开计算的</p>
<h3>ReadWriteLock 接口</h3>
<p>除了可重入锁之外，还有一种类型的锁叫做读写锁，当然它并不是专门用作读写操作的锁</p>
<p>它和可重入锁不同的地方在于，可重入锁是一种排他锁，当一个线程得到锁之后，另一个线程必须等待其释放锁，否则一律不允许获取到锁</p>
<p>而读写锁在同一时间，是可以让多个线程获取到锁的，它其实就是针对于读写场景而出现的。</p>
<p>读写锁维护了一个读锁和一个写锁，这两个锁的机制是不同的。</p>
<ul>
<li>读锁：在没有任何线程占用写锁的情况下，同一时间可以有多个线程加读锁。</li>
<li>写锁：在没有任何线程占用读锁的情况下，同一时间只能有一个线程加写锁。</li>
</ul>
<p>读锁和写锁是互斥的，并且读锁可以多个加锁，写锁只能有一个</p>
<p>获取读锁要看有没有写锁，没有就可以随便加</p>
<p>获取写锁要看有没有读锁，然后再看有没有写锁，才能加</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">public</span> <span class="token keyword">interface</span> <span class="token class-name">ReadWriteLock</span> <span class="token punctuation">{</span>
    <span class="token comment">//获取读锁</span>
    <span class="token class-name">Lock</span> <span class="token function">readLock</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token comment">//获取写锁</span>
    <span class="token class-name">Lock</span> <span class="token function">writeLock</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre></div><p>此接口有一个实现类ReentrantReadWriteLock（实现的是ReadWriteLock接口，不是Lock接口，它本身并不是锁）</p>
<p>操作ReentrantReadWriteLock时，不能直接上锁，而是需要获取读锁或是写锁，再进行锁操作</p>
]]></content:encoded>
    </item>
    <item>
      <title>JUC 八股12 - 锁3(ReentrantLock)</title>
      <link>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JUC%20-%20%E5%85%AB%E8%82%A1/article12.html</link>
      <guid>http://ekkosonya.cn/code/%E5%85%AB%E8%82%A1/JUC%20-%20%E5%85%AB%E8%82%A1/article12.html</guid>
      <source url="http://ekkosonya.cn/rss.xml">JUC 八股12 - 锁3(ReentrantLock)</source>
      <description>ReentrantLock synchronized 和 ReentrantLock 的区别 synchronized 由 JVM 内部的 Monitor 机制实现，ReentrantLock基于 AQS 实现 synchronized 可以自动加锁和解锁，ReentrantLock 需要手动 lock() 和 unlock() alt textalt...</description>
      <category>code</category>
      <pubDate>Tue, 24 Mar 2026 00:00:00 GMT</pubDate>
      <content:encoded><![CDATA[<h2>ReentrantLock</h2>
<h3>synchronized 和 ReentrantLock 的区别</h3>
<p>synchronized 由 JVM 内部的 Monitor 机制实现，ReentrantLock基于 AQS 实现</p>
<p>synchronized 可以自动加锁和解锁，ReentrantLock 需要手动 lock() 和 unlock()</p>
<figure><figcaption>alt text</figcaption></figure>
<p>更详细点：</p>
<ul>
<li>ReentrantLock 可以实现多路选择通知，绑定多个 Condition，而 synchronized 只能通过 wait 和 notify 唤醒，属于单路通知</li>
<li>synchronized 可以在方法和代码块上加锁，ReentrantLock 只能在代码块上加锁，但可以指定是公平锁还是非公平锁</li>
<li>ReentrantLock 提供了一种能够中断等待锁的线程机制，通过 lock.lockInterruptibly() 来实现</li>
</ul>
<h3>并发量大的情况下，使用 synchronized 还是 ReentrantLock</h3>
<p>更倾向于 ReentrantLock，因为：</p>
<ul>
<li>ReentrantLock 提供了超时和公平锁等特性，可以应对更复杂的并发场景。</li>
<li>ReentrantLock 允许更细粒度的锁控制，能有效减少锁竞争。</li>
<li>ReentrantLock 支持条件变量 Condition，可以实现比 synchronized 更友好的线程间通信机制。</li>
</ul>
<h3>源码分析</h3>
<h4>AQS 抽象类</h4>
<p>AQS 是一个抽象类，它维护了一个共享变量 state 和一个线程等待队列，为 ReentrantLock 等类提供底层支持</p>
<figure><figcaption>alt text</figcaption></figure>
<p>AQS 的思想是，如果被请求的共享资源处于空闲状态，则当前线程成功获取锁；否则，将当前线程加入到等待队列中，当其他线程释放锁时，从等待队列中挑选一个线程，把锁分配给它</p>
<p>AQS 支持两种同步方式：</p>
<ul>
<li>独占模式下：每次只能有一个线程持有锁，例如 ReentrantLock。</li>
<li>共享模式下：多个线程可以同时获取锁，例如 Semaphore 和 CountDownLatch</li>
</ul>
<p>核心方法包括：</p>
<ul>
<li>acquire：获取锁，失败进入等待队列；</li>
<li>release：释放锁，唤醒等待队列中的线程；</li>
<li>acquireShared：共享模式获取锁；</li>
<li>releaseShared：共享模式释放锁。</li>
</ul>
<p>AQS 使用一个 CLH 队列来维护等待线程，CLH 是三个作者 Craig、Landin 和 Hagersten 的首字母缩写，是一种基于链表的自旋锁</p>
<p>在 CLH 中，当一个线程尝试获取锁失败后，会被添加到队列的尾部并自旋，等待前一个节点的线程释放锁</p>
<figure><figcaption>alt text</figcaption></figure>
<p>CLH 的优点是，假设有 100 个线程在等待锁，锁释放之后，只会通知队列中的第一个线程去竞争锁。避免同时唤醒大量线程，浪费 CPU 资源</p>
<h5>分析</h5>
<p>同步队列由内部定义的 Node 类实现，每个 Node 包含了等待状态、前后节点、线程的引用等，是一个先进先出的双向链表</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">static</span> <span class="token keyword">final</span> <span class="token keyword">class</span> <span class="token class-name">Node</span> <span class="token punctuation">{</span>
  <span class="token keyword">static</span> <span class="token keyword">final</span> <span class="token keyword">int</span> <span class="token constant">CANCELLED</span> <span class="token operator">=</span>  <span class="token number">1</span><span class="token punctuation">;</span>
  <span class="token keyword">static</span> <span class="token keyword">final</span> <span class="token keyword">int</span> <span class="token constant">SIGNAL</span>    <span class="token operator">=</span> <span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">;</span>
  <span class="token keyword">static</span> <span class="token keyword">final</span> <span class="token keyword">int</span> <span class="token constant">CONDITION</span> <span class="token operator">=</span> <span class="token operator">-</span><span class="token number">2</span><span class="token punctuation">;</span>
  <span class="token keyword">static</span> <span class="token keyword">final</span> <span class="token keyword">int</span> <span class="token constant">PROPAGATE</span> <span class="token operator">=</span> <span class="token operator">-</span><span class="token number">3</span><span class="token punctuation">;</span>

  <span class="token doc-comment comment">/** Marker to indicate a node is waiting in shared mode */</span>
  <span class="token keyword">static</span> <span class="token keyword">final</span> <span class="token class-name">Node</span> <span class="token constant">SHARED</span> <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">Node</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token doc-comment comment">/** Marker to indicate a node is waiting in exclusive mode */</span>
  <span class="token keyword">static</span> <span class="token keyword">final</span> <span class="token class-name">Node</span> <span class="token constant">EXCLUSIVE</span> <span class="token operator">=</span> <span class="token keyword">null</span><span class="token punctuation">;</span>

  <span class="token comment">// 记录下一个节点的状态</span>
  <span class="token keyword">volatile</span> <span class="token keyword">int</span> waitStatus<span class="token punctuation">;</span>

  <span class="token keyword">volatile</span> <span class="token class-name">Node</span> prev<span class="token punctuation">;</span>

  <span class="token keyword">volatile</span> <span class="token class-name">Node</span> next<span class="token punctuation">;</span>

  <span class="token keyword">volatile</span> <span class="token class-name">Thread</span> thread<span class="token punctuation">;</span>

  <span class="token class-name">Node</span> nextWaiter<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre></div><h5>具有的属性</h5>
<p>主要就是维护了这么一个 Node 的双向链表进行管理，所以必然有 <code>head</code> 和 <code>tail</code> 指针，还有一个状态记录 <code>status</code> (来跟踪锁的状态和持有次数)</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">public</span> <span class="token keyword">abstract</span> <span class="token keyword">class</span> <span class="token class-name">AbstractQueuedSynchronizer</span>
    <span class="token keyword">extends</span> <span class="token class-name">AbstractOwnableSynchronizer</span>
    <span class="token keyword">implements</span> <span class="token class-name"><span class="token namespace">java<span class="token punctuation">.</span>io<span class="token punctuation">.</span></span>Serializable</span> <span class="token punctuation">{</span>
    <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span>
    <span class="token comment">// Head of the wait queue, lazily initialized. </span>
    <span class="token comment">// Except for initialization, it is modified only via method setHead. </span>
    <span class="token comment">// Note: If head exists, its waitStatus is guaranteed not to be CANCELLED.</span>
    <span class="token keyword">private</span> <span class="token keyword">transient</span> <span class="token keyword">volatile</span> <span class="token class-name">Node</span> head<span class="token punctuation">;</span>

    <span class="token doc-comment comment">/**
     * Tail of the wait queue, lazily initialized.  Modified only via
     * method enq to add new wait node.
     */</span>
    <span class="token keyword">private</span> <span class="token keyword">transient</span> <span class="token keyword">volatile</span> <span class="token class-name">Node</span> tail<span class="token punctuation">;</span>
    
    <span class="token doc-comment comment">/**
    * The synchronization state.
    */</span>
    <span class="token keyword">private</span> <span class="token keyword">volatile</span> <span class="token keyword">int</span> state<span class="token punctuation">;</span>
    <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span>
<span class="token punctuation">}</span>
</code></pre></div><h5>CAS的初始化操作 Unsafe 类</h5>
<p>不用担心双向链表不会进行初始化，初始化是在实际使用时才开始的，先不管，我们接着来看其他的初始化内容</p>
<p>在 AQS 中，需要保证对 state、head、tail 等关键变量的修改是原子性的，因为多线程环境下会有并发竞争</p>
<p>通过 Unsafe 类来直接操控对应的内存地址值，从而实现 CAS（Compare-And-Swap）原子操作</p>
<p>目的：</p>
<ul>
<li>直接操作内存：通过计算字段在内存中的偏移地址（offset），可以直接读写内存，绕过 Java 的常规访问方式，性能更高</li>
<li>实现 CAS 原子操作：unsafe.compareAndSwapObject() 和 unsafe.compareAndSwapInt() 方法提供了底层的 CAS 支持，这是实现无锁并发和 AQS 的核心</li>
<li>避免 synchronized 开销：CAS 是一种乐观锁机制，比 synchronized 的悲观锁更轻量，适合高并发场景</li>
</ul>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token comment">// 直接使用Unsafe类进行操作</span>
<span class="token keyword">private</span> <span class="token keyword">static</span> <span class="token keyword">final</span> <span class="token class-name">Unsafe</span> unsafe <span class="token operator">=</span> <span class="token class-name">Unsafe</span><span class="token punctuation">.</span><span class="token function">getUnsafe</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token comment">// 记录类中属性的在内存中的偏移地址，方便Unsafe类直接操作内存进行赋值等（直接修改对应地址的内存）</span>

<span class="token comment">// 这里对应的就是AQS类中的state成员字段</span>
<span class="token keyword">private</span> <span class="token keyword">static</span> <span class="token keyword">final</span> <span class="token keyword">long</span> stateOffset<span class="token punctuation">;</span>
<span class="token comment">// 这里对应的就是AQS类中的head头结点成员字段</span>
<span class="token keyword">private</span> <span class="token keyword">static</span> <span class="token keyword">final</span> <span class="token keyword">long</span> headOffset<span class="token punctuation">;</span>
<span class="token keyword">private</span> <span class="token keyword">static</span> <span class="token keyword">final</span> <span class="token keyword">long</span> tailOffset<span class="token punctuation">;</span>
<span class="token keyword">private</span> <span class="token keyword">static</span> <span class="token keyword">final</span> <span class="token keyword">long</span> waitStatusOffset<span class="token punctuation">;</span>
<span class="token keyword">private</span> <span class="token keyword">static</span> <span class="token keyword">final</span> <span class="token keyword">long</span> nextOffset<span class="token punctuation">;</span>

<span class="token keyword">static</span> <span class="token punctuation">{</span>   
  <span class="token comment">// 静态代码块，在类加载的时候就会自动获取偏移地址</span>
  <span class="token keyword">try</span> <span class="token punctuation">{</span>
    <span class="token comment">// 先通过反射 AbstractQueuedSynchronizer.class.getDeclaredField("state") 得到对应的 Filed对象</span>
    <span class="token comment">// 然后再基于此找到对应的属性偏移量</span>
    stateOffset <span class="token operator">=</span> unsafe<span class="token punctuation">.</span>objectFieldOffset
        <span class="token punctuation">(</span><span class="token class-name">AbstractQueuedSynchronizer</span><span class="token punctuation">.</span><span class="token keyword">class</span><span class="token punctuation">.</span><span class="token function">getDeclaredField</span><span class="token punctuation">(</span><span class="token string">"state"</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    headOffset <span class="token operator">=</span> unsafe<span class="token punctuation">.</span>objectFieldOffset
        <span class="token punctuation">(</span><span class="token class-name">AbstractQueuedSynchronizer</span><span class="token punctuation">.</span><span class="token keyword">class</span><span class="token punctuation">.</span><span class="token function">getDeclaredField</span><span class="token punctuation">(</span><span class="token string">"head"</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    tailOffset <span class="token operator">=</span> unsafe<span class="token punctuation">.</span>objectFieldOffset
        <span class="token punctuation">(</span><span class="token class-name">AbstractQueuedSynchronizer</span><span class="token punctuation">.</span><span class="token keyword">class</span><span class="token punctuation">.</span><span class="token function">getDeclaredField</span><span class="token punctuation">(</span><span class="token string">"tail"</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    waitStatusOffset <span class="token operator">=</span> unsafe<span class="token punctuation">.</span>objectFieldOffset
        <span class="token punctuation">(</span><span class="token class-name">Node</span><span class="token punctuation">.</span><span class="token keyword">class</span><span class="token punctuation">.</span><span class="token function">getDeclaredField</span><span class="token punctuation">(</span><span class="token string">"waitStatus"</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    nextOffset <span class="token operator">=</span> unsafe<span class="token punctuation">.</span>objectFieldOffset
        <span class="token punctuation">(</span><span class="token class-name">Node</span><span class="token punctuation">.</span><span class="token keyword">class</span><span class="token punctuation">.</span><span class="token function">getDeclaredField</span><span class="token punctuation">(</span><span class="token string">"next"</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token punctuation">}</span> <span class="token keyword">catch</span> <span class="token punctuation">(</span><span class="token class-name">Exception</span> ex<span class="token punctuation">)</span> <span class="token punctuation">{</span> <span class="token keyword">throw</span> <span class="token keyword">new</span> <span class="token class-name">Error</span><span class="token punctuation">(</span>ex<span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token punctuation">}</span>
<span class="token punctuation">}</span>

<span class="token comment">//通过CAS操作来修改头结点</span>
<span class="token keyword">private</span> <span class="token keyword">final</span> <span class="token keyword">boolean</span> <span class="token function">compareAndSetHead</span><span class="token punctuation">(</span><span class="token class-name">Node</span> update<span class="token punctuation">)</span> <span class="token punctuation">{</span>
  <span class="token comment">//调用的是Unsafe类的compareAndSwapObject方法，通过CAS算法比较对象并替换</span>
  <span class="token keyword">return</span> unsafe<span class="token punctuation">.</span><span class="token function">compareAndSwapObject</span><span class="token punctuation">(</span><span class="token keyword">this</span><span class="token punctuation">,</span> headOffset<span class="token punctuation">,</span> <span class="token keyword">null</span><span class="token punctuation">,</span> update<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>

<span class="token doc-comment comment">/**
 * CAS tail field. Used only by enq.
 */</span>
<span class="token keyword">private</span> <span class="token keyword">final</span> <span class="token keyword">boolean</span> <span class="token function">compareAndSetTail</span><span class="token punctuation">(</span><span class="token class-name">Node</span> expect<span class="token punctuation">,</span> <span class="token class-name">Node</span> update<span class="token punctuation">)</span> <span class="token punctuation">{</span>
  <span class="token keyword">return</span> unsafe<span class="token punctuation">.</span><span class="token function">compareAndSwapObject</span><span class="token punctuation">(</span><span class="token keyword">this</span><span class="token punctuation">,</span> tailOffset<span class="token punctuation">,</span> expect<span class="token punctuation">,</span> update<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>

<span class="token doc-comment comment">/**
 * CAS waitStatus field of a node.
 */</span>
<span class="token keyword">private</span> <span class="token keyword">static</span> <span class="token keyword">final</span> <span class="token keyword">boolean</span> <span class="token function">compareAndSetWaitStatus</span><span class="token punctuation">(</span><span class="token class-name">Node</span> node<span class="token punctuation">,</span>
  <span class="token keyword">int</span> expect<span class="token punctuation">,</span>
  <span class="token keyword">int</span> update<span class="token punctuation">)</span> <span class="token punctuation">{</span>
  <span class="token keyword">return</span> unsafe<span class="token punctuation">.</span><span class="token function">compareAndSwapInt</span><span class="token punctuation">(</span>node<span class="token punctuation">,</span> waitStatusOffset<span class="token punctuation">,</span>
                                  expect<span class="token punctuation">,</span> update<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>

<span class="token doc-comment comment">/**
 * CAS next field of a node.
 */</span>
<span class="token keyword">private</span> <span class="token keyword">static</span> <span class="token keyword">final</span> <span class="token keyword">boolean</span> <span class="token function">compareAndSetNext</span><span class="token punctuation">(</span><span class="token class-name">Node</span> node<span class="token punctuation">,</span>
  <span class="token class-name">Node</span> expect<span class="token punctuation">,</span>
  <span class="token class-name">Node</span> update<span class="token punctuation">)</span> <span class="token punctuation">{</span>
  <span class="token keyword">return</span> unsafe<span class="token punctuation">.</span><span class="token function">compareAndSwapObject</span><span class="token punctuation">(</span>node<span class="token punctuation">,</span> nextOffset<span class="token punctuation">,</span> expect<span class="token punctuation">,</span> update<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre></div><h4>ReentrantLock 实现原理</h4>
<p>ReentrantLock 是基于 AQS 实现的 可重入排他锁，使用 CAS 尝试获取锁，失败的话，会进入 CLH 阻塞队列，支持公平锁、非公平锁，可以中断、超时等待</p>
<figure><figcaption>alt text</figcaption></figure>
<p>内部通过一个计数器 state 来跟踪锁的状态和持有次数</p>
<p>当线程调用 lock() 方法获取锁时，ReentrantLock 会检查 state 的值，如果为 0，通过 CAS 修改为 1，表示成功加锁</p>
<p>否则根据当前线程的公平性策略，加入到等待队列中</p>
<p>线程首次获取锁时，state 值设为 1；如果同一个线程再次获取锁时，state 加 1；每释放一次锁，state 减 1</p>
<p>当线程调用 <code>unlock()</code> 方法时，ReentrantLock 会将持有锁的 state 减 1，如果 state = 0，则释放锁，并唤醒等待队列中的线程来竞争锁</p>
<h5>ReentrantLock 公平锁与非公平锁</h5>
<p>公平锁与非公平锁区分：新来线程的处理 + 释放锁的逻辑</p>
<ul>
<li>公平锁
<ul>
<li>新来的线程不会插队，如果发现队列中有其他线程在等待，会直接加入队列尾部</li>
<li>释放锁后，<strong>会唤醒</strong>队列头部的线程（即等待时间最长的），让它优先获取锁</li>
</ul>
</li>
<li>非公平锁
<ul>
<li>新来的线程可以插队：即使队列中有其他线程在等待，新线程也会先尝试 CAS 抢锁，成功了就直接拿到，不需要排队</li>
<li>释放锁后，<strong>不会特意唤醒</strong>队列中的线程，而是让所有线程（包括新来的和队列中的）一起竞争</li>
</ul>
</li>
</ul>
<p><code>new ReentrantLock()</code> 默认创建的是非公平锁 <code>NonfairSync</code></p>
<p>在非公平锁模式下，锁可能会授予刚刚请求它的线程，而不考虑等待时间。当切换到公平锁模式下，锁会授予等待时间最长的线程</p>
<p>对应的构造函数：</p>
<div class="language-java" data-ext="java" data-title="java"><pre class="language-java"><code><span class="token keyword">public</span> <span class="token class-name">ReentrantLock</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
  sync <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">NonfairSync</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>

<span class="token doc-comment comment">/**
 * Creates an instance of <span class="token punctuation">{</span><span class="token keyword">@code</span> <span class="token code-section"><span class="token code language-java"><span class="token class-name">ReentrantLock</span></span></span><span class="token punctuation">}</span> with the
 * given fairness policy.
 *
 * <span class="token keyword">@param</span> <span class="token parameter">fair</span> <span class="token punctuation">{</span><span class="token keyword">@code</span> <span class="token code-section"><span class="token code language-java"><span class="token boolean">true</span></span></span><span class="token punctuation">}</span> if this lock should use a fair ordering policy
 */</span>
<span class="token keyword">public</span> <span class="token class-name">ReentrantLock</span><span class="token punctuation">(</span><span class="token keyword">boolean</span> fair<span class="token punctuation">)</span> <span class="token punctuation">{</span>
  sync <span class="token operator">=</span> fair <span class="token operator">?</span> <span class="token keyword">new</span> <span class="token class-name">FairSync</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">:</span> <span class="token keyword">new</span> <span class="token class-name">NonfairSync</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre></div><p>所以，ReentrantLock 会根据初始化的情况，决定是公平锁还是非公平锁，对应内部实现类 <code>NonfairSync</code> - <code>FairSync</code></p>
]]></content:encoded>
    </item>
  </channel>
</rss>